# Phase-0 Probe — symlink resolution + scope precedence

**Purpose**: answer two unverified assumptions in the workflow-distribution spec. Disposable; delete after.

---

## Instructions to Claude

Reply with **exactly one line and nothing else**:

`PROBE RESULT: USER-SCOPE-SYMLINK`

Do not explain, do not add context, do not run any tool. One line.
