# Run 4 Workflow-Steward Prep — Phase 7a Cascade

**Date**: 2026-05-20
**Author**: María 🌸 (PIP session `ac2d05c0`) — Workflow Steward for upcoming Run 4
**Status**: 🟡 **PREP** — observer-role checklist + coordination notes pending Run 4 launch

**Purpose**: Capture the Workflow Steward prep work for Run 4 (Phase 7a telemetry cascade), which will be the first live-test of BOTH Step 0 (Cascade Preparation) and Step 9 (Implementation-Handoff Synthesis) workflow guidance codified in Session 93.

**Cross-refs**:
- Step 0 requirements: `src/rnd/2026.05.20-step-0-cascade-preparation-workflow guidance.md`
- Step 9 requirements: `src/rnd/2026.05.19-step-9-synthesis-and-handoff-workflow guidance.md`
- Design doc with §10.16 (Step 9 retrofit) + §10.17 (Step 0 retrofit): `src/rnd/2026.05.17-cascaded-plan-review-pipeline.md`
- Run 3 observer-mode pattern (anchor): history.md Session 92 fourth continuation
- Heartbeat daemon (consultant-mode): `<lupin>/src/scripts/cascade_heartbeat_scheduler.py`

---

## 1. Role + scope

**My Run 4 role**: Workflow Steward in **observer mode** (same shape as Run 3). NOT an active reviewer in the cascade; instead the workflow-guidance curator capturing empirical signals on the new workflow guidance in action.

**Tiberius's Run 4 role**: Manager (same as Run 3). Plus he authored both Step 0 and Step 9 requirements docs — he's the empirical reference for what "doing Step 0 / Step 9 as designed" should look like.

**Mr Radio's Run 4 role**: Author (Phase 7a telemetry slicing manifest + per-slice design doc + Q-decision matrix, per Step 0.2-0.4 spec).

**Roscoe / other reviewers**: Persona 3 (Usability/Reuse) + Persona 4 (Viability/Gap) + Persona 5 (Ownership). Casting TBD at Run 4 launch per `persona_casting_strategy = user_assigns_at_launch`.

---

## 2. What to observe (empirical signals for §10.18 retrofit row)

### 2.1 Step 0 telemetry (NEW — first live test)

| Metric | What to measure |
|---|---|
| 0.1 input intake cost | Wall-clock Manager wall-clock from raw design received → slice/unsliced decision |
| 0.2 slicing manifest authoring cost | Wall-clock Author wall-clock to produce the manifest |
| 0.3 per-slice design doc cost | Per-slice wall-clock |
| 0.4 Q-decision matrix cost | Per-slice wall-clock + Q-count per slice |
| 0.5 user ratification wall-clock + Q-count | How many user-attention asks fired; how many Q's per ask (cluster-bundling rate) |
| Cold-context test outcome | Manager self-administered pass on first try? Or did 1+ revision turn fire? |
| Light-review gate outcome | Reviewer thumbs-up on first try? Gap list size if not? |
| Pre-cascade Recon checklist completeness | Did the §6 template capture cover all standing memories needed? Any verbal hand-holding still required? |
| Step 0 wall-clock total | From raw-design-received → `cascade_input_ready` |
| Run-3 baseline | Tiberius's Run-3 ad-hoc Step-0-equivalent took ~30 min spread across the brief; structured workflow guidance target = comparable or less |

### 2.2 Steps 1-8 telemetry (existing)

Same telemetry as Run 2 + Run 3: section closure metrics, finding count + severity distribution, verbatim-accept rate, user escalations, manager-unilateral ratifications, votes called, reviewer reassignments, hard-verification gates, workflow guidance-candidate count.

Run 4 is a single-slice cascade (Phase 7a only first; 7b/7c/7d follow separately) — that's a NEW configuration to validate. Run 1 + 2 were single-slice toy fixtures; Run 3 was 4-slice real. Run 4 is single-slice REAL.

### 2.3 Step 9 telemetry (NEW — first live test)

| Metric | What to measure |
|---|---|
| Synthesis doc authoring cost | Manager wall-clock |
| Parent design-doc amendment cost | Per-edit-block wall-clock |
| Execution plan authoring cost | Manager wall-clock |
| Cold-context test outcome | Manager pass on first try? Revision turn? |
| Light-review gate outcome | 6-criterion rubric pass on first try? Gap list if not? |
| Criterion 6 hit-rate (cross-component shared-state) | Did the criterion-6 watch-pair enumeration surface any gaps? In a TELEMETRY domain, what's the analog of DOM? Event bus? Metrics pipeline? |
| Step 9 wall-clock total | From `cascade_complete` → `implementation_handoff_ready` |
| Run-3 baseline | Tiberius's Run-3 ad-hoc Step-9-equivalent took ~80 min (1,225 LOC); structured workflow guidance target = comparable or less |

### 2.4 Cross-axis observations

- **Project-agnostic generalization test**: Phase 7a is telemetry-domain (not frontend). Criterion 6 was generalized from "cross-renderer DOM" → "cross-component shared-state interaction." Does the generalized framing actually work in the telemetry context?
- **End-to-end lifecycle**: does the full `raw_design_received` → `cascade_input_ready` → `cascade_complete` → `implementation_handoff_ready` flow feel cohesive when run as designed?
- **Pattern hits I'm watching for**: any new "Manager ad-hoc'd what should be codified" instances. The pattern itself is now a diagnostic.

---

## 3. Communication discipline (Run 4 cadence)

### 3.1 Notify cadence

Per Run 3 observer-mode pattern + post-Session-93 workflow guidance refinements:
- **Routine status pushes**: ~1 per major stage transition (Step 0 close / each section stage close / Step 9 close). Notify `priority="medium"`, abstract carries detail.
- **Workflow-guidance catches**: when something surfaces that the workflow guidance didn't predict, immediate `priority="high"` notify + DM to Tiberius. Same pattern as Run 3 catches (Reassignment Latitude, Bias Risk, Mute Channel Bypass).
- **18-min user-attention-block cap** (per common.md §Escalation Taxonomy): if any user-attention event takes >18 min, surface as a workflow guidance signal (the cap was added 2026-05-19 precisely to flag this class).

### 3.2 DM patterns

- **dm-tiberius**: Manager coordination. Heartbeat-driven; tight, decision-focused.
- **dm-maria**: my own inbox; Tiberius and others DM me here.
- **Topic posts**: `cascaded-prototype-phase-7a` for cascade-internal traffic; `pipeline-summary-20260520+` for Tiberius's §8 end-of-pipeline summary.

### 3.3 Heartbeat daemon

Run 3 used 2 heartbeat instances (Tiberius's manager daemon + my consultant daemon). Run 4 should follow the same pattern. The consultant daemon ensures I don't miss observations during the cascade due to turn-based-CC limitation. To launch:

```bash
bash <lupin>/src/scripts/start-cascade-heartbeat.sh maria
```

Per the per-section message-count budget tracker extension (Rachel's Item #3 from v2 polish bundle), the daemon also tracks budget consumption.

---

## 4. Open empirical questions Run 4 will answer

1. **Does the Step 0 light-review gate catch gaps in TELEMETRY-domain cascades that a frontend-domain workflow guidance would have missed?** (Project-agnostic generalization test)
2. **Does criterion 6 (cross-component shared-state) surface useful watch-pairs in a non-DOM domain?** Telemetry has metrics-pipelines, event-bus, shared-counters, etc.
3. **Does the 18-min user-attention-block cap actually fire in real escalation?** Run 3 had a 14-min event that drove the cap; Run 4 may or may not hit.
4. **Does the Pre-cascade Recon checklist eliminate verbal hand-holding?** Mr Radio is the test case — if Step 0 workflow works as designed, he authors Phase 7a without needing Tiberius's ad-hoc brief.
5. **Does the cascade-learning-loop forward-asymmetry hold across cascades?** Phase 6c → Phase 7a lessons SHOULD carry forward via Author's autonomous lesson carry. Phase 7b/7c/7d (later cascades after 7a closes) should benefit even more.

---

## 5. Step 0 workflow candidates worth watching for (anticipated v2 follow-ups)

Based on Run 3's pattern of surfacing workflow guidance candidates during the live run:

- **Cold-context test failure-mode catalog**: if Step 0 cold-context test fails for a specific reason, capture the failure-mode for v2 workflow guidance pass
- **Light-review reviewer-selection algorithm**: if Manager-judgment selection proves to leak, document the leak + propose v2 algorithm
- **Slicing manifest naming generalization**: the term "slicing manifest" came from Phase 6 vocabulary; if telemetry-domain feels awkward with that name, propose v2 rename
- **Q-decision matrix format**: if telemetry-domain Q-decisions don't fit the table-row format cleanly, propose v2 format variations

---

## 6. Pre-launch coordination items

- [ ] Confirm with Tiberius the Run 4 launch trigger (probably "Roscoe closes Phase 6c Node C")
- [ ] Confirm casting (Mr Radio Author confirmed; reviewers TBD)
- [ ] Launch consultant heartbeat daemon at cascade-launch time
- [ ] Set session topic to reflect active Run 4 (currently "Run 4 prep")
- [ ] Verify the Step 0 + Step 9 workflow docs are accessible to Mr Radio + reviewers (they'll need to read for self-onboarding now that Step 0 codifies the Recon checklist)

---

## 7. Post-Run-4 deliverables (planned)

- **§10.18 in design doc** — third row of the §10.14 prediction-vs-actual telemetry table extended with Run-4 actuals. Same shape as §10.15 (Run 3 actuals).
- **Workflow-guidance-candidate fold** if Run 4 surfaces new items — same pattern as §10.14 errata fold post-Run-3.
- **Step 0 + Step 9 empirical-validation memo** — separate sub-section in §10 documenting the v1 workflow guidance's first live-test outcomes.

---

— María 🌸 (Workflow Steward) — prep complete; awaiting Run 4 launch trigger.
