# María 🌸 — Fresh-Eyes Review Memento (post-respin rehydration)

**Date**: 2026-06-17 · **Persona**: María 🌸 (PRESERVE — do NOT re-allocate) · **Project**: `plan` (planning-is-prompting) · **Session**: `0ba3e504-f42c-4aed-8a7e-73c0926f8cd2` · **Role**: author/Steward of the store-only engagement → now **FRESH-EYES REVIEWER**.

**Purpose**: Rick is re-spinning me (/clear) SO THAT a fresh context runs an adversarial **fresh-eyes review** of the just-shipped store-only task-management undertaking. Read this, then RUN THE REVIEW (§3 is the charter). The whole undertaking shipped overnight under time pressure + intermittent Opus instability + reviewer stalls — exactly the conditions where false-passes and rushed edits hide. Be skeptical. Reproduce, don't trust.

---

## 1. FIRST ACTIONS on rehydrate
1. MCP Phase A (get_session_info; confirm persona María 🌸; set topic "Fresh-eyes review: store-only task-mgmt").
2. **Store-only is now LIVE** — your owed work lives in the unified store, NOT the native harness list. Query it: `task_query(owner_persona=maria, status=open)` → you should find the **fresh-eyes-review task** (created 2026-06-17; see §6). Don't rebuild a harness list; query the store.
3. Read this memento + the two anchor docs (§4), then execute the §3 review.
4. Report findings to Rick via notify + a written review doc in `src/rnd/`.

## 2. What shipped (the thing under review) — receipts
- **Cutover LIVE**: `heartbeat.owed_source_from_store=True` (verified `~/.claude/settings.json:119`); drain --apply → store/transcript count-parity 4/4; harness→store mirror retired. Bugs `9bf1dc4a` (mirror write-gate), `9b23d5bc` (/clear collision), `82e4eaf0` (dual source) dead by construction.
- **Spine (Mr Radio, lupin)**: Step-1 `513b184c`, Step-2 stop-hook store-count seam `8d77ba91`, Step-3 arbiter no-code, cutover-prep `2b9c891d`; cutover commit `09b323ae`; pushed `7178ab5d..09b323ae` (27 commits); backed up DATA02. Court gate `538ec09d` → done.
- **Card (Tiberius, lupin)**: JS `notifications.js` task-list card `4eeab529`; E2E `69f1871f` (Playwright 6/6, job ts-0e2e10c5); **+ TWO real-data FIXES `7d169442` (toolbar toggle) + `2724b80d` (render crash on array `blocked_by`), both HELD/unpushed on wip-v0.1.8** (verified headless on :7999, 44 real rows) — see §3.
- **Doctrine (María, PIP)**: 6 surfaces + live `~/.claude/CLAUDE.md` flipped not-live→LIVE; committed `e1ff385`; pushed `00c7b01`. Plus the v3 plan doc + not-live banners earlier (`0c3aba9`).
- **Roll-up**: net +9,506 LoC, 31 commits, 2 repos, 2 days.

## 3. THE REVIEW CHARTER (what to scrutinize — adversarial)
**Tiberius surfaced THREE real findings already — START HERE, they prove the review is needed (the card passed "done"/E2E 6/6 but FAILED ON REAL DATA TWICE; finding 3 is the shared root cause):**
1. **Card visibility gap (FIXED `7d169442`, but VERIFY):** the card section shipped WITHOUT its floating section-toolbar TOGGLE button — Rio cloned the section but not the toolbar entry (every other section has one), so Rick could NOT see/enable the card. Tiberius added the 🗒️ Task List button (`:7999` serves it, verified). **VERIFY**: 7d169442 is correct, the card is user-visible + toggleable, consistent with the other sections' toolbar pattern.
2. **Render CRASH on real data (FIXED `2724b80d`, but VERIFY):** the real `blocked_by` is a typed-ref ARRAY, which crashed `escapeHtml`'s `.replace` → rows never drew. Fixed (stringified + escaper hardened); verified headless on :7999 with REAL data (44 rows, no pageerror). **VERIFY** the fix + escaper robustness against real shapes (array `blocked_by`, null owners).
3. **E2E false-pass — the SHARED ROOT CAUSE (TEST-QUALITY GAP, needs hardening):** the Playwright E2E seeds synthetic CLEAN data + route-intercepts the section directly INTO the DOM — bypassing BOTH the real data shapes (→ finding 2) AND the toolbar entry point (→ finding 1). So it false-passed 6/6 while the card was invisible AND crashed on live data. Tiberius filed E2E-hardening task **`0a0dc74c`** (drive the real toolbar toggle + seed real shapes: array `blocked_by`, null owners). **This is the canonical "tests green but feature broken" failure — hunt for MORE of its kind across the undertaking.**

**Then broaden (fresh-eyes, whole undertaking):**
- **lupin `wip-v0.1.8` is UNPUSHED** (7 commits incl. both card fixes `7d169442` + `2724b80d`). Rick DECLINED María pushing Mr Radio's repo (2026-06-17, direct `no`) — it is HELD for Mr Radio (`f6292818`, arbiter-flagged down). **Review the fixes in their LOCAL committed state**; the lupin push stays his (or Rick's) call — do NOT push lupin. (María's PIP doctrine IS pushed `d2d0758` + backed up to DATA02.)
- **Doctrine consistency**: I flipped all 6 surfaces + live config to LIVE FAST. Re-read each — are the LIVE banners accurate, consistent, and free of stale "not-live"/"until cutover" remnants? Does any surface still tell sessions to dual-write or rebuild the harness list?
- **Cutover actually working**: spot-check that a real session's owed-work read comes from the store (not the transcript) now that the flag is on. Does the poke fire correctly off store counts? Store-down fail-safe still safe?
- **Other overnight rush-risks**: anything built/reviewed during the Opus-instability window where reviewers stalled + were reaped (Clayton ×?, Cheech, Rio, Sam) and a manager self-verified — re-check those self-verifications with genuinely fresh eyes.

## 4. Anchor docs
- Plan of record: `src/rnd/2026.06.16-store-canonical-task-management.md` (v3 — § Cascade Review Outcome has the A–J revisions + acceptance criteria).
- Cascade synthesis: `lupin/src/rnd/v0.1.8/2026.06.16-store-canonical-task-mgmt-cascade-review.md`.
- Doctrine canonical: `workflow/task-store-discipline.md` §0 (LIVE mandate).

## 5. Coordination state
- **Mr Radio 🦉** (`f6292818`) — lupin manager/spine; stood down but reachable. Owns the lupin push (incl. `7d169442`).
- **Tiberius 👑** (`570d9733`) — lupin card lane; stood down; surfaced the two §3 findings.
- Loop them in if the review surfaces issues in their lanes (they reap/respawn workers; I don't build).

## 6. The fresh-eyes-review task (in the store — dogfooding store-only)
A store task for this review is created 2026-06-17 (owner_persona=maria). On rehydrate, `task_query(owner_persona=maria, status=open)` to find it; transition it in_progress → done WITH receipts (the review doc path) per the live receipts discipline. This is the store-only system's first real tracked task — dogfood it.

## 7. Standing reminders
- Persona = **María 🌸**, PRESERVE across /clear.
- Store-only is LIVE: write owed work to the store (`task_create`); query the store to see your list; do NOT use the native harness list as the liveness source.
- Push is Rick's call; commits/merges standing authority (green+reviewed). All current work is pushed EXCEPT lupin `7d169442` (held).
- Discard this memento after the review is delivered.
