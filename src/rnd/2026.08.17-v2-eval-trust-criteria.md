# Trust criteria for the v2 eval result — written BEFORE the numbers exist

**Author**: Rio ⚡ (seat `7659463b`) · **Row**: `05ad63d7` (P1, owner rio, manager maría)
**Written**: 2026-08-17 ~15:50 EDT · **reconciled 16:15 EDT against lupin HEAD `03ec32f2`**, **re-checked 16:55 EDT against HEAD `7fe4e472`** (branch `wip-v0.2.0-2026.08.03-present-and-demo`) — the tree moved under this doc within the hour, see the banner below and §7c — **before** John's re-run of `d8d019f6` has produced any number.
**Revised 2026-08-17 ~17:30 EDT** by Rio (seat `199bf11b`, re-spun), still before any number exists: **§5 Q4 is ANSWERED** (shared-pairs floor = 30, María's ruling under standing authority — Rick can overturn it) and **D6 now names that floor**; **§2b gains the `agent_error` producer note** — that path has no live producer on the v1 arm. Working record with the full evidence chain: store row `2ebe4ccb`.

That ordering is the whole point. Criteria written after the data get tuned, unconsciously, to admit
the data. Everything below is sourced from the plan, the cascade handoff, or the harness code as it
stands today; **anything the plan does not define is left as an open question rather than filled in
with a number I made up.**

> ⚠️ **Placement note for María.** Row `05ad63d7` says to put this in "this repo's CJ Flow v2 plan
> doc." **There is no CJ Flow v2 plan in planning-is-prompting** — I checked (`grep -rl "CJ Flow"
> src/rnd/` returns only my own audit and two unrelated 2026-04/06 docs). The plan is
> `lupin/src/rnd/v0.2.0/2026.08.14-cj-flow-v2-phases-2-3-and-evaluation.md`, and I am a
> planning-is-prompting seat, so committing into lupin is not something I can do honestly. This doc
> lands here; **a lupin-resident seat should fold it into §1 of the plan**, or you can point me at the
> repo you meant.

## Sources this is derived from

| What | Where |
|---|---|
| The two headline metrics + the ≥20% bar | plan §1.1 |
| The floors (can only fail, never win) | plan §1.2 |
| Control-logic size targets | plan §1.3 |
| What a STOP looks like | plan §1.4 |
| Sampling rule (n=60 per command, stratified, seeded) | plan §1.4a |
| Rachel's three corrections — EXECUTOR, floors-as-vetoes, p95 instability | plan §1.4b |
| R-C1 exact-hit replay signal · R-C2 would-be-wrong oracle | handoff §3.C |
| `assert_measured_sha` | `lupin/src/scripts/v1_eval_arm.py:133`, called at `:680` |
| `PROVENANCE_FIELDS`, `paired_provenance_check`, `build_paired_verdict`, `PASS_FRACTION` | `lupin/src/scripts/paired_eval.py:76, 154, 301` |

---

> # ✅ THE §1a SHA GAP IS CLOSED — fixed in lupin `c9b43538`, verified at HEAD `7fe4e472`, 2026-08-17 ~16:55 EDT
>
> **Read this before §1a, §1a′, §1b, §4b or §8 — all five describe a state that no longer exists.**
> The argument is preserved below because it is what earned the fix, and because the reasoning outlives
> the defect. **The defect itself is fixed, and fixed more thoroughly than I asked for.**
>
> | what §8 asked for | what landed |
> |---|---|
> | `git_sha` into `PROVENANCE_FIELDS` | ✅ `paired_eval.py:80` — a missing sha is now a **refused missing field**, not a rendered dash |
> | the v2 arm reads its own sha | ✅ `v2_eval.py:1100` — reads it and **raises** when blank; the arm had no check at all before |
> | the emptiness guard reads the outcome count | ⏳ still open (§7b) |
> | *(the follow-on trap I flagged)* — "render the refusal, not a placeholder" | ✅ `_sha()` returns **`⚠ NOT RECORDED`**, and its docstring names my exact ambiguity: *"a reader had no way to tell 'no pin by design' from 'never checked'"* |
> | *(not asked for — they went further)* | ✅ `_is_recorded_sha` (`:85`) refuses a **present-but-EMPTY** sha, because `read_running_server_sha` returns `""` when the server does not answer — **blank is the shape a skipped read actually takes**. I said add the key; they also closed the hole the key alone would have left |
> | *(not asked for)* | ✅ the provenance table stamps a sha for **BOTH arms**, not v1-only |
>
> **The one thing I got to watch happen**: §1a′ said moving one string into one tuple converts an
> invisible absence into a refusal that names itself. That is precisely what `c9b43538` did — and then
> it went and found the blank-string variant I had not thought of.
>
> ⚠️ **This banner exists because the alternative is that this document becomes the stale prose in
> exactly the plan-versus-code split it was written to expose.** The tree stamp in the header now
> points at a tree that has moved. **A doc arguing for provenance does not get to skip its own.**

## 1. The sample signature — what a result must carry to be a result at all

A number missing any of these is **not a weak result; it is not a result.** Do not interpret it, do
not put it in a table, do not average it with anything.

**Already bound by the harness** — `PROVENANCE_FIELDS` (`paired_eval.py:76`), compared field-by-field
by `paired_provenance_check` (`:154`), which `build_paired_verdict` runs **first** and refuses on:

| field | what it pins |
|---|---|
| `arm` | which arm this stamp describes |
| `corpus` | the utterance source both arms drew from |
| `seed` | so a re-run is the same sample (plan §1.4a) |
| `n_per_command` | the stratification width |
| `sample_signature` | the authoritative binding — computed from the sampled pairs themselves |
| `sampled_n` | how many utterances the arm **intended** to measure (`len( sampled_pairs )`) — ⚠️ **not** how many it actually measured; `0` on either arm is refused, but that guard is measuring the wrong subject — see §7b |

### 1a. 🔴 The auditability gap, stated precisely

> **Correction to my own first draft.** I wrote that the observed sha "never reaches the stamp." That
> is wrong, and the truth is more interesting: **it reaches the stamp but not the contract.**

**What is actually true, verified line by line:**

| # | fact | evidence |
|---|---|---|
| 1 | The v1 arm **does** read the sha back from the running server and **raises** on a wrong tree | `assert_measured_sha( read_sha_fn( base_url ) )`, `v1_eval_arm.py:680` |
| 2 | That observed sha **is** carried into the v1 provenance dict, deliberately — the code comment insists it is the OBSERVED value, "never `V1_PIN_SHA`," because stamping the constant would read `b0735467` no matter what actually ran | `provenance[ "v1_arm_git_sha" ] = observed_sha` |
| 3 | It **is** rendered in the report, as a `v1 arm sha` row | `render_provenance_block`, `paired_eval.py` |
| 4 | **But `v1_arm_git_sha` is not in `PROVENANCE_FIELDS`** — that tuple is `arm, corpus, seed, n_per_command, sample_signature, sampled_n` | `paired_eval.py:76` |
| 5 | So `_missing_provenance_fields` **cannot report it absent** (it iterates `PROVENANCE_FIELDS`) and `paired_provenance_check` **never compares it** (it loops four binding fields only) | `paired_eval.py:154` |
| 6 | The v2 arm reads **no sha at all** — `grep` for `code-identity`, `git_sha`, `measured_sha` across `v2_eval.py` returns **nothing**. It stamps `make_provenance( "v2", … )` and writes `{metrics, provenance}`: six fields, no tree identity | `v2_eval.py:1021, 1048` |
| 7 | **There is no v2 pin to check against even if someone wired the read.** `V1_PIN_SHA` (`v1_eval_arm.py:105`) is the only pin in `src/scripts` | `grep '_PIN_SHA ='` |
| 8 | The asymmetry is **deliberate, not an oversight** — the render's own comment reads "v1-only (the v2 arm has no pinned-tree constraint), so v2 renders `—`" | `render_provenance_block` |

**Why the combination is worse than either half.** The sha is **rendered but not required.** A run that
never called `assert_measured_sha` produces a stamp with no `v1_arm_git_sha`, passes
`paired_provenance_check` intact, and renders `—` in the sha row. **A missing sha and a legitimately
absent one are the same character on the page.** Nothing downstream can tell a reader which one they
are looking at — and answering "which tree produced this number" is the entire reason that column
exists.

**The deliberate choice has also expired.** Pinning v1 alone was right when v1 was the **baseline being
protected** — you pin what you measure *against*. **v2 is now the thing under evaluation**, and it is
the arm whose code changed five times today (John's five fixes, each on a wip branch). The arm most
likely to be running a different tree than anyone believes is the one with no check at all.

⚠️ **One thing I checked and did NOT find**, so it does not become a rumour: v2's `make_provenance` call
passes a local named `corpus`, which reads like the full corpus — but it is the output of
`stratified_sample` two lines above (`v2_eval.py:1019-1021`), the same sampler v1 uses. **Both
signatures are computed over the same population. There is no signature bug here.**

### 1a′. Would a missing sha be CAUGHT? Walk it through — the answer is no, at every seam

The gap above is easy to read as *"the sha is weakly covered."* It is not weakly covered. **Nothing
catches it.** Below is the actual case — a run whose sha read never happened — traced against every
check the pipeline has, today.

| # | check | does it catch a missing sha? | why |
|---|---|---|---|
| 1 | `assert_measured_sha` | **only if it is CALLED.** The failure mode *is* that it was not | It raises on a wrong or empty sha — but a call that never happens cannot raise. This is a guard on the value, not on its own invocation |
| 2 | `_missing_provenance_fields` | ❌ **NO** | It iterates `PROVENANCE_FIELDS`, and `v1_arm_git_sha` is not in that tuple |
| 3 | `paired_provenance_check` | ❌ **NO** | It compares four binding fields — `corpus`, `seed`, `n_per_command`, `sample_signature`. The sha is not among them |
| 4 | `build_paired_verdict` | ❌ **NO** | It refuses only when check 3 returns a reason; check 3 returned `None` |
| 5 | The gate itself | ❌ **NO** | It computes a median over spans. A number's arithmetic is unaffected by which tree produced it |
| 6 | `render_provenance_block` | ❌ **NO — this is where it becomes invisible** | `v1_provenance.get( 'v1_arm_git_sha', '—' )`. **The `—` default is the fallback for a legitimately-unpinned arm, and it renders identically for an arm whose check never ran** |
| 7 | The v2 arm, any check at all | ❌ **NO** | v2 reads no sha, so there is nothing to be missing — the column is blank by design and blank by failure at the same time |
| 8 | A human reading the report | ❌ **NO, and this is the point** | The row is present, the table is complete, the provenance line says **MATCH**. Nothing on the page distinguishes *"we checked, this arm has no pin"* from *"we never looked"* |

**Verdict: a run with no sha read produces a report that is indistinguishable from a correct one.**
Not a weaker report, not a caveated report — **identical output.** It renders a dash, passes the
provenance check, prints **MATCH**, and emits a median-Δ verdict. There is no seam at which the
absence surfaces.

**After the fixes in §1b, check 2 catches it** — `v1_arm_git_sha` and `v2_arm_git_sha` inside
`PROVENANCE_FIELDS` means a stamp without them is a **missing field**, which `_missing_provenance_fields`
names and `build_paired_verdict` refuses on, **before any latency number is emitted.** That is the
whole value of moving one string into one tuple: it converts an invisible absence into a refusal that
names itself.

⚠️ **And the dash must not survive the fix.** If the shas become required and the renderer still prints
`—` on absence, the report will show a blank next to a run that was refused — the same ambiguity one
layer down. **Render the refusal, not a placeholder.**

#### The family this belongs to

This is not a novel failure. It is the fourth instance this project has recorded of **incomplete
presented as complete**, and the three siblings are all in the store:

| receipt | the shape |
|---|---|
| `f3beb6d5` — *"Test-suite report says ALL PASSED — e2e for a 5-of-N run"* | a partial run rendered as a full green |
| Mr Radio's retraction on `92062fe2` — wrote **"scheduled"** as though it meant **"ran"** | an intention rendered as an outcome |
| `paired-run-latest` holding **v1=3 / v2=2** on a live-sized run | stale unit data rendered as this run's result |
| **this one** — a `—` in the sha column | an unperformed check rendered as a checked-and-empty one |

**The common mechanism, worth naming once**: in every case the *absence of evidence* was rendered in
the same visual slot as *evidence of absence*, and the reader had no way to tell them apart. A blank
that means "not applicable" and a blank that means "never ran" must not be the same character.

### 1b. What the stamp must carry to be auditable

The test is not *"did the run check itself."* It is **"can someone six weeks from now, holding only the
artifact, say which trees produced this number and where it ran."** Today that answer is no for both
arms: v1's sha is present but unenforced, v2's was never read.

| field | today | required |
|---|---|---|
| `arm`, `corpus`, `seed`, `n_per_command`, `sample_signature` | ✅ in the contract, compared, refused on mismatch | keep |
| `sampled_n` | ⚠️ in the contract, but it counts **intent** — its `== 0` emptiness guard cannot detect an arm that measured nothing (§7b) | keep it as the denominator, and add the outcome count beside it |
| `ok_n` / `n_ok` (the outcome count) | ❌ lives in `metrics`, not in the stamp — so the pairing check never sees how much either arm actually measured | **add to the stamp**, and make the emptiness guard read it |
| `v1_arm_git_sha` | ⚠️ stamped and rendered, **not** in the contract | **move into `PROVENANCE_FIELDS`**, so absence is a refusal rather than a dash |
| `v2_arm_git_sha` | ❌ does not exist | **add** — same `assert_measured_sha`, second call site, against a v2 pin that must also be created |
| `venue` | ❌ does not exist | **add** the `base_url` each arm actually talked to, so a side-doored arm cannot pass as a monopolize run |
| `run_id` | ❌ not stamped | **add** the submitting job id, so an artifact can be tied to the run that claims it — this is what makes D8 detectable |
| `completed` | ❌ not stamped | **add**, written at dump time. An artifact with no completion marker is a partial run, which is D2 |

**Rule**: a result is auditable only if its stamp carries all six existing fields **plus both arms'
observed shas, the venue, the run id, and a completion marker** — and the arms agree on every field
that must match. **A field that is rendered but not required is decoration.**

---

## 2. The numbers the plan actually needs

Named precisely, with their source. **`PASS_FRACTION = 0.20` is the only threshold in code**
(`paired_eval.py:80`); everything else below is a comparison, not a margin — which is itself a finding
(see §5).

### 2a. The two headline metrics (plan §1.1)

| metric | the number | bar |
|---|---|---|
| **Latency** | paired median Δ (v1 − v2), positive ⇒ v2 faster, over the shared utterances | Δ ≥ **20%** of the v1 median. This is the gate that fires or declines |
| **Cache-hit rate** | warm-pass hit rate, **absolute number for each arm**, not just the delta | v2 ≥ v1 on the same corpus |

Two qualifiers that must travel with those numbers:

- **The instrument string prints in the verdict table, not in prose** (Cheech's ruling, carried into
  `INSTRUMENT` at `paired_eval.py:85`). v1's span **includes its full FIFO queue-dwell**; v2 is one
  synchronous round-trip with no queue. A reader who takes `delta_ms` for a compute-only difference
  has been misled, and the caveat has to sit next to the number to prevent it.
- **A hit is a tier-1 exact match** via `question_normalized` string equality (`find_exact_normalized`,
  handoff R-C1) — **not** a float comparison against `100.0`. The float path understates the very
  number the experiment exists to measure.

### 2b. The floors — vetoes, not scores (plan §1.2, §1.4b(2))

Any one at FAIL forces **STOP** regardless of how good the headline numbers look, and the report must
print an `OVERALL` line stating `INVEST` / `STOP` / `SPLIT — port the shape` **computed**, so the
decision is printed rather than inferred.

| floor | bar |
|---|---|
| Routing accuracy | v2 ≥ v1 on the shared corpus |
| Replay correctness | hand-checked sample of cache hits, **≥ 30 utterances**, **zero** answers to a materially different question |
| No new silent failures | `router_error`, `extract_error`, `replay_failed`, `agent_error` each appear in the trace with a **distinct** value |
| v1 untouched | v1's existing suites green, `expedite()` output unchanged |

> ⚠️ **`agent_error` on the V1 ARM HAS NO LIVE PRODUCER — added 2026-08-17 ~17:30 EDT, verified in
> lupin, refutable by one call site.** The v1 arm tags `agent_error` when completion metadata carries a
> non-empty `error` (`v1_eval_arm.py:330`), but nothing real ever hands it that: the arm captures
> metadata **only** on a `"completed"` transition (`:486`), while the one emitter carrying a real error
> message rides `FAILED` (`running_fifo_queue.py:867`, emitted at `:880`). All five COMPLETED emitters
> — plus the STALLED one — hardcode `error = None` (dicts at 599, 713, 1416, 1622, 1698, 1906). A v1
> failure therefore lands `ok=False, failure="no_completion"`, never a tagged success.
>
> **Two consequences when reading a v1 report.** (1) The floor above gates **v2's redesign**, not v1's
> baseline — v1's degradation column is informational, and an empty `agent_error` there is expected,
> not a defect. (2) If a v1 artifact **does** show `agent_error`, either its input was synthetic or a
> producer exists that this sweep missed — **find the call site before believing the number**. Refute
> this note with a COMPLETED emitter carrying a non-empty `error`; a fixture does not refute it. Both
> Krishna and Tiberius advanced the opposite claim from hand-built input and withdrew it independently
> once the emitters were read.

**The replay-correctness floor is human-executed and the harness must not pretend otherwise.** Every
row carries an `EXECUTOR`. R-C2 (matched snapshot's `routing_command` ≠ the utterance's expected
command) is a **one-way pre-filter that can only ever FAIL a row** — agreement does not pass it,
because the same command with a different city is still the wrong answer. The floor stays
`EXECUTOR: HUMAN` and unchecked rows print `PENDING HUMAN`, never a checkmark. R-C2 is a **lower
bound** and under-counts; keep that caveat verbatim.

### 2c. Control-logic size (plan §1.3)

`ast` walks both request paths and counts — a real number, not a vibe, and falsifiable in the
direction that matters:

| quantity | v2 target |
|---|---|
| statements on the request path | ≤ **200** |
| distinct command→agent mechanisms | **1** |
| cache lookups per request | **1** |
| places a request can block on a human | **0** |

If v2's flow lands over 200, **the "minimal control logic" claim failed**, whatever the latency did —
say so in the report rather than quietly widening the target.

### 2d. The decision rule (plan §1.4)

STOP requires **both**: not meaningfully faster (§1.1 margins unmet) **and** not meaningfully smaller
(§1.3 unmet). Either alone earns a conversation. And the pre-registered better-than-a-win case:
**not faster but much simpler ⇒ port v2's structure onto v1** rather than keeping two flows.

---

## 3. Disqualifiers — the shapes that mean "re-run, do not interpret"

Each of these has a receipt in this project's own history. None is hypothetical.

| # | shape | how to detect it | why it is on this list |
|---|---|---|---|
| **D1** | **Unit-sized numbers on a live-sized run** | **`ok_n` (v1) / `n_ok` (v2)** — the OUTCOME count — in the single digits when the run declared n=60 per command (~480/pass). ⚠️ **Not `sampled_n`**, which is the intended count and reads 480 for an arm that measured twelve — see §7a | John, this morning: `paired-run-latest` "holds STALE unit data v1=3/v2=2 — never trust it without a `sample_signature` match **and** `ok_n`/`n_ok` in the hundreds" |
| **D2** | **Died before the dump** | no artifact written for one or both arms; the latest file predates the run | the N=60 closer ran 3h02m and died on a `ReadTimeoutError` in the v2 arm **before** `_dump` — nothing recoverable. A file being present is not evidence it belongs to this run |
| **D3** | **Provenance mismatch** | `build_paired_verdict` returns `provenance_ok: False` | already handled correctly: it refuses and emits **no latency number** |
| **D4** | **Wrong tree** | observed sha ≠ pinned sha on either arm | `assert_measured_sha` raises for v1; **v2 has no equivalent today** (§1) |
| **D5** | **Split venue** | the two arms did not run in the same venue | nothing checks this today (§1) |
| **D6** | **Zero or thin overlap** | `n_shared` **< 30** (María's floor, 2026-08-17 — see §5 Q4), or `n_dropped` a large share of `sampled_n` | the gate declines at zero but has **no minimum** in code — proven by execution: `v1 {u1:1000.0}, v2 {u1:100.0}` → `fired=True, verdict=PASS, n_shared=1`. A go/no-go PASS over one utterance. The floor is a ruling awaiting Rick, not yet a guard in the harness |
| **D7** | **A stale file read as fresh** | file mtime used as proof of belonging | the trap D1 actually sprang. Freshness is a `sample_signature` match, never a timestamp |
| **D8** | **"Scheduled" reported as "ran"** | a verdict asserted from a submission rather than a completion | Mr Radio's own retraction on `92062fe2`: he wrote "scheduled" as though it meant "ran," and Sam found the truth the next morning |
| **D10** | **Selection bias — the survivors are not a random sample** | a per-arm failure rate materially above zero, with the failures **clustered** (by time, by `seq`, or by corpus block) rather than scattered | **ruled by María 2026-08-17 19:10 EDT, and observed live the same minute on `ts-95e3c93b`** — see §4a. The gate pairs only surviving utterances, so a clustered failure silently deletes whole regions of the corpus from one arm. **Report the per-arm failure rate NEXT TO the delta and refuse the verdict outright** |
| **D9** | **The validity guard weakened alongside the cold-start assert** | `assert_cold=False` **is** ruled legitimate (Tiberius, `d8d019f6`, 2026-08-17: the Δ is warm-vs-warm and any deterministic cache effect cancels in the pair) — but store isolation and the stale-store validity check must still be **intact** | one ruling dropped one orthogonal assertion; it is not licence to run without the contamination guard |

---

### 4a. Selection bias, measured on `ts-95e3c93b` — the failures are CATEGORICAL, not random

**María's ruling, 2026-08-17 19:10 EDT**: `ts-95e3c93b`'s output is **DIAGNOSTIC, never go/no-go** —
*"recorded now so nobody reads a verdict off it later."* Her reasoning: killing the run costs the
evidence of why a third of v2 asks are 500ing, and **that failure rate is a bigger finding than the
latency delta the run was meant to produce.** She also killed the hope that the floor of 30 protects
us here — at 32% failure you still clear ~300 shared pairs, so the gate fires happily over a
two-thirds sample.

**What the attempts log actually shows** (read at 19:12 EDT from `lupin/io/v2-flow/ask-attempts.jsonl`,
255 completed requests, 83 failed = 32.5%). The failures are **not scattered**, and that is worse than
if they were:

| `seq` block | failed / total | what the block contains |
|---|---|---|
| 0–49 | 5 / 48 | to-do list operations |
| 50–99 | 5 / 50 | |
| 100–149 | 2 / 50 | math / geometry mode |
| 150–199 | 19 / 50 | |
| **200–249** | **47 / 50** | **mode-switch + routing commands** |
| 250–299 | 5 / 7 (partial) | general Q&A |

**The corpus is ordered by category, so a time-clustered failure deletes whole CATEGORIES from one
arm's sample.** This is sharper than "the easy ones survived": seq 200–249 — *"Stop direct mode and go
back to normal," "Actually, just let the system decide," "Stop using the calendar agent"* — is 94%
gone from v2.

🔴 **This reaches a FLOOR, not just the headline delta.** Routing accuracy (§2b: *v2 ≥ v1 on the
shared corpus*) is computed over `ok` records only. If v2's mode-switch block is 94% absent while v1's
is intact, **the two arms' routing accuracy is measured on differently-composed corpora** — and the
arm missing its hardest routing category is the one that looks better. Report routing accuracy with
its own per-arm surviving-`n` per category, or do not report it.

**Two supporting numbers, and the honest caveat on each.** Failed utterances are shorter (median 36
chars vs 55) — but that is **confounded with position**, since the corpus is category-ordered, so it
is evidence about *which block* failed, **not** that short questions are fragile. Failed spans are
fast (median 961 ms vs 10,411 ms), so failures do not distort the surviving median directly; they
distort it by **removing** rows.

**Escalating, not flaky**: 10% → 10% → 4% → 38% → **94%**. The endpoint is degrading as the run
proceeds rather than failing at random, so a rerun that starts healthy proves nothing about its tail.

## 4. What should RAISE, not report

**A note telling readers not to trust a file is not a guard.** John's warning about `paired-run-latest`
is correct and will be forgotten by the third reader. These are the criteria that should fail the run
rather than appear in its output:

### 4a. The three seams, and which behaviour belongs at each

A criterion is not simply "an assertion" or "a report" — **where it fires decides whether it can be
ignored.** Three seams, three different correct behaviours:

| seam | what it is | correct behaviour | why |
|---|---|---|---|
| **Measurement boundary** — inside an arm, at the moment it touches the live server | `assert_measured_sha` sits here | **RAISE, before spending.** | The v1 arm proves the tree *before* running two passes. An integrity failure caught after three hours of measurement has already cost the run |
| **Pure comparison** — `paired_provenance_check`, `build_paired_verdict` over two artifacts | no I/O, no side effects | **RETURN A REFUSAL DICT.** Never raise | Correct as built. A pure function over two dicts should be testable without catching exceptions, and its refusal string names *every* disagreement rather than dying on the first |
| **Run entry point** — whatever submits the job and writes the exit code | the `main()` a human or the scheduler invokes | **EXIT NON-ZERO on any refusal** | This is the gap. A DECLINE that leaves a green exit code reads as a pass to everything automated downstream, and to every human skimming for red |

**That middle row is why "make it raise" is the wrong instruction and I am not giving it.** The
library is right. The failure is that **nothing converts its refusal into a failed run.**

### 4b. The criteria, and what each must do

| criterion | today | must be | seam |
|---|---|---|---|
| Observed sha, **v1 arm** | ✅ raises before the passes run — **but only when it is called; a skipped call is caught by nothing, see §1a′** | keep, and make its absence detectable downstream | measurement |
| Observed sha, **v2 arm** | ❌ nothing at all | **raise** — same function, second call site, plus a v2 pin that does not yet exist | measurement |
| Both shas **in `PROVENANCE_FIELDS`** | ⚠️ v1's is stamped but unenforced; v2's absent | **refuse** on a stamp missing either — this is what turns a rendered dash into a detectable fault | comparison |
| Venue recorded, and matched across arms | ❌ nothing | **refuse** on absence or mismatch | comparison |
| `run_id` + `completed` in the stamp | ❌ nothing | **refuse** on absence — an artifact that cannot name its run, or never recorded finishing, is D8 and D2 | comparison |
| Provenance binding (corpus / seed / n / signature) | ⚠️ refuses correctly, but the refusal dies in a dict | **exit non-zero** on `provenance_ok: False` | entry point |
| Reading `paired-run-latest` | ⚠️ a note in a doc | **assert** the file's `sample_signature` equals this run's before reading a single number out of it | wherever the read happens |
| Sampling plausibility — did we DRAW what we declared | ❌ nothing | **raise** when `sampled_n` is not what the declared sampling rule implies | measurement |
| Outcome plausibility — did we MEASURE what we drew | ❌ nothing | **raise** when `ok_n`/`n_ok` is a small fraction of `sampled_n`. **This is the one that catches D1**, and it is a different subject from the row above — a run can draw 480 correctly and measure twelve | measurement |
| Floors → `OVERALL` | ⚠️ §1.4b(2) requires it; nothing computes it | **compute and print it.** Floors failing while the headline rows pass, with nothing saying STOP, shows the reader green | report |

### 4c. The rule underneath all of it

**A note telling readers not to trust a file is not a guard.** John's warning about `paired-run-latest`
is correct, was written by the person who found the problem, and will be forgotten by the third
reader — because it lives in a doc while the file lives on disk, and only one of those is in the path
of someone in a hurry.

The same shape explains the sha gap: a value that is **rendered but not required** is a note wearing
a table's clothing. **Every criterion on this page is either enforced by something that fails, or it
is decoration.** There is no third state, and "documented" is not one of them.

---

## 5. Open questions — where the plan does not define a number it needs

Standing rule: **no guessed numbers sitting among measured ones.** These are for María to rule on
**before** the re-run lands, not for me to fill in.

**Q1 · Does the p95 bar still exist? — ✅ RULED 2026-08-17: RETIRED as a gate.**

**The paired median Δ carries the latency decision. p95 is reported, never gating.** Ruled by María
(owner of `92062fe2`) on Cheech's evidence; **Mr Radio's third vote is pending** and is recorded here
as pending rather than assumed.

**This is a doc correction, not a design change** — which is why it can be settled before the numbers
land without violating anything this page stands for. The harness has never gated on p95, and says so
in its own words in several independent places:

| where | words |
|---|---|
| harness design doc §1.5 | *"p95 REPORTED, not GATING — labelled `INFORMATIONAL (n too small to gate)`"* |
| `paired_eval.py:19` (module docstring) | *"p95 is REPORTED, never GATING (at n=60 one tail outlier moves it — design §1.5)"* |
| `paired_eval.py:236, 292` | *"INFORMATIONAL (never gating)"* |
| `paired_eval.py:358` (the rendered line) | *"p95 (INFORMATIONAL, n too small to gate)"* |
| `v1_eval_arm.py:625` | *"(INFORMATIONAL, n too small to gate)"* |

**Sharper than "plan versus code": the contradiction is inside the plan.** Those code sites cite
**design §1.5** as their authority — so the plan's own §1.5 already retired the p95 gate while §1.1's
prose kept it. **§1.1 is the stale half**, and nothing needs to change but the sentence.

🔴 **Cheech's second point, recorded explicitly because it is the one that gets quietly folded in
later**: if anyone wants a p95 gate, it needs a **pre-registered margin and a bigger n**, and that is a
**SEPARATE decision** from unblocking this run. **Adding a tolerance now — after the design is built,
before John's numbers land — is exactly the after-the-fact tuning this document exists to prevent.**
Retiring a gate the harness never implemented removes a contradiction. Adding one it never had, in the
same breath, would be writing a new criterion under cover of a correction.

**Q2 · Do the `v2 ≥ v1` floors mean literally ≥, at n=60?** Routing accuracy and cache-hit rate are
both written as bare comparisons while latency got a pre-registered 20% margin. At 60 utterances per
command a **single** utterance flips a bare ≥. The plan itself argues the case against direction-
without-margin: *"'Faster' is not a result… noise wearing a number's clothes."* The same objection
applies to `≥` here. Either the ≥ is deliberate (a floor is meant to be strict) or these need margins.
**A margin invented now, after the design is built, is exactly what this document exists to prevent.**

**Q3 · How is the ≥30-utterance replay-correctness sample drawn?** The floor names a size but not the
draw. If it is not seeded from the same sample the arms measured, the hand-check is auditing a
different population than the hit-rate number it is supposed to qualify.

**Q4 · What is the `n_shared` floor for D6? — ✅ ANSWERED 2026-08-17 ~17:20 EDT: 30.** The gate declines
only at *zero* shared pairs. A run that pairs 12 of 480 fires and produces a real-looking number. The
plan did not say how thin is too thin, and I would not pick a fraction — **María did**, ruling a
minimum of **30 shared pairs against the 480 sample**, taken under standing authority after a
timed-out ask to Rick.

> **Record the authority with the number.** This is a manager's ruling, **not a finding** — Rick can
> overturn it, and it should be re-checked with him rather than inherited as settled fact. Tiberius is
> building its test: he reports `n_shared` of 1 and 29 currently passing when they should fail, 30 and
> 480 passing correctly.

---

## 6. Reconciliation — the p95 decision now lives in three places

Read at **lupin HEAD `03ec32f2`**, branch `wip-v0.2.0-2026.08.03-present-and-demo`, 2026-08-17 ~16:15 EDT.

| source | operative sentence |
|---|---|
| **lupin plan §1.1**, after Cheech's `0d64acf9` | *"v2 p50 must beat v1 p50 by **≥ 20%**. ~~and v2 p95 must not be worse than v1 p95~~ — **p95 RETIRED as a gate**"* — and §1.4b: *"**SUPERSEDED 2026-08-17 — p95 is REPORTED, never GATING.**"* |
| **this doc**, §5 Q1 | *"The paired median Δ carries the latency decision. p95 is reported, never gating."* |
| **the harness**, `paired_eval.py:19` | *"p95 is REPORTED, never GATING (at n=60 one tail outlier moves it — design §1.5)."* |

**Verdict: consistent. No difference a reader could act on.** All three retire the gate, all three name
the paired median Δ as the replacement, and all three record Mr Radio's third vote as pending rather
than implying it. **This is a clean negative and I would rather report it than manufacture a finding.**

**Three sub-checks, all clean:**

1. **No p95-as-bar survives anywhere in the plan.** Every occurrence at HEAD (lines 35, 43, 53, 178,
   191) either strikes it or marks it informational. Cheech struck it rather than deleting it, so a
   reader sees the correction instead of wondering whether p95 was ever a gate.
2. **Both authority citations resolve.** The code cites *"design §1.5"*; the plan's correction cites
   *"§1.4b(3)"*. I checked both rather than assuming one was dangling: §1.5 is *"What the harness must
   print"* and its table carries `latency p95 … informational … (n too small to gate)`, while §1.4b(3)
   is Rachel's correction. **Two different real sources, both saying it.**
3. **The plan has not moved under me otherwise.** `0d64acf9` is the *only* commit touching that file
   today, so §1.2/§1.3/§1.4 are the text I derived §2 from.

⚠️ **One undercount, in our favour.** Both María's brief and Cheech's commit say the harness states it
in **two** places (`:19` and `:292`). I find **five** in code — `paired_eval.py:19, 236, 292, 345, 358`
— plus `v1_eval_arm.py:625` and the harness design doc §1.5. The conclusion is unchanged and better
supported; noted only so nobody later "corrects" the count downward.

## 7. Turning Cheech's frame on this document

Cheech's testimony named the failure better than "stale data" does: **his measurement was fresh and
correct — he measured the CONFIG FILE and concluded about the LIVE SPAWN. Right answer, wrong
subject.** That is a sharper test than staleness, because a fresh, accurate measurement of the wrong
subject leaves no trace of being wrong.

Applied to this document, it finds two things, and one of them is mine.

### 7a. 🔴 D1's detector measures intent, not outcome — my error, and John already had it right

**My D1 said**: detect a unit-sized run by *"`sampled_n` in the single digits when the run declared
n=60 per command."* **`sampled_n` cannot answer that question.** It is stamped as
`len( sampled_pairs )` (`paired_eval.py:141`; v1 at `:695`) — **the size of the list the arm intended
to measure**, computed from the sampler's output, never from the records that came back. An arm that
sampled 480 utterances and successfully measured twelve stamps **`sampled_n: 480`.**

**The right subject is the outcome count**: `n_ok` in v2 (`v2_eval.py:421`, `len( ok )`), `ok_n` in v1.
**John's original warning named exactly those** — *"never trust it without a `sample_signature` match
**and** `ok_n`/`n_ok` in the hundreds."* He had the right subject; I quoted his finding and then wrote
a detector against the wrong field. **D1 is corrected to read `ok_n`/`n_ok`, with `sampled_n` as the
denominator it should be compared against.**

### 7b. 🔴 The same wrong subject is in the harness — `sampled_n == 0` is not an empty-arm check

`paired_provenance_check` refuses when *"either reports `sampled_n == 0` (an empty arm cannot be
paired)"* (`paired_eval.py:164, 181-183`). **That guard cannot detect an empty arm.** `sampled_n` is
the intended count; an arm that drew 480 utterances and returned **zero** successful records reports
`sampled_n: 480`, passes the guard, and proceeds to the gate. The one arithmetic protection against a
totally failed arm is measuring the wrong subject.

**Recommended lupin-side change** (I cannot commit it — see §8): make the emptiness guard read the
outcome count. Because the two arms use different key names, it must read `ok_n` for v1 and `n_ok`
for v2 — **and that asymmetry is itself worth removing**, since it is what produced the `KeyError`
that destroyed the v1 arm's data on `ts-217961e6` this morning.

### 7c. Where this document has the right subject, and where I must state my own

**The sha check is the good guard precisely because it has the right subject**: `read_running_server_sha`
asks the **live process** what it booted from, and its docstring is explicit that reading the
launcher's env or config instead would be the error. That is the pattern the rest should copy.

**And this document must apply it to itself.** Every claim in §1a, §4 and §7 is derived from **source
files at lupin HEAD `03ec32f2`** — that is a measurement of a *tree*, and every statement about what
"the run" does is therefore a conclusion about a *different subject* unless the run's own sha matches.
**Stated rather than hidden**: these are claims about the code as committed, not about the process that
executed this morning — whose tree nobody recorded, which is the whole finding of §1a′. A doc arguing
for provenance stamps must carry one, so this is that stamp.

## 8. What is owed to lupin, and by whom

I am a planning-is-prompting seat and **cannot commit into lupin honestly**. Three changes belong
there, written out so whoever picks them up does not have to re-derive them:

| # | file | change |
|---|---|---|
| 1 | `paired_eval.py:76` | ✅ **DONE in `c9b43538`** — landed as a single `git_sha` field, plus a blank-string guard I had not asked for |
| 2 | `paired_eval.py:181-183` | ⏳ **STILL OPEN** — the emptiness guard reads the **outcome** count (`ok_n` / `n_ok`), not `sampled_n` (§7b). This is the one that did not land |
| 3 | `v2_eval.py` | ✅ **DONE in `c9b43538`** (`:1100`) — the v2 arm reads its own sha and raises when blank |

**Routing note**: Cheech is over budget and re-spinning, so these go to whoever is live — check before
sending rather than addressing a seat by name.

## 9. The one-line version

**A v2 eval number is trustworthy only if it carries a full sample signature — including a sha read
back from the running server on BOTH arms and the venue — was produced by a run that dumped its
artifacts, and is read out of a file whose signature matches this run. Everything else on this page is
that sentence with its receipts.**
