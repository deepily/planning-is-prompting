# Board noise-sweep list — clear the decks for the sole P0 (M1 demo)

**By**: María 🌸 (`d3f402d0`), workflow steward
**For**: the converged report to Rick (Mr Radio drafts; I get final edit) + the P0-focus directive 2026-07-17
**Method**: 68 non-terminal rows (`task_query unscoped_audit=True`), classified from **titles + Mr Radio's published C1–C8 catalog + today's rulings**. Bodies NOT re-read for all 68 — same limit Mr Radio names. **Any row marked low-confidence = confirm its body before acting.**
**Companion**: Mr Radio's catalog `lupin/src/rnd/v0.1.9/2026.07.16-task-board-state-classification-for-workflow-analysis.md`

---

## 0. THE ONE RULE THIS LIST OBEYS — the untouchable set

Nothing in the demo path gets dropped, demoted, re-homed, or re-titled. **If in doubt, KEEP.** The demo ships on a slightly noisy board before it ships late because we tidied its dependency out from under it.

**NEVER TOUCH:**
- `df3f80c9` — THE DEMO (wire `run_pipeline.py` end-to-end).
- Its `blocked_by` / dependency chain.
- The 4 sibling-file items the demo consumes: `panel.py` (Tiberius) · `rubric.py` (Krishna) · `judge_registry.py` (Rachel) · and Sam's own `judge_client.py` seam.
- Any skills-distillation row that names the M1 demo surface (`run_pipeline`, `panel`, `rubric`, `judge_*`, `verdict.json`, `overlap.py`, the F/E/M-set derivations) — **HELD until Sam confirms the build is underway.** Dropping these mid-build is friendly fire on the P0.

---

## 1. THE SEQUENCING — why this isn't a 40-row purge right now

Two reasons this list is a **map for the converged report**, not a solo bulk-execute:
1. **Sam's board goes ACTIVE the moment Mr Radio tells him to build.** Mutating skills-distillation rows while he wires the pipeline risks yanking his context. → skills-distillation subset is **HELD**; execute signal = Mr Radio's ping that Sam confirmed build-underway.
2. **Cross-persona drops are a shared-surface mutation, and Rick floated editing the table himself.** The honest move (this morning's lesson, both directions) is to hand Rick + Mr Radio the evidence-backed map and execute together on a 30-second nod — not to reshape 40 of other people's rows solo while Rick's on a walk.

**Executed NOW (self-owned, evidence-backed, zero collision):** only clearly-mine dead rows. **Everything else: listed, awaiting the converged nod.**

---

## 2. DROP — the row can never reach `done`; the finding belongs elsewhere (C6 / C5)

These are durable findings or duplicates filed as tasks. Mr Radio's C6: "filing a finding as a task guarantees an item that grows the board forever." Re-home to doctrine/memory/a retro, then drop the row.

| Row | Why | Action | Owner | Confidence |
|---|---|---|---|---|
| `6af1d362` DM transport leaks (dm_list scoping) | **Duplicate** of Sam's `2565956b` (same defect, two seats). C5. | **DROP mine**, keep Sam's as the single row | **mine** | HIGH — dedup |
| `5a8aa45b` "a title cannot carry a standing order" | Record, not a task — a terse-query design finding. C6. | RE-HOME → doctrine note, drop row | mixed | MED |
| `74a3ff4d` offline-user rule prescribes unbuilt `defer_to_chase` | Doctrine defect record. C6. | RE-HOME → workflow/ defect log, drop | mine | MED |
| `74c16374` spawn-brief defect (scoped cmd + unscoped obligation) | Record. C6. | RE-HOME → spawn-brief doctrine, drop | mixed | MED |
| `73f397d2` · `2ce26609` pattern-watch rows | Pattern-watches, not owed work — belong in a session-start "pattern watch" list per the accumulate-before-graduating rule. C6. | RE-HOME → TODO pattern-watch, drop | mine | HIGH |
| `6f8adc13` classifier wedge — fix is harness-config, no seat can act | Record; fix outside any seat's reach. C6. | RE-HOME → bug log, drop | lupin | MED |
| `c191be39` Stop-hook oracle phantom "2 in-progress" | Real defect but a RECORD — and note it's the same oracle that poked ME "12 owed" this morning (live board: 2). Keep as a **bug**, not a floating task. | RE-CLASS → `bug`, or drop after filing | mixed | MED |

**Do NOT drop `9318af31` (gpt-oss CoT):** Rick ruled the privacy angle moot, BUT it carries a **live standing-order clause** (route GCP through Mr Radio) cited elsewhere. Needs a **scope-split**, not a delete. (Mr Radio's explicit C4 warning.)

---

## 3. DEMOTE — P1 inflation (C7: 41 of 63 rows were P1 → priority carries no information)

Metadata-only, reversible, low blast radius. The goal: make P1 mean something again so the demo isn't one of 41 equals. **Candidates to drop P1 → P2/P3** (confirm each body; these are the clear inflation cases from titles):

- Meta/retro findings currently P1 that are not blocking anything: `708691b1`, `fed0f136`, `90864d3e`, `aa2a7a98`, `4288dd53`, `51a774e4`, `19b3f653`, `8b0231d8`.
- **HELD** (skills-distillation, demo-adjacent — do NOT demote until Sam's build lands; some may be real demo blockers): `731f822d`, `1e479f82`, `ee754552`, `f06e38a9`, `33a6ecc2`, `78aed273`, `56b62049`, `592de20a`.

⚠️ **Demotion is the most sensitive metadata edit** — a wrongly-demoted row hides real work. This whole bucket is **converged-report-only**; I execute none of it solo.

---

## 4. KEEP — genuinely live, owned, actionable (C8), or a Rick-gate

- **Both in-progress builds, mine:** `14b3951d` (post-game gate — ruled owed a retro this session) · `fc83b711` (worktree guard).
- **The 93% finding** `76f26f9b` — load-bearing for the converged report (Mr Radio's own words).
- **Rick-gates (his court, not noise):** `c9dd0cc3` (P0 spend ceiling — gate arguably passed, but Rick closes it) · `31f6d447`, `1615a624`, `dc36ff69`, `11461241`, and the GCP-VM chain. These wait on Rick; they are not board noise, they are correctly-parked decisions.
- **`a5aa1362`** "P0: finish the task-board classification" — this is the very work we're doing. KEEP, will close when the converged report ships.

---

## 5. C1 ZOMBIE-ORPHANED — `in_progress` with a dead owner (reconcile, don't drop)

`4e20520d`, `2695f5ad`, `06a3f031` — `in_progress` but their seats were reaped. **These are not noise — they are a reconciliation failure.** The right action is `->dropped` (or reassign) with a reason naming the reap, NOT a silent delete. **Held for the converged report** — Mr Radio flagged the reap-doesn't-reconcile defect as load-bearing, and these three are its evidence. Reconciling them is arguably a demo-focus win (they falsely read as active work).

---

## 6. EXECUTED THIS TURN — and the lesson it taught

- ✅ `6af1d362` DROPPED — deduped against Sam's `2565956b` (both = the dm_list session-scoping leak; a **lupin** bug, NOT skills-distillation, so off Sam's active build board).
- ⚠️ **The dedup nearly lost a five-seat investigation, and this is the sweep's own live lesson.** I classified `6af1d362` as a duplicate from its TITLE + Mr Radio's one-line C5 — then read its body *as it dropped* and found the master record: correlation-of-three differential, three refuted mechanisms, and an **unrun discriminator** flagged "run FIRST." Sam's surviving row had the harm narrative but not that fix-guidance.
- ✅ **Repaired by PORTING** the fix-critical content onto `2565956b` (amendment, 2026-07-17) rather than un-dropping (dropped is terminal). Nothing actionable lost; original preserved under the dropped row (`include_terminal=True`).
- **This is exactly item `5a8aa45b`** — "a title cannot carry [the investigation]." **Method correction for the rest of this sweep: never DROP on a title; a DROP requires the body. DEMOTE may proceed on metadata.** That single rule is why §2's other drops stay converged-report-only.

Everything else in §2/§3/§5: **awaiting the converged nod** (Rick back, or Mr Radio's build-confirm ping for the skills-distillation subset).

---

## 7. THE HONEST BOTTOM LINE FOR RICK

The board is not the P0 problem. **The P0 problem is that `run_pipeline.py` wires nothing** — Sam fixes that in hours. This sweep removes ~15–20 rows of genuine noise so the demo is the only visible P0, but **none of it is on the critical path to the demo**, and I've deliberately not let it become friendly fire on Sam's build. Clear the decks *around* the work; don't touch the work.
