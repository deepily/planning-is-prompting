# Escape Audit — Corpus Half (`af314ae2` scope item 2)

**Auditor**: Cheech 🌿 (session `d701d150`) · **Manager**: Mr Radio 🦉 (`56a74d7b`)
**Date**: 2026-07-21 · **Corpus**: PIP `workflow/**/*.md` + `global/CLAUDE.md` + `.claude/**/*.md`
**Companion**: the guard half, banked separately at `1c74645` (`src/rnd/2026.07.21-escape-audit-guard-half.md`). Kept separable on purpose.

---

## 0. CONCLUSION — THE PARENT PREDICATE IS NOT EARNED

> **A class that produces one instance per audit is indistinguishable from a well-maintained corpus with one miss.**

Four independent axes, a full corpus pass, and **one** live defect outside the guard module. Against it: 19 mentions of two non-existent scripts, **all fenced**; 11 mentions of two removed parameters, **all fenced**; 6 prescribed script verbs, **all executed green**; 43 raw path misses that resolve to **zero** confirmed broken prescriptions.

**The evidence points AGAINST the doctrine on `955f7eb4`, which stays a DRAFT.** Rick authorized evidence-gathering, not the claim; the evidence came back mostly clean. That is the result, not a preliminary toward a better one — and "inconclusive, needs more sweeping" would be the reading that lets a wanted conclusion survive its own test.

What survives is narrower and real: **a structural blind spot in `[SHARED]`-section fencing** (`8f64b2cb`), which is a fixable class rather than a doctrine about escapes.

---

## 1. Standing caveat — carried with every number below

> **"Which verbs are prescribed" is solidly measured; "how many prescriptions exist" is measured to ±3% for distinctive names and is genuinely unknowable-by-regex for English-word verbs. The verb SET is complete — every verb the corpus prescribes was executed. Only the count is soft.**

Measured, not estimated. Bare (undecorated) mention rates: `task_query` 1/61 · `dm_send` 1/50 · `commons_post` 1/34 · `ask_yes_no` 3/75 · `converse` 3/59 ⇒ **~97-98% of distinctive snake_case verbs are backticked or paren-formed.** The exception is verbs whose names are ordinary English words — `notify` (335 mentions, **43 bare**), and **every `memento_io` verb**: write, amend, adopt, verify, migrate, resolve. A bare "notify the user" is indistinguishable from prose by any regex.

**This convicted my own headline number.** I first reported "4 script verbs prescribed". Measured properly, `memento_io.py` appears **27 times, 18 verb-bearing**; my adjacency predicate matched **6 of 18 = 33%**. The verb set was still complete; the prescription count was 3× under, in the direction that makes the sweep look tidier.

---

## 2. Axis 1 — MCP verbs (27 distinct prescribed) → **1 off-surface**

**`8f64b2cb` (P2)** — `plan-review-cascaded-common.md` §Observer-mode Probe Protocol, step 3, live and unfenced: *"DM the Manager directly (`commons_send_to`)"*.

| probe | result |
|---|---|
| keyword `ToolSearch("+commons send_to recipient")` | returns 6 sibling `commons_*`/`dm_*` tools, **no `commons_send_to`** |
| exact-name `select:commons_send_to,commons_who` | **returns `commons_who` only** |

The second probe carries its positive control **inside the same request**: a sibling resolved, the target did not ⇒ the absence is a measurement, not a null from a dead instrument.

**The corpus already knows**: `personas.md:77` "the **retired** `commons_send_to`" · `cross-session-communication.md:53` "⚠️ Deprecated → migrate to `dm_send`" · `doc-viewer-links.md:61` "(deprecated)". Three places say retired; one live protocol still prescribes it.

**The class, not the lapse**: the miss sits in a **`[SHARED]`** section whose live-ness is asserted from *another document* (`personas.md:551` cross-references it as *the* probe action). A fence audit reading `common.md` top-to-bottom sees a block with no banner of its own. **Measured exposure: 21 `[SHARED]` sections, all in one file, exactly 1 fenced ⇒ 20 live-by-default.**

---

## 3. Axis 2 — executables (18 distinct names) → **2 non-existent, 0 live**

`cascade_heartbeat_scheduler.py` and `start-cascade-heartbeat.sh` exist **nowhere** — not in PIP, not in lupin (`ls` → No such file, both).

**19 mentions, fence status checked individually: 13 self-marked 🗄️/RETIRED, 6 under section fences, ZERO live.** María's 2026-06-29 fencing pass holds completely on these two. This zero is what gives §2 its shape: same corpus, same retirement, same pass — **19 right, 1 wrong, and the 1 is in the structurally hardest place to see.**

---

## 4. Axis 3 — retired parameters → **0 live**

`expect_reply` and `recipient_persona`, both removed per the tools' own docstrings (`dm_send`: *"There is no separate watcher/expect_reply"*; `commons_ask_async`: *"directed-DM mode was removed 2026-06-17"*). **11 mentions: 6 self-marked, 5 section-fenced, ZERO live.**

---

## 5. Axis 4 — doc-path existence → **0 confirmed broken prescriptions**

The axis chosen because paths rot faster than verbs, so a near-clean result here kills the parent on strong evidence rather than a small sample.

| stage | count |
|---|---|
| distinct path-shaped references | **260** |
| resolvable-shaped (has a directory component) | 160 |
| raw misses from repo root | **43** |
| … resolved once rooted correctly (lupin repo / sibling repo / `google/`) | **18 of 27 checked** |
| … templates & placeholders (`YYYY.MM.DD-…`, `/path/to/project`, `{Name}` tables, "copy this template to …") | the remainder |
| … absence **is the point** (`lupin/MANDATE.md` — cited as the file a crew looks for and does *not* find) | 1 |
| **confirmed live broken prescriptions** | **0** |

**Not resolvable by this instrument, named rather than scored**: `cosa-voice/CHANGELOG.md` is a doc-viewer **scope** example (URL-shape), and scopes do not resolve on this filesystem.

**A near-miss I nearly filed**: `/plan-mandate` claims `skills-distillation/MANDATE.md` is "currently in force"; I reasoned a three-day POC issued 2026-07-17 must have expired by 07-21 and went looking for stale-claim rot. **The artifact refuted me** — it was amended 2026-07-20 (demo moved Monday → Thursday) and dies 2026-07-23. The claim is correct. The file is also its own anti-staleness mechanism: *"THIS FILE'S EXISTENCE IS THE IN-FORCE FLAG. To end it: DELETE THIS FILE."*

---

## 6. UNSCANNED — stated, not omitted

An unscanned axis silently omitted is the defect this audit hunts.

- **External-system invocations** (`systemctl`, `tmux`, `git` command prescriptions) — **deliberately skipped on the manager's ruling**: different blast radius, and running them from a sandbox tests a different thing than running them where they would actually fire.
- Verbs prescribed in prose with neither parentheses nor backticks (see §1).
- `src/rnd/` in the PIP repo; other repos' copies of these snippets.
- Doc-viewer scope targets (not filesystem-resolvable from here).

---

## 7. Instrument disclosures

1. **The fence classifier answers "is there a banner above this line", not "is this a prescription."** Any count from it is an **upper bound on defects**, never a per-line verdict; every verdict in this document was read by eye afterward. It mis-labelled `cross-session-communication.md:78` as fenced — that line is live *and correct*, documenting `dm_send`'s real return fields. Disclosed while it was working in my favour.
2. **A line-scoped grep cannot answer a question about a multi-line command.** My first `--slot` pass reported three prescriptions as broken; they are `\`-continuation blocks with the flag on line 2. **The false direction was the one that flatters the audit.**
3. **`${PIPESTATUS[0]}` reads the first stage of a pipeline** — reported `exit=0` where python had returned 1 (guard half, §5).

---

## 8. Cross-references

`af314ae2` (the audit) · `955f7eb4` (**DRAFT — evidence points against**) · `79b190e0` (this sweep) · `74a3ff4d` (the shape's first instance; its surviving mention is correctly labelled host-side-only) · `8f64b2cb` (the one live finding) · guard half `1c74645`.
