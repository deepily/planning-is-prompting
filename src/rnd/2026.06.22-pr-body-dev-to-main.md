# PR body — `wip-v0.1.3-2026.02.16-continued-development` → `main`

*(Drafted by María 🌸 for Rick to create the PR manually, 2026-06-22 night. Copy the body below into the GitHub PR description.)*

---

## Suggested title

`[PLAN] v0.1.3 dev → main: receipts-of-progress + governance doctrine, worktree lifecycle, post-game + morning agenda`

---

## Body

### Summary

Lands the `planning-is-prompting` work accumulated on `wip-v0.1.3-2026.02.16-continued-development` since the last squash-merge to `main` (PR #5). **Documentation + workflow only** — this is a meta / knowledge-base repo with no executable code. Real tree delta: **15 files, +906 / −224**.

> **Why this isn't a fast-forward**: PR #5 (`f9734cb`) **squash-merged** the dev branch into `main`, collapsing all dev commits into one. Locally the dev branch keeps full history, so git reports the branches as diverged (149 commits "ahead" by count). The **actual tree delta is small and clean** — GitHub computes it from the trees, not the commit count. Review the *Files changed* tab, not the commit list.

### Substantive commits (since the squash boundary `7d164a1`)

- **`a22b7a5` — Worktree lifecycle**: recovery (7 broken orphans / ~1.3 GB reclaimed) + prevention contract (drain-then-remove reap-hook + janitor backstop; local-only preservation, never push) + manager subagent-governance doctrine (`manager-autonomy §2.2`) + receipts-of-progress (`§9.1`).
- **`06f8ae1` — manager-autonomy §2.2**: aligned the worker-creation-channel rule to the as-built subagent-governance hook (crew managers use `spawn_sessions`, never the Agent/Task subagent).
- **`a320dc5` — TODO §3 aggressive re-cut**: archived the Resume-Here stack, 8 superseded decisions, stale backlog, and weeks-old completions to `todo-archive/` (this is the −224 on `TODO.md`).
- **`c045cf5` — Receipts-of-progress doctrine softened**: "now LIVE" → "built/merged; effective on the arbiter+listener restart" (honest wording after the backstop was proven real by adversarial review).
- **`5e221f6` — Post-game review doc**: Note 1 (question-surfacing doctrine + mechanism) + a 10-item walkthrough agenda.
- **`8e6b573` — S114 session-end**: history + TODO + post-game updates (deploys closed; morning agenda recorded).

### Themes

- **Governance + worktree lifecycle** contracts (PIP doctrine; the lupin-side code these describe shipped separately).
- **Receipts-of-progress** empirical-liveness contract + its code-layer enforcement note.
- **Post-game** review with the morning agenda: question-surfacing mechanism, arbiter-restart delegation, DM-liveness residual, late-night-commit coordination workflow.
- **TODO / history hygiene** (aggressive re-cut + archive files).

### Files changed

```
15 files changed, 906 insertions(+), 224 deletions(-)
```

`README.md` · `TODO.md` · `history.md` · `workflow/{cosa-voice-integration,manager-autonomy,swe-team-roles}.md` · `src/rnd/2026.06.22-{fleet-roster-to-user-level-migration-spec,lupin-memory-curation-dry-run-plan,manager-worker-injection-root-cause,postgame-review,receipts-of-progress-heartbeat-owed-calc,worktree-lifecycle-contract}.md` · `todo-archive/{2026-06-02-to-09-decisions-log,2026.06-resolved-pending-decisions,2026.06.22-aggressive-recut}.md`

### Verification

Docs-only repo; structure validated, all internal links resolve. The **lupin-side code** these docs describe (receipts-of-progress backstop, subagent-governance hook, fleet-roster migration) was **deployed and verified live this session** in the separate lupin repo (held for its own push).

### Merge note

A squash-merge (matching PR #5) keeps `main`'s history linear and avoids re-introducing the diverged dev commits.
