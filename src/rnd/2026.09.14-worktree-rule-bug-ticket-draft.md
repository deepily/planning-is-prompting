# Ticket draft — worktrees keep piling up (filing refused by the creation gate)

> **SUPERSEDED 2026-09-14 14:10 EDT — not filed.** The work this ticket asked for landed before the creation gate opened: Lupin commits `0fcfdc4d`, `5240cbfe` and `d9b342ab`, reviewed by Mr. Radio and merged, with the live janitor proven, and it was closed on P0 row `033538f6`. Filing it now would open a row for finished work.

**Ordered by**: Rick, voice, 2026-09-14 ~13:00 EDT — *"File a P1 bug ticket and request promotion to P0 — the implementation of the plan to deal with the work trees. I want to get this taken care of as soon as possible, so it doesn't simply start accumulating again."*

**Why this is a file and not a row**: `task_create` returned 422 at ~13:01 EDT — *"New tickets are gated: in the last window the fleet created 1 and closed 0 … (A P0 is exempt if this genuinely cannot wait.)"* A seat cannot mint a P0 (403, operator's account only). Filed verbatim below, ready to create the moment the gate opens or Rick files it as P0 himself.

---

**Class**: bug · **Priority**: P1, request promotion to P0 · **Project**: lupin · **Owner**: maria · **Reviewer**: mr radio · **Parent**: `033538f6`

**Title**: [LUPIN] Worktrees keep piling up next to the repo — implement the ruled location + reap rule (R1–R3) so they stop coming back

**Defect**: the sanctioned out-of-sight lane is `lupin/.claude/worktrees/` (gitignored, janitor-swept), but:
1. `provision-seat-worktree.sh:113` builds every seat tree next to the repo, and nothing reaps it. `dismiss_sessions` never calls `drain_then_remove`, although `worktree_reaper.py:9` says it does.
2. Hand-typed `git worktree add ../` does the same.
3. The July creation guard was never installed.

Measured: 227 `lupin-wt-*` directories next to the repo, all still registered with git (45 made by the spawner, 182 by hand).

**Ruled** (Rick, ask card, `answered: true`, `default_used: false`): adopt R1–R3; install R4 in log-only mode.
- **R1**: the spawner target becomes `<MAIN>/.claude/worktrees/seat-<slug>`.
- **R2**: reap the seat's tree when the seat is dismissed.
- **R3**: add a line on worktree location to `lupin/CLAUDE.md`.
- **R4**: install the creation guard in log-only mode (Rick's keypress).

**Must fix before R1** (each confirmed in code on 2026-09-14):
- **Risk 1**: the janitor (`worktree_reaper.py:404-406`) drains any sandbox tree idle for more than 6h, so a live seat would lose its tree. Proposed fix: lock seat trees at provisioning (the janitor already skips locked trees, `:402`) and unlock them on dismiss.
- **Risk 4**: `drain_then_remove` stages with `git add -A`, which respects `.gitignore`. Ignored files include `.claude-memento-*.md` (`.gitignore:223`) and `io/**` (`:104`). Reap must salvage ignored files before it removes the tree.
- **Risk 5**: the wrong-tree detector in `util.py` (~line 706) returns early on `caller.startswith( root + sep )`. It therefore treats a nested tree as the main checkout, and the warning goes blind. Also check `hook_credentials` and `session_bridge`.
- **Risk 6**: `disk-hygiene-report.sh:83` and `stale-seat-scan.py` glob `lupin-wt-*`, so they will stop seeing seat trees.

**Plan**: `planning-is-prompting/src/rnd/2026.09.14-worktree-location-rule.md`
