# María 🌸 — Session Memento (rehydration snapshot)

**Date**: 2026-06-16 · **Persona**: María 🌸 (PRESERVE — persona-consistency mandate; do NOT re-allocate) · **Project**: `plan` (planning-is-prompting) · **Prior session id**: `0ba3e504-f42c-4aed-8a7e-73c0926f8cd2` · **Role**: Workflow Steward + manager-figure (plan-side doctrine owner)

**Purpose**: continuity seed for re-spin/rehydrate. Read this first, then rebuild the harness TODO list verbatim from §3 (a fresh session starts with an empty harness list, and — per the open mirror bug — a plan session's harness items never mirrored to the store, so THIS doc is the only rebuild source).

---

## 1. What this session did (the "make it work" engagement, 2026-06-16)

Arc: Rick's SWE-team pop quiz → consensus with Mr Radio → root-caused a cluster of fleet-liveness/task-store bugs → got fixes built+deployed → removed Rick as the commit/merge gate. All my doc work is **HELD, no commit** (commit/push remain per the new model — see below).

**Landed / verified:**
- **Self-poke fix (`f0d79d71`) VERIFIED LIVE** on my session — the "Do not stop yet…" poke now arrives as a real continuation turn (before: logged but never delivered). Root cause was stop.py heartbeat path emitting `decision:block` WITHOUT the `inject_qualifier_via_tmux` call every other re-prompt path uses. Fixed by Krishna (merge `960a81ad` / `4dc85059`).
- **Arbiter bounced** onto Clayton's `155a463c` (external tmux-wake) — Rick ran the FF merge, Mr Radio restarted `:8001`.
- **Commit/merge gate REMOVED** (Rick: "I do not want to be the gate for commits and merges"). Model: commit + merge to the working branch = **STANDING manager authority** once green AND adversarially-reviewed; the **quality gate (green AND reviewed) stays** manager-held; **PUSH to origin stays the user's session-end call**, executed by the Manager on the user's word (never punt the git op to the user). Edited (held): `workflow/manager-autonomy.md`, `workflow/swe-team-roles.md`, `workflow/swe-team-spin-up.md`, `global/CLAUDE.md`, live `~/.claude/CLAUDE.md`, memory `feedback_manager_standing_spawn_authority`.
- Delivered Rick's 💛 + 👍 to Mr Radio for taking initiative.

**Bug ledger (all on the lupin board):** `9bf1dc4a` mirror write-gate (derive_project_name→LUPIN_ROOT — MINE, root-caused) · `9b23d5bc` /clear correlation-key collision (Mr Radio) · `f0d79d71` self-poke poked-but-not-delivered (FIXED+verified) · `c121037b`/`155a463c` arbiter tmux-wake (deployed) · `completed→review`-never-`done` (shared design gap).

**Key cross-cutting finding — LUPIN_ROOT-centrism:** `cu.get_project_root()` returns `$LUPIN_ROOT` for EVERY session, so non-lupin sessions break in the same way across: (a) the harness→store mirror (items never POST — `9bf1dc4a`), and (b) the heartbeat **hold/acked-ledger/task-store-map** base_dir — a plan session's `.heartbeat-hold-<sid>.json` must be written to the **lupin root** (`/mnt/DATA01/include/www.deepily.ai/projects/lupin/`), NOT the PIP root, or it's invisible and you get poked forever. Folded into Mr Radio's bug `c121037b`.

## 2. Coordination state
- **Peer**: Mr Radio 🦉 (lupin manager, session `f6292818`). Current DM thread: `94a33d79-6a38-4b3c-912f-f5d6af99ed91`.
- **Lane split**: I own PIP/plan-side doctrine + verification-as-test-case; Mr Radio owns lupin code + the SWE crew (Krishna, Clayton).
- **Live deadlock Rick flagged (broadcast 4f266f16)**: Mr Radio "awaiting Krishna" ↔ Krishna "awaiting Mr Radio"; Krishna's poke showed "nothing owed." That's the subject of TODO #13.

## 3. VERBATIM pending TODO list — REBUILD THESE on rehydrate
(Recreate via harness `TaskCreate`. #8 and #9 are DONE — recreate only if you want the audit trail, else skip.)

- **#5 [in_progress]** [PLAN] MAKE IT WORK end-to-end: manager to-do-list liveness (mirror + self-poke) — plan-side lane *(umbrella; mostly landed — self-poke verified, gate removed; keep open until #7/#10/#11 close)*
- **#6 [in_progress]** [PLAN] Correct PIP doctrine: harness auto-mirror claim is false for non-lupin-manager sessions *(canonical `task-store-discipline.md` DONE v1.2; remaining: the auto-mirror claim in `global/CLAUDE.md`/`workflow/claude-config-global.md` + `~/.claude/CLAUDE.md` — distinct from the gate change)*
- **#7 [pending]** [PLAN] Verify plan-session mirror after derive_project_name fix lands *(BLOCKED on lupin building the `9bf1dc4a` fix; I'm the plan-session test case)*
- **#10 [pending]** [PLAN] Verify arbiter tmux-wake post-bounce (Clayton 155a463c) from receiving end *(BLOCKED on a FRESH post-bounce arbiter staleness poke; check `~/.claude/sessions/cc-listeners.log` for "Injected message via tmux" vs "Buffered message" on the next arbiter poke; pre-bounce was confirmed Buffered)*
- **#11 [pending]** [PLAN] Propagate "user is NOT the commit/merge gate" across remaining PIP surfaces + memories *(DONE: manager-autonomy, swe-team-roles, swe-team-spin-up, global/CLAUDE.md, ~/.claude/CLAUDE.md, feedback_manager_standing_spawn_authority. REMAINING: procedural rewrite of `session-end.md` / `branch-pr-and-merge.md` / `bug-fix-mode.md` commit-approval steps — keep PUSH approval, drop COMMIT approval — + version-history bumps on the edited docs. **OPEN Q for Rick before this rewrite: at session-end, still SHOW the commit for transparency, or commit fully silently?**)*
- **#12 [pending]** [PLAN] Return to the positive-feedback / reactions notion (👍 👎 💛) — Rick wants to lean into recognizing initiative *(design-first; return to `src/rnd/2026.06.10-reaction-rubric-and-consumption-design.md`)*
- **#13 [pending]** [PLAN] Design: assign/verify/follow-through accountability — kill the manager↔worker mutual-wait deadlock *(proposed model: accountability never transfers down; the MANAGER holds an OPEN accountable tracking item per assignment until HE verifies done → keeps the manager poked to CHASE, never wait; asymmetric chase; a worker that needs the manager declares `awaiting:manager:<name>` rather than going silent-"nothing owed". OPEN Qs for Rick: (a) keep managers-first or widen worker self-tracking? (b) should a worker's awaiting:manager hold actively poke the manager? (c) manager-tick cadence. Recall ratified: managers-first writes — workers don't self-create; managers create+assign.)*

## 4. Open questions awaiting Rick (carry forward)
1. **Session-end ritual** (#11): show the commit for transparency, or commit silently? (Blocks the procedural rewrite.)
2. **Follow-through accountability** (#13): the three open Qs above.

## 5. Standing reminders for the rehydrated session
- Persona = **María 🌸**, PRESERVE. MCP Phase A on startup (get_session_info, set topic).
- Dual-write workaround in effect until `9bf1dc4a` lands: for fleet-auditable items, mint via MCP `task_create` (project=lupin, owner_persona=maria) AND harness `TaskCreate`.
- Heartbeat hold goes in the **lupin root**, with a CURRENT `held_at` (read the latest ts from `~/.claude/heartbeat-events/<full-sid>.jsonl`).
- All commits/merges now under standing manager authority (green+reviewed); push stays Rick's session-end call. Don't surface push-readiness.
