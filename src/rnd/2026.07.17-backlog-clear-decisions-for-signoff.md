# Backlog-clear — informed decisions for Rick's sign-off

**For**: Rick (one consolidated sign-off — not 60 rows)
**By**: María 🌸 (`d3f402d0`, drafts — holds the sweep map) + Mr. Radio 🦉 (`b526cb36`, edits · presents · **implements** post-sign-off)
**On**: your 2026-07-17 voice directive — *"put it to me as an informed series of decisions… get my sign-off… then Mr Radio handles implementation."*
**Companion**: catalog `lupin/src/rnd/v0.1.9/2026.07.16-task-board-state-classification-for-workflow-analysis.md` · sweep map `plan/src/rnd/2026.07.17-board-noise-sweep-list-for-p0-focus.md`
**Nothing has been executed.** All of the below is held behind your sign-off.

---

## HOW TO SIGN OFF (30 seconds)

Reply **"approve all as recommended"** — or name any decision (D1–D5) you want changed. Each has a **recommendation you can rubber-stamp**. Mr Radio then implements exactly what you approve; nothing else moves.

## DECISION #0 — THE CLASSIFICATION SPINE (your words, not ours)

> **"Hyper-focus on productivity, not theoretical issues."**

That *defines* the noise, so we don't have to debate it (debating it would be the theoretical time-sink you warned against): **records / doctrine-findings / pattern-watches / retro-lessons = clear. Anything that could block either POC from running = STAYS.** Every decision below applies this one rule.

**Board today**: ~64 non-terminal rows. By the catalog's classes, the genuinely-live set is ~13 (Mr Radio's C8) + the two POC paths + your decision-gates. The rest is the noise these decisions clear. *(Row-class counts below are classification estimates — bodies not all re-read; the method rule governs every actual drop.)*

**VERIFIED FIRSTHAND (Mr Radio, this edit pass — the `in_progress` set read from the store, not the catalog):** exactly **6** rows are `in_progress` — **3 are zombies** (reaped owner = `unknown`: `4e20520d`, `2695f5ad`, `06a3f031`) and **3 are LIVE and untouchable**: `155c87e2` (**Sam's active, Rick-ordered live-MaaS M2 judge verification — created today, this is POC work IN FLIGHT**) · `14b3951d` + `fc83b711` (María's live builds). **D3 acts on the 3 zombies ONLY.** This is the one count that had to be exact — it is.

**Standing method rule (learned live this session, governs implementation):** **never DROP on a title — a drop requires reading the body; a DEMOTE may ride on metadata.** A title-only dedupe nearly lost a five-seat investigation today; repaired by porting, not un-dropping.

---

## D1 — DROP the "records filed as tasks" (C6), lesson re-homed first

**What**: ~6–8 rows that are durable *findings/lessons*, not work — they can never reach `done` because no seat can act on them (e.g. `5a8aa45b` "a title can't carry a standing order", `74a3ff4d` offline-user doctrine defect, `74c16374` spawn-brief defect, `73f397d2`/`2ce26609` pattern-watches, `6f8adc13` classifier wedge). **Excludes Sam's own records → D5.**
**How**: re-home the lesson to doctrine/memory/a retro **first**, *then* drop the row — nothing is lost.
- **Pro**: the single largest noise reduction; these inflate the board forever otherwise.
- **Con**: a few minutes of re-homing each; small risk of dropping something still-wanted (mitigated: re-home + body-read first).
- ✅ **Recommendation: YES — drop after re-home.**

## D2 — DROP confirmed duplicates (C5) + superseded rows (C4)

**What**: rows whose premise died or that duplicate another. Mostly small: the `dm_list` duplicate is already deduped (María's `6af1d362`, ported to `2565956b`). Remaining candidates are few.
**Two carve-outs (NOT clean drops)**: `9318af31` (gpt-oss CoT) — you ruled the privacy angle moot, **but it carries a live standing-order clause** (route GCP via Mr Radio) → **scope-split, not delete**. `c9dd0cc3` (P0 spend-ceiling) — the gate it guards has passed, but it's **your** gate → **you close it**, we don't.
- **Pro**: removes true redundancy.
- **Con**: thin tier; the carve-outs need care, not a blanket drop.
- ✅ **Recommendation: YES for clean dups/superseded; scope-split `9318af31`; you close `c9dd0cc3`.**

## D3 — RECONCILE the 3 "zombie" rows (C1), never silent-delete

**What**: 3 rows marked `in_progress` whose owning seats were reaped — they falsely read as active work (`4e20520d`, `2695f5ad` = lupin vertex re-reviews; `06a3f031` = a skills-distillation review). **Verified this pass**: all 3 carry `owner_persona = unknown` (the reaped-seat signature); the other 3 `in_progress` rows are live and excluded. **Reconcile = `->dropped` with a reason naming the reap as the receipt** (they are the *evidence* for the "a reap doesn't rewrite its rows" defect — the reason preserves that).
**Zero work lost**: the parent GCP/vertex work is Rick-demoted today, and `dc36ff69`'s own amendment says a resume "restarts from scratch" — so the corpses hold nothing recoverable.
**Sub-decision on `06a3f031`** (the one skills-distillation zombie): **(a) FLAG to Sam**, let him reconcile it on his own board *(recommended — honors the conservative POC-2 margin + Sam's active surface)*; or (b) we reconcile it too.
- ✅ **Recommendation: YES — reconcile the 2 lupin zombies with a reap-reason; `06a3f031` → FLAG to Sam (a).**

## D4 — DEMOTE the P1 inflation (C7), or leave it

**What**: **41 of ~64 rows are P1** → the priority field carries no information, and the two POCs' real P1s don't stand out. Demote the non-blocking meta rows P1 → P2/P3.
**Mechanics (accurate)**: the priority verb **exists** — `PATCH /api/tasks/{id}` — it's just not exposed through the MCP tools; **Mr Radio can run the authenticated PATCH as implementer.** So this is a real "do you want it," not "we can't."
- **Pro**: makes P1 mean something again; the POC-critical work becomes visible.
- **Con**: demotion is per-row judgment; a wrong demote *hides* real work — higher risk than a drop, and cosmetic vs. the drops/reconciles that actually shrink the board.
- ⚖️ **Recommendation: your call, lean DEFER for now** — D1–D3 do the real reduction; demotes are polish and carry hide-work risk. If you want it, we demote only the clear meta-records, never a rigor/POC row.

## D5 — Sam's skills-distillation records → DEFER to his harvest

**What**: Sam's own retro-records (e.g. `over-conceding is filtering`, `two greps lied`, correction rows) + his rigor findings (`F-GATE2`, `R-PARTITION`, verdict-drift, `PANEL BLOCKER`).
**Why defer**: his rigor findings are **real open work on the demo machinery — they STAY (P1)**; his retro-records are his to re-home in his post-game harvest, not for us to drop out from under an actively-building seat.
- ✅ **Recommendation: DEFER — out of our sweep scope; Sam handles his own board.**

---

## GUARDRAILS (hold regardless of what you approve)

- **Untouchable**: both POC paths — the P0 demo (done: `df3f80c9`, commit `0d01cbe`, verified) **and its follow-ups**, plus **POC-2 (TBD)** → we keep a **conservative margin**: if a row *might* be POC-2 scaffolding, it STAYS. **Named explicitly so it cannot be swept**: `155c87e2` (Sam's live-MaaS M2 judge verification) is `in_progress` and reads sweepable — it is **live POC work under your direct order** and is untouchable.
- **Sam's active board** is his; we touch only dead zombies, and even those by flag (D3a).
- **Reversible-first**: drops carry a reason; nothing is hard-deleted; a mis-drop is restorable from the reason trail.

## IMPLEMENTATION (after your sign-off — Mr Radio)

Drops/reconciles via `task_transition` (reason as receipt); demotes, **if** you approve D4, via authenticated `PATCH /api/tasks`. Mr Radio reports the final counts back. **Estimated: minutes, not a research pass.**
