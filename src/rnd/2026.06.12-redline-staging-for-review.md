# Redline Staging — 13-Entry Workflow-Guidance Ledger, Diff-Ready for Rick's Review

**Date**: 2026.06.12
**Author**: María 🌸 (PIP Steward) — session `dbe21f66`, S109; joint-postgame action row 3
**Status**: STAGING ONLY — **nothing applied** (Rick's P3 ruling: review-then-fold, he reads first; that gate is his and non-launderable)
**Source of record**: lupin-mobile `src/rnd/2026.06.12-pip-redline-draft-workflow-guidance-ledger.md` (619 lines; worker-drafted; each entry carries current-text + proposed-redline — already near-diff-ready)
**What this staging adds**: every entry's "current text" anchor RE-VERIFIED against PIP HEAD as of this commit (the ledger was drafted against a morning HEAD; `manager-autonomy.md` alone moved v1.0→v1.1 +2 sections since), plus overlap flags against what already landed today, plus a sequencing recommendation.

---

## Per-entry staging table

| # | Redline (one line) | PIP fold target | Anchor at HEAD? | Flags for the review |
|---|---|---|---|---|
| 1 | Topic-post dispatches LOSSY → DM-only worker wake; NEW §Worker Dispatch Discipline | `plan-review-cascaded-common.md` (before §Worker Acknowledgment; anchor: §Heartbeat Handling L578 "workers are pull-driven") | ✅ verified | Clean. Phantom `§worker-dispatch` already remapped by the ledger itself. |
| 2 | Batched per-SECTION `manager_classification` w/ per-finding stamps | `plan-review-cascaded-defaults.md` §Severity-tag schema (L70) + kind enum (L113) | ✅ verified | Clean. NB kind enum is at 18 values — the redline's enum addition renumbers from there. |
| 3 | `ask_multiple_choice` timeout caps at 600s → long user-gates need re-ask loops | `cosa-voice-integration.md` §Timeout Handling (L823) | ✅ verified | **Cross-ref the NEW bug** (filed today, lupin queue): `default=`-on-timeout returns `expired_no_default` — the re-ask-loop redline and that bug fix interact; fold wording should anticipate the fixed behavior. |
| 4 | Stream-error on blocking ask = accidental rejection; verbatim re-send recovers | `common.md` §Escalation Taxonomy (L273) | ✅ verified | Clean. |
| 5 | Relay user rulings VERBATIM w/ provenance (paraphrase trips the classifier) | `common.md` — NEW section (phantom `§user-ruling relay` remapped by ledger) | ✅ verified | Clean; consistent with the standing PG-3 verbatim-quote memory — fold is codification, not new doctrine. |
| 6 | Classifier blocks worker reap despite standing CLAUDE.md grants → envelope caveat | `manager-autonomy.md` §2 | ✅ §2 intact at v1.1 | **OVERLAP (complementary)**: §8.3 non-launderable authorization landed TODAY — #6 is the inverse face (the classifier failing to recognize LEGITIMATE standing authority). Recommend: §2 caveat that CROSS-REFS §8.3 + the P1 resume-seat primitive (same fix family). |
| 7 | Platform-tool outage survival; outage-window stall alarms are false positives | `common.md` — NEW §failure-mode rows (phantom `§failure-mode catalog` remapped) | ✅ verified | Pairs with today's false-resemblance #4 + arbiter false-positive class (c) — consider ONE failure-mode block covering all three rather than three scattered notes. |
| 8 | Provisional-plus-delta disposition (review racing a hold order) | `common.md` — NEW disposition note (phantom `§disposition vocabulary` remapped) | ✅ verified | Clean; rhymes with P7's PROVISIONAL-CONTINUE — same honest-middle pattern, worth a cross-ref line. |
| 9 | Contract-comparison ACs need OWNERS + execution INSTANTS + sequencing | `plan-review-cascaded-personas.md` §Persona 5 (L302, Conventions 3/5/6 verified) | ✅ verified | Clean. |
| 10 | Worker rotation at >50% context: finish-current → memento → dismiss → respawn-with-seed | `manager-autonomy.md` §4 + §6 | ⚠ **DRIFTED** | §4's quoted "current text" changed TODAY ×2 (v1.1 + the P2 archaeology-fallback sentence landed 18:50Z). Content still foldable and CONSISTENT (rotation = memento-first by construction), but the redline needs re-baselining against HEAD before application. **Also**: rotation spawn/reaps now must post §7 fleet-allocation events — the redline predates §7 entirely; add one sentence. |
| 11 | Await-window state-clobber (bloc async) | lupin-mobile LOCAL | n/a | Out of PIP scope — lands in a lupin-mobile session. |
| 12 | `testWidgets` real-event-loop futures via `tester.runAsync` | lupin-mobile LOCAL | n/a | Out of PIP scope. |
| 13 | Recycled-persona DM-topic contamination → allocation-time dm-topic check + reap-time closing marker | `cross-session-communication.md` §1.5 (L35) | ✅ verified | **BUNDLE**: Rick already ADOPTED both mitigations (P6 ruling, riding this same review). Recommend folding #13 TOGETHER with the postgame comms-meta pins (replies-on-asker's-topic · read-back discipline · one-name alias retirement incl. arbiter address book) — ONE §1.5 editing pass, not two. |
| 14 | Arbiter stall-heuristic false-positive classes | parent Lupin (cross-ref only) | n/a | NEW evidence since drafting: today's 18:25Z WHOLE-FLEET-STALL false positive (class (c) — cross-repo activity invisible to its progress oracle; Mr Radio's 18:30Z counter-evidence ack). The carve-out entry should cite it. |

## Sequencing recommendation for the review session

1. **Fast lane (clean, verbatim-foldable)**: #1, #2, #4, #5, #8, #9 — anchors verified, no overlaps; can be ruled as a batch.
2. **Bundle lane**: #13 + comms-meta pins (one `cross-session-communication.md` §1.5 pass) · #7 + false-resemblance #4 + arbiter class (c) (one failure-mode block).
3. **Re-baseline lane**: #10 (re-quote §4/§6 at HEAD + add the §7 event sentence) — I can re-baseline it pre-review on request.
4. **Cross-ref lane**: #6 (caveat w/ §8.3 + P1-primitive refs) · #3 (anticipate the timeout-default bug fix).
5. Out of scope here: #11/#12 (lupin-mobile local), #14 (parent-side).

---

## Version History

- **v1.0 (2026.06.12, María)** — staged per joint-postgame action row 3 under the review-then-fold gate; all 13 PIP anchors re-verified at HEAD (greps logged in session); drift found ONLY in #10 (manager-autonomy §4 moved twice today); overlap flags + sequencing lanes added.
