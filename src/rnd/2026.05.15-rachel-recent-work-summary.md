# Rachel's Recent-Work Summary — Sessions 81 → 88

**Author**: Rachel 🕊️ (cosa-voice session `02f528ee`)
**Date**: 2026-05-15
**Scope**: planning-is-prompting repo, branch `wip-v0.1.3-2026.02.16-continued-development`
**Window**: 2026.05.04 → 2026.05.15 (12 days, 8 sessions)
**Trigger**: Mr. Rick's morning broadcast — "@rachel to summarize work accomplished recently"

---

## TL;DR

The recent arc hardened Claude Code's communication contracts — both **human-facing (TTS)** and **machine-facing (MCP / cross-session)**. Three themes carry it:

1. **Voice channel discipline** (Sessions 81, 82, 88) — TTS brevity mandate, tiered length caps, persona-first identity.
2. **Cross-session infrastructure** (Sessions 86, 87) — commons blackboard guidance and cross-repo doc-viewer scope routing.
3. **Procedure tightening** (Sessions 83, 84, 85) — sequential plan-review, day's-work summary at session end, ternary `ask_yes_no()` Neither contract.

Every session followed the **documentation-first hub-and-spoke pattern with grep verification**: R&D doc serialized first, canonical hub patched, mirror sync pair kept byte-identical, callsite spokes patched, all closed with verification greps.

The **headline from yesterday**: Session 88's Persona-First Mandate — `get_session_info()` must fire before any user-facing text, and its return value (persona + `doc_scope`) must be fully unpacked before the first acknowledgment.

---

## Session-by-Session

### Session 81 — 2026.05.04 — Conversation Mode + TTS Brevity Mandate

Hub-and-spoke across 31 files. New global behavioral mandate: spoken `notify(message=...)` is **conversational prose, re-crafted for the voice channel**, NOT a verbatim copy of the terminal reply. Strip markdown structure, paths, JSON, URLs, table syntax. Cap ~80–120 words for routine work. Use `abstract` aggressively for rich terminal-side content.

- Canonical hub: `workflow/cosa-voice-integration.md` — new §Conversation Mode section.
- Global sync pair: `~/.claude/CLAUDE.md` ↔ `global/CLAUDE.md` byte-identical.
- Callouts added to 14 workflow files + 13 slash-command wrappers.
- Memory persistence: `feedback_tts_brevity.md`.

### Session 82 — 2026.05.05 — TTS Brevity Concision Pass

Refinement of Session 81 mandate after user feedback that 190-word real-use close-out was still too verbose. Three new principles:

- **Tiered length cap**: routine ≈ 60w / 20s; substantive ≈ 80–120w / 30s.
- **Headline, don't enumerate**: numbers, file lists, paths live in `abstract` only.
- **No justification for non-actions in spoken line**.

Anchor example: 190-word real-use case → 45-word tightening (**76% reduction**, zero loss because abstract still carries full detail).

### Session 83 — 2026.05.06 — Plan Review Sequential Execution Mandate

Closed a real-world loophole: Claude Code ran the three plan-review passes (REUSE pre-pass, Pass 1 Fitness, Pass 2 Adversarial) **in parallel** by spawning concurrent `Agent` subagent calls in a single message, silently bypassing the §6 / §9 user gates (which only function in a serial pipeline).

- Four additive hooks across `workflow/plan-review.md` and `.claude/commands/plan-review.md`.
- New top-of-doc **SEQUENTIAL EXECUTION MANDATE (NON-NEGOTIABLE)** banner.
- §3 section title amended: "Fitness Before Adversarial — and Strictly Sequential".

### Session 84 — 2026.05.06 — Day's Work Summary at Session-End (Step 6)

Closed gap: session-end "trails off" without a closing artifact. Added new Step 6 between Step 5 (Backup Prompt) and Final Verification.

- Reused lupin's `BranchChangeAnalyzer` and `DirectoryAnalyzer` — no new code.
- Three-tier fallback: **rich path** (cosa Branch Analyzer with code/comment/docstring breakdown), **native fallback** (`git diff --shortstat`), **skip path** (orphan/empty branches).
- Slash-command flags: `--summary` / `--no-summary` and `--baseline` / `--no-baseline` on `/plan-session-end`.
- Tier-2 venv quirk caught: system Python lacks PyYAML; cosa venv has it.

### Session 85 — 2026.05.11 — `ask_yes_no()` Neither Response Language Sync

Closed documentation drift: cosa-voice MCP `ask_yes_no()` gained ternary `yes` / `no` / **`neither`** (re-frame escape hatch). Guidance still described it as binary. Hub-and-spoke patched ~14 files.

- Canonical hub: new ~70-line `#### Handling Neither — the Re-Frame Escape Hatch` subsection.
- **CRITICAL destructive-op rule**: Neither MUST NOT proceed — re-frame and re-prompt; `default` offers no fallback because Neither requires an explicit user click and cannot arrive via timeout.
- Response Format table expanded 4 → 6 rows (yes/no/neither × ±comment).
- Callsite spokes patched: session-end, session-checkpoint, branch-pr-and-merge, bug-fix-mode, installation/uninstall wizards, etc.

### Session 86 — 2026.05.14 — Cross-Session Communication Guidance

Closed guidance gap opened when Lupin shipped v0.1.7 Phase 1 (Claude↔Claude commons blackboard with 5 MCP tools) and Phase 2 (user→all-sessions broadcast with persona-aware `@PersonaName:` directives).

- **Three-tier autonomy**: Tier 1 (Read — always allowed). Tier 2 (Self-disclosure write — at session initiative). Tier 3 (Attention-demanding — explicit user trigger or clear coordination need).
- **Reserved topic vocabulary**: `presence`, `coordination`, `help-wanted`, `incidents`, `broadcasts`, `broadcast-acks` + organic extension. **Topic names ARE the tier marker** — couples autonomy with topic discipline elegantly.
- **Broadcast routing rules**: persona match → ACT; other persona → ACK-ONLY; no persona → all ACT on default body.
- New canonical doc: `workflow/cross-session-communication.md` (1509 words, mermaid routing diagram).
- Bug #2 swept: stale `workflow/notification-system.md` references cleaned from live docs.

### Session 87 — 2026.05.14 — Cross-Repo Doc Viewer Scope Guidance (Rachel, last session)

Closed guidance gap: doc-viewer `scope=` query param was undocumented across cross-repo sessions. Two architecture pivots accepted in conversation:

- **Pivot 1**: original sketch was standalone `get_doc_scope()` MCP tool. Mr. Rick pattern-matched the existing `voice_persona` field — put `doc_scope` in the same envelope. Accepted: zero new tool, zero extra round trip.
- **Pivot 2**: original sketch had cosa-voice re-parsing Lupin's INI. Mr. Rick flagged ownership smell: registry lives on Lupin side. Accepted: Lupin owns registry, cosa-voice consumes via API.

Phase 2 guidance landed in `~/.claude/CLAUDE.md` § DOCUMENT VIEWER LINKS + `global/CLAUDE.md` mirror. Phase 3 per-repo `## Doc Viewer Scope` pins added to 5 of 7 target repos (lupin, planning-is-prompting, lupin-mobile, google/lookml, google/par-pacific). Deferred: cosa-voice (not on host), claude-plans (no CLAUDE.md convention). Lupin TODO filed for `/api/docs/scopes` exposure.

### Session 88 — 2026.05.15 — TTS Brevity Self-Violation Recovery + Persona-First Mandate (Rio)

Recovery from Rio's session-opening audit `notify()` that itself violated the TTS Brevity Mandate (175 spoken words of inventory: terminology, file:line callouts, recommended-fix rationale). Mr. Rick called it verbatim and the work crystallized as a 5-hook recovery plan.

**Three failure modes diagnosed**, each gets a hook:

1. **Inventory creep into spoken channel** (Hooks 1, 2, 3) — pre-`notify()` MUST-audit gate, 175→22 word anchor example, reference-trigger MANDATE (abstract referencing a file MUST link it).
2. **Persona-blind first response** (Hook 4) — Persona-First Response Mandate strengthened in MCP STARTUP PROTOCOL Phase A. Identity MUST be resolved before first user-facing text.
3. **Doc-link illiteracy at startup** (Hook 5, coupled to Hook 4) — `doc_scope` envelope MUST be extracted at `get_session_info()` time, doc-link literacy mandated (URLs live ONLY in `abstract`, never in spoken `message`).

**One root cause**: the first MCP call MUST be `get_session_info()`, and its return value MUST be fully unpacked (persona + doc_scope) BEFORE composing any user-facing text. Codified as new **"Preliminary 0" section** in `workflow/session-start.md`.

Language hardened in v2 of the recovery plan: 17 MUST/MANDATORY/PROHIBITED/VIOLATION occurrences across 7 sections, with top-of-doc LANGUAGE MANDATE banner.

---

## Mechanics All Eight Sessions Share

- **Documentation-first**: R&D doc serialized to `src/rnd/yyyy.mm.dd-slug.md` BEFORE any code/guidance edits.
- **Hub-and-spoke**: canonical hub patched; live-mirror sync pair (`~/.claude/CLAUDE.md` ↔ `global/CLAUDE.md`) byte-identical; callsite spokes patched.
- **Grep verification at close**: target hooks present, stale wording removed, live↔mirror byte-identical.
- **Plan files**: each session's plan serialized per plan-serialization mandate to `src/rnd/`.
- **Parallel session safety**: per-session `.claude-session.md` sections; selective staging at commit.

---

## Resume Point

**Active branch**: `wip-v0.1.3-2026.02.16-continued-development`
**Last commit**: `1c29c9d` Checkpoint — TTS Brevity Self-Violation Recovery (Session 88)
**Today's broadcast assignments**:

- **Maria** — Lead remaining Commons blackboard implementation (Lupin-side).
- **Rachel (me)** — Recent-work summary (this doc). ✅ DELIVERED.
- **Rio** — Top-5 TODO and bug items in descending importance, excluding Maria's Commons/duplication scope.
- **Everyone else** — Stand by until assigned.

---

## Cross-Links

- Active session history: `history.md`
- Pending work tracker: `TODO.md`
- Session 88 R&D: `src/rnd/2026.05.15-tts-brevity-mandate-self-violation-recovery.md`
- Session 87 R&D: `src/rnd/2026.05.14-doc-link-scope-cross-repo.md`
- Session 86 R&D: `src/rnd/2026.05.14-cross-session-communication-guidance.md`
- Canonical hubs: `workflow/cosa-voice-integration.md`, `workflow/cross-session-communication.md`, `workflow/plan-review.md`, `workflow/session-start.md`, `workflow/session-end.md`
