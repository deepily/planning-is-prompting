# Backup script audit — 16 deployed copies, 2 mirroring the wrong project

**Date**: 2026-09-01 · **Author**: María 🌸 (`3396ba14`) · **Row**: `68106100` (Mr Radio's P1)
**Method**: read-only — `ls`, `find`, `grep`, `git log/status/remote`, plus each script's DEFAULT dry run. No writes, no `--write`, no rsync mutation.

---

## The one sentence

**A green backup run is not evidence the right thing was backed up.** `par-pacific-financials-er-442109`'s script prints a success banner and *0 created, 0 deleted* every time — for a project that has no backup at all. Nothing in the output names the mistake, because `SOURCE_DIR` and `DEST_DIR` agree with each other. They just agree about the wrong project.

## Status, stated the way it should be read

**Fixed for future clones. Zero deployed copies covered.** The guard landed in the canonical template (`scripts/rsync-backup.sh`, v1.0 → v1.1, commit on the working branch). Measured: **16 deployed copies, 16 unguarded, all still stamping v1.0.**

A template fix reads as done and covers nobody until that 16 moves. **Propagation is the real work; the guard is already done.**

⚠️ **And a stale copy is silent about being stale.** The version check that would tell it — `check_for_update` — begins `if [[ -z "$PLANNING_IS_PROMPTING_ROOT" ]]; then return 0`, and skips again when the canonical file is missing. Fail-open twice, and even when it fires it only *warns*; it never installs anything. So a deployed copy is not merely unguarded, it is unguarded and quiet about it.

---

## The 16, with what each actually backs up

| Script lives in | Actually backs up (`SOURCE_DIR`) | Writes to (`DEST_DIR`) | Verdict |
|---|---|---|---|
| `google/adk-with-memory-bank-demo` | `google/adk-with-memory-bank-demo` | `google/adk-with-memory-bank-demo` | ok |
| `google/agent-in-a-day-er-422675` | `google/agent-in-a-day-er-422675` | `google/agent-in-a-day-er-422675` | ok |
| `google/gemini-live-er-418855` | `google/gemini-live-er-418855` | `google/gemini-live-er-418855` | ok |
| `google/harvey-labs` | `google/harvey-labs` | `google/harvey-labs` | ok |
| `google/kotlin-client-er-418855` | `google/kotlin-client-er-418855` | `google/kotlin-client-er-418855` | ok |
| `google/lookml` | `google/lookml` | `google/lookml` | ok |
| `google/parallel-search` | `google/parallel-search` | `google/parallel-search` | ok |
| `google/par-pacific-financials-er-442109` | `google/lookml` | `google/lookml` | 🔴 **WRONG PROJECT** |
| `google/skills-distillation` | `google/skills-distillation` | `google/skills-distillation` | ok |
| `google/terraforming-vms` | `google/terraforming-vms` | `google/terraforming-vms` | ok |
| `google/weil-nda-drafting-suite` | `google/skills-distillation` | `google/skills-distillation` | 🔴 **WRONG PROJECT** |
| `google/weil-parallel-search` | `google/weil-parallel-search` | `google/weil-parallel-search` | ok |
| `lupin-mobile` | `lupin-mobile` | `lupin-mobile` | ok |
| `lupin` | `lupin` | `lupin` | ok |
| `pdf-reduce-to-text` | `pdf-reduce-to-text` | `pdf-reduce-to-text` | ok |
| `planning-is-prompting` | `planning-is-prompting` | `planning-is-prompting` | ok |

**14 correct · 2 wrong project · 0 with `SOURCE` ≠ `DEST`.** That last figure matters: no script writes to a destination that disagrees with its own source, so nothing here is mirroring project A's files *onto* project B's mirror. Both defects are a cloned script pointing wholly at a third project.

## Dry-run receipts — what each of the two would actually do

Verified the default is dry first (line 107: `DRY_RUN="--dry-run"`; only `--write`/`-w` switches it), then ran both with no arguments.

| script | Source / Dest it uses | created | **deleted** | verdict |
|---|---|---|---|---|
| `par-pacific-…-442109` | `google/lookml` | 0 | **0** | clean no-op, green banner, backs up nothing of its own |
| `weil-nda-drafting-suite` | `google/skills-distillation` | 1,046 | **101** | would mutate a *third* project's mirror |

⚠️ **I corrected myself here.** I first reported both as harmless self-consistent no-ops, reasoning from the configuration. Half of that was wrong: weil-nda's would delete 101 files in skills-distillation's mirror. Those deletions are almost certainly correct **for skills-distillation** — its own source no longer has them — so this is not damage. It is the **wrong agent** performing them, and the operator running it has no reason to look.

⚠️ **Shared destination.** `skills-distillation`'s own script and `weil-nda`'s now write the same destination. Two scripts, one destination, neither aware of the other. Nothing has bitten; the structure is there.

## What it costs, measured

| project | DATA01 (live) | DATA02 (mirror) | last commit |
|---|---|---|---|
| `weil-nda-drafting-suite` | 577 files | 🔴 **absent — none has ever existed** | 2026-08-29 (active) |
| `par-pacific-…-442109` | 11,982 files | 433 — a stale partial | 2026-05-07 (dormant) |

**Severity: REGENERABLE, not irreplaceable.** Both are git repos with GitHub remotes at `cloud-ai-fde`, both **fully pushed** — 0 unpushed commits each; 0 uncommitted in weil-nda, 1 in par-pacific. The local-mirror gap is real and worth closing; nothing is at risk of being lost.

## The decision for Rick

Three items, all held for 22:30. None is inside my envelope or Mr Radio's.

1. **Fix the two scripts** — lines 15-16 in each. Then each script's default dry run to confirm the deleted count, then `--write`.
2. **Propagate the guard to the 16** — a re-copy of the template plus a re-edit of lines 15-16 per repo. This is the fleet-wide change and the item that actually closes the exposure.
3. **`lupin-mobile --write`** — already queued for him by Mr Radio; safe, dry run reports 0 deleted / 788 created.

⚠️ **Consider fixing the fail-open too.** Item 2 only ever gets *noticed* by the version check, and that check skips silently on most seats. Propagating once without changing that leaves the next drift equally invisible.

## Also observed, not findings

- Four orphan DATA02 destinations with no DATA01 source: `genie-in-the-box-v0`, `genie-plugin-firefox`, `lupin-lancedb-archive-2026.08.02`, `lupin-worktrees`. No live source means no job deletes them — clutter, not exposure.
- `projects-data/` is live on DATA01 with no mirror and no `backup.sh` of its own.
- 117 `lupin-wt-*` worktrees are unmirrored, which is **correct** — disposable by design. Naming it so the raw "118 unprotected" count is not mistaken for 118 findings.

## What is NOT established

- Whether either broken script has ever been **run** since it was cloned. The dry runs say what they *would* do, not what they *have* done. par-pacific's 433 DATA02 files came from somewhere and I did not determine from where.
- DATA02 trees outside the `projects/` root were not examined.
