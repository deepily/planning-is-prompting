# Lupin MEMORY.md Curation — Dry-Run Plan

**Date**: 2026-06-22 · **Author**: María 🌸 (`2f4d71cb`) · **Task**: `5209c6c6` (P2, in-progress) · **Status**: DRY-RUN — awaiting Rick's blessing before any destructive edit

> **Mandate honored**: dry-run-first (same discipline as the PIP TODO §3 re-cut). This document proposes the cuts; **nothing is applied to the live file until Rick approves the scope.** Preserve every *distinct* rule; merge only *true* near-dups; retire only *clearly* superseded.

---

## 1. Problem & target

- **File**: `~/.claude/projects/-mnt-…-lupin/memory/MEMORY.md` — **48.5 KB** (200 lines, ~90 substantive entries).
- **Read-limit**: ~24.4 KB. **Everything past the cutoff is SILENTLY DROPPED on session load** — today the bottom half of `## Feedback` (lines ~150–197) and `## Active Campaign` are *invisible* to a rehydrating Lupin session. The most recently-added durable rules are the ones being lost.
- **Hook target**: **< 17.1 KB** (≈ 65% reduction). Aggressive but reachable via (a) section consolidation, (b) merging redundant clusters, (c) retiring stale session-resume pointers.

## 2. What Clayton already did (lossless — DONE, do not redo)

Per the task body: collapsed 6 multi-line Active-Investigations blocks to one-liners; removed a verbatim Unicode-persona dup + one stale `never-auto-commit` line whose summary contradicted the current ratified commit/merge rule. **The remaining work is the owner-judgment layer only.**

## 3. Structural disorder (consolidate — lossless, high-value)

The corpus has fragmented into duplicate section headers as it grew:

| Current (scattered) | Lines | → Proposed (single home) |
|---|---|---|
| `## Feedback (additional)` | 7 | **`## Feedback`** (one section, themed subheads) |
| `## Feedback (more)` | 38 | merge ↑ |
| `## Feedback` | 109 | merge ↑ |
| `## References (additional)` | 45 | **`## References`** (one section) |
| `## References` | 97 | merge ↑ |
| `## Active Investigations` | 85 | **retire most** (§5) |
| `## Active Campaign` | 198 | **retire** (§5) |
| `## Key Patterns Learned` | 51 | keep as-is (code patterns, low churn) |

Consolidating headers + theming the Feedback block into subsections (Comms/TTS · Git & Push · Server Ops · Plan Discipline · Testing · Manager/Fleet) is **lossless** and makes the dedup obvious.

## 4. Merge clusters (owner-judgment — the bulk of the win)

Each cluster below is multiple near-duplicate rules that should collapse to one tight entry preserving every distinct nuance. **Names, not hashes**, per the broadcast rule.

### 4a. The Push family (4 → 1)
- `never_auto_commit_push` · `never_ask_to_push` · `not_a_gate_drive_to_completion` (push-half) · `never_prompt_about_pushing`
- **All say the same load-bearing thing**: commit+merge = standing manager authority once green+reviewed; **push = Rick's word, never prompted/offered, but the manager EXECUTES it**. Merge to ONE canonical "Commit/merge standing; push is Rick's, never surfaced" entry; keep the `[[…]]` cross-links.

### 4b. TTS brevity family (4 → 2)
- `notify_spoken_under_450_chars` (sentences-max-3 + reject-boundary) · `tts_body_headline_and_takeaway_only` · `tts_questions_one_line_not_packed` · `recraft_speech_dont_pipe_terminal`
- Merge to: **(1)** "Spoken = 3 sentences max, headline+takeaway, re-crafted for speech; ~500-char cap is the reject boundary; detail→abstract" and **(2)** "ask_* question = ONE short line + recommendation; pros/cons→options+abstract."

### 4c. Pros/cons/recommendation (2 → fold into 4b)
- `always_include_pros_cons_recommendation` overlaps `tts_body_headline_and_takeaway_only`. Fold the "synthesis not inventory + flip-condition" nuance into 4b-(2); retire the standalone.

### 4d. Server-ops family (~10 → 3)
- :8000/:7999/test-server entries: `idle_server_bounce_self_authorized` · `test_server_static_snapshot` · `test_server_monopolize_mode` · `fastapi_auto_reload` · `dev_server_bounce_courtesy` · `dev_server_bounce_via_docker` · `small_ad_hoc_runs_go_to_7999` · `test_server_free_for_baseline_capture` · `baseline_capture_disable_tfe` · `port_routing_dual_container` · `test_ports_minus_1000`
- Merge to **3 entries**: **(1)** :7999 dev = auto-reload, bounce via `docker restart lupin-rest-dev`, advise-first; **(2)** :8000 test = static snapshot, monopolize-mode submit-only, idle-bounce self-authorized but live-job kill = Rick; baseline-capture standing perm + `auto_fix_on_failure:False`; **(3)** port map (dev 7999 / test 8000 / cloud-test −1000 / in-container 7999). Every distinct fact survives; the repetition dies.

### 4e. Plan-mode / ExitPlanMode family (6 → 2)
- `plan_mode_discipline` · `plan_approval_means_go` · `exit_plan_mode_is_not_user_approval` · `walk_through_plan_before_asking_proceed` · `documentation_step_stops_at_doc` · `always_serialize_plan_to_rd_scope_post_exit`
- **Tension to preserve, not flatten**: `plan_approval_means_go` (approval = go) vs `exit_plan_mode_is_not_user_approval` (framework-default text ≠ real approval). These are NOT contradictory — the second qualifies the first for voice/AFK contexts. Merge to **(1)** "ExitPlanMode discipline: serialize→R&D first, walk findings, no 'proceed?', framework-accept ≠ live approval in voice/AFK" and **(2)** "Documentation-step stops at the doc."

### 4f. Coverage / test-fix family (3 → 1)
- `100pct_coverage_multiplexer` · `fix_all_failing_tests` · `never_defer_test_fixes_hold_fire_exception`
- Merge to ONE: "100% line+branch+function is a hard Lupin-wide gate; never defer/classify-as-pre-existing — you wrote it, you make it pass, even during quiet windows."

### 4g. Directed-DM / handoff family (2 → 1, AND update for `dm_send`)
- `commons_post_is_blackboard_not_push` · `worker_handoff_must_be_directed_dm` — both still say `commons_send_to`, which is **DEPRECATED** in favor of `dm_send` (per the S110 PIP migration). Merge to one entry AND refresh the verb to `dm_send`. **(Flag: this is a content correction, not just a merge.)**

## 5. Retirement candidates (stale — verify-before-cut)

- **`## Active Investigations` (lines 85–95)** — mostly **session-resume mementos for COMPLETED/held work owned by OTHER personas** (Tiberius DM-API "COMPLETE/held", GCP-M2 "SHIPPED+VALIDATED+PUSHED", Mr Radio notification-native "DONE+held", arbiter loops "DONE", line 93 literally "Superseded by later sessions"). These are *pointers into `io/mementos/`*, not durable facts — they belong in those mementos, not long-term memory. **Retire the DONE/superseded ones**; keep only genuinely-open investigations (`ws3_layout_parity_oracle`, `voice_persona_clear_investigation`, `speakerphone_thought_exercise`) if still live — **confirm with Rick**.
- **`## Active Campaign` (lines 198–199)** — CoSA coverage campaign "resume state" dated 2026-05-30; CoSA folded into the mono-repo 2026-05-29 and the campaign is stale. **Retire** (the runbook path is preserved in src/rnd).
- **`skip_arnold_yes_no_neither_ux`** — very narrow ("omit one TODO item from summaries"); candidate to retire. **Confirm.**
- **Heartbeat-hold id inconsistency**: `heartbeat_hold_uses_stable_session_id` (line 98) cross-links `[[reference_heartbeat_hold_uses_harness_session_id]]` — one *corrects* the other (stable vs harness id). Not a cut — **reconcile to the verified-correct one** (stable id, per line 98's body) and fix the dangling link.

## 6. Cross-repo note (the bury-the-ask dedup)
Clayton saved `feedback_never_bury_the_decision_ask_directly.md` (lupin memory) and I saved `feedback_gate_is_a_direct_ask.md` (plan memory) for the **same** 2026-06-22 Rick broadcast. These live in *different repos* so it's not a within-file dup — but they should **cross-link** (`[[…]]`) so the rule reads consistently from either repo. No deletion.

## 7. Estimated reduction
| Action | Approx. saving |
|---|---|
| Push family 4→1 | ~0.9 KB |
| TTS family 4→2 + pros/cons fold | ~1.4 KB |
| Server-ops ~10→3 | ~3.2 KB |
| Plan-mode 6→2 | ~1.8 KB |
| Coverage 3→1 | ~0.9 KB |
| Retire Active Investigations (most) + Active Campaign | ~3.0 KB |
| Section-header consolidation + theming | ~0.3 KB |
| **Total** | **~11.5 KB → lands ≈ 37 KB** |

⚠️ **One pass may not reach < 17.1 KB.** The merges above are the *safe* high-confidence layer (~11.5 KB). Reaching 17 KB needs a second, more aggressive judgment pass (tightening every remaining entry's prose to one line). **Recommendation: ship the safe pass first, re-measure, then decide whether the aggressive second pass is worth the nuance-loss risk** — rather than over-cut in one swing.

## 8. Execution plan (reversible)
1. **Back up first** — copy the live file to `MEMORY.md.bak-2026.06.22` (the file is outside any git repo → no git-object backup; this is the only undo).
2. Apply §3 consolidation + §4 merges + §5 retirements via **Edit** (never wholesale Write — keeps the diff reviewable).
3. Re-measure size; report the before/after to Rick.
4. Verify a simulated load reads cleanly (no broken `[[…]]` links).
5. Mark `5209c6c6` done with the new byte-count as the receipt.

## 9. Open questions for Rick (the gate)
1. **Scope**: ship the **safe ~11.5 KB pass** now (lands ≈37 KB, still over target but lossless-ish), or push straight for **< 17 KB** in one aggressive pass (higher nuance-loss risk)? *(Rec: safe pass first, re-measure.)*
2. **Active Investigations**: retire all the DONE/held other-persona memento-pointers, or keep any as live? *(Rec: retire DONE/superseded; keep only confirmed-open.)*
3. Anything in §4/§5 you want **preserved verbatim** that I've marked for merge/retire?
