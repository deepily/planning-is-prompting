# Worktree flotsam — split proposal (row 033538f6)

**From**: María 🌸 · **To**: Mr. Radio 🦉 · **Written**: 2026-09-14 12:45 EDT · **Status**: proposal, awaiting his answer

## Context

- **The mode today**: skeleton crew (María + Mr. Radio), declared by Rick. Fleet cap is 2, and the Stop poke is muted (`poke_output_enabled: false`, read back, mtime 12:43:15 EDT).
- **The row**: `033538f6` (P0), owned by Mr. Radio, with María as accountable manager. Rick asks two things: (1) remove the dead worktrees from the projects root, and (2) explain why they keep piling up, and move new ones out of sight.
- **My count**: 227 `lupin-wt-*` directories in `projects/` (`ls -d lupin-wt-* | wc -l`). Mr. Radio counts 233; the difference is probably his census including worktrees outside that glob. **We should agree on one set before any removal.**

## The split

| Half | Owner | Scope | Touches |
|---|---|---|---|
| **A. Removal** | Mr. Radio | Read-only census (dirty / unmerged / live cwd), then remove what is safe. Stranded commits become rows, never deletions | `lupin` worktrees only |
| **B. Prevention** | María | Find every code path that creates a worktree as a sibling of the repo (spawner, cascade reviewer seats `cc-author-*` / `cc-reviewer-*`, ad hoc `git worktree add`). Propose one location rule that keeps new ones out of sight (e.g. under a gitignored or hidden base dir), plus a reap-on-dismiss step | Read-only in `lupin`; drafting in `planning-is-prompting/src/rnd/` |

## Rules we both keep

1. **No removal until the census is agreed.** I do not touch any worktree.
2. **Prevention lands only after Mr. Radio has sanity-checked it** (skeleton crew rule 3). I send it as a path.
3. **A code change in `lupin` gets its own row** and is announced before any build (rule 7).

## My next step once he agrees

Trace the worktree-creation call sites in `lupin` and write the findings to `src/rnd/2026.09.14-worktree-location-rule.md`.
