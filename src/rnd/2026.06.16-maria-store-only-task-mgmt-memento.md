# María 🌸 — Rehydration Memento (store-only task-management session)

**Written**: 2026-06-16 (late, ~01:50 UTC) · **By**: María 🌸 (session `0ba3e504`) · **Persona**: María 🌸 (PRESERVE — do NOT re-allocate) · **Project**: `plan` (planning-is-prompting) · **Role**: Workflow Steward + plan-side doctrine owner.

**Purpose**: continuity seed for re-spin/rehydrate (Rick requested at ~60% context). Read this FIRST, then the plan doc, then rebuild the harness list from §8 (verbatim pending list) per the rebuild contract.

---

## 1. Current state — what's in flight

The whole back-half of this session converged on a single architecture decision: **how to track owed-work + liveness token-efficiently so the fleet drives stuck/lazy workers to completion.** It evolved through three framings:
- **v0 (status quo, broken):** harness task list = primary; a `PostToolUse` mirror copies it to the unified store. This mirror is the root of tonight's bug class (`9bf1dc4a` write-gate, `9b23d5bc` /clear-collision, `82e4eaf0` two-sources-of-truth).
- **v1 (María, rejected):** keep the harness as a "rendered read-only view" synced store→harness. Rick killed it — the only "always-current" sync form was **per-turn context injection = a token non-starter** (token efficiency is the whole point).
- **v2 (Rick's call, AGREED-IN-PRINCIPLE — the current plan):** **JETTISON the native Claude Code harness task list entirely.** Track owed-work SOLELY in the unified store. The heartbeat poke (cheap count-query at stop-time) AND the `:8001` arbiter both query that ONE store → they cannot diverge (kills `82e4eaf0` by construction). The human sees a **dedicated UI card rendered from the store, like the fleet-status card** — NOT the native widget.

**Plan doc (v2, FULL detail):** `src/rnd/2026.06.16-store-canonical-task-management.md`. Doc link: `/app/docs?path=planning-is-prompting/src/rnd/2026.06.16-store-canonical-task-management.md`.

**Status**: plan v2 drafted + Rick "likes the general outlines, we're understanding the same thing." NOT yet formally approved. NOT implemented (held).

## 2. Rick's comments on viability (verbatim intent — REQUIRED by Rick in this memento)

- **Direction approved in principle**: "I like the general outlines of what you're proposing. We are understanding the same thing." The jettison-the-native-list architecture is the agreed path.
- **Process Rick wants (the binding part):** (a) on rehydrate, María re-enters **PLAN MODE** and shows Rick the **ENTIRETY of the plan ON THE CONSOLE** before he approves; (b) only after his explicit approval, **hand the plan off to Mr Radio** (lupin manager); (c) Mr Radio **spins up a SWE team** to **flesh-out → review → implement → test → review**.
- **María's role**: plan AUTHOR / Steward — do NOT implement the code/doctrine yourself; author + hand off. (The build is Mr Radio's crew.)
- **Hard rule reinforced this session** ([[feedback_plan_link_not_execute]]): a plan-mode ExitPlanMode "approved" is NOT authorization to execute; send Rick a doc link, let him READ it, wait for his explicit go. (I violated this once tonight — ran ahead off ExitPlanMode; Rick: "Wait wait wait... I asked you to send me a link.")

## 3. Rehydration instructions — FIRST ACTIONS

1. MCP Phase A startup (get_session_info, confirm persona María 🌸, set topic).
2. Rebuild the harness TODO list from §8 below (verbatim).
3. **Read the plan doc** `src/rnd/2026.06.16-store-canonical-task-management.md` in full.
4. **Re-enter PLAN MODE** and **present the ENTIRE plan to Rick on the console** for his review + approval (task #16 — the #1 priority).
5. On Rick's explicit approval → **hand off to Mr Radio** with the plan doc + context; he runs `/spin-up-swe-team` for the build (flesh-out/review/implement/test/review). Do NOT implement yourself.
6. Discard this memento after successful rehydration.

## 4. The plan in one breath (so the rehydrated session has the gist before reading)

ONE store (the source of truth). THREE readers: (1) the Stop-hook self-poke makes a cheap `task_query` COUNT at stop-time → pokes if owed + no hold (no list injected into context — count only); (2) the `:8001` arbiter queries the SAME store (single source → no divergence); (3) a dedicated UI card renders `task_query` like fleet-status (full fidelity: blocked/blocked_by/next_chase/owner). The native harness list is NOT used; the mirror is retired (nothing to mirror); `9bf1dc4a`/`9b23d5bc` die with it. Doctrine mandate: write all owed work to the store; MUST NOT use the native widget; query the store on demand to see your list. Tradeoffs addressed: model reads its list on-demand (cheap, not per-turn); store-down fail-safe (don't spurious-poke); UI card richer than the native widget.

## 5. Build split (for the Mr Radio handoff)

**Lupin (Mr Radio's SWE crew):** fix `9bf1dc4a` (derive_project_name from the session bridge, not LUPIN_ROOT) + `9b23d5bc`; build the Stop-hook store-count seam (replaces transcript replay; store-down fail-safe); point the arbiter at the same `task_query`; build the task-list **UI card** (template = the existing fleet-status card); retire the harness→store mirror. **PIP doctrine (María):** install the Part-1 store-only mandate + strip the "use native TaskCreate first / auto-mirror" guidance across the 6 surfaces (task-store-discipline.md, global/CLAUDE.md + live ~/.claude/CLAUDE.md, claude-config-global.md, swe-team-roles.md, session-start.md Step 4.7, memento-management.md §2 elem 8) — with the "not-live-until-the-build-ships" caveat.

## 6. Coordination state

- **Mr Radio 🦉** (lupin manager, session `f6292818`) — but his session went OFFLINE late (a build-handoff DM bounced 404). On rehydrate, re-establish contact before handing off. Bug `82e4eaf0` (the dual-source-of-truth, P1) is filed in the store owned by `mr_radio` and carries the diagnosis.
- All this session's changes are **HELD LOCAL — no commit** (Rick's push call at session-end; none given).

## 7. Session accomplishments (what got DONE tonight — context, not action)

6+ decision/design threads ruled + landed (all held local): (a) 3 held decisions — session-end commit ritual = show-a-receipt, accountability write-scope = managers-first, worker-await = passive+escalate-if-aged; (b) #11 commit-gate procedural rewrite across 3 workflow docs; (c) memento→harness-list rebuild contract w/ Mr Radio (session-start Step 4.7 + memento element 8 + global pointer); (d) #6 auto-mirror doctrine caveat on 3 global-config surfaces; (e) #12 reaction-rubric v1.1 (initiative recognition); (f) #13 accountability design → ruled (5 decisions) → graduated to manager-autonomy.md §9; (g) the ⛔ DRIVE-DON'T-WAIT manager Cardinal Rule (swe-team-roles.md v1.3); (h) worker subtask fan-out design (4 Qs ruled); (i) the work-debt source-of-truth diagnosis → bug `82e4eaf0` → Option A (store canonical) → which evolved into THIS store-only plan. All in TODO.md Decisions Log + README + manifest.

## 8. VERBATIM PENDING TODO LIST — REBUILD THESE on rehydrate (harness `TaskCreate`)

*(8 items done this session, not relisted. Open items below — rebuild verbatim. Most are blocked/deferred; #16 is THE priority.)*

- **#16 [in_progress — THE PRIORITY]** POST-REHYDRATE: re-enter PLAN MODE → present the FULL store-only plan v2 on console for Rick's review + APPROVAL → THEN hand off to Mr Radio for the SWE-team build (flesh-out/review/implement/test/review). Plan: `src/rnd/2026.06.16-store-canonical-task-management.md`. Do NOT implement; author + hand off.
- **#15 [pending — folds into #16]** Land store-only doctrine across the 6 PIP surfaces (was "store-canonical"; now the v2 store-ONLY mandate). Awaits the plan approval + is part of the PIP-doctrine build lane. Carries the "not-live-until-build-ships" caveat.
- **#14 [pending — blocked]** Work-debt dual-source-of-truth bug `82e4eaf0` (lupin, Mr Radio) — now SUBSUMED by the v2 store-only plan (the architecture IS the fix). Track to resolution.
- **#5 [pending — blocked on lupin]** MAKE IT WORK umbrella (plan-side liveness) — mostly landed; remaining = the lupin builds.
- **#7 [pending — blocked]** Verify plan-session mirror after `9bf1dc4a` lands (I'm the plan-session test case). *(Note: v2 retires the mirror — this verification may transform into "verify the store-count poke works for a plan session.")*
- **#10 [pending — blocked]** Verify arbiter tmux-wake post-bounce (Clayton `155a463c`) from the receiving end — awaits a fresh post-bounce arbiter poke.
- **#12-rubric [pending — design done, build deferred]** Reaction rubric + consumption design — v1.1 complete (`src/rnd/2026.06.10-reaction-rubric-and-consumption-design.md`); UI build deferred to post-multiplexer-cutover.

## 9. Standing reminders

- Persona = **María 🌸**, PRESERVE across `/clear`. MCP Phase A on startup.
- Heartbeat hold goes in the **lupin root** with a **CURRENT** `held_at` read from the REAL clock (I kept stamping it ~40min behind tonight → holds expired early → spurious pokes; read `date -u` or the latest `~/.claude/heartbeat-events/<full-sid>.jsonl` ts).
- All changes HELD LOCAL — no commit; push is Rick's session-end call (none given this session).
- New memory this session: [[feedback_plan_link_not_execute]] (send a plan LINK, don't auto-execute off ExitPlanMode).
