# Mini Plan 02 — Offline-user doctrine prescribes verbs no agent can call

**Store row**: `74a3ff4d-df93-490d-b49d-77d2f786c6b1` (bug, P1, owner maria, project plan)
**Author**: María 🌸 (d3254802) · 2026-07-21 (S142)
**Executor**: María (solo — it is doctrine text in my own repo, reversible, standing authority)

---

## 1. What is actually true, re-derived at source today

The row's original diagnosis (*"NEITHER VERB EXISTS"*) is **FALSE and already corrected in-body by Cheech**. I re-verified both halves myself rather than inherit either claim:

| Claim | Verdict | Receipt |
|---|---|---|
| The verbs don't exist | **REFUTED** | `heartbeat_user_gates.py:62` `make_gate(...)` · `:395` `defer_to_chase( gates, gate_id, next_chase_ts )` |
| Agents can call them | **REFUTED** | not on the 28-verb MCP surface; and the signature is **host-shaped** — `gates` is the list living inside the hold file, read/written by the Stop hook. An agent has no handle to pass. |
| Zero production call sites | **CONFIRMED** (mine, today) | `grep -rn 'defer_to_chase\|make_gate' --include=*.py` over lupin: every hit outside the def is a unit test or the module's own `quick_smoke_test()`. `arbiter_job.py:4495` is a **comment**, not a call. |

**So the defect is not "the verb is missing." It is "the doctrine prescribes an act the reader cannot perform,"** and the tests prove the functions work while proving nothing uses them. Anyone who asked *"is it tested?"* got a confident yes — which is exactly why this survived since 2026-07-07.

## 2. The two live surfaces

- `workflow/cosa-voice-integration.md:244-249` — the MANDATE block, steps 1-3 + the post-chase re-defer rule.
- `~/.claude/CLAUDE.md:363` (mirrored at `global/CLAUDE.md:363`) — the one-line pointer.

Step 3 of the doctrine — *"block the STORE gate with the same `next_chase_ts`"* — **is agent-callable and correct today**: `task_transition( ..., "blocked", next_chase_ts=…, blocked_by=[{kind:user,id:…}] )`. It is steps 1-2 that point at vapor.

## 3. The fix — option (b), stop-the-bleed

Cheech **struck option (a)** (build the verbs onto the MCP surface): zero callers means exposing them builds a road to a dead building. I agree, and I am not re-opening it.

Rewrite both surfaces so the prescribed act is the one that works:

1. **Promote step 3 to be THE mechanism.** The store transition is the deferral — it is what the Stop-hook oracle, the UI card, and the arbiter chase-clock all actually read.
2. **Demote the hook verbs to a "how the host implements this" footnote**, explicitly marked NOT agent-callable, with the host-shaped-signature reason stated. Keep the file/line reference — it is true and useful to whoever builds the host side.
3. **Keep the post-chase RE-DEFER rule** — it is behaviourally right and expressible entirely through `task_transition` (park/re-block with a NEW future chase). This is the half that must survive the edit.
4. **Preserve my standing interim ruling** (*"at the chase, leave it stale rather than bounce through `in_progress`"*), because `blocked→blocked` is still rejected 422 as an illegal edge — the underlying friction that produced this row is NOT fixed by the doc edit.

## 4. What this does NOT do — and the residue that goes to Rick

The row's real open question is a **design** call, not a build ticket: **should agent-side gate-deferral exist at all, and who executes it — the agent, or the host hook?** Today the hook owns the hold file and the agent owns the store row, and the doctrine straddles both as if one actor held both. Option (b) makes the doctrine honest; it does not answer that.

⇒ Minted as a separate `decision` row for Rick. **This plan does not wait on it.**

## 5. The CLASS, not the instance — the sweep

If one workflow doc prescribes an unreachable call, others may. Owed sweep: every prescribed tool/verb across `workflow/*.md` + `global/CLAUDE.md`, each checked for **reachability** (on the MCP surface AND callable with what an agent holds), not merely existence.

Cheech ran a 147-item census on 2026-07-18 and found **4 new instances, including a retired verb still live in the cascade Manager's tool list** — so the class is confirmed non-empty and the sweep is real work, not a formality. **This is the piece to hand to Mr Radio's crew**, scoped as a mechanical reachability audit with a reported scanned-count (a sweep that doesn't print what it scanned can't be distinguished from a sweep that scanned nothing).

## 6. Acceptance criteria

1. Neither surface instructs an agent to call `defer_to_chase` or `make_gate`.
2. The prescribed deferral act is `task_transition(..., blocked, next_chase_ts, blocked_by)` — verifiably agent-callable.
3. The post-chase re-defer rule survives, expressed in agent-callable terms.
4. The "leave it stale at the chase" interim ruling survives with its 422-illegal-edge reason intact.
5. The host-side verbs are still findable by file:line, marked NOT agent-callable.
6. `global/CLAUDE.md` and `~/.claude/CLAUDE.md:363` agree byte-for-byte after the edit.
