# Plan-Review Dispatch — Canonization DRAFT (for María's pen)

**Date**: 2026-07-18
**Author**: Cheech 🌿 (session `c2940374`), worker to María 🌸 (`c0d19219`)
**Store**: `525acac8` (canonization) · folds D3 from `74a3ff4d` (corpus sweep)
**Status**: ⚠️ **DRAFT — NOTHING HAS LANDED IN `workflow/`.** María holds the pen on doctrine.
**Scope discipline**: **slot + minimum contract ONLY.** Mechanics remain held for pattern-watch (`73f397d2`).

---

---

## ⚠️ READ FIRST IF YOU ARE ABOUT TO SWEEP THIS CORPUS

**Every count in this document states its POPULATION and its FILTER in the same sentence.** That is not
style. **Eight** counts in this document were wrong before they were right, and **every one was wrong
because of a scoping choice, never because of broken logic** — and a scoping choice is invisible in the
output.

| # | Count | Wrong because | Caught by |
|---|---|---|---|
| 1 | exists-universe = 105,695 defs | scan **included** `.venv` + worktrees | reading its own output |
| 2 | D2 live sites = "2" (→4) | banner coverage **eyeballed**, not computed | verifying the fix |
| 3 | uncovered = "7" (→21) | sweep **keyword-gated** on "skip" | auditing my own citations |
| 4 | "21" (→22) | two sweeps, **neither seeing the union** | assembling the union by script |
| 5 | "22" (→24) | **both sweeps keyed on VOCABULARY — a SHARED failure mode** | **Clayton, critic seat** |
| 6 | `README.md:91` missed | his `sed` cut **COLUMNS** at 95 chars; the rule begins at char 130 | **Clayton, on himself** |
| 7 | homonyms = "2" (→**3**) | census was **case-SENSITIVE**; the sites are `Skip-with-reason` | re-running the instrument with scope widened |
| 8 | population = "97" (→**111**) | **97 excluded root `*.md` — including a file the landing itself edited** | **Clayton, reconciling two definitions** |

> **The instrument was never wrong. The POPULATION was — eight times, three seats, one day, and only
> ever in the direction that made the work look finished.**

**#7 is the one to read if you only read one.** The census returned a **clean, confident, EMPTY result**
for the very file holding two of the three homonyms. A flag-hunt run on that population would have
deleted a live mechanism *in the name of a retirement* — and nothing in the output would have hinted at
it. **It was caught by a seat re-running its own instrument with the scope widened, not by a peer.**

**#8 is self-refuting and that is why it is listed.** `97` = `workflow/`(58) + `.claude/commands/`(39).
`110` = `git ls-files '*.md'` minus `history/`, todo-archive, `src/rnd/`. **`111` = the WORKING TREE,
and it is the reconciled figure this document now uses.** The `97` is refuted **by a commit that had
already landed when it was quoted**: `0e245b8` edits `README.md`, a root file — inside 110, outside 97.
> **A denominator that excludes a file the landing modified cannot verify the landing.**

**AND BOTH PRIOR NUMBERS SHARED A BLINDNESS NEITHER SEAT NAMED**: both came from `git ls-files`, which
**cannot see untracked files by construction**. Exactly one such file exists in these directories —
`.claude/commands/plan-authoring-cascaded.md`, present, readable, invocable, and invisible to every
sweep either seat ran. **110 + 1 = 111**, independently confirmed by
`git ls-files --others --exclude-standard`. *(It has since been read for this purpose and carries **no**
pattern-keyed gate statement — a checked null, not an assumed one.)*

**POPULATION, STATED ONCE AND USED THROUGHOUT**: **111 files** = every `*.md` in the working tree
(tracked **and** untracked) under `workflow/`, `.claude/`, and the repo root. **`src/rnd/` and
`history/` are excluded DELIBERATELY** — they are the research and historical record, where stale rules
are *supposed* to remain quoted; editing them would falsify the record rather than correct the corpus.

**🔴 AND WIDENING IS NOT THE ANSWER, IT IS A TRADE.** The final sweep — vocabulary keying dropped,
±2-line window — returned **50 hits · 28 not already known · ~2 genuine.**
> **~7% precision. A high-recall sweep does not remove the judgment; it RELOCATES it — from a scoping
> choice into a triage choice — and the relocation is not visible in the output either.**

**Triage is the step where BOTH seats on this document have already erred, in opposite directions**
(§14.3). So the operative rule, which cost two careful people two opposite mistakes to learn:

> ### A "skip" site is UNCLASSIFIED until its TRIGGER is read — never classified by its verb.

**Three known instrument failure modes** (§14.9): **(1) wrong population** — fixed by widening ·
**(2) vocabulary keying** — fixed by dropping verb filters · **(3) line granularity** — **MITIGATED,
NOT SOLVED.** A rule split across a heading and a table three lines down is invisible to every sweep in
this document. **`installation-wizard.md:1184/:1185` is a live instance**: one sentence, two lines, and
the first draft of A7 would have edited half of it.

**🔴 THREE homonyms found — not two. Any future flag-hunt must discriminate THREE mechanisms wearing
the same words**, or it will delete a working mechanism in the name of a retirement:

| Site | Mechanism | Status |
|---|---|---|
| `.claude/commands/plan-review.md` · `INSTALLATION-GUIDE.md` (4 sites) | the **`--skip-with-reason` FLAG** | **RETIRED 2026-07-18** (Class C) |
| `workflow/plan-review.md:47` | bare string — **per-convention** exemption logged in `00-index.md` | **LIVE, untouched** |
| `p-is-p-02-documenting:532` | bare string — **per-convention** exemption | **LIVE, untouched** |
| `p-is-p-02-documenting:552` | bare string — **cascade-DECOMPOSITION** exemption | **LIVE, untouched** |

**`:552` was found during the landing and appears in NO prior artifact** — not this draft, not the
critic's pass, not the author's memento, all of which state the family has two members. **The homonym
was never a one-off and it is not even a pair; it is a family, and the family was still growing while
the retirement that must avoid it was being written.**
**Computed, not eyeballed** (case-insensitive, over the 111-file population): **4 flag sites, 3 homonym
sites, disjoint** — re-run *after* the deletions landed: **0 flag definitions/examples remain, 3/3
homonyms intact.** That post-hoc re-run is the control; the pre-hoc count alone would not have been one.

---

## 0. What this document is

A **proposed diff, stated in full, not applied.** Every edit below is written so María can rule it line
by line and either hand it back or land it herself. Three of the six proposed edits are one sentence;
the two structural ones are the mermaid branch and a new `plan-review.md` section.

**It is also a record of two things I decided and want overruled if wrong** — flagged inline as
**⚑ MY CALL**. I have not smoothed them into the ruling as though Rick settled them; he did not.

---

## 1. THE RULINGS THIS IMPLEMENTS (settled — do not re-litigate)

**Rick, 2026-07-18**, relayed by María (DM thread `7edb4251`), after asking unprompted *"did you stand
down the routing for the critique work — every plan gets a reviewer? I wanted you to finish that."*

| # | Ruling | Consequence |
|---|---|---|
| R1 | **`/plan-review` is the single generic entry**, dispatching **internally** | No new command is minted. One entry point **removes a decision** instead of adding one. |
| R2 | Dispatch by **size**: **≥2 sections → cascade; else → the critique** | The existing cascade keeps its trigger; the else-branch is what's new. |
| R3 | **Every plan DOCUMENT enters the gate** — *not* every pattern | **This is what closes Pattern 3 and Pattern 4.** An investigation with no plan doc has nothing to gate. |
| R4 | The else-branch **SPAWNS ONE CRITIC SEAT** | **Self-critique explicitly rejected.** The branch supplies a *second person*, not a different procedure. |
| R5 | Scope = **slot + minimum contract only** | Mechanics (timeboxes, templates, spawn ordering, re-entry) stay held for pattern-watch. |
| R6 | **The bar**: a refutation of ≥1 specific cited claim, **OR** a written account of what was attacked and why it held | **An account, not a verdict.** *"Looks good" is a contract violation, not a pass.* |

**Prior ruling still in force** (Rick, 2026-07-16): the NAME question — `/plan-critique` vs
`/plan-precritique` vs "Phase 0" — was deferred with *"do NOT self-name."*
⇒ **R1 DISSOLVES IT RATHER THAN ANSWERING IT.** There is no new command, therefore nothing to name.
`525acac8`'s stated blocker (*"BLOCKED ON: N1 (the name)"*) should be **retired as moot**, not answered.
**I have not named anything.** The branch is referred to below as "the critique branch" — a description,
not a command name.

---

## 2. THE GAP, RE-VERIFIED FROM THE DOCS TODAY (not recalled from `525acac8`)

Receipts pulled 2026-07-18, cited to file:line:

```
Pattern 1/2/5/6 → /plan-review (2-pass, mandatory) → cascade at ≥2 sections    [OTHER SEATS]
Pattern 3       → /plan-review-reuse ONLY (REUSE-scoped, optional, standalone)  [**SELF**]
Pattern 4       → nothing
```
- `plan-review.md:5` — *"Mandatory for … Pattern 1, 2, 5, or 6 … Optional standalone REUSE pre-pass
  available for Pattern 3 plans (`/plan-review-reuse`). Pattern 4 (Investigation) skips entirely."*
- `p-is-p-00-start-here.md:273` — same split, restated at the gate.
- `p-is-p-00-start-here.md:267` (mermaid) — `Branch -->|"Pattern 3, 4 (Small/Simple)"| Skip["Skip to
  execution"]` → `Skip --> Code` (`:270`). **The bypass is drawn in the diagram.**
  *(This citation read `:266` in the first version of this draft. A machine citation-audit caught it
  off by one. Noted rather than silently corrected — it is the same error class as §11's.)*

⇒ **AT THE SMALL TIER, EVERY AVAILABLE REVIEW IS A REVIEW OF YOURSELF, BY YOURSELF.**

**🔴 AND IT IS WORSE THAN `525acac8` FILED IT.** My corpus sweep (`74a3ff4d`, D3) found that
**`/plan-review-reuse` DOES NOT EXIST** — prescribed 3× (`p-is-p-00-start-here.md:273`,
`plan-review.md:5`, `plan-review.md:98`), **no command file, no workflow doc.** Verified against the
command directory, with a planted control confirming the check fires.
⇒ **Pattern 3 does not have a weak self-review. It has NO review at all, and has been told it does.**
That is the same class as `74a3ff4d` itself — **doctrine prescribing a verb in the imperative that
nobody built** — and it is what forced R3.

**Confirmed by grep today**: `critique`, `pre-implementation`, `precritique` appear **ZERO times** in
`plan-review.md`, `p-is-p-00-start-here.md`, `swe-team-roles.md`. **The slot is genuinely empty**; this
is not a rename of something already there.

---

## 3. ⚑ MY CALL #1 — the critique lives as a SECTION of `plan-review.md`, not a new doc

**Not ruled by Rick. Mine, and I want it checked.**

R1 settles the *command* surface (one entry). It does **not** settle the *document* surface. Options:

| Option | Cost | Risk |
|---|---|---|
| **(A) A new `plan-critique.md` workflow doc** | new doc + README entry + cross-refs | **A doc wants a door.** `/plan-authoring-cascaded` is a doc that sat 14 months without a command; `plan-critique.md` would generate exactly that pressure — and minting the command later **violates R1**. |
| **(B) A section inside `plan-review.md`** ✅ | one new section, no new file | The doc gets longer. |

**Recommend (B).** The dispatch is *internal to the gate*, so its specification belongs *inside the
gate's own document*. **(A) manufactures the precise artifact my own sweep just filed a defect about.**

---

## 4. THE PROPOSED DIFF — six edits, stated in full

### EDIT 1 — `plan-review.md:5` (the "When to use" line) — **REPLACE**

> **Current**: *"Mandatory for `/p-is-p-01-planning` Pattern 1, 2, 5, or 6 plans (the patterns that fire
> `/p-is-p-02-documentation`). Optional standalone REUSE pre-pass available for Pattern 3 plans
> (`/plan-review-reuse`). Pattern 4 (Investigation) skips entirely."*

> **Proposed**: *"**Mandatory for every plan DOCUMENT, regardless of pattern.** The trigger is the
> existence of a plan document, not the pattern that produced it — if `/p-is-p-01-planning` yielded a
> plan doc, that doc enters this gate before code is written. An investigation that produces no plan
> document has nothing to gate and is out of scope by construction, not by exemption. **The gate then
> dispatches by size (§4a): ≥2 reviewable sections → the cascade; otherwise → the critique branch.**"*

**Note the wording carries R3's distinction deliberately**: *out of scope by construction, not by
exemption.* A pattern-based exemption is what let Pattern 4 read as "skips the gate"; a
document-based trigger cannot produce a skip, only a no-op.

### EDIT 2 — `plan-review.md` — **NEW SECTION §4a**, placed immediately before the REUSE pre-pass (§4)

```markdown
## 4a. Dispatch: Cascade or Critique

Every plan document that reaches this gate is routed by SIZE, not by pattern:

| Shape | Route | Why |
|---|---|---|
| **≥2 independently-reviewable sections** | `/plan-review-cascaded` | Reviewer attention is the binding constraint; sections pipeline. |
| **Otherwise (single-section / small)** | **The critique branch (below)** | One plan, one adversary. |

### The critique branch

**SPAWN ONE CRITIC SEAT.** The branch is not a different procedure you run on yourself — it exists to
supply a SECOND PERSON. **Self-critique does not satisfy this branch.** A session that reviews its own
plan has produced zero other-seat checks, which is the exact gap this dispatch was added to close.

**The critic receives**: the plan document, and the standing mandates it must honor.
**The critic's mandate** is the Reviewer charter already in force (`swe-team-roles.md` § Reviewer):
*adversarial review — you try to refute, not rubber-stamp.* Nothing new is created here; the charter
is applied one phase earlier, to a spec instead of a diff.

### 🔑 THE BAR (non-negotiable)

The critique MUST produce ONE of:
- **(a) a refutation of ≥1 specific claim in the plan, CITED** to the claim it refutes; or
- **(b) a written ACCOUNT of what was attacked and why it held** — what they tried to break, how they
  tried, and why it survived.

**AN ACCOUNT, NOT A VERDICT.** *"Looks good"* is a **contract violation, not a pass.** A clean critique
is one you *tried and failed* to break, and the account is the evidence that you tried.

> **Why the bar is the feature**: without it, "reviewer glanced, looks good" is a check that cannot
> fail — a control whose passing state is indistinguishable from its own absence. The branch would
> then certify plans while measuring nothing.
```

### EDIT 3 — `plan-review.md:98` — **REPLACE the `/plan-review-reuse` standalone reference**

> **Current**: *"**Standalone**: For Pattern 3 plans, invoke `/plan-review-reuse` with the single-doc
> plan path."*
> **Proposed**: *"**Single-doc plans**: the REUSE pre-pass works on any doc shape, not just multi-doc
> Pattern A/B/C structures. It runs as part of this gate under the critique branch (§4a) — there is no
> separate standalone command."*

⚠️ **`/plan-review-reuse` DOES NOT EXIST AND MUST NOT BE BUILT.** The three dangling references
resolve **BY THE DISPATCH**, not by minting the missing command — minting it would violate R1.
**This note is written into the doc precisely so a later reader does not "fix" the dangling reference
by building the command.** (My own D3 finding; María's ruling stands.)

### EDIT 4 — `p-is-p-00-start-here.md:267` (mermaid) — **REPLACE the branch**

> **Current**: `Branch -->|"Pattern 3, 4 (Small/Simple)"| Skip["Skip to execution<br>Use history.md
> for tracking"]` … `Skip --> Code`

> **Proposed**:
> ```
> Branch -->|"Pattern 3, 4<br>(Small/Simple)"| Skip["Skip Step 2 docs<br>Use history.md for tracking"]
> Skip --> HasDoc{Is there a<br>plan document?}
> Step2 --> Review
> HasDoc -->|Yes| Review
> HasDoc -->|"No (e.g. investigation)"| Code
> Review["GATE: /plan-review<br>• Dispatch: ≥2 sections → cascade<br>• else → critique (1 critic seat)<br>• REUSE / Fitness / Ownership"]
> Review --> Code
> ```

**The rename `Skip["Skip to execution"]` → `Skip["Skip Step 2 docs"]` is load-bearing**: the old label
said these patterns skip *to execution*, which is what made the gate-bypass read as intended design.
They skip the **documentation step**, not the **gate**.

### EDIT 5 — `p-is-p-00-start-here.md:273` (the gate prose) — **REPLACE**

> **Proposed**: *"**Gate: `/plan-review` (before code)** — **every plan document passes this gate,
> whatever pattern produced it.** The gate dispatches internally: ≥2 reviewable sections → the cascade;
> otherwise → the critique branch, which **spawns one critic seat** (self-critique does not satisfy it).
> Pattern 4 work that produces **no plan document** has nothing to gate. The gate is the doc-quality bar
> that the global `DOCUMENTATION-FIRST PROTOCOL` doesn't impose on its own."*

### EDIT 6 — ⚑ **MY CALL #2 — `swe-team-roles.md` Manager flow (§3)** — **FLAGGED, NOT DRAFTED**

Rick's original directive was *"SWE-driven plan critique **before implementation**."* The Manager's
gate flow (`swe-team-roles.md:29`) reads **implement → review → test**; no named step has the reviewer
attack the **spec** before code exists. `525acac8` calls this *"ONE PHASE BOUNDARY."*

**I have NOT drafted this edit.** Two reasons: (1) it touches the SWE crew's hard gate, which is
Manager-authority territory, not plan-gate territory; (2) `525acac8`'s minimum-contract scope is
arguably satisfied by Edits 1–5 without it. **But leaving it out means the critique exists in the
planning workflow and is invisible to a spawned SWE crew** — the exact distribution failure this board
keeps filing (`aecfe27b`). **María's call: in scope for the minimum contract, or a separate seam?**

---

## 5. ⚠️ THE CONSEQUENCE I WILL NOT SOFTEN — the solo small-fix case

R3 + R4 combined mean: **a solo session with a one-section plan document must now spawn a critic seat.**

That is the **direct reading** of the rulings, and I am stating it plainly rather than quietly adding
an exemption. It is also the **most expensive consequence** of this canonization and the one most
likely to be worked around in practice.

`525acac8` already flagged the neighbouring hole as HELD for pattern-watch: *"SOLO-SESSION FALLBACK —
a crew-only tier leaves the solo small fix where it started."* **R4 does not create that hole; it
closes it by requiring the spawn.** But it converts a cheap action into a spawn, and **a rule whose
cost is felt on every small fix is a rule that gets routed around** — which is how we got 22 hand-written
hold files instead of `write_hold()` calls (`c53204b8` correction #3: *"a mechanism nothing forces you
to use is a rule with extra steps"*).

**I am NOT proposing a softening.** Rick rejected self-critique explicitly; inventing a solo exemption
would be me overruling him at the cheapest point of contact. **I am flagging it as the thing to watch
on runs 1–3**, and it belongs in `73f397d2`'s pattern-watch: *does the critique branch actually get
spawned for solo small fixes, or does it get skipped?* **If it gets skipped, the ruling didn't fail —
the mechanism did, and that is the pattern-watch's job to catch.**

---

## 6. HELD FOR PATTERN-WATCH — do NOT write these from zero runs

Per R5 and `accumulate-pattern-before-graduating-doctrine` (Rick 2026-06-29). Routed to `73f397d2`:
- deliverable template · timeboxes · spawn ordering (critic before vs beside implementer) · re-entry
  protocol on a refutation
- **solo-session behaviour in practice** (§5 above)
- whether the branch ever applies to Pattern 4 work that *does* produce a doc
- **the leak floor** (`525acac8` F1, Mr Radio): *"redact and diff" has a per-doc leak floor that must be
  MEASURED, never assumed away.* **A tier shipping an unmeasured control IS the check-that-cannot-fail,
  canonized.** ⇒ this MUST land before the branch hardens into any mechanized form.
- **the account-vs-manager-filter mechanic** (`525acac8`): the account reaches the Steward **UNFILTERED**;
  a manager's attack is **appended, never merged** — *"a manager filter launders a weak critique into
  looking stronger, and you'd have no way to tell which you got."* N=1, held.

---

## 7. THE TIER'S OWN FIRST-CLASS RISK — must survive into the doc

**A confident, receipt-dense design is HARDER to refute, not easier.**
Tiberius: *"I attacked the multiset and accepted the key — the key came with a RATIONALE."*
This is the failure mode the branch is *least* equipped to catch, and it is the one most likely to be
present in exactly the plans that reach a small-tier gate.

**FIRST TEST CASE, per `525acac8`, and it should stay the first test case**: the heartbeat-hold design
(`3ebc6c3d`) — *"nothing but rationale, authored by the Steward who wrote this contract, underlying bug
ALREADY DIAGNOSED WRONG TWICE (once by her). IF THE TIER CANNOT BITE THAT DOC, THE TIER DOESN'T WORK."*

**Live corroboration from this week, which strengthens the case rather than merely illustrating it**:
that design's census was *"counted, not sampled"* — and **three rounds of diagnosis inherited one
unexamined premise** (`c53204b8` correction #3: zero files contained `null`; 22 had the key *absent*).
**The rigor was the camouflage.** A critique branch that cannot bite a receipt-dense doc will pass
exactly the documents most worth stopping.

---

## 8. THE MODEST CLAIM — R-2 stays open, recorded so nobody reads this as its closure

*"What forces the listening?"* A pre-implementation critique forces **ONE compelled read** — the
implementer cannot start until it lands. It does **NOT** force the author to **HEED** it, and does
nothing for deposit-listening at large. **R-2 STAYS OPEN.**

---

## 9. WHAT I DID NOT DO

- **Nothing landed in `workflow/`.** This document is the draft; the six edits are unapplied.
- **Nothing named.** R1 dissolved the name question; I did not answer it.
- **`/plan-review-reuse` NOT built** — resolved by dispatch, per María's standing ruling.
- **The two D2 sites NOT reopened** — `plan-review-cascaded-common.md:767` and
  `plan-review-cascaded.md:776` remain exactly as found, distinction intact (`:776` would be made
  *worse* by a verb swap; its surrounding mechanism is itself retired).
- **`post-game.md` §7 NOT touched** — inside Rick's live wording gate.
- **Nothing committed, nothing pushed.**

## 11. 🔴 THE DRAFT ABOVE WAS MATERIALLY INCOMPLETE — found by auditing my own citations

**Appended after the six edits were written. I am not reshaping §4 to look like this was always there.**

Running a machine citation-audit on my own draft (does each cited file:line actually say what I claim?)
turned up one off-by-one — and, chasing it, **the pattern-keyed rule is stated in far more places than
my six edits touch.**

### 11.1 THE FULL SITE CENSUS — computed, then hand-corrected

Machine sweep over every `Pattern 3|4` line containing skip/optional/entirely/nothing/escape:
**17 sites examined.** Classified into two claims that share the word "skip" but are NOT the same claim:

| Claim | Affected by R3? | Sites |
|---|---|---|
| **GATE-BYPASS** — "Pattern 3/4 skips `/plan-review`" | **🔴 YES — must change** | **11** |
| **DOCS-SKIP** — "Pattern 3/4 skips Step 2 / impl docs" | ✅ No — legitimate, leave alone | **6** |

**GATE-BEARING (11)** — my six edits covered only **4** of these:
- `plan-review.md:5` ✅ Edit 1 · `plan-review.md:98` ✅ Edit 3 · `p-is-p-00-start-here.md:267` ✅ Edit 4
  · `p-is-p-00-start-here.md:273` ✅ Edit 5
- **NOT COVERED**: `plan-review.md:362` (*"Pattern 1/2/5/6 fire the gate; Pattern 3/4 skip"*) ·
  `p-is-p-00-start-here.md:420` · `p-is-p-00-start-here.md:840` (both *"Pattern 3 or 4? → Skip to
  execution"*) · `INSTALLATION-GUIDE.md:2117` · `:2166` · `:2195` · `installation-wizard.md:270` · `:1185`

**LEAVE ALONE (6)** — genuinely about skipping *documentation*, untouched by R3:
`p-is-p-00:309` · `:486` · `:618` · `p-is-p-02-documenting:68` · `:69` · `INSTALLATION-GUIDE:409`

⚠️ **MY CLASSIFIER MIS-BINNED THREE OF THESE AND I HAND-CORRECTED IT.** The keyword rule put `:267`,
`:420`, `:840` in *docs-skip* because they contain "use history.md" — but **"Skip to execution" means
skip to CODE, which is precisely the gate-bypass.** *The classifier is a triage aid, not an oracle;
the binning is my judgment and should be checked as such.* **Stating this because an automated-looking
table is exactly the artifact that stops the next reader from checking** — my own §1 lesson, one more time.

### 11.2 🔴 `--skip-with-reason` — A LIVE FLAG THAT NULLIFIES R3, AND IT IS ALREADY BUILT

`.claude/commands/plan-review.md:35`, **verbatim**:
> `--skip-with-reason "<reason>"` — **Pattern 3 escape hatch**; logs reason to `00-index.md` "Open
> follow-ups" and **exits without running the gate**

Documented again at `INSTALLATION-GUIDE.md:2117` + a worked example at `:2166`
(*`/plan-review --skip-with-reason "research-only plan, no executable work"`*).

⇒ **R3 says every plan document enters the gate. This flag lets any invocation exit without running
it, and it is named for the exact pattern R3 was ruled to close.** Landing Edits 1–5 while this flag
stands canonizes a gate with a documented one-line bypass — **a control whose own command surface
ships the override.**

**MY RECOMMENDATION — retire the flag, but this is a user-facing escape hatch and therefore Rick's call,
not mine.** Reasoning: the flag's stated legitimate use is *"research-only plan, no executable work"* —
and **R3 already covers that case by construction**: work that produces no plan document has nothing to
gate. If a plan document *does* exist, R3 says it enters. **So the flag's honest case is subsumed and
its remaining function is nullification.** Counter-argument I can't dismiss: an escape hatch that
*logs its reason* is more honest than one that gets routed around silently — and §5's cost worry
predicts routing-around. **⚑ NEEDS A RULING. Do not let it land un-ruled either way.**

### 11.3 🔴 THE INSTALLER PROPAGATES THE OLD RULE TO EVERY NEW PROJECT

`installation-wizard.md:1185`, shown to every installing project, **verbatim**:
> *"Mandatory for Pattern 1/2/5/6 plans; REUSE-only available for Pattern 3 via `/plan-review-reuse`;
> Pattern 4 skips entirely"*

**This teaches the pattern-keyed rule AND the command that does not exist**, at install time, forever.
`INSTALLATION-GUIDE.md:2195` and `installation-wizard.md:270` say the same.

⇒ **If the canonization lands in the five core docs and stops there, every future install re-imports
the defect this canonization exists to remove.** That is **RECIPIENTS(claim) − RECIPIENTS(correction)
applied to my own canonization** — the identical shape as D2, where nine mentions were corrected and
the four that told a seat what to call were never swept. **I would have manufactured the exact
un-swept correction I filed a P1 bug about, in the fix for it.**

**The installer surfaces are IN SCOPE for the minimum contract** — not because they are doctrine, but
because **a correction that doesn't reach the propagation path isn't a correction, it's a local edit.**

### 11.4 What this says about the draft you are reading

The six edits in §4 were **not wrong — they were incomplete, and confidently formatted.** The gap was
found by auditing citations, **not by re-reading the draft**, which is the same lesson as D2: *the
fix-verification out-measured the search that produced the finding.* **Twice in one session, same
shape, and the second time I went looking for it.**

---

## 12. 🔴 THIRD RECOUNT — IT IS 21 UNCOVERED SITES, NOT 7. And D3 needs correcting.

**Appended 2026-07-18 after María ruled 3 of 4 and said "draft the seven." There are not seven.**

### 12.1 The recount

§11's sweep was keyword-gated on *skip / optional / entirely / nothing / escape*. **That filter was my
choice, and it was wrong** — it cannot see `plan-review.md:98` (*"Standalone: For Pattern 3 plans,
invoke `/plan-review-reuse`"*), which contains no such keyword and is unambiguously gate-bearing.

Recomputed with the filter removed — **gate-bearing = any line stating WHO enters the gate, OR
referencing `/plan-review-reuse`, OR referencing `--skip-with-reason`**:

| | §11 said | Actual |
|---|---|---|
| gate-bearing sites | 11 | **25** |
| covered by Edits 1–5 | 4 | 4 |
| **UNCOVERED** | **7** | **🔴 21** |

**They collapse into four EDIT CLASSES, which is the tractable way to land them:**

- **CLASS A — pattern-keyed gate statements → restate as document-keyed** (8 sites)
  `plan-review.md:347` · `:362` · `p-is-p-00:420` · `:840` · `INSTALLATION-GUIDE.md:2195` ·
  `installation-wizard.md:270` · `:1185` · `:1826`
  *(`plan-review.md:347` is its own small landmine — a table row reading "Running plan-review on
  Pattern 3 / Pattern 4 trivia | Cost > value." **That is the bypass stated as a cost argument**, and
  it survives every edit aimed at the word "skip.")*
- **CLASS B — `/plan-review-reuse` references → resolved by the dispatch + the DO-NOT-BUILD note** (8 sites)
  `INSTALLATION-GUIDE.md:2103` · `:2118` · `:2170` · `installation-wizard.md:257` · `:1181` ·
  `.claude/commands/plan-review.md:59` · `:81` · `:82`
- **CLASS C — 🔒 ESCAPE-HATCH sites → FLAG-DEPENDENT, do not draft until Rick rules** (5 sites)
  `INSTALLATION-GUIDE.md:2117` · `:2167` · `plan-review.md:47` · `.claude/commands/plan-review.md:35` · `:79`
  *(§11.2 said "one edit changes if Rick keeps the flag." **It is five.**)*
- **CLASS D — cascade-trigger lines still keyed to PATTERN, not SIZE** (2 sites)
  `p-is-p-00:275` · `:330` — both read *"for larger **Pattern 1/2/5/6** plans (typically ≥2 sections)."*
  **Under R2 the cascade trigger is SIZE ALONE.** A ≥2-section Pattern 3 plan now routes to the cascade,
  and these two lines still say it can't. **Nobody flagged this, including me, until the filter came off.**

### 12.2 ⚠️ D3 WAS RIGHT IN ITS CONCLUSION AND WRONG IN ITS DESCRIPTION

I reported `/plan-review-reuse` as having **"no command file, no workflow doc."** Both clauses are
literally true and **together they gave the wrong picture.**

`.claude/commands/plan-review.md:59` carries a **full `## Sub-command: /plan-review-reuse` section** —
behavior, pass-skipping semantics, output format, "useful invocation contexts," and **two worked usage
examples** at `:81`/`:82`. **It is completely specified.**

**And it is not invocable.** Commands resolve one-file-per-command; there is no
`.claude/commands/plan-review-reuse.md`. Control: `/plan-review`, `/plan-review-cascaded`,
`/plan-authoring-cascaded` each resolve to their own file.

⇒ **THIS IS `defer_to_chase` AGAIN, IN THE ARTIFACT THIS CANONIZATION IS ABOUT.**

| | `defer_to_chase` | `/plan-review-reuse` |
|---|---|---|
| Specified? | ✅ full DbC docstring | ✅ full sub-command section |
| Looks complete? | ✅ 6+ test files | ✅ 2 worked usage examples |
| Reachable? | ❌ not on the agent surface | ❌ not a resolvable command |
| Anything call it? | ❌ zero call sites | ❌ zero — nothing can |

**"Specified, exemplified, and unreachable" is the shape.** My D3 phrasing ("no doc") implied *nobody
wrote it down*; the truth is **somebody wrote it down thoroughly and nobody built the door** — which is
strictly worse, because the spec is what makes readers confident it exists.
**Correction to §2**: Pattern 3's review is not merely absent — **it is documented in detail, with
examples, and cannot be run.**

### 12.3 THE PATTERN IN MY OWN COUNTS — three for three, same cause

| # | Count | Wrong because | Correction came from |
|---|---|---|---|
| 1 | exists-universe = 105,695 defs | scan **included** `.venv` + worktrees | reading its own output |
| 2 | D2 live sites = "2" | banner coverage **eyeballed**, not computed | verifying the fix |
| 3 | uncovered sites = "7" | sweep **keyword-gated** on "skip" | auditing my own citations |

**Every one was low. Every one was caused by a SCOPING choice I made, not by broken logic.**
⇒ **A scoping choice is invisible in the output.** A filtered sweep and a complete sweep produce
identically confident-looking tables; nothing in the artifact says which one you're holding.
⇒ **The instrument was never wrong. The POPULATION was, three times, and only ever in the direction
that made the work look finished.**

---

## 13. THE COMPLETE EDIT SET — all 22 remaining sites, four classes

**All four rulings in hand**: installer surfaces IN · critique-as-section APPROVED · `swe-team-roles.md`
phase boundary IN (one line, no mechanics) · **`--skip-with-reason` RETIRED (Rick)** ·
`.claude/commands/plan-review.md` IN SCOPE (María: *"the flag LIVES there — a retirement that leaves
the flag documented in its own command file is not a retirement"*).

### 13.0 THE COUNT, WITH ITS POPULATION AND FILTER IN THE SAME SENTENCE

> **26 gate-bearing sites** — where POPULATION = every line in `workflow/*.md` plus
> `.claude/commands/plan-review*.md`, and FILTER = *(a)* states who enters the gate, **or** *(b)*
> references `/plan-review-reuse`, **or** *(c)* references the `--skip-with-reason` **flag**, **or**
> *(d)* is a pattern-keyed "skip to execution" line — **of which 4 are covered by Edits 1–5, 1 is
> excluded as a verified homonym, and 22 are drafted below.**

**⚠️ FOURTH CORRECTION, and this one was caught BEFORE drafting rather than after.**
I told María **21**. It is **22** — `p-is-p-00:420` and `:840` were found by §11's sweep but NOT by
§12's regex (they say *"Skip to execution"* without naming `plan-review`), so **neither sweep alone
saw the whole set; only their union does.** *Two instruments, each complete-looking, each blind
somewhere the other wasn't.*

**AND CLASS C IS 4, NOT 5.** `workflow/plan-review.md:47` matched on the string `skip-with-reason` and
is **a different mechanism wearing the same words**: *"log a skip-with-reason in `00-index.md` so the
affected pass exempts the missing convention"* — a per-**convention** exemption, not the gate flag.
**Verified**: the flag string `--skip-with-reason` **does not appear anywhere in
`workflow/plan-review.md`.** ⇒ **EXCLUDED. Retiring the flag must not touch it** — that would delete a
working, unrelated mechanism in the name of a retirement.

### 13.1 CLASS A — pattern-keyed gate statements → restate DOCUMENT-keyed (8 sites)

| # | Site | Current → Proposed |
|---|---|---|
| A1 | `plan-review.md:347` | *"Running plan-review on Pattern 3 / Pattern 4 trivia \| Cost > value. The doc-set shape isn't there. Use `/plan-review-reuse` standalone…"* → **DELETE THE ROW.** It is a "when NOT to run this gate" entry, and under R3 there is no such case for a plan document. |
| A2 | `plan-review.md:362` | *"Pattern 1/2/5/6 fire the gate; Pattern 3/4 skip"* → *"every plan document fires the gate; work that produces no plan document has nothing to gate"* |
| A3 | `p-is-p-00:420` | *"Pattern 3 or 4? → Skip to execution, use history.md"* → *"Pattern 3 or 4? → Skip Step 2 docs, use history.md. **If a plan document exists, it still enters `/plan-review`.**"* |
| A4 | `p-is-p-00:840` | *"If Pattern 3 or 4 → Skip to execution, use history.md"* → same replacement as A3 |
| A5 | `INSTALLATION-GUIDE.md:2195` | *"Pattern 1,2,5,6 **must** invoke… Pattern 3 **may** invoke `/plan-review-reuse`… Pattern 4 **skip** entirely"* → *"**Every plan document must invoke `/plan-review` before code begins**, whatever pattern produced it. The gate dispatches internally: ≥2 sections → cascade; else → the critique branch (one critic seat). Work producing no plan document has nothing to gate."* |
| A6 | `installation-wizard.md:270` | same rewrite as A5, inside the JSON `"notes"` string |
| A7 | `installation-wizard.md:1185` | *"Pattern 3 via /plan-review-reuse; Pattern 4 skips entirely"* → *"every plan document enters the gate; ≥2 sections → cascade, else → critique"* |
| A8 | `installation-wizard.md:1826` | drop *"and a `/plan-review-reuse` sub-command for Pattern 3 single-doc plans"*; keep the `--from=` note |

**⚠️ A1 IS THE LOAD-BEARING ONE AND IT IS NOT A WORDING FIX.** *"Cost > value"* is **the bypass stated
as a cost argument.** It survives every edit aimed at the word "skip," and **a cost argument is more
durable than a rule — the next reader agrees with it before checking whether it is still policy.**
Deleting the row, not softening it, is the proposal.

### 13.2 CLASS B — `/plan-review-reuse` references (8 sites)

**Resolution for ALL EIGHT: the REUSE pre-pass runs inside the gate under the critique branch. The
standalone command is NOT built.** Each site loses the command reference; none gains a new one.

| # | Site | Action |
|---|---|---|
| B1 | `INSTALLATION-GUIDE.md:2103` | drop `/plan-review-reuse` from the Slash Commands list |
| B2 | `INSTALLATION-GUIDE.md:2118` | delete the bullet |
| B3 | `INSTALLATION-GUIDE.md:2170` | delete the worked example |
| B4 | `installation-wizard.md:257` | remove the `"name": "/plan-review-reuse"` entry from the installable-command manifest |
| B5 | `installation-wizard.md:1181` | delete the menu line |
| B6 | `.claude/commands/plan-review.md:59` | **DELETE the entire `## Sub-command: /plan-review-reuse` section** (through `:70`) |
| B7 | `.claude/commands/plan-review.md:81` | delete the usage example |
| B8 | `.claude/commands/plan-review.md:82` | delete the usage example |

**B4 is the propagation kill**: `installation-wizard.md:257` is a **manifest of installable commands**
— it offers `/plan-review-reuse` for installation into new projects. **Every install has been offering
a command that cannot resolve.**
**B6 is the one that matters most** — see §12.2. That section is *why* everyone believed the command
existed. **Deleting the spec is what makes the absence visible instead of inferable.**

### 13.3 CLASS C — the retired flag (4 sites; `plan-review.md:47` EXCLUDED as homonym)

| # | Site | Action |
|---|---|---|
| C1 | `.claude/commands/plan-review.md:35` | **DELETE the `--skip-with-reason` flag definition.** This is the retirement's load-bearing edit — the flag's *implementation contract* lives here. |
| C2 | `.claude/commands/plan-review.md:79` | delete the usage example |
| C3 | `INSTALLATION-GUIDE.md:2117` | delete the Modes bullet |
| C4 | `INSTALLATION-GUIDE.md:2167` | delete the worked example |

**⚑ RECOMMENDATION ON THE RETIREMENT'S WORDING** — mine, flagged as mine: follow the precedent already
in this corpus. The `--from=adversarial` retirement (2026-05-15) is documented as a **hard break with
no backward-compat alias**, stated *in the doc*, so a stale invocation **fails loudly** rather than
silently doing nothing. **Recommend one line at C1's location**: *"`--skip-with-reason` was retired
2026-07-18 (Rick) — every plan document enters the gate; there is no bypass flag. No backward-compat
alias; stale invocations fail loudly."* **A deletion alone leaves the next reader to conclude the flag
was never there** — and this document's whole subject is corrections that don't reach their readers.

### 13.4 CLASS D — cascade-trigger lines still keyed to PATTERN (2 sites)

| # | Site | Current → Proposed |
|---|---|---|
| D1 | `p-is-p-00:275` | *"for larger **Pattern 1/2/5/6** plans (typically ≥2 reviewable sections)"* → *"for larger plans (**≥2 reviewable sections, whatever pattern produced them**)"* |
| D2 | `p-is-p-00:330` | *"if a **Pattern 1/2/5/6** plan will be reviewed via `/plan-review-cascaded`…"* → *"if a plan will be reviewed via `/plan-review-cascaded` (**≥2 sections, any pattern**)…"* |

**This class is a contradiction the canonization would have CREATED, not a leftover it inherited.**
Under R2 the cascade trigger is **size alone** — so a ≥2-section Pattern 3 plan now routes to the
cascade, and both lines currently say it cannot. **Landing Classes A–C without D ships a gate that
routes a plan to a cascade its own decision matrix declares ineligible.**

### 13.5 EDIT 6 (now ruled IN) — `swe-team-roles.md` Manager flow, ONE line

At `swe-team-roles.md:29`, the flow *"implementer reports done + self-tested → adversarial reviewer
renders a refute-first verdict → tester posts integration/e2e green"* gains one preceding clause:

> *"**→ where the task carries a plan document, the reviewer critiques the SPEC before implementation
> begins (`plan-review.md` §4a) —** implementer reports done + self-tested → …"*

**Deliberately one clause.** No mechanics, no timeboxes, no spawn ordering — those stay held
(`73f397d2`). This exists so **a spawned SWE crew encounters the gate at all**, which is the surface
Rick named when he asked for *"SWE-driven plan critique before implementation."*

### 13.6 LANDING ORDER (cost-ordered; each step leaves the corpus coherent)

1. **Class D first** (2 sites) — removes the contradiction *before* the dispatch that would expose it.
2. **Edits 1–5 + §4a** — the dispatch itself.
3. **Class A** (8) — the pattern-keyed statements, **A1 deleted not softened**.
4. **Class B** (8) — the unreachable command, **B6 first** (kill the spec, then the references).
5. **Class C** (4) — the retired flag, **with the hard-break note**, not a silent deletion.
6. **Edit 6** — the SWE seam.

**Rationale for D-first**: any other order has a window where the corpus tells a ≥2-section Pattern 3
plan both to enter the cascade and that it is ineligible.

---

## 14. CLAYTON'S CRITIQUE — REFUTED THE SET. Revision below. Every finding re-verified by me on disk.

### 14.1 ✅ F-1 ACCEPTED — the FIFTH count error, at dead centre
`.claude/commands/plan-review.md:89–91`, the gate command's own **`## When to Use`** block — carrying
**the Pattern-4 bypass** *and* a false *"Optional (REUSE only)"* pointing at the unreachable command.
**In my population. Matches my filter clause (a), three times. Uncovered by all 22.** Verified: my
draft cites that file at `:35 :59 :79 :81 :82` — **never `:89`.** **Landing 22 edits would have left
the canonical bypass sitting in the canonical file.** → **EDIT A9.**

**⭐ HIS DIAGNOSIS SUPERSEDES MY §12.3 CONCLUSION.** I wrote *"only their union does."*
> **"Both instruments key on VOCABULARY, so they SHARE a failure mode. Two blind instruments do not
> compose into a sighted one."**

§12 keys on `plan-review`/`reuse`/`skip-with-reason`; `:89` names `/p-is-p-02-documentation`. §11 keys
on *"Skip to execution"*; `:91` says ***"skip entirely."*** **Different words, same act.** My union
claim was **true of those two sites and false as a general rule.** ⇒ **my own `ToolSearch` finding —
*independence of seats is not independence of METHOD* — arriving at the SEARCH layer, against me.**

### 14.2 ✅ +1 SITE HE DIDN'T HAVE — `README.md:91`, the repo's front page
Found by applying his diagnosis rather than accepting it: *"mandatory for Pattern 1/2/5/6, REUSE-only
available for Pattern 3 via `/plan-review-reuse`"* — **the pattern-keyed rule AND the unreachable
command, on the front page.** Outside both his population and my original. → **EDIT A10.**
Also `installation-wizard.md:1184` = the **wrapped first half** of the sentence A7 fixes at `:1185`
(**A7 would have edited half a sentence** → `:1184–1185`); `INSTALLATION-GUIDE.md:2166`/`:2169` are the
**comment lines** heading the examples C4/B3 delete (→ extended, else the file keeps comments describing
deleted examples).

### 14.3 ⚠️ F-2 — POPULATION RULING ACCEPTED; EDIT FORM DISPUTED
María ruled both sites land, and **the 40-file population expansion I accept without reservation.**
**But I do not think these two are gate-bearing, and a rewrite would introduce an error:**
- `.claude/commands/p-is-p-02-documentation.md:51–52` — *"Skip this command when: Pattern 3 or 4"*
  skips the **documentation command**, which R3 explicitly permits (R3 never requires a plan document,
  only that one entering the gate *if it exists*). **The line is TRUE as written.**
- `p-is-p-02-documenting-the-implementation.md:1084` — *"Skip to execution phase"* sits under
  **"If no to dedicated docs"** — **not pattern-keyed**, already document-triggered.

**Rewriting either into "enters the gate" asserts that skipping documentation implies gating — confused,
and it would make the corpus worse.** The real risk here is not a false statement but a **false
inference** (*"no docs ⇒ no gate"*). **⚑ PROPOSED: the RIDER form, not the REWRITE form** —
> *"Skipping the documentation step does **not** exempt any plan document you do write — if a plan
> document exists, it enters `/plan-review` (§4a)."*

Serves the propagation principle exactly, asserts nothing untrue. **Named rather than quietly complied
with: a silent compliance would put a confused sentence into doctrine under María's name.**
**AND NOTE WHERE THIS SITS**: I mis-binned this boundary in §11.1 (docs-skip → gate); **Clayton has now
mis-binned it in the opposite direction.** ⇒ **Both of us crossed the docs-skip/gate-skip line, opposite
directions, same corpus.** A "skip" site is **unclassified until its TRIGGER is read** — never classified
by its verb.

### 14.4 ✅ F-3 ACCEPTED — THE BAR CHANGES, AND IT IS TWO SENTENCES, NOT ONE
Clayton's first exhibit: a conforming account with target ✓ method ✓ why-it-held ✓ that **establishes
nothing** — the bar constrained the **FORM** and left the **epistemic standard of "how"** unconstrained,
so *"I read it"* satisfied it. **Reading has a measured 0% hit rate on this board today.**
**Then he refuted his own remedy**: *"I ran `grep -c '^###' §4a` → 3. The threshold held."* — ran-not-
considered ✓, re-runnable receipt ✓, **and the command bears on nothing.** The fix moved the bar from
*did you do work?* to *did you run something?* — **neither asks whether the check could have come out
the other way.**

**→ §4a's bar, FINAL, both sentences IN THE BAR, not a footnote:**
> **"An account whose only method is reading is not an account. State what you RAN, not what you
> considered. A receipt shows the work happened; it does not show the work bore on the claim. The
> reader is the check on relevance."**

**The second sentence is non-negotiable**: without the residual stated where the rule is, *the next
reader inherits a bar they believe is closed* — **my own D2 lesson (an unresolved judgment inside a
confident frame becomes a resolved one) arriving in the artifact written to prevent it.**

**⚑ THE STRONGER FORM IS DELIBERATELY NOT WRITTEN** — recorded as a choice, not an oversight. It would
be *"state what result would have REFUTED the claim, and show your check was capable of producing it."*
**Clayton named it and declined to mandate it**, citing Rick's standing `ec8defde`: *"you are injecting
an unnecessary amount of precision and complication… WE NEED TO MAKE PROGRESS."* A negative control per
claim, on a two-line fix's plan doc, is that ruling's target.
**→ §8 pattern-watch gains it UNWRITTEN, with an explicit trigger**: *the first conforming account that
carries a receipt and still establishes nothing.* One constructed counter-example is not recurrence —
**accumulate-before-graduate, applied by the seat who would have benefited from breaking it.**

### 14.5 ⭐ THE RATIONALE FOR BOTH SENTENCES — one object, three altitudes, all measured today
> **Two vocabulary-keyed sweeps. Two reading-based reviews. Two seats agreeing because neither ran
> anything. Same failure, three layers: search, review, contract.**

**My F-1 miss is that failure at the SEARCH layer. A vacuous conforming account is the same failure at
the CONTRACT layer.** The canonization is the only one of the three we can still get right before it ships.

### 14.6 ✅ F-4 → §5 gains a second unsoftened consequence
R3 triggers on **an artifact whose creation is optional and unenforced**, so the rule **taxes the very
document we want.** At the small tier nobody expects a plan doc, so **its absence is as silent as a
missing review** — "the dodge is visible" holds at the large tier and **collapses exactly where the tier
is aimed.** **Three independent seats have landed on this shape and NOBODY HAS MEASURED IT.**
**→ §5**: *the rule's most likely real-world effect at the small tier is FEWER PLAN DOCUMENTS, not more
critiques.* **→ pattern-watch, FIRST thing to measure**: plan-document creation rate at the small tier,
**not** critique spawn rate — *otherwise the dodge reads as compliance.*

### 14.7 ✅ TWO OF MINE HELD UNDER ATTACK — recorded as survivals, not omissions
- **The homonym exclusion** — he ran `grep -c -- "--skip-with-reason" workflow/plan-review.md` → **0**;
  flag string repo-wide = exactly **4** = my Class C. **He tried to break it and could not.**
  *(Since found a SECOND homonym at `p-is-p-02-documenting:532`. **The homonym is a pattern, not a
  one-off.**)*
- **A1's deletion** — *too small to gate ⇒ too small for a plan document ⇒ no document ⇒ no gate.*

### 14.8 ⚠️ THE COST OF DROPPING VOCABULARY KEYING — now MY risk, stated
Re-swept per María: **POPULATION = 111 files (working tree, tracked AND untracked: `.claude/` + `workflow/` + root
`*.md`; `src/rnd/`+`history/` excluded deliberately — CORRECTED from the refuted 97, front matter #8); FILTER = names a numbered Pattern AND a skip/mandate verb within a ±2-line WINDOW** (window
added for his multi-line case). **50 hits · 28 not already in my set · of those, ~2 genuine** — the rest
docs-skip, pattern prose, table rows, and one `### ❌ Anti-Pattern 2` heading matching on "Pattern."
⇒ **Precision ~7%. Dropping the vocabulary filter raised recall and moved the whole burden onto human
triage — and triage is exactly where Clayton and I have EACH already erred, in opposite directions.**
**A high-recall sweep does not remove the judgment; it relocates it, and the relocation is not visible
in the output either.**

### 14.9 ⚠️ INSTRUMENT FAILURE TAXONOMY — three deep, one unsolved
**(1) wrong population** (`.venv`; missing `README.md`) — *fixed by widening.*
**(2) vocabulary keying** (F-1) — *fixed by dropping verb filters.*
**(3) line granularity** — `.claude/commands/p-is-p-02-documentation.md` states its rule across `:51`
(verb) and `:52` (subject); **no single-line filter matches it at any vocabulary.** The ±2-line window
fixes *that instance*; **the general case (a rule split across a heading and a table, or across a page)
remains unsolved.** *Mitigated, not solved.*

### 14.10 REVISED TOTAL
> **24 edits + 2 riders** — POPULATION = **111 files** (working tree, tracked AND untracked: command files +
> `workflow/*.md` + root `*.md`; **corrected from the refuted 97**); FILTER = names a numbered Pattern AND a skip/mandate verb within ±2 lines, **hand-triaged by
> me at ~7% precision.** = 22 from §13 + **A9** (`plan-review.md:89–91`) + **A10** (`README.md:91`);
> **A7 → `:1184–1185`**; **C4/B3 → their comment lines**; **+2 riders** (§14.3) pending the
> rider-vs-rewrite ruling. **Known uncovered by construction**: any rule spanning more than ±2 lines.

**Class-D-first remains UNVERIFIED** — Clayton ran no edit-order dependency analysis and claimed no
other transient windows exist. **Label kept.**

**⚠️ THIS REVISION IS THE DRAFT'S FIFTH PASS BY ITS OWN AUTHOR. It goes back to Clayton before it
lands.** F-1 was found in a re-sweep of a document already audited four times — **a fifth self-pass is
precisely the shape this tier exists to distrust, and exempting my own draft from it would be the
loudest possible way to prove the tier decorative.**

---

## 15. CLAYTON'S RE-PASS — F-5 is a LANDING BLOCKER. B6 DISSOLVED. Rejection log adopted.

### 15.1 🔴 F-5 CONFIRMED — the Decision Matrix, and it is NOT a widening
**`p-is-p-00-start-here.md:320–328`** — the table the doc introduces as ***"the **heart** of 'Planning
is Prompting'"*** (`:318`, verified). Three rows:
```
| Small feature    | … | Pattern 3: Feature Dev      | ✗ No | → 01 only → history.md |
| Bug investigation| … | Pattern 4: Investigation    | ✗ No | → 01 only → history.md |
| Quick research   | … | Pattern 2: Research (small) | ✗ No | → 01 only → history.md |
```
**The Workflow Path column IS the rule**, and it routes Pattern 3/4/2-small from planning **straight to
`history.md` with no gate.** Under R3 a Pattern 3 plan document must enter the gate. **Direct contradiction.**

**RECEIPTS (verified by me):** my 24 edits cite `p-is-p-00-start-here.md` at
`:267 :273 :275 :309 :330 :420 :840` — **they BRACKET the table (`:309`, `:330`) and never enter it.**
Sweep-keyword count in rows `320–328`: **0** — no `plan-review`, no `reuse`, no `skip-with-reason`,
no *"Skip to execution."*

**⭐ AND IT IS STRICTLY HARDER THAN MY FAILURE MODE #3.** #3 is a rule split across lines — findable
with a wider window. **This rule is expressed by ABSENCE**: the cell states where you DO go; the gate's
non-application is what the cell OMITS.
> **You cannot grep for what a line does not say, and a ±N-line window returns the same rows with the
> same silence.**

**Clayton's kill, and it is the reason this is a blocker rather than a finding**: a high-recall sweep
**does** surface these rows on "Pattern 3" — and **my triage rejects them, correctly by its own
criterion, because no gate word appears.** ⇒ **F-5 does not merely survive the new sweep; it survives
by passing through the exact triage step I named as my #1 risk.**

**→ EDIT E1** — `p-is-p-00-start-here.md:322–324`, Workflow Path column:
`→ **01** only → history.md` becomes `→ **01** only → history.md · **plan doc? → /plan-review**`
**✅ RULED (María, 2026-07-18 12:40 ET): LANDING BLOCKER — IN, not FILED.** Same class as Class D — **a
contradiction the canonization CREATES**, and that class gets ordered around rather than deferred.
*"Drafting it is one column, three rows, and it is part of THIS work rather than new work."*
**E1 lands with Class D, before the dispatch.**

> **⛔ BUT NOTHING LANDS TODAY.** Rick ordered María + Sam to re-spin (broadcast `32171736`); worker DM
> traffic to managers is paused until her successor is live. **This set is COMPLETE AND UNREVIEWED —
> Clayton's final pass never ran.** A successor must treat it as *unreviewed*, **not cleared.**

### 15.2 ✅ B6 ABSORPTION — DISSOLVED. No work needed, and I should have checked three flags ago.
Clayton's falsifiable test: *"after B6 lands, can a reader who never saw the deleted section answer
'what does the REUSE pre-pass check?' from `plan-review.md` alone?"*
**RAN IT. YES.** `workflow/plan-review.md` §4 (36 lines) already carries the **full** specification: the
*why-first* rationale, the **verbatim prompt**, the output table format, all three verdicts
(`reuse-as-is` / `extend-existing` / `genuinely-new`), and the after-review step.
The deleted `## Sub-command` section adds **only standalone-invocation framing** — *"skips Pass 1 and
Pass 2," "no gate," "useful invocation contexts"* — **which is precisely what is being retired.**
⇒ **B6 deletes nothing substantive. No absorption required.**
**⚠️ AND THIS IS MY OWN LESSON, AGAIN**: I called B6 *"the weakest seam"* **three times** and never ran
the thirty-second check that dissolves it. **I reasoned about it instead of reading §4.** The seam was
never weak; **my confidence that it was went unexamined for three rounds** — the same object as
everything else in this document, committed by me while documenting it.

### 15.3 ✅ REJECTION LOG ADOPTED — Clayton's bar, turned on my triage
His point: **26 of 28 candidates were rejected by unreceipted hand-judgment, on the exact boundary we
have BOTH now got wrong in opposite directions.** *"Not a smaller problem than the one the sweep solved
— the same problem wearing a different hat, and the output doesn't show it."*
**→ ADOPTED**: the landing set publishes its rejections as `site → trigger → why not gate-bearing`, one
line each, **so a reader can CHECK the binning instead of trusting it.**
**F-5 is the argument for it**: F-5 appears in those 26 and is rejected on a criterion that is *correct
and insufficient.* **A rejection log is the only artifact in which that failure is visible.**

### 15.4 ⭐ CLAYTON'S SIXTH FAILURE MODE — COLUMN truncation, on his own search
He found `README.md:91` in his own grep output and **his display filter cut it**:
```
full line : …mandatory for Pattern 1/2/5/6, REUSE-only available for Pattern 3 via `/plan-review-reuse`
what he saw: - ✅ **[C.5]** Plan Review Gate: `/plan-review` (fitness + ownership-language audit of imple
```
**Verified**: `README.md:91` is **220 chars**; the rule begins at **char 130**; his `sed 's/\(.\{0,95\}\).*/\1/'`
cut at 95. ⇒ **Not `grep | head` truncating ROWS — truncating COLUMNS, applied deliberately, for
readability, to his own output, in the search built to find that exact string.**
**The count is now six, and it is no longer only mine**: my `.venv` · my eyeball · my keyword gate · my
union · my vocabulary keying (his catch) · **his column width.** Same object, six instruments, two seats.

### 15.5 STILL UNSIGNED — neither seat will sign it
**Class-D-first sufficiency: UNVERIFIED.** No edit-order dependency analysis has been run by either of
us, and **neither of us should sign it.** Kept labelled.

### 15.6 REVISED TOTAL
> **25 edits + 2 riders + a rejection log** — POPULATION and FILTER as stated in the front matter
> (**111 files**, reconciled — working tree incl. untracked; Pattern-name + skip/mandate verb within ±2 lines; hand-triaged at ~7% precision).
> = 24 from §14 + **E1** (`p-is-p-00-start-here.md:322–324`, the Decision Matrix).
> **B6 requires no absorption** (§15.2). **Known uncovered by construction**: rules expressed by
> ABSENCE (§15.1) — **for which no sweep in this document is an instrument at all.**

---

## 10. Four things for María to rule before anything lands

1. **⚑ MY CALL #1** (§3): critique as a **section of `plan-review.md`** rather than a new doc. My
   reasoning is that a new doc generates command-minting pressure that would violate R1 — but the
   document surface was never ruled, and this is me choosing it.
2. **⚑ MY CALL #2** (Edit 6): is the `swe-team-roles.md` phase-boundary amendment **inside** the
   minimum contract, or a separate seam? Leaving it out keeps scope tight; leaving it out also means a
   spawned SWE crew never sees this gate.
3. **🔴 `--skip-with-reason`** (§11.2): retire the flag, or keep it as a logged exemption? **It exits
   without running the gate and is named for the pattern R3 closes.** My rec is retire — its honest
   case is already subsumed by R3 — but it is user-facing, so it is Rick's call. **Landing Edits 1–5
   while this stands ships a gate with a documented one-line override.**
4. **🔴 THE INSTALLER SURFACES** (§11.3): `installation-wizard.md:270`/`:1185`,
   `INSTALLATION-GUIDE.md:2117`/`:2166`/`:2195` teach the pattern-keyed rule and the nonexistent
   `/plan-review-reuse` to **every new install**. My rec: **in scope** — a correction that misses the
   propagation path is a local edit, not a correction. Otherwise this canonization gets re-imported
   away, project by project.

**Revised edit count**: the draft's §4 has **6 edits covering 4 of 11 gate-bearing sites**. A complete
minimum-contract landing needs **~11 site edits + the flag ruling**. §11 has the full list; I have
**not** drafted the remaining seven pending your rulings on #3 and #4, since both change what they say.

---

## 16. THE REJECTION LOG (§15.3, now WRITTEN — and it is not the log that was adopted)

### 16.0 ⛔ THE LOG THAT WAS ADOPTED DOES NOT EXIST AND CANNOT BE RECOVERED

**§15.3 marks the rejection log `✅ ADOPTED`. It was never written.** The critic seat searched the whole
corpus plus all three untracked drafts: **no enumeration of the original 28 candidates exists anywhere.**
Only the counts survived; the working scripts lived in a session scratchpad that is gone.

> ### `✅ ADOPTED` reads exactly like a thing that exists.

**Third instance of one shape in this single document, and the third is the worst:**

| Artifact | Was | And was |
|---|---|---|
| `/plan-review-reuse` | specified, exemplified in the gate's own command file | **never invocable** |
| `defer_to_chase` / `make_gate` | built, tested, DbC-documented, 6+ test files | **zero production call sites** |
| **the rejection log** | **adopted, checkmarked, cited as F-5's remedy** | **never written** |

**The remedy adopted specifically because F-5 would be visible in it was itself vapor**, unnoticed for
three passes because **the checkmark satisfied the reader.** The spec is what made readers confident the
command existed; **`✅ ADOPTED` is what made two seats confident the log existed.**

⇒ What follows is **NOT the original 26 rejections** — it is a fresh reconstruction from the stated
filter against the corrected population. **The original triage is unrecoverable.** A second seat
reconstructed the same set independently and **the two were deliberately not compared before writing.**

### 16.1 THE INSTRUMENT COLUMN IS THE POINT, NOT THE REASON COLUMN

> **A log that does not say what looked implies something that could have seen it.**

**F-6 is the proof.** `p-is-p-01-planning-the-work.md` — *the workflow that PRODUCES plan documents* —
never routed one into the gate: six gate mentions, all cascade; the serial gate appearing once, as a
disclaimer. **Outside the original 26 and outside every vocabulary sweep over the whole population** — a
keyword sweep across those files came back **green, and the green was worth nothing.** It was found by
asking *"does this routing surface name the gate?"* — **a question no keyword can express.**

### 16.2 🔴 THE REJECTIONS THAT WERE WRONG — one class, four more times

**F-5 was never one site. It is a REPLICATED TABLE, and E1 fixed the copy that was found.**

| Site | Status |
|---|---|
| `p-is-p-00-start-here.md:325–327` | ✅ fixed by E1 (`6fc0f1e`) |
| `README.md:158–159` | ✅ fixed after this log surfaced it |
| `INSTALLATION-GUIDE.md:360–361` | ✅ fixed after this log surfaced it (`aa43893`) — **the installer** |
| `p-is-p-02-documenting:1098–1099` | ⚪ **NOT a defect — see §16.2b. Correctly rejected.** |

> ### E1 fixed the instance that was found, not the class that was named.
**A landing blocker was ruled, one of its copies was fixed, and both seats reported it closed.** Nobody
asked how many copies the table had, because a matrix inside the document that calls itself *"the heart
of Planning is Prompting"* reads like the original rather than one replica of four.

**THE CLASS IS NOT "THE DECISION MATRIX" — IT IS ROUTING BY OMISSION**, and it unifies F-5 with F-6: a
routing surface that states where you DO go, whose **gate-silence is the rule**. `→ 01 only →
history.md` is a route that never names the gate. **Five instances, five files, three unrelated
accidents, zero instruments.**

### 16.2b ⚠️ AND ONE OF THE FOUR WAS MIS-BINNED — BY THIS LOG'S AUTHOR, IN THE OPPOSITE DIRECTION

`p-is-p-02-documenting:1098` was listed above as a fifth wrongly-rejected site. **It is not one.**
The column header is **`Implementation Docs?`** — *not* `Workflow Path`. The cell answers a
**documentation** question, and *"No - use history.md"* is **TRUE under R3**, which never requires a plan
document — only that one entering the gate *if it exists*. **Editing the row would assert that skipping
docs implies gating.**

**That is verbatim the position on which this document's author refused a manager's rewrite ruling
(§14.3) and was upheld — and then crossed the same line himself, the other way, six hours later**, by
classifying a table row **on its shape without reading its column header.** The trigger was one line
above the row.

> ### Third crossing of this boundary today, by the third seat, in both directions.
> **"A skip site is UNCLASSIFIED until its TRIGGER is read" is not a lesson any of the three has
> learned. It is a rule all three keep needing** — which is a different and less comfortable claim.

**WHAT THAT SITE DID HAVE** is failure mode (3) — **line granularity, in its general form, live inside
its own mitigation.** The rider landed at `:1085` under `### The Complete Flow`; the table sits at
`:1094` under `### Decision Matrix` — **a different subsection.** A reader arriving by TOC, search hit,
or deep link never sees it. **Fixed with a SECOND rider directly beneath the table**, naming what the
column does and does not answer. **No row rewritten.**

### 16.2c AND THIS RECONSTRUCTION'S OWN SWEEP MISSED ONE OF THE FOUR

`p-is-p-02-documenting:1098` reads *"| Pattern 3 | No - use history.md |"*. The filter requires a
skip/mandate verb within ±2 lines; **"No - use" is not a verb in any vocabulary any seat chose.** It
surfaced only via a second, differently-shaped grep for matrix rows.
> **The instrument written to reconstruct the rejection log failed on the exact class the log exists to
> expose — and was caught, like all eight before it, by a DIFFERENT instrument rather than a better one.**

### 16.3 THE LOG — 58 candidates, instrument stated per row

**POPULATION**: 111 files (working tree, tracked + untracked; `workflow/` + `.claude/` + root `*.md`;
`src/rnd/` + `history/` excluded deliberately). **FILTER**: names a numbered Pattern **AND** a
skip/mandate verb within a ±2-line window. **58 candidates** after dropping 10 gitignored
session-ephemeral root files — *which `git ls-files --others --exclude-standard` also cannot see: the
same blindness as untracked files, one layer further out.*

**LEGEND — the instrument that did the rejecting:**
`[T]` trigger read (the site's own condition, not its verb) · `[S]` surface form (prose/example, no rule
present) · `[H]` homonym discrimination (computed, case-insensitive) · `[∅]` **NO INSTRUMENT — rejected
by a criterion that is correct and insufficient**

| # | Site | Trigger | Why not gate-bearing | Instr |
|---|---|---|---|---|
| 1 | `.claude/commands/p-is-p-02-documentation.md:52` | skips the **documentation command** | TRUE under R3. **Rider added, not rewritten** | `[T]` |
| 2 | `CLAUDE.md:156` | this repo's own worked example | narrative record of a past session, not a rule | `[S]` |
| 3–4 | `README.md:158–159` | **Workflow Path routing row** | 🔴 **WRONGLY REJECTED — routing by omission (§16.2)** | `[∅]` |
| 5–7 | `README.md:160–162` | routing rows, Patterns 5/2/1 | route TO Step 2, which routes to the gate — no bypass asserted | `[T]` |
| 8–9 | `INSTALLATION-GUIDE.md:360–361` | **Workflow Path routing row** | 🔴 **WRONGLY REJECTED — routing by omission; the INSTALLER** | `[∅]` |
| 10–12 | `INSTALLATION-GUIDE.md:362–364` | routing rows, Patterns 5/2/1 | route TO Step 2 — no bypass asserted | `[T]` |
| 13 | `INSTALLATION-GUIDE.md:401` | shell comment, docs step | documentation-step trigger, true as written | `[T]` |
| 14 | `INSTALLATION-GUIDE.md:409` | *"skips discovery questions"* | skips an **elicitation phase**, unrelated to the gate | `[T]` |
| 15 | `INSTALLATION-GUIDE.md:415` | when to invoke the **docs command** | documentation-step trigger | `[T]` |
| 16 | `INSTALLATION-GUIDE.md:428` | *"Only for Pattern 1, 2, 5"* | documentation-step trigger. **⚠️ Pattern 6 missing — FILED, not fixed** | `[T]` |
| 17 | `INSTALLATION-GUIDE.md:2194` | docs step, in the gate's own flow list | documentation-step trigger; the gate line 3 lines above is now universal | `[T]` |
| 18 | `installation-wizard.md:2761` | *"Only for Pattern 1, 2, 5"* | documentation-step trigger. **⚠️ Pattern 6 missing — FILED** | `[T]` |
| 19–20 | `p-is-p-00:266–267` | mermaid branch | **IN THE EDIT SET** (Edit 4, `8bdcb04`) | — |
| 21–22 | `p-is-p-00:304, :312` | when to use / skip **Step 2** | documentation-step trigger | `[T]` |
| 23–25 | `p-is-p-00:325–327` | Decision Matrix rows | **IN THE EDIT SET** (E1, `6fc0f1e`) | — |
| 26–28 | `p-is-p-00:328–331` | matrix rows, Patterns 2/5/6 | route TO Step 2 — no bypass asserted | `[T]` |
| 29 | `p-is-p-00:422` | docs step | documentation-step trigger | `[T]` |
| 30 | `p-is-p-00:423` | Pattern 3/4 skip | **IN THE EDIT SET** (A3) | — |
| 31–38 | `p-is-p-00:434,436,464,466,487,489,619,621` | worked examples / walkthrough transcripts | narrative demonstrations, not rules. **⚠️ `:489`/`:621` QUOTE the matrix (*"DECISION MATRIX: Pattern 3 → Skip Step 2"*) — re-triage if the class lands everywhere** | `[S]` |
| 39 | `p-is-p-00:842` | docs step | documentation-step trigger | `[T]` |
| 40 | `p-is-p-00:843` | Pattern 3/4 skip | **IN THE EDIT SET** (A4) | — |
| 41 | `p-is-p-01:152` | *"skip this phase"* | skips a **research phase**, not the gate | `[T]` |
| 42–44 | `p-is-p-01:561–563` | pattern-selection advice | tells you which pattern to pick; asserts nothing about gating | `[S]` |
| 45–49 | `p-is-p-02-documenting:66–70` | pattern→docs routing block | documentation-step trigger, all five rows | `[T]` |
| 50 | `p-is-p-02-documenting:425` | when to skip the **working contract** | skips a doc CONVENTION, not the gate | `[T]` |
| 51 | `p-is-p-02-documenting:532` | bare-string `Skip-with-reason` | **HOMONYM — per-convention exemption, not the flag. Live, untouched** | `[H]` |
| 52–56 | `p-is-p-02-documenting:1070–1074` | *"create structure?"* decision list | documentation-step trigger | `[T]` |
| 57 | `plan-review-cascaded-common.md:954` | *"applied … pattern 4 times"* | **WRONG SENSE** — "pattern" as recurring shape; "4 times" is a count | `[S]` |
| 58 | `plan-review.md:28` | anchor-layer table | *"optional"* qualifies a **Layer 2 anchor file**, not gate entry | `[T]` |
| **+1** | `p-is-p-02-documenting:1098–1099` | column = **`Implementation Docs?`** | ⚪ **correctly rejected — TRUE under R3.** Not surfaced by this sweep at all (§16.2c); mis-binned as a defect on first pass (§16.2b) | `[T]` |

### 16.4 WHAT THE LOG SAYS ABOUT ITSELF

**4 of 59 wrongly rejected — all four in ONE class, all four `[∅]`.** Not scattered errors: **every
failure is the same failure**, in the class whose honest instrument entry reads *none exists.*

**The number to distrust most is the one that looks best.** 54 of 58 correctly binned — but **the four
that were wrong were invisible to the method that binned the other fifty-four**, so the 54 does not
evidence the method; it evidences that the method was applied consistently **to the sites it can see.**
*A high pass-rate on a filtered population is the signature this document opened by warning about.*

**And the log's own author mis-binned one site in each direction on the same day** (§16.2b) — the
boundary is not hard because people are careless with it; **it is hard because a row's shape is visible
and its trigger is not.**

> ### The log was written because F-5 would be visible in it. Writing it surfaced three more F-5s, one mis-binning by its own author, and a sixth failure of its own instrument. It did not make the corpus clean — it made the boundary CHECKABLE, which is the most any of the nine corrections managed.
