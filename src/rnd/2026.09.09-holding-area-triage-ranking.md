# Holding-Area Triage — María's Independent Ranking

**Date**: 2026-09-09
**Author**: María 🌸 (session 31297e6d)
**Row**: `76032470` — *"FIRST THING: María + Mr. Radio triage the whole holding area — 4 to promote, 4 to kill"* (P0, Rick's direct voice instruction)
**Status**: my half of an independent two-person ranking. Mr. Radio's list is separate and unseen at the time of writing. **This is not the agreed output.**

---

## 0. The count, measured before any row was opened

| source | count |
|---|---|
| Rick, 2026-09-09 ~03:10 UTC | 21 |
| María, 2026-09-09 ~02:33 UTC | 22 |
| 🔴 **`task_query( status="not_approved" )`, 2026-09-09 14:2x UTC** | **23** |

**Both spoken numbers were low.** Everything downstream of this document says 23.

### The two "easy kills" I promised are already dead

| row | title | state |
|---|---|---|
| `49e52a90` | Both JS test doors run 268 tests and skip the notifications_js suite | **already `dropped`** |
| `64b6a88f` | Should the 31,627-line legacy JS layer come under the coverage mandate? | **already `dropped`** |

Verified by `task_query( status="dropped", project="lupin" )`. My ids were right; they were killed overnight.

⇒ **We owe four *fresh* kills, not two.** The half Rick warned would be done badly just got harder, exactly as he predicted.

---

## 1. Method, settled before any row was read

**Mr. Radio's tie-break, adopted unchanged: unblock-most-others first, freshest evidence secondary.**

It replaces "top 4 by value" — two people's taste arguing — with something checkable against the store.

**Kill criteria, from Rick's own row.** A row dies only when its *premise* is dead:

- already ruled elsewhere
- measured and no longer true
- superseded by shipped work
- a question the code already answers

🔴 **"Nobody got to it" is not a reason to kill a row.** That is what the holding area is for.

---

## 2. PROMOTE — four, ranked, each with the strongest line against

### #1 · `75044ab5` — Un-park is merged and invisible on the multiplexer

> The bundle predates the work by 15 hours.

**FOR** — it carries **Rick's standing ruling on what "fixed" means**, quoted on the row: *"it's fixed should mean that it's available to me to see and use… it should also be applied to both the client and the multiplexer both of them get this facility."* Nothing today enforces either clause. The deliverable is a **gate that reddens when `dist/` is older than its sources**, and that gate protects every future "fixed" claim on the compiled client. Highest unblock-count on the board, and the freshest evidence (measured last night).

**AGAINST** — the *rebuild* is probably one command, and if we stop there this is a ten-minute row wearing a P1's clothes. The gate is the real work and it is the part most likely to get deferred once the verb shows up in the UI. **If we promote this and ship only the rebuild, we will have done the thing this row exists to prevent.**

**Assign: María** — my defect, my ruling to honour.

---

### #2 · `7bc67019` — Wire the three typechecks as a blocking merge gate

**FOR** — **Rick already ruled YES**, on the fourth ask, `answered: true` / `default_used: false`. The work is authorized and unstarted. The precondition cleared when `b3d17502` landed: all three configs are green at the working tip, so gating now costs 14 of 16 tips nothing. The bisect says the type-error surface is **live and accumulating** — all five errors landed *after* `checkJs` was switched on. A ruled, authorized, unstarted gate is the cheapest unblock available.

**AGAINST** — it was scoped for Cheech, and **we cannot spawn Cheech before 17:00**. Whoever takes it also owes my un-waived condition: **a positive control on each of the three configs, not one for the set** — plant a type error in the nav tree, confirm `typecheck:nav` catches it, remove it; then diagnostic; then the mux. That is three plant-and-restore cycles on top of the wiring. **This is not a small row for a two-person day.**

**Assign: Mr. Radio.**

---

### #3 · `508449b7` — Memento delivery announces a full record as "carries no state"

**FOR** — **the fix exists, is verified behaviourally at Krishna's tip, and is not landed.** Two arms: the real record routes to *"YOU HAVE A MEMENTO — ALL OF ITS STATE IS IN THE BODY"* with 6,721 bytes delivered; a record *with* an amendment tail still routes to the amendment branch, so the fix **discriminates** rather than merely changing output. 210 of 341 records are affected. Until it lands, **every seat that re-spins is told its memento is blank.** Landing verified work is the cheapest unblock there is.

**AGAINST** — the thing this row was named for is *not* the thing that actually bit Tiberius. His memento block never arrived at all, and Krishna's fix changes what a block *says*; it cannot explain receiving **none**. That half is **unmeasurable retrospectively** and needs a receiving-end receipt nobody has built. **Landing this closes the announcement defect and leaves the delivery question exactly where it was.**

**Assign: María** — same lane as #4, same repo, same sitting.

---

### #4 · `507183ff` — The multiplexer cannot offer "Fixed"

**FOR** — Rick's own asymmetry complaint, alive on the client he actually uses: *"I see something's fixed, I'm going to mark it as fixed."* And there is a **mechanical chain behind it**: a board whose only human-driven terminal verb is a negative one inflates the open count → **the ratio gate reads that count** → the ratio gate has been refusing `task_create` fleet-wide. The real deliverable is a **parity guard between the multiplexer's list and the shared module** — the same *shape* as #1's stale-bundle gate, which is why doing them in one sitting is cheaper than doing either alone.

**AGAINST** — a test at `task_verbs.test.ts:49` currently **pins the six-verb list**, so a guard and that test will fight; and `holdingAreaBatch.ts` may carry a **third** copy of the vocabulary that nobody has grepped for. **The row's own "not established" section says we do not know whether `fixed` was omitted for a reason nobody wrote down.** Adding a verb on that basis is the move Rick has stopped twice this week.

**Assign: Mr. Radio** — pairs with his #2 on the same client.

---

### Runners-up, named so the non-promotion is not silent

| row | why it is not in the four |
|---|---|
| `c46ba7c0` | Four smoke tests send the API key as a **query param** and have been passing the merge gate while making **no successful request at all**. A genuine three-fault staircase. Not promoted only because its urgent half — an **uncommitted** split on a dead seat's worktree — has to be verified as still existing before anyone plans around it. **Strongest runner-up.** |
| `2761e6b4` | 98 genuinely unlanded commits by content across 49 branches; 3 safe-delete candidates content-verified. But the verdicts belong to branch owners, and 49-branch triage is not a two-person day. |
| `9ab0bddb` | Symlink laundering past the doc-viewer blocklist. One-line fix already verified as ARM F, 42 passed. **Real hole, no live exploit path** — 9 dangling symlinks, 0 escaping. |
| `8d043758` · `456f694c` | Both need a ruling on remedy shape before anyone builds; neither is blocked on us today. |
| `80513825` | Needs a **deployment** ruling (distinct service UID, or move the enforceable copy behind an authenticated door). Outside a worker's lane. |

---

## 3. KILL — four, each with a dead premise

### K1 · `3fdf4fb4` — "Main is RED: the prototype-chain sweep fails"

**Premise: measured and no longer true.**

```
npx tsx --test src/tests/unit/multiplexer/no_lookup_walks_the_prototype_chain.test.ts
→ pass 4, fail 0, 226ms
```

All four subtests green, including *"the shared refusal actually refuses — every inherited member, not just toString."*

**And the row's own wording is wrong in a second way**: it says *"main is RED"*, but `main` is at `8bf71a64`, **1,431 commits behind**, and **neither file exists there**. "Main" meant *the mainline we work on*. A future reader chasing that sentence checks the wrong branch and finds no such test.

⚠️ I did **not** prove which commit closed it — `05d7f5df` is a candidate, not a verdict. **I do not need to.** The row reports a red that does not exist.

---

### K2 · `2a2ee2f2` — "The multiplexer typecheck is RED at HEAD"

**Premise: superseded by shipped work.**

`b3d17502` merged; verified at the working branch: all three typechecks `exit 0`, duplicate `ownLookup` import 2 → 1. The row's own last line reads *"Everything else on `2a2ee2f2` has shipped."* Its one remaining item — the merge-requirement wiring — was deliberately re-filed as the typed decision `7bc67019`, which is promote **#2**.

🔴 **The honest verb here is `done` with `receipt_refs={"commit": "b3d17502"}`, not `dropped`.** Rick asked for four to *"delete or drop or mark won't fix"*; this one earned a close. **Recording the distinction rather than taking the easier verb to fill a slot.**

---

### K3 · `b4bc91f2` — "Adversarially review the un-park verb"

**Premise: already answered elsewhere.**

The row's stated purpose is *"row `03d3bf78` sits at status `review` with nobody named as reviewer… this review is the only thing standing between it and closure."* **`03d3bf78` is closed.** The residual — that the verb was never built for the multiplexer — is now tracked on `75044ab5`, promote **#1**, with a stronger acceptance test than this review asked for (Rick using it, on each client, rather than five proofs against source).

⚠️ Honest caveat: **that close was premature under Rick's later "fixed means he can use it" ruling**, and I said so against myself on `75044ab5`. So this is not "the review happened" — it is "the review's subject moved to a row that supersedes it." **Folding, not dismissing.**

---

### K4 · `97ff4426` — "PostToolUse fires COMPLETE at ~120s, then FAILED at 660s"

**Premise: a question the code already answers, and the answer is *not here*.**

All three layers measured:

| layer | evidence | verdict |
|---|---|---|
| server | 42/42 asks declaring >120s expired at their declared value ±0.1s | **innocent** |
| client | live probe answered at 151.4s, returned at 151.4s with the real answer | **innocent** |
| PostToolUse | fires at `min( answer, ~120s )`, 24/24, whatever the declared timeout | **the defect** |

The emitter holds no timer and no deadline — **the decision to invoke at ~120s is the harness's, above this repo.** Both owed items have since landed: the upstream report is filed, and the recovery path is documented in two homes (`src/docs/notification-api.md` § 6.7 and the `post_tool_use.py` module docstring).

⇒ **There is nothing in this repo left to do on it.** Its own title has already been corrected once because the original sent readers at innocent client and server code.

🔴 **This is Mr. Radio's row and he ruled on 2026-09-06 that it stays open.** That ruling's stated reason — *"what closed here is the reporting and the findability of the workaround"* — has **since been satisfied**. So I am re-opening the question to him, **not overriding him**. If he holds the line, it comes out of the kill list and a runner-up moves up.

---

## 4. What I am least sure of

**Four of my own instruments lied to me in one evening on 2026-09-08, and every one was caught by somebody else, not by me.** Applied here:

1. **K2 and K3 are both bookkeeping kills**, not premise-dead-in-the-world kills. They are *correct*, and they are also the two cheapest to defend — which is precisely the temptation Rick's row named. **If Mr. Radio's list has genuinely dead rows I missed, his should win over mine.**
2. **Promote #2 is the largest row in the four** and lands on a two-person day with no spawns. I ranked it #2 on unblock-count. **Ranked on what we can finish today it might not make the list at all.**
3. `c46ba7c0` may deserve promotion over #4. I ranked it out on a **verification gap of my own** — I did not check whether the uncommitted split still exists. That is a reason to go look, not a reason to rank it low.

---

## 5. Provenance

Every one of the 23 rows was read in full — bodies and amendments, not the terse projection — via `task_query( status="not_approved" )` paged at offsets 0/6/12/18 plus `task_get` on the four rows too large to serialize in a page. Counts and both dropped-row confirmations are from the store, this morning.

Promotion is Rick's act. This document recommends; admission stays executive.
