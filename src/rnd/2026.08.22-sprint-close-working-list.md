# Sprint close — the two v2 epics, row by row

**Author**: María 🌸 (`76904b6c`) · **Date**: 2026-08-22, ~10:50 EDT
**For**: the walkthrough with Rick — dispatch or eliminate, one row at a time
**Read at**: lupin wip `9b49f04d`, board generated 10:20 EDT

---

## The honest headline

**I closed nothing on my own, and the reason is not caution.** Every open row in these two epics is either owned by another live persona, or correctly blocked on work that has not happened yet. There is no row I could close where closing it would be true.

What I *did* do is move one row to the edge of closing (`0d4bc1ff`, DM'd to Rachel with both receipts) and find **two rows that are stale in a way that matters** — one P0 marked `urgent` that has not moved in six days, and one closed row filed under the wrong terminal status.

**Ten open rows. My read: 3 close today, 5 keep, 2 need your call.**

---

## Brain integration v2 — 6 open

| Row | P | Owner | Status | My read |
|---|---|---|---|---|
| `f7b57d88` | P0 | cheech | in_progress | **KEEP** — the epic manager row. Steps 1–9b done, all ten doors retired, merged to wip. Still owed: step 13, the final `:8000` gate at the merged head, the `:7999` bounce, the push. Not closeable until those land. |
| `4b37565a` | P0 | rachel | in_progress | **CLOSES TODAY** — revert `19c198e8` + merge `9b49f04d` done, unit tier green (16,968/0). Waiting only on Rachel's cosa tier. She closes it with her own number. |
| `0d4bc1ff` | P1 | rachel | blocked → **unblocked** | **CLOSES TODAY.** You discharged the blocker at 10:15 by ruling the window closed. Receipt is the revert; the window report already exists (6h35m, 6 boots, 65 transcribe + 2 ask + 1 push, **zero trips**). DM'd to Rachel with both. **She cannot close it herself** — her own row `4b37565a` carries Mr Radio's hard limit *"do not close 83fe98aa or 0d4bc1ff"*, so she has gone back to him to have the limit lifted. Correct of her; noting it so the row is not read as ignored. |
| `a40e462c` | P1 | pocholo | in_progress | **YOUR CALL — I think it closes.** 49 amendments of adversarial per-commit review for a build that is now merged. Its last entry names one residual (stub two seams in the arbiter-loop tests, one line each) which TODO.md already carries as a follow-up. A review stream outliving its build reads as live work forever. |
| `1e597a65` | P2 | mr radio | blocked | **KEEP** — step 13 cache dump. Tooling built, reviewed twice by Pocholo, merged. Needs Cheech's GO, and Cheech is not a live seat. |
| `262346bf` | P1 | mr radio | queued | **KEEP** — my Q&A-card plan, which he has now reviewed: **PASS with four blockers**, all in the proving layer or the phasing, none in the design. |

---

## Ship CJ Flow v2 — 4 open

| Row | P | Owner | Status | My read |
|---|---|---|---|---|
| `92062fe2` | **P0 urgent** | cheech | in_progress | 🔴 **YOUR CALL — I recommend closing it as superseded.** Last touched **2026-08-16, six days ago**. It is the original "implement the v2 plan with the SWE crew" row from the 08-14 broadcast. All six units exist; its DoD 3 and DoD 4 were shipped and verified; its one live residual — the eval run — is `d212f54b`, which exists as its own row. The actual build happened under `f7b57d88`. **It is a P0 marked `urgent` that nobody is working and nobody could work**, and it is the single most misleading thing on the board. |
| `d212f54b` | P2 | maría | blocked | **KEEP** — the v2 eval re-run. Genuinely blocked behind step 13; the cache dump is what makes a clean cold pass possible. |
| `647f3733` | P2 | maría | blocked | **KEEP** — v1 baseline, behind `d212f54b`. Still carries one unsettled choice for whoever runs it: which v1 sha is the referent. |
| `2ebe4ccb` | P1 | maría | blocked | **KEEP** — trust-criteria checklist, behind `d212f54b`. Every denominator is now pinned; **two labelling rulings are escalated to you** (below). |

---

## Two things that are not rows and should be

### 1. `83fe98aa` was closed as `dropped`, not `done`
The merge decision you ruled on this morning was executed with full receipts and then filed as **dropped**. Nothing is lost — the receipts are all in the row — but *dropped* means abandoned. A P0 decision that was answered, executed and verified reads as one that was walked away from, to anyone auditing this sprint later. Worth a one-line correction while we remember why.

### 2. The two labelling rulings owed by you — and one is time-sensitive against this afternoon
From `2ebe4ccb` §10b, both escalated deliberately rather than defaulted:

- 🔴 **math ↔ calculator.** Of 60 cold math-expected utterances, **23 were answered by the Calculator.** Does that count as a routing hit or a routing miss? **This needs ruling before the clean re-run and before a retrain**, because the split is only visible today by accident — calculator snapshots survive the codeless carve-out while math ones die on replay. Once the write-back fix persists code, they all replay cleanly, the split vanishes from the error number, and it stays exactly as true.
- **§2b's literal "v2 ≥ v1" at n=60.** One utterance flips it. Margin, or known knife-edge? Both defensible.

---

## What Mr Radio found in my plan, since it changes this afternoon's shape

**PASS with four blockers** — write-up at `lupin/src/rnd/v0.2.0/2026.08.22-qa-card-plan-review.md`. Two are mine to fix, two are yours to weigh:

1. `user_initiable` is the right field, **but my gate 3 still tests `speakable`** — so a command that is typeable-but-not-sayable could go missing from the dropdown and the gate stays green. That is the exact case the field exists for. *Mine to fix.*
2. **The receptionist exception is a rendering rule in my plan and a routing hole in the code** — its spec has `factory=None`, so `resolve()` returns `None` and nothing says what actually serves a deliberately-picked receptionist. *Mine to fix, and it is the sharper of the two.*
3. Gate 2's grep fires on the Auto-Route option, and gate 3's own falsification is a **count** — which §6 disqualifies three lines later. *Mine.*
4. 🔴 **Phase 5 retires `/api/test-suite/submit`, which both `CLAUDE.md` and `CLAUDE.local.md` name as the ONLY sanctioned `:8000` door.** That is a fleet-wide protocol change plus a chicken-and-egg, not a door retirement the size of the ten already done. *Yours.*

**He has not spun a crew**, deliberately: it would compete with this afternoon's PEFT run for the box and the Max window. He is putting the timing to you rather than deciding it.

---

## 🔴 OUTCOMES — the walkthrough with Rick, 2026-08-22 11:00–11:30 EDT

Every one of these is a genuine keypress, not a timeout default.

| Row | Ruling | Receipt |
|---|---|---|
| `92062fe2` | **CLOSED as superseded** | `done`, receipts `26972be7` + successor `f7b57d88` |
| `a40e462c` | **CLOSED**; its one leftover rides in `TODO.md` | `done`, event 8878, receipts `9b49f04d` + this doc |
| `0d4bc1ff` | Rick lifted Mr Radio's hard limit; **Rachel closed it herself at 14:59Z** with her final window reading | closed by owner, receipts on the row |
| `4b37565a` | **Already closed by Rachel at 10:52** on her own cosa number — before the lift arrived | merge `9b49f04d` verified; cosa **8,622/0**, and **8,624/0** on the brake-flag rebase `01a67280`, both proof arms exercised |
| `83fe98aa` | Status is unrecoverable (both words terminal); **note appended** + Mr Radio's wording carried into the session history | event 8880, `amended_post_terminal` |
| `:8000` door | **KEEP IT IN SCOPE** — retire the door and rewrite both CLAUDE files in the SAME change | folded into the plan with a hard ordering gate (below) |

### ⚠️ One number that will look wrong to the next reader
Rachel's cosa tier reads **8,622 passed** against a stated baseline of **8,796**. That is **174 fewer tests, and it is not a regression** — it matches the integration head's own count from the night before, and reconciles against the class deletions on row `7a20a09d` (112 test functions removed in four files) plus the 119-case drop from `1c319db0`. Recorded here because a smaller green number is exactly the kind of thing that gets read as loss six weeks later.

### 🔴 The phase-5 ordering gate Rick's ruling requires
He chose the coherent-change option: the door retires and both CLAUDE files are rewritten together, so no session is left following a stale instruction. That is the stronger end state, and it carries the one risk I named — a chicken-and-egg on the fleet's only test venue. It is now written into the plan as a **hard precondition, not advice**:

> `/api/v2/submit` must be **proven to schedule** — one real test-suite job through it with `scheduled_at` set, landing and running on `:8000`, its `ts-` id as the receipt — **before** `/api/test-suite/submit` becomes a 410, and only in the same commit that rewrites both CLAUDE files.

Wrong order and the fleet loses its only sanctioned test route mid-sprint, on a training-run day — and a revert does not fix it, because by then the instruction and the route have parted company in everyone's context.

### What is left on these two epics after today
- `f7b57d88` — the epic manager row: step 13, the final `:8000` gate at the merged head, the `:7999` bounce, the push. **Cheech's, and he is not on a seat.**
- `1e597a65` — step 13, tooling built and twice reviewed, waiting on Cheech's GO.
- `262346bf` — my Q&A-card plan, reviewed PASS with four blockers.
- `d212f54b` → `647f3733` + `2ebe4ccb` — my eval chain, all correctly blocked behind step 13, all re-chased past their blocker this morning.
- The 11 deselected serial-bridge-guard tests still need a quiescent box, and the `:7999` bounce and the push are still Rick's word.
