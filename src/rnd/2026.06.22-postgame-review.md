# Post-Game Review — 2026.06.22 (María 🌸, session 2f4d71cb)

> **🌅 RESUME IN THE MORNING (Rick, 2026-06-22 night).** We're picking ALL of this up in the morning. Live agenda below (items 1–10). Top of the list to chase first: **(4) DM-liveness residual** — verify whether the running arbiter binary actually contains the bridge-mtime-ack path (Mr Radio's hypothesis (a)); **(1) question-surfacing** doctrine+mechanism; **(2) arbiter-restart delegation** (Bash permission rule); **(10) late-night-commit coordination workflow**. Session-end ritual (backup + push + global roll-up) run tonight on the as-committed state.

**Context**: Late-session deploy run. Receipts-of-progress backstop + subagent-governance hook both shipped, verified, and closed; fleet-roster migrated to the user-level `~/.claude/fleet-roster.env`; PIP doctrine softened + committed (`c045cf5`, push held). Rick opened a post-game and is dictating a couple of notes — captured below for later design.

---

## Note 1 — Doctrine + mechanism for surfacing worker→Rick questions (the 10-min timer was a HACK)

**Rick's framing (verbatim intent, 2026-06-22 voice):** *"How do we get the fact that you and other workers — Mr Radio, Tiberius — have questions that require my attention [to him]? The 10-minute timer for pinging me was just a hack to force you to surface your questions. We really do need doctrine and mechanism for surfacing questions that you have for me that require an answer."*

**The problem.** When a worker/manager-figure (María, Mr Radio, Tiberius) reaches a decision only Rick can make, there is no *reliable, unified* path for that question to reach him — especially when he is AFK or the asking session is parked/idle. Today it depends on the asking session being active and choosing to ask, plus a crude timer to force re-surfacing.

**The hack being retired.** The ~10-min `ScheduleWakeup`/`/loop` re-ask loop (Rick directive `cd610b8a`, "re-ask any unanswered gate every ~10 min") is a *forcing function*, not a real mechanism: it makes each session independently re-poke Rick on a flat cadence. Problems: (a) noisy — N sessions each re-ask on their own timer; (b) unreliable — depends on each session staying alive + remembering to re-fire; (c) no single place for Rick to see *everything awaiting him*; (d) it conflates "keep the session alive" with "surface the question."

**What already exists (build on, don't reinvent):**
- **cosa-voice `ask_*` / `converse`** — surfaces a question to Rick *when the asking session is active and chooses to ask*. The live channel; no persistence, no cross-session aggregation.
- **`pending_user_gates` + heartbeat `outstanding_user_gate` work-owed signal** (shipped tonight, task `6929f4ac`) — an open gate counts as `work_owed`, keeps the session alive, and the Stop-hook NAMES the due gates so the session re-fires the `ask_*`.
- **Arbiter dark-session gate-resurface** (case-18, the receipts backstop deployed tonight) — if the owning session goes DARK with an aged open gate, the arbiter re-surfaces it to Rick. This is the closest thing to a real backstop already in place.
- **Task-store `gate_class = ricks_court`** — the store ALREADY has a typed "Rick's court" class. A latent substrate for a unified question queue.

**The gap the hack papered over.** There is no **single, Rick-facing "questions awaiting you" queue** across the whole fleet, and no **clean push** that reaches him when a NEW question is raised (vs. a flat re-poll). Each session surfaces independently.

**Design seed / open questions (for the design session, NOT to build now):**
1. **Canonical substrate** — make every user-blocking question a typed `gate` item (`gate_class=ricks_court`) in the unified store at the moment it's raised. One query (`task_query(gate_class=ricks_court)`) = everything awaiting Rick, fleet-wide. (Aligns with store-canonical.)
2. **Surfacing/push** — who actively notifies Rick on a NEW gate, and re-surfaces an unanswered one? Candidate: a SINGLE aggregator (the `:8001` arbiter) reads the ricks_court queue and pushes/re-surfaces — replacing N per-session timers with one queue-driven reminder. (Extends the case-18 resurface from dark-session-only to all-open-gates.)
3. **Urgency tiering** — urgent question interrupts immediately; normal batches into a digest; low sits in the queue. Avoids both flooding and starvation.
4. **Answer routing back** — Rick answers once (voice / a queue UI); the answer reaches the right asking session and clears the gate (stops the re-surface). The asking session may be parked → needs a wake/inject path.
5. **Doctrine half** — WHEN does a worker mint a `ricks_court` gate vs. ask inline vs. decide under standing authority? (Ties to blast-radius authorization + "gate = a direct ask, never buried.") The mechanism is worthless if workers don't reliably *classify* a question as Rick-blocking.
6. **Relationship to the 10-min loop** — the loop becomes a fallback keep-alive, NOT the surfacing mechanism. The queue + aggregator-push is the mechanism; the timer is demoted.

**Owner**: María (design) + Mr Radio / Tiberius (build, lupin arbiter + store). **Priority**: P1 (Rick-flagged as a real need). **Status**: captured for the design session; NOT building tonight (Rick's no-new-board-items).

---

## Note 2 — (awaiting Rick's second note)

*Placeholder — Rick said "a couple of notes." Second note to be dictated (may resolve into one of the agenda items below).*

---

## Post-Game Agenda — candidate topics to walk through

Built at Rick's request (2026-06-22). Prioritized; each is one line. **Item 9 is reserved for the "something else" Rick wanted to raise.**

**Tonight's follow-ons (concrete, from this session):**
1. **Question-surfacing doctrine + mechanism** (= Note 1) — replace the 10-min re-ask timer (a hack) with a real fleet-wide "awaiting Rick" queue + single pusher, on the `ricks_court` + case-18 substrate. [P1]
2. **Arbiter-restart delegation** — the classifier blocked my relaying your deploy GO (human-auth-not-launderable); you want a lupin worker, not you, to own `:8001` restarts → needs a Bash permission rule + a delegation rule for who may bounce shared infra. [P1]
3. **Deploy-honesty as a named rule** — Rachel's "advertise-live-while-inert" catch (the inert backstop + present-tense comments) recurred tonight; worth a standing check so "LIVE" claims are wired-and-verified, not aspirational.

**Parked / deferred (currently blocked by your no-new-items — decide if/when to file):**
4. **DM-activity-as-liveness gap** — **UPDATE (verified in lupin code 2026-06-22, NOT a guess):** the fix Mr Radio imagined ALREADY EXISTS and is ON — `arbiter count dm as liveness = True` (`lupin-app.ini:564`, built 2026-06-17, runtime-tunable). A manager's most-recent **sent** `ai_to_ai` DM (≤~1h, attributed by sender `#`-session-id) is a **5th liveness signal**, built precisely for a DM-only manager (no Read/Edit/Bash → bridge-mtime never bumps); correctly scoped as life-not-progress. So Mr Radio's "dm_send doesn't count → false DOWN" was an **un-verified guess**. **RESIDUAL puzzle** (why he STILL tripped a false-DOWN tonight) — hypotheses to chase: (1) the **~1h sent-DM window** ages out a quiet stretch; (2) **re-spin session-id attribution** drift (he re-spun tonight — the known cross-session correlation gap); (3) a **separate tap-ACK 600s path** that may not consult the 5-signal union. **DISPOSITION (Rick): don't chase tonight — investigate in the morning.** [P2, morning]
   - **Mr Radio's precise diagnosis (DM `a0b4c868`, 3x recurrence, captured fresh):** 5 commons presence beacons did NOT stop the escalations → beaconing is ineffective for his identity. Code path (wip-v0.1.9): `_check_manager_acks` acks on `max(commons_activity, bridge_activity) ≥ tapped_at`; `resolve_active_managers` emits bridge persona form `'mr radio'`; `_manager_last_activity` exact-matches `who_rows` persona_name `'mr radio'` — so commons form-match SHOULD work AND bridge-mtime ack SHOULD catch his tool activity, yet neither clears it. **His two live hypotheses (refine mine):** (a) the DEPLOYED arbiter (PID 792508, restarted 21:54) **predates the F-B `canonical_persona_key` / bridge-mtime-ack code on wip-v0.1.9** — running binary older than source (≈ my hypothesis 3); (b) his session's bridge file isn't at the path `find_active_voice_persona_sessions` scans, or its mtime isn't bumped — he couldn't locate `~/.claude/cosa-voice-bridges/*9998b3ef*`. **Morning first move:** verify (a) — confirm the running arbiter's code actually contains the bridge-mtime-ack path; cheap to check. **Mr Radio's sharpening (DM `44912107`):** expect (a) to be **FALSE** — `_manager_bridge_activity` is already present in the deployed `arbiter_job.py` (same file `acfac855` touched, loaded at the 21:54 restart) — so pivot straight to **(b): a runtime bridge-LOOKUP miss** (`find_active_voice_persona_sessions` not matching his session, or bridge mtime not bumped). **Chase bridge-discovery, not a redeploy.** Note: this does NOT impugn the receipts backstop — that's live by construction (`acfac855` loaded post-restart).
5. **Receipts-of-progress v2: inward-arbiter classify** — deferred per the ratified v1/v2 split (don't synthesize per-manager liveness early); decide trigger to file/build.
6. **Reaction-feedback rubric** (👍 👎 🏅 🟥) — design + build-prep authored, held for your walkthrough; build-go decision.

**Standing (your broadcast `59f2d129`, still on deck):**
7. **Task-list / TODO sprawl** — make the unified store/TODO surface actually usable (this TODO.md is Exhibit A).
8. **cosa-voice MCP token efficiency** — the MCP burns a large share of inference budget; design how to cut it.

**Rick-added topics:**
9. *(Rick's "something else" resolved → item 4, the DM-liveness gap.)*
10. **Late-night commit + backup coordination workflow** (Rick, 2026-06-22) — author a workflow defining how EVERY manager behaves when Rick calls for an end-of-night commit + backup. Shape: (1) each manager reaches a safe checkpoint + commits its held work (standing commit authority; PUSH stays Rick's); (2) reports completion + commit SHAs / repo state to **María** (the `plan` steward); (3) María waits for all managers to report "in," then runs the **global cross-repo LoC roll-up** (`workflow/loc-delta-global.md`); (4) backup runs. Open questions: sequencing + who signals "all in," a dark/non-responsive-manager timeout, and how it composes with `session-end.md` + the hub-spoke manager handoff + the standing roll-up authorization. [workflow to author — P1]

**Process wins to ratify (quick):** verify-before-close worked (I checked PID + `~/.bashrc` before closing tonight's tasks); hold-field kept terse after your catch.
