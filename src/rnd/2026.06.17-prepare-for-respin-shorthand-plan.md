# Implementation Plan — "prepare for re-spin" Shorthand (Extend-existing `/plan-memento`)

**Date**: 2026-06-17 · **Author**: María 🌸 · **Status**: ⏸ PLAN — awaiting Rick's "go" before any edit
**Decision**: Rick chose **Extend existing (Recommended)** (2026-06-17 voice). Store task: `7af4e664`.
**Gate**: per `feedback_plan_link_not_execute` — this is a multi-file doctrine sweep, so it's serialized here for review; **no edits made yet**. On Rick's "go" I execute §3 in order; the one out-of-repo item (§5) waits for his **direct** word.

---

## 1. Goal

Make **"prepare for re-spin"** (spoken to any worker, in any repo) a recognized shorthand that means: *reach a safe checkpoint → write your memento → ACK ready-to-be-reaped.* Reuse the EXISTING memento workflow + `/plan-memento` command — **no new command, no duplicate surface.**

**Why extend, not build new**: `workflow/memento-management.md` + `.claude/commands/plan-memento.md` already do the memento. The only gaps are (a) the phrase isn't a recognized trigger, and (b) `/plan-memento` isn't in the install-wizard catalog (so it isn't portable to other repos yet).

---

## 2. Pre-flight findings (verified, not from memory)

| Check | Result |
|---|---|
| `workflow/memento-management.md` exists | ✅ (§1 when-to-use, §2 the 8-element contract) |
| `.claude/commands/plan-memento.md` exists | ✅ (a **command**, not a `SKILL.md` — trigger lives in its description) |
| "re-spin" / "prepare for" already a trigger? | ❌ none — `grep` empty in both files |
| `/plan-memento` in wizard catalog? | ❌ not in `INSTALLATION-GUIDE.md` or `plan-install-wizard.md` |
| Always-create-task mandate exists? | ✅ `task-store-discipline.md` §35 (all sessions, mirrored in global CLAUDE.md) |

---

## 3. Edit plan (in-repo — execute on "go", in this order)

**E1 — `workflow/memento-management.md`** (the canonical workflow)
- Add a short **"§0 Trigger phrases"** note at the top: *"prepare for re-spin"*, *"respin prep"*, *"ready yourself for re-spin"*, *"make a memento"* all invoke this workflow.
- Add a **"Worker re-spin framing"** subsection: the 3-beat sequence **(1) reach a safe checkpoint → (2) write the memento (the §2 8-element contract) → (3) ACK "ready for re-spin"** to the requesting manager/Rick.
- Version-history entry (vN+1).

**E2 — `.claude/commands/plan-memento.md`** (the command/skill surface)
- Add the trigger phrases to the command's description/intent block so it activates on *"prepare for re-spin"* (intent-based activation, not just `/plan-memento`).
- One-line cross-ref to the worker re-spin framing in E1.

**E3 — `workflow/INSTALLATION-GUIDE.md`**
- Add `/plan-memento` (+ `memento-management.md`) to the installable catalog, with a one-line description, so any repo can install it.

**E4 — `.claude/commands/plan-install-wizard.md`**
- Add `/plan-memento` to the wizard's offered-commands list so the interactive installer presents it.

**E5 — `workflow/swe-team-roles.md`** (the worker spawn charters)
- Reinforce in the **worker** charters: (a) the always-create-a-task-item mandate (`task-store-discipline.md` §35) — restate it so a spawned worker carries it even without the full global CLAUDE.md context; (b) recognize **"prepare for re-spin"** → produce a memento + ACK.

**Verify after E1–E5**: `grep` the phrase resolves in both the workflow + command; confirm the wizard catalog lists `/plan-memento`; `py_compile` n/a (docs only). Update README R&D link + session manifest. Commit **held** (no push — your gate).

---

## 4. Open micro-decisions (my recommendation in **bold** — answer only if you disagree)

1. **Does "prepare for re-spin" auto-commit/stage held work, or only memento?** → **Memento + reach-safe-checkpoint only.** Leave commit to the session-end ritual / the manager; bundling a commit into the shorthand mixes concerns and could stage another session's files. *(Recommend NOT bundling a commit.)*
2. **Scope of the task-item reinforcement?** → **Worker charters in `swe-team-roles.md`.** That's where a spawned worker reads its standing rules; the global mandate stays the source of truth.

---

## 5. Out-of-repo item — SEPARATE gate (needs Rick's DIRECT word)

If you want the **live `~/.claude/CLAUDE.md`** (and its repo mirror `global/CLAUDE.md`) to carry a one-line *"prepare for re-spin = /plan-memento"* pointer, that's a separate edit to your live global config — I will **not** touch it without your explicit direct word (blast-radius rule). Flag it and I'll fold it in.

---

## 6. Sequencing & status

1. ⏸ **THIS DOC** — sent to Rick for review. **HOLD.**
2. ▶ On "go": execute E1→E5 (§3), verify, README + manifest, commit held.
3. ◻ (Optional, separate direct-word gate) §5 live-global pointer.

*Anchor docs: `workflow/memento-management.md`, `.claude/commands/plan-memento.md`, `workflow/INSTALLATION-GUIDE.md`, `task-store-discipline.md` §35. Decision provenance: the 2026-06-17 ask_multiple_choice "Re-spin shorthand" → Extend-existing.*
