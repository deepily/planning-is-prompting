# Mini Plan 01 — Memento guard: coverage is INVERTED

**Store row**: `3e96f667-5d00-4275-847c-787131ead4b4` (bug, P1, owner maria)
**Author**: María 🌸 (d3254802) · 2026-07-21 (S142)
**Status**: plan written, awaiting Rick's un-park + assignment to Mr Radio's SWE crew
**Executor proposed**: Mr Radio 🦉 + SWE crew (Implementer / Reviewer / Tester)

---

## 1. The defect, re-derived at source today (not taken from the row)

| Act | Recoverable? | Guard today | Verified at |
|---|---|---|---|
| `Write`/`Edit` on an **existing** record | YES (content in hand) | **BLOCKED** | `memento_record_guard.py:138-142` |
| `Write` to **CREATE** a record | NO — silent orphan (no mirror, no pointer) | **PERMITTED** | `memento_record_guard.py:139` `if not os.path.exists( path ): return 0` |
| `rm` of a **live** record | NO — total, silent | **PERMITTED** | matcher is `Write\|Edit` only (`~/.claude/settings.json:56`); Bash never reaches the hook |

**The guard blocks the one recoverable act and permits both silent ones, one of which is destructive.**

Fourth finding, also re-verified: the refusal the operator is most likely to read **recommends the bypass** —
`memento_io.py:746` prints *"(Same persona, same session? **Append to it by hand**, or write a new session's record.)"* on exit 3.
"Append by hand" **is** the raw-`Write` orphan path.

Settings matcher is correct today (`"Write|Edit"`, settings.json:56) — that half is not the bug.

## 2. Scope — four fixes, in dependency order

- **F1 — close the CREATE hole.** Refuse a raw `Write` to a non-existent record path; the denial names `memento_io.py write` (which already picks every path itself and cannot overwrite). Drops `memento_record_guard.py:139`.
- **F2 — guard `rm` of a live record.** Add `Bash` to the hook matcher; parse the command for `rm` targeting a RECORD path; refuse, and name the safe verb. Fails open on anything it can't parse (the file's existing stated posture — keep it).
- **F3 — kill the promotional surface.** Rewrite `memento_io.py:744-747` so exit 3 recommends `amend`, never "by hand."
- **F4 — make the backup a precondition, not a habit.** The documented repair recipe is `cp` → build → `rm` → `write`, and the `cp` is load-bearing with nothing enforcing it. Add a `memento_io.py repair` verb that does backup→verify-non-empty→replace→re-mirror→re-point as ONE call that fails loud, so the destructive step is never hand-typed.

**Out of scope**: the record/pointer split itself, the mirror design, `worktree_creation_guard.py`.

## 3. Acceptance criteria (all must be receipted, no self-report)

1. Raw `Write` to a NEW record path is refused (exit 2), stderr names `memento_io.py write`.
2. `memento_io.py write` to a NEW record path still succeeds — F1 must not break the sanctioned path.
3. `rm` of a live record via Bash is refused; `rm` of a non-record file is untouched.
4. `Write`/`Edit` on an existing record still refused (no regression on the one path that worked).
5. `memento_io.py write` against an existing record exits 3 with a message containing **no** "by hand" recommendation.
6. `repair` verb: refuses to proceed when the backup is absent or zero-length.
7. **Negative control**: the guard allows an ordinary `Write` to an unrelated `.md`. A guard that blocks everything is an outage.
8. Verification is by **sha256 on both sides + pointer read-back**, never by the script's own success report — that report is exactly what lied in the founding incident.

## 4. Test plan

Extend `workflow/scripts/test_memento_io_postgame.py`. Each AC above = one test. Run from a neutral registered directory (not a worktree — a worktree `conftest.py` can manufacture a false green).

## 5. Risk

Low blast radius, fully reversible (hook + one script). The one real hazard is F1 breaking legitimate first-writes — AC-2 exists solely to catch it, and it is a hard gate on the merge.

## 6. Open question for Rick — the ONLY thing blocking execution

This row is parked past the Thursday 07-23 demo, and its body carries **"NOT TO BE GRADUATED OR IMPLEMENTED TODAY"** under the 2026-06-29 accumulate-before-graduate ruling. That ruling was about *graduating doctrine*, not about *fixing a P1 bug*. My read: the ban applies to the lesson, not the code.

**Recommendation: un-park and implement now.** It is off the demo path entirely (PIP tooling, not skills-distillation), and every re-spin between now and Thursday runs on the inverted guard.

**RULED 2026-07-21: Rick said yes** — un-park, hand to Mr Radio's crew. Executor is Mr Radio 🦉 (`56a74d7b`); his build brief + F5 (canonical-slot enforcement, row `af0c5700`) are in the companion doc `2026.07.21-mini-plan-01b-memento-canonical-slot-and-build-brief.md`.

---

# ⚠️ AMENDMENT — 2026-07-21, adversarial review: **RE-CUT**

An adversarial reviewer verified every claim above at source. **§1 survives; §2 does not.** This amendment governs; where it conflicts with §2–§6, this wins. Relayed to Mr Radio before his crew cut code (DM `d128cc46`).

## A. Corrections to §1 (my errors, do not inherit)

- **The matcher is at `settings.json:57`, not `:56`** — cited wrong twice above. `:56` is the block's opening brace.
- **`MUTATING_TOOLS` also covers `MultiEdit`** (`memento_record_guard.py:72`) — my table understates by one tool.
- **§6's framing is not supported by its source.** The row says *"NOT TO BE GRADUATED **OR IMPLEMENTED** TODAY"* — "implemented" is explicit — and the `park_reason` I set myself ~20h earlier says the same. My *"the ban applies to the lesson, not the code"* re-litigated my own park with no new evidence. Rick's ruling authorizes the work regardless; the reasoning I offered for it does not.

## B. Verdict per fix — §2 is superseded

| Fix | Verdict | Why |
|---|---|---|
| **F3** kill the `:746` "append by hand" text | **SHIP AS-IS** | smallest, correct, independently shippable |
| **F1** refuse raw `Write`-to-CREATE | **SHIP WITH 4 ADDITIONS** | safe, but narrower than advertised |
| **F2** guard `rm` via a Bash matcher | **CUT — or `MODE="LOG_ONLY"`** | reverses a documented ruling; reproduces the defect it fixes |
| **F4** `repair` verb with enforced backup | **REPLACE with `adopt`** | opens a larger hole than it closes |

### F1 — safe on the axis I feared, incomplete on the axis the row indicts

**The hazard I wrote AC-2 for is dead**: `memento_io.py` writes via `Path.write_text` (`:771`/`:778`) and `open("a")` (`:973`) — it issues no `Write`/`Edit` tool call, so no PreToolUse hook fires on it. F1 cannot break the sanctioned path.

Four additions, all missing from §2:
1. **Bash still creates records freely** (`cat > io/mementos/x-1af4b598.md`). F1 *narrows* the CREATE hole; it does not close it. **The denial text must say what the guard cannot see** — otherwise the row's own P1 rationale ("the seat now believes the decision was removed everywhere") applies to F1 itself.
2. **F1 blocks the RESCUE path.** `rescued-<persona>-<sid8>.md` matches `RECORD_IO_RE` (`:66`), and rescue artifacts have historically been hand-written (`test_rescued_records_are_not_seats`). Needs a sanctioned verb or an exemption.
3. **F1 makes `memento_io.py write` the SOLE create path — and that path can REFUSE** (exit 6 post-game gate, exit 4 gitignore). In a fresh worktree a seat could be hard-blocked from recording state. AC-2 covers only the solo happy path; a crewed/worktree AC is required.
4. **The guard's docstring must be rewritten** (`:17-28`, `:43-51`) — it currently documents ALLOW-new-record as *intentional design*. A guard whose docstring contradicts its code is this row's parent defect.

### F2 — cut it; I silently reversed a standing ruling

`memento-management.md:257` already decided this, with reasons I neither cited nor rebutted:

> *"`echo >`, `sed -i`, `rm`, `cp` — the guard matches tool calls, and Bash is one tool call carrying arbitrary bytes. **Covering it would mean parsing shell, which is a different and much worse problem.** The mirror is the only thing standing behind this, which is exactly why the mirror — not the hook — is the load-bearing layer."*

Evasion surface against an `rm`-token matcher, all trivially reachable: `rm "$F"` · `find io/mementos -delete` · `mv` · `> rec` / `: > rec` / `truncate -s0` · `sed -i` · `python3 -c "os.remove(...)"` · `xargs rm` · `eval` · heredoc · `cd … && rm` — and **`git clean -xdf`, the documented catastrophic case (§3.5, nine records), which contains no `rm` token at all.**

**A partial rm-guard IS the false-confidence defect this row exists to fix**, and "fails open on what it can't parse" is what manufactures it: the operator watches it fire on `rm <path>` and infers coverage of deletion. **In-repo precedent settles it** — `worktree_creation_guard.py` does exactly this shlex-parsing class, classifies unparseable input `UNKNOWN`, and ships `MODE = "LOG_ONLY"`, uninstalled, behind a Rick gate (`:19`, `:84`). Its own authors did not trust their parse to enforce.

If kept at all: `LOG_ONLY`, a census phase first, and an AC that the message **enumerates its own blind spots**.

### F4 — replace; the destructive step never needed to exist

- **The `cp → build → rm → write` recipe is NOT documented anywhere** — not `memento-management.md`, not `plan-memento.md`. It lives only in the store-row body, and the row already records its mitigation (*"a `test -s` precondition on the backup, sent to Cheech"*). F4 was scoped against an undocumented, already-mitigated procedure.
- **An orphan is adoptable TODAY, non-destructively**: `sync_record()` (`:844-874`) does mirror + pointer for any record in one call, failing loud on sha mismatch · `migrate --apply` (`:1064`) mirrors every record, idempotent, copy-only · `regenerate-pointer` (`:1004`) re-points to the newest.
- **F4's "replace" leg would mint a sanctioned overwrite of an immutable record** inside the one script whose thesis is *"the overwrite is not spellable"* (`memento_io.py:13`, §3.1) — and the guard cannot see it, because `memento_io` runs through Bash.

⇒ **Expose `sync_record` as an `adopt` verb; DELETE the `rm`-based recipe.** Plus an AC that `adopt` cannot be aimed at a **foreign** record — `amend` needed exactly that guard (`memento_io.py:908-941`, added 2026-07-18) and any new verb reintroduces it.

## C. Test plan — §4 is WRONG, re-home it

`test_memento_io_postgame.py` is scoped by its own docstring to the R-1 post-game gate, and every fixture drives the `memento_io` **CLI via subprocess with stdin body text**. **Nothing in it can feed a JSON tool-call payload to a PreToolUse hook.** ACs 1/3/4/7 are guard tests with no harness there.

⇒ **New `workflow/scripts/test_memento_record_guard.py`**, following `test_worktree_creation_guard.py`'s convention (`sys.path.insert` + `import guard` + `SCRIPT` subprocess with JSON on stdin) — whose docstring already argues a guard test must be tracked beside the thing it tests. ACs 2/5/6 fit the existing postgame file.

## D. Added ACs

9. The guard's docstring + `DENIAL` text match the new behavior, and the denial **states what the guard cannot see**.
10. A **Bash-created** record is still possible — asserted explicitly, so F1 cannot ship green while reading as "CREATE is closed."
11. The **rescue-record** path has a sanctioned verb or an exemption; `rescued-*` is not hard-blocked.
12. `memento_io.py write` succeeds after F1 in a **crewed/worktree** context (exit-6 interaction).
13. `adopt` **refuses a foreign record** (mirrors the `amend` targeting guard).
14. Pre-existing characterization tests (`test_UNRULED_*`, `:438`/`:452`) stay green.
15. If F2 ships at all: an AC that its message **enumerates its blind spots**, incl. `git clean -xdf`.

## E. Spun out — not mine to hold

**`memento_record_guard.py` has ZERO checked-in tests, while `memento-management.md:246` claims *"Unit-tested 13/13 in both directions."*** A doc asserting coverage that does not exist, on the guard we are about to change — the same scratchpad-loss shape `test_worktree_creation_guard.py`'s docstring was written to document. **This is the precondition for every guard-side AC**, and it is why the guard could ship inverted without anything going red. Filed as its own row for Mr Radio's crew.
