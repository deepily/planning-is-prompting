# All-Tiers Overnight Coverage Grind — Consolidated Post-Game (Session 100)

**Date**: 2026.06.03
**Authors**: María 🌸 (Workflow Steward — framework synthesis) + Tiberius 👑 (Manager — Lupin-infra root-cause companion)
**Scope**: Retrospective on the 2026-06-02 → 06-03 all-tiers 100%-coverage overnight marathon (Session 100), per Rick's post-game broadcast `c1d94d9e`. This grind followed and built on the 2026-06-01/02 WAVE-2 campaign.
**Status**: 🟡 **DRAFT — steward hub.** Split **CONFIRMED** with Tiberius via DM `7e54d9dd`/`8d677d2a`: this PIP doc owns the framework/steward synthesis; Tiberius owns the Lupin-infra root-cause companion (`:7999`/MCP, prod bugs, hermetic-config fixture). Numbers-subordinate to the certification doc. Awaiting his Lupin-companion publish for reciprocal cross-link.

**Companion artifacts**:
- **Certification of record** (committed-tree numbers): Lupin `…/23-overnight-grind-certified-complete.md` (Tiberius). This PIP doc is numbers-subordinate to it.
- **Observer log** (per-wake ledger + ratification): `src/rnd/2026.06.03-all-tiers-overnight-grind-observer-log.md`.
- **Framework** (reliability spine + FM catalog): `src/rnd/2026.06.01-dependable-coverage-campaign-framework.md` (§8 FM-1..22-held; §13 WAVE-3 prerequisites).
- **Prior post-game** (shape mirrored here): `src/rnd/2026.06.02-cosa-coverage-campaign-post-game.md`.
- **Open bug doc**: `src/rnd/2026.06.02-empty-broadcast-injection-bug.md` (updated this session with the intermittency clue).
- **Keep-alive fix design**: `src/rnd/2026.06.02-stop-hook-natural-heartbeat-poker.md`.

---

## TL;DR

The grind hit its target **cleanly and honestly**: a reviewer-audited **100% unit tier** — 13,300 passed / 0 failed / 2 xfailed; cosa 100% line+branch+function across **412 files**; Krishna 8/8 review batches, 0 hollow; 11 test-only commits — since **pushed to origin** (Lupin HEAD `da4d296`, 0 ahead — `[verified-María]` + Tiberius git-verified; my memento's "held for push" was stale). The headline positive: **discipline held end-to-end under an unsupervised overnight run** — green-before-commit, verify-before-catalog, blast-radius gating, no fake-green, and both prod bugs deferred to Rick's gate rather than fixed at 4am. The ratification was self-verified to bytes (both xfails git-confirmed pre-existing, one strict → **zero mask**). The headline negative: **keep-alive fragility** — the run survived on manual push-DM pokes every ~10 min because the agentic poker could not launch (`owner_id` unresolvable while Rick was AFK). The three improvements, in order: **build the per-instance heartbeat poker + pre-resolve `owner_id` before AFK (P0)**, **finish fixing the coordination plane including the now-confirmed-intermittent empty-broadcast bug (P1)**, and **land the global hermetic-config fixture to systemically kill FM-21 (P2)**.

---

## 1. What Went Well (preserve these)

1. **The win is real and honest.** Honest, reviewer-audited 100% unit tier — not a masked one. The io_files_router red was evidenced non-repro and was **not** skip/xfail-masked; no production logic was touched; both xfails were git-verified pre-existing (one `strict=True` ⇒ cannot mask). Coverage + reviewer-audit + xfail-honesty all independently confirmed. (Ratification ledger: observer log Wake 4b, `[verified-María]` via own `git`+`grep`.)
2. **Discipline held end-to-end under an unsupervised overnight run.** The framework's two unbreakable rules held all night with no human in the loop: **green-before-commit** (red tree → commits HELD until green, every batch gated) and **never edit production logic** (both prod bugs → findings doc for Rick's gate, NOT fixed at 4am). 11 commits banked, full gate green between batches — all now on origin (`da4d296`, 0 ahead/0 behind).
3. **The framework named its own first-of-night failure-mode instead of whack-a-mole.** FM-21 (non-hermetic config/registry test bleed) surfaced under parallel multi-author authoring, was verified to threshold (≥2 recurrences) before cataloging, and got a sequenced blast-radius-gated fix path rather than ad-hoc patching.
4. **Verify-before-catalog and no-confab held under pressure.** The slippery io_files_router polluter was NOT minted as FM-22 — it was a single observation, self-healed across 3 green runs (2 fresh hashseeds), mechanism never captured. Naming an un-pinned mechanism would be mild confabulation; it was logged as a held-candidate pointer, promotable only on recurrence with a captured `--tb=long`. (FM-22: NAME HELD.)
5. **Harvest-over-authoring worked.** io_models hit 100% via pure git-mv relocation (zero new lines) — the "already-implemented-elsewhere" recovery pattern Rick named, applied correctly.
6. **Blast-radius gating prevented a 4am unilateral landing.** The global autouse hermetic-config fixture (the systemic FM-21 fix) was correctly NOT landed unilaterally overnight — an autouse fixture runs for every test, so slightly-off reset logic would red the whole tree across all 5 authors at once. That irreversible-in-the-moment shared-infra blast radius was deferred to a clean gate. Tiberius's instinct not to land it was confirmed correct.

## 2. What Did Not Go Well

1. **Keep-alive fragility — the #1 operational fault.** There is no per-instance heartbeat poker. The agentic poker could not launch because `owner_id` was unresolvable while Rick was AFK (no-confab: it was never started, not "started and failed"). The marathon survived only on **manual push-DM pokes from Tiberius every ~10 minutes**, with María replying to reset and logging a ledger row each wake. Fragile by construction — a single missed poke chain could have silently stalled the fleet for hours.
2. **Coordination-plane fragility (carried from WAVE-2, partially fixed).** Two distinct faults:
   - **Empty-broadcast-injection bug — NEW CLUE: it is INTERMITTENT.** Broadcast `c1d94d9e` carried full content where two earlier broadcasts arrived blank (fleet-wide directive loss on AFK hand-off). Intermittency narrows the fix-space toward a race/timing fault rather than a deterministic drop. Folded into the open bug doc this session.
   - **Messaging black-holes (FM-7/11/15/18)** remain WAVE-3 prerequisites — the `:7999`-coupled synchronous-handler saturation + no durable pull-able inbox. The async-handler + AFK-inbox fix (ratified D1 last session) is Tiberius's Lupin-side build, not yet confirmed landed.
3. **Test-hermeticity is the dominant *technical* friction.** FM-21 (config/registry/`sys.modules` bleed without hermetic teardown) recurred under parallel authoring and was fixed at-source per-polluter to keep the tree green and the grind moving — the systemic fix (global fixture) is still pending Rick's gate. This is mechanical drag that recurs every parallel run until the systemic fixture lands.
4. **Two production bugs surfaced and remain open (correctly, for Rick's gate).** `dispatcher.py self.debug` uninitialized (tripwire-pinned) and Bug #12 `cosa_interface.ask_yes_no` → missing `_dispatcher.ask_yes_no` on `AgentNotificationDispatcher` (strict-pinned 2026-06-01, self-announcing the instant the method is added). Neither fixed overnight — prod-logic is Rick's call — but both are live debt awaiting the morning gate.

## 3. Proposed Improvements (descending priority)

### Headline (descending)

**P0 — Per-instance Stop-hook heartbeat poker + `owner_id` pre-resolution** *(kills the #1 operational fault)*
- Build the per-instance natural-heartbeat poker (R&D drafted in `2026.06.02-stop-hook-natural-heartbeat-poker.md` — now top-priority, no longer "deferred").
- **Pre-resolve `owner_id` BEFORE Rick goes AFK** so the agentic poker *can* launch overnight — the precondition that blocked it last night.
- Removes the dependency on a human (or a manually-driven manager) issuing keep-alive pokes every ~10 min for an unsupervised marathon.
- **Owners**: Tiberius → Lupin-side hook build + `owner_id` resolution. María → framework §13 codification of the keep-alive prerequisite.

**P1 — Finish fixing the coordination plane** *(carried P0 from WAVE-2 + the new intermittency clue)*
- **Empty-broadcast-injection bug**: now known INTERMITTENT (full vs blank across consecutive broadcasts) → chase a race/timing fault, not a deterministic drop. Bug doc updated.
- Land the WAVE-2-ratified D1 fixes if not already landed: async/non-blocking `:7999` handlers + durable pull-able AFK inbox + load-isolation. Closes FM-7/11/15/18.
- **Owners**: Tiberius → Lupin-infra build + bug root-cause. María → framework §13 carry.

**P2 — Land the global hermetic-config fixture (blast-radius-gated)** *(systemically kills FM-21)*
- The global autouse hermetic-config fixture: Rachel designs + isolation-verifies (full suite green with it active, zero currently-green tests broken); lands only on a clean verification, instant-revert staged; else defers — exactly the gate that held overnight.
- Replaces per-polluter at-source fixes (mechanical drag every parallel run) with one systemic guard.
- **Owners**: Tiberius/Rachel → Lupin-side fixture + verification. María → framework §8 FM-21 mitigation note.

### Lower tier (captured, not ranked against the headline)

- **`:8000` integration tier** — the next coverage frontier now that the unit tier is certified. Scoping + assignment is a fresh dev-task series, not part of this retro's fix-space.
- **FM-22 promotion discipline** — held-candidate; promote ONLY on a recurrence with a captured `--tb=long`. Do not mint on the single self-healed observation.
- **Stale `CLAUDE.md §STRUCTURE`** — documentation drift surfaced overnight; routine fix at Rick's convenience.
- **Automated global LoC aggregator** — classifier-blocked this session (distrusts the commons-message lift); delivered PIP+Lupin manually. Full all-registered-repos sweep needs a Bash permission rule for `run_git_loc_delta_global` OR a fresh typed user go.
- **Fleet reap — CLOSED (flag was a false alarm).** The lingering-Rachel flag (`7bca7a96` active 15:07Z post-reap-memento) is **resolved**: Rick confirmed (2026-06-03 walkthrough PG-D6) the unused workers were already reaped and **Rachel was summoned by Rick himself for ad-hoc work** — a legitimate live session, not a stray re-spawn. No reap action needed. `[verified-Rick]`

---

## 3.6 Ratification Set — PREPARED, HELD for Rick's return (2026-06-03)

**Status**: 🟡 **STAGED, not yet run.** Rick directed (via `ask_multiple_choice` answer 15:31Z): *"Prepare to run the decision walkthrough but hold until I return."* The set below is framed and ready; run one-at-a-time via `ask_multiple_choice` (`/plan-decide`) when he returns. Each carries options + pros/cons + a recommended choice (recommended first). Descending priority. Rulings land here as `→ RULING` lines, then mirror to the Decisions Log.

### Headline (the 3-list)

**PG-D1 — Improvement priority order.** Confirm **P0** heartbeat-poker / **P1** coordination-plane / **P2** hermetic-fixture, or reorder?
- **(A) Confirm P0/P1/P2 as drafted (Recommended).** Pro: keep-alive was the night's single highest-impact operational fault — an unsupervised run that survives only on manual pokes is the thing most likely to silently fail next time. Con: coordination-plane (P1) is the longer-standing carry. *Why rec:* keep-alive is the precondition for *any* future unattended run.
- (B) Swap to P1/P0/P2 — coordination plane first. Pro: it's the older debt with a ratified fix already designed. Con: a perfect message plane still stalls if instances go idle with no poker.
- (C) Other ordering.
- **→ RULING (2026-06-03, Rick): (A) — order CONFIRMED P0 heartbeat-poker / P1 coordination-plane / P2 hermetic-fixture.**

**PG-D2 — P0 heartbeat-poker: greenlight the build + `owner_id` pre-resolution?**
- **(A) Greenlight the build now + pre-resolve `owner_id` before the next AFK window (Recommended).** Pro: directly kills the #1 fault; design is already drafted. Con: net-new hook surface to validate. *Why rec:* the precondition (`owner_id` unresolvable) is exactly what blocked the agentic poker last night.
- (B) Design-review first, build after a cast review. Pro: more scrutiny. Con: delays the fix another cycle.
- (C) Defer.
- **→ RULING (2026-06-03, Rick): (A) YES build it** — but the agentic-poker *design* is a **separate dedicated conversation** (Rick wants to dig into what the agentic poker looks like). `owner_id` clarified live (see note). Follow-up filed: "Agentic heartbeat-poker design deep-dive" → TODO.
- **`owner_id` clarification (grounded in `2026.05.30-cosa-coverage-campaign-observer-log.md` F2):** an agentic poker is a scheduled job that nudges idle sessions; submitting/running it requires a valid `owner_id` — the authenticated identity the job acts on behalf of (permissions + notification routing). `owner_id` is a `get_session_info()` field; primary (Rick-authenticated) sessions carry it, spawned worker/observer sessions often have it **ABSENT** (F2: María's observer session had no `owner_id` → couldn't self-submit a poker). While Rick is AFK/asleep there is no live authenticated owner to resolve → the poker can't launch. **"Pre-resolve `owner_id` before AFK"** = while Rick is still present, capture + pin that owner identity so the poker job can be submitted under a valid owner and run unattended overnight.

**PG-D3 — P1 coordination plane: prioritize the now-intermittent empty-broadcast bug, and confirm D1 (async handlers + AFK inbox) status?**
- **(A) Prioritize the empty-broadcast bug fix now + confirm whether last session's D1 async/AFK-inbox fix landed (Recommended).** Pro: intermittency narrows it to a race — a captured repro now is high-value; fleet-wide directive loss is severe. Con: intermittent bugs are slippery to pin. *Why rec:* every blank broadcast silently drops a fleet directive.
- (B) Bundle into the broader plane rework, no separate priority. Pro: one coherent effort. Con: the bug keeps biting meanwhile.
- (C) Defer to the next coverage cycle.
- **→ RULING (2026-06-03, Rick): (A) — prioritize the empty-broadcast bug fix now + confirm last session's D1 (async handlers + AFK inbox) actually landed.**

**PG-D4 — P2 hermetic-config fixture: authorize Rachel to design + isolation-verify now (blast-radius-gated land)?**
- **(A) Authorize design + isolation-verify now; land ONLY on a clean full-suite gate, revert staged; else hold (Recommended).** Pro: systemically kills FM-21's per-run drag; the gate is exactly the overnight discipline. Con: autouse fixture = whole-tree blast radius if reset logic is off. *Why rec:* preserves the blast-radius guard while making forward progress.
- (B) Hold entirely for a dedicated window.
- (C) Keep per-polluter at-source fixes only (accept the recurring drag).
- **→ RULING (2026-06-03, Rick): (A) — Rachel authorized to design + isolation-verify the global autouse fixture; land ONLY on a clean full-suite gate (revert staged); else hold. Blast-radius guard preserved.**

### Lower tier (morning-gate + hygiene)

**PG-D5 — `:8000` integration tier: greenlight as the next coverage frontier?**
- **(A) Greenlight; scope + assign after the post-game closes (Recommended).** Pro: natural next frontier, unit tier certified. Con: needs a fleet → gated on the coordination-plane/keep-alive fixes (don't re-create FM-7). (B) Greenlight + start immediately. (C) Hold.
- **→ RULING (2026-06-03, Rick): (A) — `:8000` integration tier GREENLIT as the next coverage frontier; scope + assign after the post-game closes, sequenced behind the P0/P1 plane fixes.**

**PG-D6 — Fleet reap: the lingering Rachel `7bca7a96` (active 15:07Z post-reap-memento).**
- **(A) Liveness-sweep + reap dead/lingering, hard-protect any live session (Recommended).** Pro: closes the reap cleanly (P2-harvest discipline). Con: must not kill a live re-spawn. (B) Hold all. (C) Investigate before reaping.
- **→ RULING (2026-06-03, Rick): NO ACTION — unused workers already reaped; Rachel `7bca7a96` is Rick-summoned (ad-hoc work), a legitimate live session, NOT a stray. The earlier "lingering" flag is a false alarm, cleared.**

**PG-D7 — The 2 pinned prod bugs (`dispatcher.self.debug` + Bug #12 `cosa_interface`).**
- **(A) Authorize the prod-fix + de-arm tripwires now (Tiberius/Rachel, Lupin-side) (Recommended).** Pro: both are honestly tripwired; correct behavior known (Bug #12 self-announces). Con: prod-logic change needs care. (B) Schedule a dedicated prod-fix window. (C) Hold.
- **→ RULING (2026-06-03, Rick): (A) — fix BOTH prod bugs + de-arm tripwires now (Tiberius/Rachel, Lupin-side); re-verify green. Bug #12's strict-xfail self-verifies on fix.**

**PG-D8 — Stale `CLAUDE.md §STRUCTURE` (Lupin).** (A) Quick fix now (Recommended) / (B) Backlog.
- **Verified drift (María, live tree-check 2026-06-03):** `src/cosa/app/` listed but gone; only 5 of ~15 real `src/cosa/*` subdirs named (missing `config/ orchestration/ repo/ rest/ tests/ training/ io/ crud_for_dataframes/ docs/ rnd/`); `src/cosa/rest/routers/` misfiled under the `fastapi_app` bullet.
- **→ RULING (2026-06-03, Rick): (A) fix now — PROVISO: keep a MIRRORED COPY inside María's repo (PIP).** Plan: María authors the corrected `§PROJECT STRUCTURE` listing from the real tree; the live Lupin `CLAUDE.md` edit is coordinated with Tiberius (his lane) + logged as an `[lupin, out-of-repo]` manifest entry; the corrected listing is mirrored into a PIP-tracked file for the record.

**PG-D9 — Automated global LoC aggregator.** (A) Grant a Bash permission rule for `run_git_loc_delta_global` (Recommended — unblocks the all-repos sweep) / (B) Keep manual / (C) Fresh typed go each run.
- **→ RULING (2026-06-03, Rick): (A) — grant a standing Bash permission rule for `run_git_loc_delta_global`. María to propose the exact `settings.json` allowlist entry for Rick's approval before adding.**

---

### Ratification summary (2026-06-03, all 9 closed)

| # | Decision | Ruling |
|---|---|---|
| PG-D1 | Priority order | **CONFIRMED** P0 poker / P1 coordination / P2 hermeticity |
| PG-D2 | Heartbeat-poker | **BUILD IT** (A); agentic-poker *design* = separate conversation; `owner_id` clarified |
| PG-D3 | Coordination plane | **PRIORITIZE** empty-broadcast bug + confirm D1 landed (A) |
| PG-D4 | Hermetic fixture | **AUTHORIZE** Rachel design+verify, gated land (A) |
| PG-D5 | `:8000` tier | **GREENLIT**, scope after post-game (A) |
| PG-D6 | Fleet reap | **NO ACTION** — already reaped; Rachel is Rick-summoned (flag cleared) |
| PG-D7 | 2 prod bugs | **FIX + DE-ARM now** (A), Lupin-side |
| PG-D8 | Stale §STRUCTURE | **FIX now + mirror copy in PIP** (A) |
| PG-D9 | LoC aggregator | **GRANT** permission rule (A); María proposes exact entry |

---

## 4. Division of Labor (per DM `7e54d9dd`/`8d677d2a`)

| Track | Owner | Deliverable |
|---|---|---|
| Framework synthesis (this doc) + §13 fold (keep-alive P0 + coordination P1 + hermeticity P2) + FM-21/FM-22 §8 | María 🌸 (Steward) | PIP `src/rnd/` + `dependable-coverage-campaign-framework.md` edits |
| Lupin-infra root-cause companion (`:7999`/MCP, async-handler + AFK-inbox status, heartbeat-poker build, `owner_id` resolution) | Tiberius 👑 (Manager) | Lupin `src/rnd/` companion |
| 2 prod-bug fixes (dispatcher `self.debug` + Bug #12) + global hermetic-config fixture | Tiberius 👑 / Rachel 🕊️ | Lupin-side, gated on Rick |

---

## 5. Open Items Before This Closes

- [x] **Split confirmed with Tiberius** (DM `7e54d9dd`/`8d677d2a`): María = PIP framework synthesis; Tiberius = Lupin-infra companion. Cross-linked.
- [ ] **Reconcile numbers + cross-link** his Lupin companion once he publishes it (he starts on Rick's live nod).
- [ ] **Fold the synthesis into framework §13** — keep-alive prerequisite (P0), coordination-plane carry (P1), FM-21 systemic-fixture note (P2).
- [x] **Empty-broadcast bug doc** — intermittency clue added this session.
- [ ] **Present the 3-list summary to Rick** via guided decision walkthrough when he's ready to ratify.
- [ ] README link to this doc.

---

*Version history: v0.1 (2026-06-03, María) — steward synthesis draft from the Session 100 observer log + ratification ledger + memento v3 §4 synthesis. Status DRAFT pending Tiberius reconciliation.*
