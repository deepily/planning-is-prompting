# Holding-Area Triage — María's Blind Ranking (2026-09-11)

> ## ✅ AGREED CONSENSUS — María 🌸 + Mr. Radio 🦉, DM thread `e87c9a46`, 12:20–12:21 EDT
>
> Swapped blind lists (Mr. Radio's: his session scratchpad `2026.09.11-holding-triage-mr-radio-BLIND.md`). **Both lists named 7 of the same rows**; the rest were settled on measurements.
>
> | | Rows |
> |---|---|
> | **PROMOTE (6)** | `698a5aaf` self_respin fails silently · `11793820` mux re-renders every card · `6ce9f4a1` legacy XSS · `6bf8a819` red DOM-assert ratchet · `d90baf3d` notification-history authz · `d04ff119` mux focus-mode parity |
> | **DROP (6)** | `9f6d05a7` branch doc_path close (chloe/acct María) · `95843262` solution_embedding hazard (maya/acct Mr. Radio) · `0534f9da` stranded replay docs (pocholo/acct Mr. Radio) · `8a784c50` include_terminal (Mr. Radio) · `456f694c` negative-assertion sweep (María) · `7350b450` gsm8k command (María) |
> | **CLOSE (5, not counted)** | `82b0a96b` duplicate · `8c29d8c2` landed (`d08d3635`, `ab5c9236` are in HEAD `0cac7d44`; test file tracked — verified by María 12:20) · `b4281428` superseded (`MANAGER_ATTESTATION_KEY` in `CLOSING_RECEIPT_KEYS`, `task_store_rules.py:196`) · `360de81b` merged `d1728048` · `4311c503` merged `e57bcffc` |
>
> **What changed from the blind list below**: `8c29d8c2` promote → close (already landed; I missed it). `b4281428` drop → close (superseded counts as finished). I conceded `f069fcda` (it stays in holding); Mr. Radio conceded `9ab0bddb` (stays) and `ad7bd01f` (first alternate). New drops: `456f694c` and `7350b450`, which were both on both our lists. New promote: `d90baf3d`.
>
> ⚠️ **`7350b450` caveat**: dropping it leaves `lupin/CLAUDE.md:9` pointing at a command that does not run (re-checked 12:17). If you'd rather keep the command honest, deleting that line is a one-line change and nobody has to promote a row for it.

**Author**: María 🌸 (session 31d4590e) · **Order**: operator broadcast `d8cec65a`, skeleton crew until 17:00
**Status**: my half only, written before reading Mr. Radio's list (skeleton-crew rule 4). **Not the agreed output.**

**Population**: `task_query( status="not_approved" )` at ~12:17 EDT → **30 rows**, every one opened with `task_get`.
**Method** (kept from 09-09): promote what unblocks the most or hurts the operator's daily use, freshest evidence second. Drop only a row whose premise is dead, subsumed, or too low-value to ever reach. A finished or duplicate row is a **close**, not a drop (Sword of Damocles §5 ruling 1).

---

## 1. PROMOTE (6)

| # | Row | Pri | Owner | Why now |
|---|---|---|---|---|
| 1 | `8c29d8c2` PermissionRequest denies on a DM-only buffer and says "The user said this" | P5 | sam / acct María | Hits **every seat**: a peer DM or context tick falsely denies a permission prompt and blames the operator. Design agreed (Sam, DM 7ebace66), branch exists. P5 is a worker-filing cap, not severity |
| 2 | `698a5aaf` self_respin reports "scheduled", no /clear lands | P2 | María | Five instances across 4 stages (09-06 → 09-10). The 15-minute context rule relies on this verb, and it fails silently. First step is one file read (the fire-token consumer) |
| 3 | `11793820` Multiplexer re-renders every sender card twice per message | P2 | María | Cost grows with each card (5 → 20 cards: 551 → 2185 ms). Operator has 3,233 senders in 48 h. A reason he won't use the multiplexer. Benchmark tool preserved |
| 4 | `d04ff119` Multiplexer focus mode: 3 legacy-parity gaps | P2 | María | His standing rule: "fixed" means both clients. Small, file:line already mapped |
| 5 | `6ce9f4a1` Legacy notifications.js puts sender text into innerHTML unescaped | P1 | Mr. Radio | Could run injected script, and no CSP is set anywhere. The legacy page is the client he uses today |
| 6 | `6bf8a819` DOM-assert ratchet red: 5 violations in finished_tasks_renderer.test.ts | P2 | Mr. Radio | **Re-measured today**: `scan_text` still returns 5 (lines 144, 146, 147, 585, 587). It stays red on every unit run until fixed; a cheap test-side fix |

**Next in line if one is refused**: `d90baf3d` (notification-history authz), `158a9865` (5 SSE smoke tests cannot fail), `ad7bd01f` (stale approver docstring — still at `task_approval_settings.py:634`, re-checked today).

## 2. DROP (6)

| # | Row | Pri | Owner | Why it can go |
|---|---|---|---|---|
| 1 | `b4281428` A seat can only close a row by citing a commit | P1 | Mr. Radio | **Premise superseded**: `manager_attestation` now closes non-commit work (operator ruling 2026-09-10, row `adaf7698`), and the `task_transition` docstring was corrected to name every key. A worker asks its manager to close |
| 2 | `9f6d05a7` A worker on a branch can only close on `commit` | P5 | chloe / acct María | Same family as #1, and resolved by the same ruling. The author rated it friction only: "Nothing is blocked" |
| 3 | `8a784c50` include_terminal also un-suppresses the holding area | P2 | Mr. Radio | **The code already answers it**: `task-list-query.js:55-68` and `test_task_row_state_controls.py:932-945` document `status=not_approved` as the intended door and say a new flag "would have been the wrong fix" |
| 4 | `95843262` solution_embedding scores 100.00 on different answers | P5 | maya / acct Mr. Radio | The row itself calls it a prospective hazard: it only affects an admin browse view and is not on the replay path. No affected user |
| 5 | `0534f9da` Commit 5681ca10 (3 replay-threshold docs) on neither wip nor main | P5 | pocholo / acct Mr. Radio | Research docs only, still safe on `origin/maya-threshold-measure` (re-checked today). Mr. Radio ruled "not ours to merge" |
| 6 | `f069fcda` Notifications record no origin the sender cannot omit | P3 | Mr. Radio | The row says it itself: "NOT urgent. No live application path is affected." The 740 rows it concerns can't be recovered either way |

**Alternates**: `456f694c` (its one instance was fixed in `4a71d3f7`, and the rest is an unscoped sweep) · `7350b450` (gsm8k command, still broken today, P5) · `80513825` (single-UID file layer on a one-user dev box).

## 3. CLOSES — not counted as drops (Sword §5 ruling 1)

| Row | State | Recommended |
|---|---|---|
| `82b0a96b` Markdown-tables design | Duplicate of the operator's own `5ae3ce90` (Mr. Radio amendment, 21:04) | Close as duplicate |
| `360de81b` Multiplexer Action Required: one card at a time + stepper | Merged `d1728048`, built, baseline `2cb539c4`. All seat-side work done | Approve + close on commit; the operator's landing check is the last step |
| `4311c503` Multiplexer answer shape | Step 1 `f2555c9d`, step 2 `e57bcffc` both merged (re-checked in git log today) | Close on `e57bcffc` after the same landing check |

## 4. Kept in holding, not ranked either way

`2d786391` · `73fdc7ef` · `966b46ba` · `6444feeb` · `8d043758` · `4163a015` (carries `b52b430b`'s folded hazard, so don't drop it) · `158a9865` · `9ab0bddb` · `b6cfbf8d` (the operator's own design idea) · `47f33bba` (operator ruled (a) on 09-06; still unbuilt, since `memento_io.py:1420` still injects `**Written**`) · `d90baf3d` · `ad7bd01f` · `456f694c` · `7350b450` · `80513825`.

---

## 5. Update — re-rank under the operator's lens (2026-09-11, after broadcast `0c97cb37`)

**What happened since the consensus above**
- **The 5 closes are done** (operator: "Yes, managers close them"). Closed by María at 12:26 EDT, each with a manager attestation, plus a commit where one exists: `82b0a96b` · `8c29d8c2` (commit `d08d3635`) · `b4281428` · `360de81b` (commit `d1728048`) · `4311c503` (commit `e57bcffc`). **The holding area is now 25 rows.**
- **The operator reopened the list** at 12:25: *"review your list with the lens of ensuring that the most impactful feature related and utility related bugs or requests for enhancement get finished first."*

**How I applied the lens**: a row ranks high when it changes something the operator or the fleet **uses every day** — a feature, a client, or a tool. Test hygiene, coverage, receipt plumbing and security-in-principle on a one-user box rank lower. They're still real, but they don't finish a feature.

### 5a. María's independent re-rank (written before reading Mr. Radio's)

| | Row | Kind | Why, under the lens |
|---|---|---|---|
| **PROMOTE 1** | `11793820` Multiplexer re-renders every sender card per message | feature bug | Cost grows with each card on the client you get up to 20,000 notifications a day on |
| **PROMOTE 2** | `d04ff119` Multiplexer focus mode: unread badge, click, persistence | feature parity | Your "fixed means both clients" rule; each of the three gaps is visible every time you use focus mode |
| **PROMOTE 3** | `b6cfbf8d` Deep research: confirm topics before the research runs | **enhancement (your design)** | Your own addition: summarize the local doc, tick the topics, research only those. The park/resume rails already exist (`flow.py:1364`, `:679`) |
| **PROMOTE 4** | `698a5aaf` self_respin says "scheduled" and nothing clears | utility bug | The context rule depends on this verb; 5 silent failures across 4 stages |
| **PROMOTE 5** | `6ce9f4a1` Legacy page renders sender text as HTML | feature bug | The client you use today can run injected script, and no CSP is set |
| **PROMOTE 6** | `47f33bba` Amended mementos show a stale identity block first | utility bug | You already ruled option (a) on 09-06 and it's still unbuilt (`memento_io.py:1420`). Every handoff reader sees the stale block first. Small, and in this repo |

| | Row | Why it goes |
|---|---|---|
| **DROP 1** | `9f6d05a7` Branch doc_path close | Receipt plumbing; the manager close now covers it |
| **DROP 2** | `8a784c50` include_terminal flag | The code documents the `status=not_approved` door as deliberate |
| **DROP 3** | `456f694c` Negative-assertion sweep | Test meta-work; its one instance is fixed (`4a71d3f7`) |
| **DROP 4** | `95843262` solution_embedding hazard | Admin browse view only; no user affected |
| **DROP 5** | `0534f9da` Replay docs stranded on a branch | Docs, safe on origin; no feature change |
| **DROP 6** | `966b46ba` task_promotion_gate at 97% coverage | Two partial exits and one line on one module. Nothing a user sees changes |

**Changes vs the 12:21 consensus**: promote `d90baf3d` → `b6cfbf8d` (a feature request beats authz on a one-user box) and `6bf8a819` → `47f33bba` (a ruled utility fix beats a red lint on test code). Drop `7350b450` → `966b46ba` (the broken gsm8k command is at least a utility; coverage on one module isn't).

**Alternates**: promote `6bf8a819`, `d90baf3d`, `9ab0bddb` · drop `7350b450`, `8d043758`, `f069fcda`.

### 5b. ✅ FINAL CONSENSUS under the lens — María 🌸 + Mr. Radio 🦉, DM thread `e87c9a46`, 12:29 EDT

Both re-ranks were written blind (Mr. Radio's: his session scratchpad `2026.09.11-holding-triage-mr-radio-RERANK-feature-utility.md`). **They named 5 of the same 6 promotes and 5 of the same 6 drops.** One difference on each side, both settled between us:

| Difference | Outcome | Why |
|---|---|---|
| 6th promote: `6ce9f4a1` (María) vs `6bf8a819` (Mr. Radio) | **`6ce9f4a1`** | The concessions crossed: María gave up `6ce9f4a1` at 12:27 and Mr. Radio gave up `6bf8a819` at 12:28, and he caught it before the card went out. Settled on the lens. `6ce9f4a1` is a bug in the page you use every day (sender text rendered as HTML, no CSP). `6bf8a819` is test hygiene, and features merged all week with that red (`d1728048` on 09-10), so fixing it clears the unit run but doesn't unblock merges. **`6bf8a819` is first alternate**, kept in holding |
| 6th drop: `966b46ba` (María) vs `7350b450` (Mr. Radio) | **Neither → `f069fcda`** — Mr. Radio's compromise | On María's first list. It's a spec that calls itself "NOT urgent, no live application path", and not a feature. Dropping it leaves nothing broken, unlike `7350b450`, which would leave `lupin/CLAUDE.md:9` pointing at a dead command. Both `7350b450` and `966b46ba` stay in holding |

**PROMOTE — in finish-first order** (each paired with the drop beside it, Sword of Damocles)

| # | Promote | Kind | Paired drop | Drop's owner |
|---|---|---|---|---|
| 1 | `11793820` Multiplexer re-renders every sender card per message | feature bug | `9f6d05a7` branch doc_path close | chloe / acct María |
| 2 | `d04ff119` Multiplexer focus mode: unread badge, click, persistence | feature parity | `8a784c50` include_terminal flag | Mr. Radio |
| 3 | `b6cfbf8d` Deep research: confirm topics before the run | enhancement (your design) | `456f694c` negative-assertion sweep | María |
| 4 | `698a5aaf` self_respin says "scheduled" and nothing clears | utility bug | `95843262` solution_embedding hazard | maya / acct Mr. Radio |
| 5 | `47f33bba` Amended mementos show a stale identity block first (you ruled (a) 09-06) | utility bug | `0534f9da` replay docs on a branch | pocholo / acct Mr. Radio |
| 6 | `6ce9f4a1` Legacy page puts sender text into HTML unescaped | feature bug | `f069fcda` notification origin columns | Mr. Radio |

**Back in holding, not dropped**: `6bf8a819` (DOM-assert ratchet, first alternate) and `d90baf3d` (notification-history authz); `7350b450` and `966b46ba`, the two drops we swapped out.

**Keypresses are yours**: 6 approves and 6 drops.

> **🔨 RULED — "Approve all 12 (Recommended)"**, operator by keypress on the ask_multiple_choice card, 2026-09-11 ~12:31 EDT (`answered: true`, `default_used: false`). The 12 store clicks (approve ×6 in finish-first order, drop ×6) stay with the operator: an API-key manager is refused 403 on admission and on drop.
