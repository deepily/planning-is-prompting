# TODO

Last updated: 2026-05-19 (Session 93 — §10.14 redline of cascade workflow docs, María)

## Pending

- [ ] **Lupin/cosa-voice feature — Mute-Channel Bypass for Manager-Escalation** (Session 93, María — Run-3 §10.14 errata Item C, Rick's catch). When the user mutes a critical-path persona (manager) for chatter relief, the manager's `urgent`-priority `notify()` escalations should bypass the mute via a separate channel. Run-3 evidence: Rick muted Tiberius; Tiberius's urgent escalation request waited 14 min for time-out because `priority="urgent"` didn't bypass mute. Three design candidates: (a) default rule never mute manager (workflow-level fix; doesn't need MCP change); (b) priority-threshold mute-bypass via separate channel (cosa-voice MCP change — `priority="urgent"` triggers a separate auditory channel that mute doesn't suppress); (c) tool feature request — new `bypass_user_mute=True` flag on `notify()`. Owner: Tiberius or next Lupin session. Out-of-scope for PIP-side cascade doctrine; filed here as cross-reference because it surfaced as a Run-3 cascade observation. Refs: `pipeline-summary-20260519` commons topic; `workflow/plan-review-cascaded.md` Version History 2026-05-19 Run-3 doctrine fold entry (named as out-of-scope item).

- [ ] **Lupin-side bug — `commons_send_to` recipient pool-key vs display-name routing gap** (Session 93, María — surfaced by Tiberius DM 2026-05-19T17:28). `commons_send_to(recipient="María", ...)` returns `recipient_resolution_error` because the persona pool key is lowercase no-diacritic ("maria"), but display-name variants ("María", "MARIA", "Maria") are what callers naturally write. Tiberius's workaround for now: lowercase + strip diacritics before passing to `recipient`. Proper fix: the resolver should accept any case + diacritic variant and normalize to the pool key before lookup. Owner: Tiberius or next Lupin session. Filed as Lupin bug. Same root-cause family as the topic-name case-fragmentation bug already on this list (Session 90); consider a single resolver-normalization pass that fixes both.

- [ ] **Phase D: Cascaded plan-review pipeline prototype run** (Session 91, María — design doc at `src/rnd/2026.05.17-cascaded-plan-review-pipeline.md` §6 Phase D, tasks #24-#28 in the TodoWrite list). Five sub-tasks: (1) pick a real ≥2-section input plan to use as prototype input (avoid the cascaded-pipeline plan itself — dogfooding loop risk; candidates: a recent feature implementation plan from another project, or a synthesized 2-section test plan); (2) define telemetry — intervention count, inter-session message count, wall-clock duration, per-stage timing breakdown; (3) run prototype end-to-end with 5 CC sessions launched in 5 tmux panes, user assigning roles at launch per `persona_casting_strategy = user_assigns_at_launch`; (4) baseline comparison — run serial `/plan-review` on the same input plan with the same telemetry; (5) write findings memo into design doc §10 (new section) — prototype-vs-baseline numbers + surprising behaviors + v2 recommendations. **Why deferred**: needs the user (Mr. Rick) live + 5 CC sessions hot simultaneously; not solo authoring work. Status: Phase A (scaffolding) + Phase B (manager spec) + Phase C (rubric refinements) + Phase E (doctrine integration) all complete. Workflow is invokable now via `/plan-review-cascaded` (dynamically picked up by Claude Code as soon as the wrapper landed).

- [ ] **Cascaded plan-review: Testing Reviewer role-name decision** (Session 91, María). Phase C aligned the third reviewer's rubric with `/plan-review` Pass 2 Ownership-Language Audit (NOT generic test-coverage analysis). Role name "Testing Reviewer" kept as conversational shorthand from the design conversation; rubric is actually the ownership audit. Decision needed: rename role to "Ownership Reviewer" (or similar) across 3 files (`workflow/plan-review-cascaded-personas.md`, `workflow/plan-review-cascaded.md`, `workflow/plan-review-cascaded-defaults.md`) — OR leave the shorthand and let the rubric content carry the meaning. Trade-off: rename improves doctrine clarity but breaks the convenient "testing" mental anchor from the design conversation.

- [ ] **Joint summary doc with María: DM functionality + iterative-correction-loop pattern** (Session 90, Tiberius — per Mr. Rick broadcast `7bff2029-7bb3-4093-941e-adf361ac9b85` 2026-05-16). Mr. Rick noticed in my Session 90 doctrine refresh the line *"the iterative correction loop produced sharper output than either of us would have produced alone, the DM thread as a mini design doc pattern is a viable workflow for cross-session architectural alignment"* and wants this captured as a process artifact. Plan: create a summary document at `src/rnd/2026.05.16-iterative-correction-loop-dm-as-design-doc.md` (or similar) that (a) summarizes the accomplishments made possible by the new DM functionality with concrete examples from today's María ↔ Tiberius arc (the bug-fix-arc, the framework convergence, the instructions-payload review), (b) names the iterative-correction-loop workflow as a reusable pattern, (c) demonstrates the DM-thread-as-mini-design-doc with selected DM excerpts. Then update `README.md` with a pointer to the summary doc. Coordination: María will collaborate post-session-end-ritual; Tiberius standing by post-commit to begin.

- [ ] **Topic-name case-fragmentation bug** (Session 90, Tiberius, discovered live 2026-05-16). `commons_send_to` does `f"dm-{recipient}"` literally, so case-variant recipient names produce separate topic files on disk (e.g. `dm-Tiberius.md` vs `dm-tiberius.md`). Silent message loss between case variants. María's earlier DMs landed in capital-T variant while her later DMs converged to lowercase; my listener was reading capital-T, so 4 DMs from her were silently missed for ~30 minutes mid-session. Fix proposal: lowercase the persona name in the wrapper before formatting the topic name. Owner: María (Lupin-side fix). Filed: she acknowledged via DM 2026-05-16T00:19Z and queued for the post-doc-consensus bug-cleanup batch.

- [ ] **System-reminder payload truncation on push-injection** (Session 90, Tiberius, discovered live 2026-05-16). Long DM bodies (~4000 chars verified) appear truncated in the receiving session's `<system-reminder>` view even when the full body is correctly written to the topic file on disk. The recipient's read of the *system-reminder* preview disagrees with their read of the *file*. Mitigation Claude callers should learn: always `commons_read` the topic to fetch canonical body; don't trust the system-reminder excerpt. Fix proposal: either lift the truncation cap or include a "[truncated — call commons_read for full body]" footer when the cap fires. Owner: María (Lupin-side investigation needed). Filed: surfaced via DM 2026-05-16T00:20Z; queued for the post-doc-consensus bug-cleanup batch.

- [ ] **MCP `instructions` payload expansion follow-up** (Session 90, Tiberius — supporting María). María shipped a draft expansion of the cosa-voice MCP `instructions` payload + per-tool docstring upgrades (R&D doc at `<lupin>/src/rnd/v0.1.7/2026.05.16-mcp-discovery-surface-expansion.md`). My review covered all 5 questions she asked (section flow + tier markers + failure modes + cross-refs + receipt etiquette). Awaiting (a) María's incorporation of my Q1 forward-pointer fix (Speakerphone + VPSA → Startup Protocol dependency), (b) Mr. Rick's review, (c) commit ship. After ship, verify the planning-is-prompting `cross-session-communication.md` cross-references still resolve cleanly.

- [ ] **Lupin-side header pointer for Pass-2 rename** (Session 89, Rachel). PIP renamed Pass 2 from "Adversarial Review (Ownership Language)" → "Ownership-Language Audit" on 2026-05-15 and hard-broke the slash-command flag (`--from=adversarial` → `--from=ownership`, no alias). The originating Lupin artifacts at `<lupin>/src/rnd/v0.1.7/2026.04.23-cj-flow-async-multi-lane/05-adversarial-review-prompt.md` and `06-fitness-review-prompt.md` are marked `[DONE, DO NOT REEXECUTE]` and were preserved verbatim per archival-fidelity rule. Add a header pointer to BOTH Lupin files noting: (a) PIP-canonical workflow is at `workflow/plan-review.md`; (b) PIP renamed Pass 2 → "Ownership-Language Audit" on 2026-05-15; (c) Lupin's original order was Adversarial→Fitness while PIP swapped to Fitness→Ownership-Audit. Do NOT rename the Lupin filenames (preserves history). Owner: Maria or next Lupin session. Filed by Rachel (PIP session `02f528ee`).

- [ ] **Session 88 follow-on — fresh-session dogfood test of Preliminary 0 (Phase A MCP Startup)**. The 5-hook recovery plan from `src/rnd/2026.05.15-tts-brevity-mandate-self-violation-recovery.md` landed in `workflow/session-start.md` as a new "Preliminary 0" section, in `~/.claude/CLAUDE.md` + `global/CLAUDE.md` as Phase A strengthening, and in `workflow/claude-config-global.md`. Greps verify presence; live behavioral validation requires a NEW Claude Code session. Test plan: (a) start a fresh session and verify it calls `get_session_info()` BEFORE first user-facing text, (b) confirm it names its assigned persona in the first acknowledgment (chorus disambiguator), (c) confirm any doc-link emitted lives in `abstract` only (no `(/app/docs?...)` in spoken `message=`), (d) confirm the `doc_scope` envelope is extracted at startup (not re-derived later via manual INI parsing). If any of (a)–(d) fail, file as bug-fix-mode item against the canonical startup doctrine. Filed by Session 88 (Rio, `8ebced94`).

- [ ] **Lupin-side cosa-voice surface drift fix** — the original audit finding from Session 88's opening (before it was redirected into the recovery plan): the `cosa_voice_mcp.py` MCP startup instructions blob (line ~572) calls `speakerphone_on=false` **"phone mode"** with legacy silent semantic ("respond normally; speaking via TTS only happens when YOU explicitly call notify()"), while the per-turn hook rider (`hook_common.py:_speakerphone_reminder_body`, line 1138-1148) and exit reminder (`speakerphone_exit_reminder`, line 1313-1322) both call the same state **"QUIET mode (per-session DND)"** with the OPPOSITE quiet-mode semantic (keep calling `notify()` with `priority='medium'`, `suppress_ding=False`). 2026-05-14 evening rewrite per `<lupin>/src/rnd/v0.1.7/2026.05.14-per-session-dnd-toggle-and-slider-move.md` updated the rider but missed the MCP startup blob. Fix: rewrite `cosa_voice_mcp.py:572` to match the rider's QUIET mode semantic — rename "phone mode" → "QUIET mode (per-session DND)" and swap the silent-semantic bullet for the quiet-mode directive. Owner: Maria / next Lupin session (outside PIP scope). Filed by Session 88 (Rio).

- [ ] **Tier-5 follow-on from Session 85 — update `~/.claude/skills/cosa-voice-notifications/SKILL.md` with Neither semantics**. Source-of-truth `ask_yes_no()` now returns ternary `yes`/`no`/`neither` with optional `[comment: ...]` qualifier on any value; the skill description and body still describe the binary contract. Update the skill's progressive-disclosure body to include the ternary parser pattern, anchor example showing a Neither re-frame, and trigger-rich description hook (`neither escape`, `re-frame`, `ternary`). Skill lives outside the PIP repo; this is a separate commit from Session 85's in-repo work. Reference the canonical hub at `workflow/cosa-voice-integration.md` → "Handling Neither — the Re-Frame Escape Hatch".

- [ ] **Cosa-voice MCP commons tools — audit consumers + update documentation** — After Lupin lands the 5 new commons MCP tools (`commons_post`, `commons_read`, `commons_who`, `commons_ask_sync`, `commons_ask_async`) on the cosa-voice MCP server (Lupin Phase 1 of the inter-session-commons initiative; design at `<lupin>/src/rnd/v0.1.7/2026.05.09-inter-session-commons/`), interrogate the new MCP interface and update documentation in EVERY repo that consumes cosa-voice MCP. Scope includes (non-exhaustive): Lupin (`CLAUDE.md` cosa-voice section, `src/docs/`, `src/lupin_mcp/README.md`), planning-is-prompting (`workflow/cosa-voice-integration.md`, `~/.claude/CLAUDE.md` live + `global/CLAUDE.md`, the cosa-voice-notifications skill at `~/.claude/skills/cosa-voice-notifications/SKILL.md`), and any other Claude-Code-using repo that references the MCP tool catalog. Goal: every consumer-facing reference reflects the expanded tool catalog (post + read + who + ask_sync + ask_async). Filed 2026-05-10 by Lupin session `f9608a41` during commons Phase 1 plan-review REUSE walk (finding 4/12 ratification proviso). **Session 86 progress (2026-05-14)**: PIP-side complete via the new Cross-Session Communication Doctrine — `workflow/cross-session-communication.md` (new canonical doctrine doc), `workflow/cosa-voice-integration.md` (cross-link), `global/CLAUDE.md` + `workflow/claude-config-global.md` (new `## CROSS-SESSION COMMUNICATION` section). Remaining: Lupin-side docs (`<lupin>/CLAUDE.md` cosa-voice section, `<lupin>/src/lupin_mcp/README.md`), and `~/.claude/skills/cosa-voice-notifications/SKILL.md` update — both outside PIP repo.

- [ ] **Update codebase-analysis SKILL.md with venv-selection guidance** - The SKILL.md at `~/.claude/skills/codebase-analysis/SKILL.md` documents `python -m cosa.repo.run_branch_analyzer` invocation but doesn't mention the PyYAML dependency or the cosa venv. Tier-2 verification of session-end Step 6 caught this: system Python raises `ModuleNotFoundError: No module named 'yaml'`; cosa venv at `$LUPIN_ROOT/src/cosa/.venv/bin/python` has yaml 6.0.2. Add a "Python interpreter selection" section to the skill (prefer cosa venv → lupin top-level venv → system python3 fallback). Out of scope for Session 84 since the global skill lives outside the PIP repo and edits there are a separate concern.
- [ ] **Step 6 dogfood test** - Once the next `/plan-session-end` runs, confirm the new Step 6 fires correctly: rich path renders the markdown table, notify() carries headline + abstract, fallback path works when LUPIN_ROOT is unset. If anything breaks, file into bug-fix-queue.md per Test Ownership Mandate.
- [ ] **Filename-parsing artifact in BranchChangeAnalyzer output** - Tier-2 run against PIP showed `"ug-fix-queue.md"` in `files_changed_set` (corrupted from `bug-fix-queue.md`). Likely a leading-character strip in lupin's analyzer's diff parser. Out of scope here (lupin-side bug); flag and forward to lupin's bug-fix-queue when attention is on lupin.
- [ ] **Plan-review dogfood test** - Run `/plan-review` (or the standalone `/plan-review-reuse`) against `src/rnd/2026.04.27-promote-plan-review-pattern-to-pip.md` itself as a Pattern 3 single-doc test. Goal: verify the gate catches any ownership-language drift introduced by the writing process AND surface any shape-fit issues (Pattern 3 doc-set vs. multi-doc Pattern A/B/C calibration). Findings fold back into `workflow/plan-review.md`. **NOTE**: now that the canonical doc is fitness-first (Session 80 restructure), the dogfood run also validates the new ordering rationale §3.
- [ ] **Plan-review fidelity readthrough** (`EXECUTOR: HUMAN` — design-review fidelity check) - Read `workflow/plan-review.md` end-to-end against the source `<lupin>/src/rnd/v0.1.7/2026.04.23-cj-flow-async-multi-lane/{05-,06-}-*.md` prompts. Confirm no signal was lost in the abstraction. The AI cannot validate "this captures the spirit of the original" against its own extraction. **NOTE**: post-Session-80, the readthrough also has to confirm the Fitness-then-Adversarial swap doesn't drop signal that the Lupin Adversarial-then-Fitness ordering carried.
- [ ] **Plan-review Phase 3 (Lupin-side wrapper)** - In a Lupin-rooted session, create `<lupin>/.claude/skills/plan-review/SKILL.md` thin wrapper that auto-discovers docs in target dir, injects Lupin-specific tags (`EXECUTOR: AI/HUMAN`, `:7999`/`:8000` venues), and enforces Q-N decision-anchor format. Out of scope for this PIP-side change set; lives where projects own it.
- [ ] **Lupin source-artifact header pointers** - Add a header note to `<lupin>/src/rnd/v0.1.7/2026.04.23-cj-flow-async-multi-lane/{05-,06-}-*.md` pointing at PIP's canonical `workflow/plan-review.md`. Don't replace with stubs (revisionist); leave as historical instances + cross-reference. Note the Lupin order was Adversarial→Fitness; PIP is Fitness→Adversarial — the header pointer should call this out so future readers don't assume invariance.
- [ ] **Verify Test Ownership Mandate uptake across repos** - Session 78 added the mandate to `~/.claude/CLAUDE.md` / `global/CLAUDE.md` / `workflow/claude-config-global.md`. Start fresh sessions in Lupin and other active repos; make a small code change; confirm Claude's post-change behavior is "write + run tests, report pass/fail table" rather than "please verify this works."
- [ ] **Verify interactive-requirements-elicitation skill activation** - Start a fresh session and use vague phrasing ("I'm thinking about...", "Not sure exactly...") to confirm the skill fires and the compact stub behavior matches the previous inline section.
- [ ] **BUG**: `/plan-history-management` is hardcoded to Planning-is-Prompting project paths - when invoked from Lupin it manages PiP's history.md instead of Lupin's. Slash command needs to be project-aware (use project root detection, not hardcoded paths)
- [ ] Update install wizard (`workflow/installation-wizard.md`) to offer template-based installation for testing workflows (use `workflow/slash-command-templates/` instead of copying PiP commands verbatim)
- [ ] Audit other repos (besides Lupin) for verbatim-copy slash command bug in testing commands
- [ ] Verify testing-development skill activation in new Claude Code session
- [ ] Consider future extractions if CLAUDE.md drifts back toward 40k (candidates: PATH MANAGEMENT ~4.8k, Code Style ~3.9k) — monitor after a few sessions of organic use
- [ ] Add global mandate: all planning documents should assume serialization to the local repo's `src/rnd/` directory by default. Workflow should proactively ask user to confirm this default assumption. Goal: chronologically ordered, semantically titled planning artifacts that are easy to find in each project
- [ ] Verify Session Isolation Rules coverage: confirm the rules removed from `global/CLAUDE.md` (during Session 71 trimming) are fully covered by existing workflow documents (`workflow/session-start.md`, `workflow/bug-fix-mode.md`, `workflow/session-checkpoint.md`, `workflow/session-end.md`). Ensure no gaps in isolation enforcement for concurrent Claude sessions
- [ ] Test new token estimation on Lupin repo history.md after compaction
- [ ] Verify estimate is within 5% of Claude Code's reported count
- [ ] Consider updating get-token-count.sh script if it exists
- [ ] Test all four bug fix mode commands work correctly
- [ ] Update INSTALLATION-GUIDE.md with new command variants (bug-fix-mode split)
- [ ] Consider similar split for other argument-based commands

## Completed (Recent)

- [x] **§10.14 redline of cascade workflow docs — Run-3 doctrine fold (María, Session 93)** — Folded the 12 doctrine candidates from Tiberius's Run-3 §8 pipeline summary (+ 3 Rick-voice catches: Manager Reassignment Latitude, Reassignment Bias Risk Guardrail, Mute-Channel Bypass) into the canonical cascade workflow docs. Edits across 4 files:
  - `workflow/plan-review-cascaded-common.md`: NEW §Reviewer Reassignment with Manager Latitude 5-element doctrine + Bias Risk Guardrail + Rate-Limit failure-mode entry; NEW §Cascade-Learning-Loop Sub-patterns (forward-only-asymmetry + symmetric-application + context-aware-application); expanded `closure_action` enum (3 new values); Manager System Prompt self-audit item 6 (`blocked_waiting_on_user`); 18-min user-attention-block cap in §Escalation Taxonomy
  - `workflow/plan-review-cascaded-personas.md`: Persona 2.A point 14 (doctrine-sweep on revision-mechanism change with 3 sub-patterns); Persona 5 §Stage-3 Cosmetic-Cluster Recognition sub-section
  - `workflow/plan-review-cascaded-defaults.md`: closure_action worked-example table; new commons `kind` enumeration (3 new + 6 pre-existing formalized)
  - `workflow/plan-authoring-cascaded.md` + `workflow/plan-review-cascaded.md`: version-history bumps cross-referencing the canonical edits in common.md
  - `src/rnd/2026.05.17-cascaded-plan-review-pipeline.md`: NEW §10.15 Run-3 actuals (third row of comparison table — 37× count reduction vs 10× predicted, 3-4× better than prediction); §10.11 verdict extended to reflect Run-3 empirical strengthening
  - Cross-repo paired with Tiberius's Lupin-side Phase 6C consolidation track. Mute-Channel Bypass filed as Lupin/cosa-voice feature request (above on this list); CSS-var visible-text safety + ask_multiple_choice Path-B subprocess-restart cost noted as out-of-scope. — Session 93 (María)

- [x] **Per-Repo Preferred-Persona Env Var (`COSA_VOICE_PREFERRED_PERSONA__<PROJECT>`)** — Feature shipped end-to-end. PIP-side (María): plan doc `src/rnd/2026.05.19-cosa-voice-preferred-persona-env-var.md`, workflow integration via new `workflow/session-start.md` Preliminary -1 (env-var path) + Preliminary 0.5 (slash-command swap path), slash-command shim `.claude/commands/plan-session-start.md` v1.0 → v1.1 ($ARGS handling), `workflow/INSTALLATION-GUIDE.md` "Optional: Per-Repo Default Persona via Env Var" subsection. Cross-repo Lupin allocator at Tiberius commit `3bc7b9e` (49/49 tests green). Rollout (plan §9) executed end-to-end: Rick exported `__PLAN=María` + `__LUPIN=Tiberius`, restarted PIP session, María session `ac2d05c0` is the proof point (`voice_persona.name=maria` deterministically on Phase A startup). Path A locked on `/clear` semantics: env var fires on FRESH allocation only; preserved across `/clear`. — Session 93 (María)

- [x] **Cross-Session DM Doctrine + Daily LoC Integration** — Wired María's new `cosa.repo.git_loc_delta` module into `workflow/session-end.md` §6.2 as the new default daily LoC trace (with §6.2.alt for `--rich` branch_analyzer opt-in); refreshed `~/.claude/skills/codebase-analysis/SKILL.md` with Decision Rule + tool routing for "code analytics" intent; major refresh of `workflow/cross-session-communication.md` (2→3 surfaces with full DM mechanics in §1.5, new patterns section §6.5 incl. mermaid-diagrammed cross-session bug-filing flow); added thin `## CROSS-SESSION COMMUNICATION` pointer in `~/.claude/CLAUDE.md` per María's 5-layer doc-architecture framework; filed two upstream bugs in Lupin's queue (cross-repo CSV path + `commons_send_to` FunctionTool), both fixed by María in commit `f4e0370` within 4 min, verified end-to-end via live DM round-trip (`push_mode_active: true` + `dm_dispatched: true`). Plan: `src/rnd/2026.05.16-git-loc-delta-session-end-integration.md` (status 🟢 IMPLEMENTED + §9 upstream-bug-report resolved). Process insight: iterative-correction-loop with peer session via DM thread produced sharper output than either alone (5-layer doc architecture refined jointly; framework convergence across multiple correction cycles). — Session 90 (Tiberius)

- [x] **Plan-Review Pass 2 Rename — drop "Adversarial"** — Eliminated the word "adversarial" from PIP's plan-review nomenclature after Mr. Rick reported session-level confabulation (Pass 2 mis-characterized as security/threat-model review with OWASP-style examples like path traversal, manifest tampering, Unicode bypass). Investigation confirmed canonical doctrine was correct (Pass 2 = ownership-language audit / executor-tagging) but the word "adversarial" pulled sessions into attacker-mindset semantics. Rename: Pass 2 (Adversarial Review (Ownership Language)) → **Pass 2: Ownership-Language Audit**. Doc title (Fitness + Adversarial Gate) → (Fitness + Ownership Gate). Prompt body "hostile outsider" → "forensic auditor". Slash flag hard-break `--from=adversarial` → `--from=ownership` (no alias). NEW top-of-doc disambiguation banner: "NOT A SECURITY REVIEW". NEW §13 anti-pattern row: "Treating Pass 2 as a security/threat-model review (path traversal, manifest tampering, Unicode bypass, attack surface)". Scope: 6 live files patched, 49 occurrences renamed, 0 stale "Pass 2 Adversarial" references remain (all surviving "adversarial" mentions are rename-callouts or historical Lupin-origin references). Plan: `src/rnd/2026.05.15-plan-review-rename-drop-adversarial.md`. Verification: 7 greps clean. Lupin-side header-pointer work filed as new TODO (owner: Maria). — Session 89 (Rachel)

- [x] **Sync workflow language with `ask_yes_no()` Neither option** — cosa-voice MCP `ask_yes_no()` now returns ternary `yes` / `no` / **`neither`** (Neither = "the question needs re-framing", never a default, requires explicit user click). Hub-and-spoke patch across PIP: canonical §Handling Neither subsection (~70 lines) added to `workflow/cosa-voice-integration.md` with ternary parser, anti-pattern table, destructive-op CRITICAL rule. Response Format table expanded 4→6 variants. Global config sync pair (`~/.claude/CLAUDE.md` live + `global/CLAUDE.md` mirror) re-synced byte-identical. Template `workflow/claude-config-global.md` updated. Callsite spokes patched: `session-end.md` (2 callsites + 2 hint lines), `session-checkpoint.md` (2), `branch-pr-and-merge.md` (4 — destructive ops tagged **CRITICAL**), `bug-fix-mode.md` (4), `installation-wizard.md` (1), `uninstall-wizard.md` (1 — **CRITICAL**), `session-start.md` (1 mention), `.claude/commands/plan-review.md` (2), `.claude/commands/plan-skills-management.md` (1). Verification: all 5 greps clean (hub subsection present, 6 response-format rows, zero "Binary yes/no" hits, every Tier-3 spoke references Neither, live↔mirror byte-identical). Plan: `src/rnd/2026.05.11-neither-response-language-sync.md`. Tier-5 (cosa-voice-notifications skill outside repo) queued as follow-on TODO - Session 85

- [x] **TTS Brevity Mandate concision pass** (refinement of Session 81 mandate). Three new principles added to `workflow/cosa-voice-integration.md` and `~/.claude/CLAUDE.md`/`global/CLAUDE.md` headline: (1) **tiered length cap** — routine close-outs ≈ 60 words / 20s, substantive turns ≈ 80–120 words / 30s; (2) **headline, don't enumerate** — numbers, file lists, percentages live in `abstract` only; speak the verdict; (3) **no justification for non-actions in spoken line** — skip confidence statements, process meta, deferred-work rationale. **Anchor example** added to canonical doc memorializing a 190-word real-use case → 45-word tightening (76% reduction). Memory file `feedback_tts_brevity.md` updated with refinements - Session 82
- [x] **Conversation-mode reinforcement across PIP** + new global **TTS Brevity Mandate**. Hub-and-spoke pattern: canonical spec in `workflow/cosa-voice-integration.md` (new §Conversation Mode with full TTS Brevity Mandate sub-section), live `~/.claude/CLAUDE.md` re-synced to `global/CLAUDE.md` with new headline rule, callouts added to 14 workflow files + 13 slash-command wrappers (31 files total). Verification clean: every spoke that uses cosa-voice tools references the hub AND mentions the brevity mandate; live + repo CLAUDE.md byte-identical. Memory persisted at `~/.claude/projects/.../memory/feedback_tts_brevity.md` - Session 81
- [x] Wire `/plan-review` into install wizard (entry [N] under new "REVIEW WORKFLOWS" section in `workflow/installation-wizard.md`; Step 3 dependency validation block; Step 5 install instructions; bulk-select strings updated). Catalog/flag-set fidelity verified across canonical doc, wrapper, wizard, and INSTALLATION-GUIDE.md (`--from=reuse|fitness|adversarial` consistent in all four). Added "Plan Review Gate Workflow" section to INSTALLATION-GUIDE.md - Session 80
- [x] Restructure `workflow/plan-review.md` to Fitness-first ordering (REUSE → Pass 1 Fitness → Pass 2 Adversarial). Added §3 Pass Ordering rationale. Synced wrapper, p-is-p-00 mermaid + prose, p-is-p-02 conventions section (13 Pass references swapped), README [C.5] entry. Origin-artifact note preserved that Lupin's order was Adversarial→Fitness - Session 80
- [x] Extract INTERACTIVE REQUIREMENTS ELICITATION to skill (`~/.claude/skills/interactive-requirements-elicitation/SKILL.md`) and reduce CLAUDE.md to a compact stub; live/repo CLAUDE.md now 38,297 chars (reclaimed 4,122 chars, under the 40k performance-warning threshold again) - Session 78
- [x] Add TEST OWNERSHIP MANDATE — embedded role-separation rule in `~/.claude/CLAUDE.md`, `global/CLAUDE.md`, `workflow/claude-config-global.md`; reinforced in 3 testing workflow docs; revised anti-sentiment in `workflow/bug-fix-mode.md` Step 7; reframed scope options in `workflow/testing-remediation.md`; cross-referenced in `testing-development` skill - Session 78
- [x] Update `global/CLAUDE.md` template to match trimmed `~/.claude/CLAUDE.md` (full replacement, 1378→845 lines) - Session 72
- [x] Reduce ~/.claude/CLAUDE.md below 40k chars (42.4k → 31.7k) - Session 71
- [x] Create cosa-voice-notifications skill with full API reference - Session 71
- [x] Implement persistent TODO.md pattern - Session 50
- [x] Add suppress_ding parameter to cosa-voice documentation - Session 49
- [x] Fix token estimation undercount bug - Session 48
- [x] Split bug fix mode into separate slash commands - Session 47
- [x] Implement bug fix mode workflow - Session 46

---

*Completed items older than 7 days can be removed or archived.*
