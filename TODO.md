# TODO

Last updated: 2026-06-22 (**TODO §3 aggressive re-cut applied** — Resume-Here stack + 8 superseded Pending Decisions + stale Session-78–94 backlog + weeks-old completions → `todo-archive/2026.06.22-aggressive-recut.md`; 288→90 lines, ~32k→~11k tokens; task `68217eac` DONE). — Prior 2026-06-18 (**María S112**: false-idle heartbeat bug root-caused + filed [`c6751cf8` → Mr Radio: oracle alias-normalizes project on READ but `task_create` stores RAW → aliased repos false-idle]; orphan triage [0 true orphans — 3 reaped-owner rows all Tiberius's, closed/live]; self-healing orphan-reclaim design [`src/rnd/2026.06.17-orphan-task-self-healing-reclaim-design.md`, task `d18b0ed1`, HELD for Rick]; TODO §3 archival **tooling** built [`/plan-todo archive` v1.1 + session-end Step 0.6 + Archival Strategy + horizon-tag] — **first-run re-cut DEFERRED** [branch-v0.1.3-vs-v0.1.8-content horizon ambiguity + dry-run-first mandate; 1 confirm away]; 11:30 checkpoint + push. — Prior: **María S111**: fresh-eyes review of the store-only undertaking → cutover empirically **PROVEN** [real-data divergence test, reproduced 6/6 green firsthand, merged `f8354d2c` → DoD `33b2b0be` closed]; **doctrine body-sweep across 6 surfaces** [task-store-discipline v1.4 + live `~/.claude/CLAUDE.md` + claude-config-global + session-start Step 4.7 + manager-autonomy + memento-management + README — dual-write/rebuild/managers-first fenced 🗄️ HISTORICAL]; PM explainer [→`executive-briefings/`]; **reaction-feedback build-prep authored + HELD for walkthrough**; whole-list cruft sweep + María's list → ZERO. Checkpoint+push+memento at session-end. — Prior: 2026-06-16 (María — BIG session: 6 decisions ruled across 2 walkthroughs [3 held: commit-ritual/write-scope/worker-await + 3 accountability mechanism]; #11 commit-gate rewrite landed [3 docs]; memento→harness-list rebuild contract standardized w/ Mr Radio [session-start Step 4.7 + memento element-8 + global pointer]; #6 auto-mirror doctrine caveat on 3 global-config surfaces; #12 reaction-rubric v1.1 initiative-recognition; #13 accountability design COMPLETE v0.2 [5/5 ruled — T_escalate adaptive-2x-tick+INI-configurable, tiered verification, widening-invariant-holds]. See Decisions Log. Prior same-day: always-create-a-task-item policy DOCS landed per Rick option-1 ruling: task-store-discipline.md §1 mandate + "harness list is NOT session-local" myth-buster, swe-team-roles.md v1.2 charters, claude-config-global.md new UNIFIED TASK-STORE block [closes adoption gap §11-A]; hook layer + gated live ~/.claude/CLAUDE.md edit queued. Prior: 2026-06-15 S110-cont-2 `bc77a5e9` — task-store-discipline v1.1 [Krishna E2E + edge matrix]; Manager-Tick Loop 5 decisions ruled; STT reconciled w/ Mr Radio.)

## 📍 Resume Here

→ Stale S102–S108 session-state stack archived 2026-06-22 → `todo-archive/2026.06.22-aggressive-recut.md` (that work is done / in the task-store / in the Decisions Log). Live owed work: query the store (`task_query`).

## Pending Decisions

*Queue for `/plan-decide` / "walk me through the pending decisions." One-line topics; the skill frames options live. See `workflow/decision-walkthrough.md`.*

- [ ] **🆕🔝 POST-GAME TODAY (Rick 2026-06-22) — Incorporate receipts-of-progress into the heartbeat Stop-hook WORK-OWED calc for a MANAGER persona.** Doctrine is codified (`manager-autonomy.md §9.1`); Rick wants the CODE counterpart explored: extend a manager's "owed work" to include *"you have in-flight workers whose progress you haven't empirically verified this cycle"* → the oracle keeps poking the manager to verify instead of letting it rest on an unproven "worker is working" assumption. Open Qs: where the oracle reads each worker's last-progress-receipt ts (capture-pane delta / last-commit / task-status transition); Stop-hook (`heartbeat_work_owed.py`) vs arbiter (`arbiter_job.py` liveness≠progress gap); manager-only vs all; rides ON TOP of repairing the currently-BLIND base detection (Wednesday's bugs). **🆕 Rick refinement (2026-06-22, voice): the DEBOUNCE proviso — the Stop-hook fires too often, so internalize the 10-min tick INTO the work-owed calc. The manager maintains (or the hook records) a `last_looked_in_on_workers_ts`; on each Stop, the hook computes elapsed = now − last-check: if < 10 min → NOT owed (do nothing, no poke); if ≥ 10 min → OWED → poke the manager to go look in on / empirically verify their workers (then the check resets the ts). Net: moves today's manual 10-min self-regulation timer into the hook as a per-manager throttle, so the manager is poked to verify only when overdue, never on every Stop. Threshold INI-configurable (default ~10 min), mirroring the `T_escalate` pattern.** Task `6929f4ac`. | priority: P1 | raised: 2026-06-22 | owner: María (design) + Mr Radio/Tiberius (build)

- [ ] **🆕🔝 NEXT-SESSION PRIORITIES (Rick broadcast `59f2d129`, 2026-06-12, Opus 4.8, through Monday AM) — TWO topics for CLEARED sessions:** **(A) The task list** — make the unified task-store / TODO surface actually work; this ~275-line TODO.md is itself Exhibit A of the sprawl the store is meant to fix (ties to the Task-Store design + discipline v0.2). **(B) cosa-voice MCP token efficiency** — the MCP server is burning ~75% of Rick's inference budget; design how to cut it (audit per-turn rider + `instructions` payload size, tool-schema reload churn, commons-read verbosity, notify/abstract duplication). **Delegation OFFLINE until Jun 15 11am EDT** (weekly Max-plan reset; only credit-funded sessions work); GCP migration BACK-BURNERED until further notice. | priority: P0 | raised: 2026-06-12 | owner: Rick + María/Tiberius/Mr Radio

- → **5 RULED Pending Decisions archived 2026-06-18** → `todo-archive/2026.06-resolved-pending-decisions.md` (F1–F4 task-store forks · manager-role-def · global-CLAUDE mirror · heartbeat-poker design · manager-spawn/harvest envelope). Rulings preserved in the Decisions Log below + cited design docs.

- [ ] **🆕 Cascade heartbeat tick + pokes — FINAL keep/drop/tune ruling (Rick 2026-06-12, tentative ruling issued: KEEP-WITH-TUNINGS).** Rick wants to dig deeper into María's S108 observer experience before finalizing. Input data: 55+ ticks over ~3h — emission 100% reliable, delivery ~44% failed-to-wake (FM-18 family), ~4:1 quiet-tick noise, flat 180s cadence vs documented adaptive M=8/4/2, pokes hit unsaveable sessions (quota-dialog/context-EOL). Tentative tunings ruled in: (1) delta-aware ticks (wake only on change/threshold), (2) honor adaptive cadence, (3) pane-state differential before poking (bug-queue cross-ref). Long-term frame: detection half migrates to arbiter+task-store; observer-discipline half stays. Vehicle for the dig: the cascade post-game session w/ María's tick ledger (54 acks on dm-maria) as primary evidence. **S109 UPDATE: María's experience report is DRAFTED** — `src/rnd/2026.06.12-observer-tick-experience-report.md` (S108 60-tick vs S109 4-tick comparison; emission ~100% / delivery ~56%; recommendation KEEP-WITH-TUNINGS with delivery-fix FIRST, ahead of the tentative three) — ready as the dig's working paper. | priority: P1 | raised: 2026-06-12 | owner: Rick (ruling) + María (experience report ✅ drafted)
- → 8 superseded Pending Decisions (heartbeat-hook v1 / arbiter-consumption / v2-work-owed §C.3-Q4 — all subsumed by the store-canonical ruling; acting-manager-TODO-discipline [landed]; decision-walkthrough rollout; coverage graduation; context-rate instrumentation; Tiberius diagrams-commit) archived 2026-06-22 → `todo-archive/2026.06.22-aggressive-recut.md`.
- [ ] **🆕 Reaction-UI palette + meaning RUBRIC (Rick 2026-06-10, "thinking out loud" — LOWER priority).** UI controls so Rick reacts to manager/implementer declarations: 👍 👎 ❤️ 🏅 **plus a negative/corrective axis** — how to express "fucking up / slacking / obstinate / unproductive" (the explicit "opposite of a medal"). Deliverable = a sharp rubric defining each symbol. **Sketch:** two axes — CORRECTNESS (👍 acknowledged/proceed ↔ 👎 wrong/redo) and CHARACTER·EFFORT (🏅 exemplary-remember-this ↔ 🟥 reap-candidate; ❤️ appreciation + 🟨 warning as the middles) — the football-card metaphor maps cleanly onto the existing reap/escalation model. **The architectural meat is CONSUMPTION:** a reaction must feed something — persona memory/mementos (medal = remembered achievement; red-card = remembered failure-mode), the arbiter/manager (👎 / red-card on a worker → intervene or reap-recommend), and a feedback ledger for post-games. **NOTE:** target the MULTIPLEXER, not the dying JS notifications client (Lupin sprint #1 deprecates it this weekend); a "prediction-hint thumbs-vote" precedent already exists there. **✅ RULED 2026-06-10 (Rick, walkthrough): DESIGN NOW, BUILD LATER** — María captures the rubric + the CONSUMPTION design (reaction → mementos / arbiter intervention / feedback ledger; surface-independent) now while the thinking is fresh; the UI build is deferred to after the multiplexer cutover (costs the Lupin sprint nothing). | priority: P2 | raised: 2026-06-10 | owner: María (rubric) + Lupin (UI) | ✅ design-now ruled 2026-06-10 | 🆕 2026-06-17 (S111, Rick ask): BUILD-PREP authored (`src/rnd/2026.06.17-reaction-feedback-implementation-prep.md`) — surface map (store-as-ledger now that the store is live), 4 build lanes (L0 foundation → L1 UI / L2 memory / L3 loop), acceptance criteria, **7 walkthrough-ready decisions**. **BUILD HELD until Rick reviews the plan + we walk it through.**
- [ ] **🆕 FOLLOW-UP (Rick flagged "don't let this slip" 2026-06-10; RE-FLAGGED verbatim 2026-06-12 at the F4 ruling: "make sure this doesn't get lost after initial implementation"): widen BOTH managers-first scopes → ALL sessions** once proven in prod: (a) the TODO-as-work-owed poke (gate change only); (b) **the unified task-store WRITE scope** (F4 ruling — managers-first writes at launch, reads universal from day one). Revisit after the store's Phase 1-2 land + a calibration window. | priority: P1 | raised: 2026-06-10, extended 2026-06-12 | owner: María + Lupin
- [ ] **🆕 Manager comms-funnel SWAMP risk — revisit with Tiberius (Rick 2026-06-10, "make a note").** Current doctrine: all workers communicate UP THROUGH their manager (Tiberius) to reach Rick — good in principle (single accountable throat, no worker-floods-Rick), but it risks **swamping the manager** as the fleet grows. Revisit WITH Tiberius later: load-shedding / batching / triage tiers / a direct-to-Rick escape hatch for true-urgent, so the funnel doesn't become a bottleneck. Not urgent; a standing topic for a manager-doctrine session. | priority: P2 | raised: 2026-06-10 | owner: María + Tiberius

## Decisions Log

*ADR-lite: `YYYY-MM-DD — decision → ruling. Why: rationale.` Full records in the governing doc where noted.*

- 2026-06-22 — **Receipts-of-progress / empirical liveness contract → CODIFIED** (Rick broadcast a8c4070e; empirical-probe layer co-authored w/ Tiberius 👑). Rick's catch: "waiting for a worker's notification" implicitly trusts the unproven claim *work is being performed* — the SAME face-value trust receipts-not-claims forbids, shifted from the RESULT to the IN-PROGRESS state. Review FINDING: loophole real (the chase verified liveness/re-asserted claims, not progress). Empirical answer: working CAN be proven, but ONLY by progress receipts, never liveness (liveness ≠ progress: a heartbeat proves a session EXISTS, not that it PRODUCES; the arbiter can even mis-infer an active worker as "blocked" — Tiberius's Clayton catch). FIX codified in `manager-autonomy.md §9.1`: proof = an artifact-delta within the chase window; probes = tmux capture-pane freeze-detection + `git show <hash>`/growing diff + fresh-reviewer reproduce-not-trust; `awaiting:X` is NEVER terminal (must cite a progress observation + next_chase_ts); default flips to "demonstrate progress or it's a stall." Task `ba27a555` DONE. Follow-up (future): arbiter could verify artifact-delta, not just liveness (the liveness≠progress code gap). | priority: P1
- 2026-06-22 — **Manager subagent governance → DOCTRINE + HARD-BLOCK HOOK** (Rick, 2026-06-22, per rec). Problem: managers spinning up workers via the Agent/Task tool (in-process subagents) instead of spawn_sessions → subagents have NO persona/bridge → invisible in fleet roster/focus bar + not independently manageable (Mr Radio admitted doing exactly this for the reviewer + orphan-diff). Rule (aligns with MANAGE-not-BUILD): **a crew manager uses spawn_sessions, never Agent/Task; Agent/Task is reserved for a WORKER parallelizing its OWN assigned task.** Enforcement = PIP doctrine (manager-autonomy + swe-team-roles) + a PreToolUse hook that DENIES the Agent/Task call for a manager-role session, redirecting to spawn_sessions. **Proposed role signal (María, the crux):** a session marks itself manager-of-a-crew when it assumes that role (calls spawn_sessions / runs /spin-up-swe-team) → a per-session marker the hook reads; solo non-manager sessions (e.g. a builder using Explore for read-only scouting) are EXEMT — which also resolves the scope question (block crew-managers, not all Agent use). Doctrine home = PIP; hook+marker home = lupin/harness (coordinate role-marker write-seam w/ Mr Radio). Task `899fc760`. | priority: P1
- 2026-06-22 — **Worktree Lifecycle Contract — all 5 gating decisions RULED** (Rick, guided walkthrough, all per rec). Anchor (Rick's no-push law): preservation is LOCAL-ONLY — commit WIP→branch, remove dir, keep branch ref, merge-after-review locally, NEVER push. **D1 impl home → lupin CODE + PIP DOCTRINE** (hub-spoke; rule wizard-installable; honors "defaults travel with the workflow"). **D2 reap-hook site → SHARED FLEET UTILITY** called by both manager-harvest AND Agent-tool teardown (+TFE) — coverage prevents "fixed one path, leaked the other." **D3 janitor → MANAGER-TICK + ~6h AGE THRESHOLD** (reuse existing always-on tick, no new cron; manager-death gap covered by session-start reconcile + :8001 arbiter; cron = documented hardening). **D4 WIP-on-reap → ALWAYS AUTO-COMMIT** labeled WIP commit, never silent-discard (zero-loss with no human in loop; reversible). **D5 branch retention → KEEP ALL, prune only under review** (branches are now the ONLY preservation copy; janitor removes DIRS never branches; deletion stays Rick-gated). Design + ratified table: `src/rnd/2026.06.22-worktree-lifecycle-contract.md` §6. Build (phases 2–3) awaits Rick's "build it"; recovery of the existing 7 orphans awaits Mr Radio's tip-SHAs + Rick's deletion go.
- 2026-06-16 — **Task-management architecture → STORE-CANONICAL, harness = rendered view (PLAN DRAFTED — pending Rick's doc review; implementation HELD)** (Rick asked for a link to read the plan, NOT to execute — the plan-mode ExitPlanMode "approved" was a misread; no doctrine edits made). The unified store is the SINGLE source of truth everyone writes to; the Claude Code harness list flips from a write-surface to a **rendered read-only view** synced store→harness; the work-driving signal moves onto the store (transcript = fail-open). Retires the lossy harness→store mirror + its whole bug class (`9bf1dc4a`/`9b23d5bc`/`82e4eaf0`). 3-move build: **M1** write to store · **M2** self-poke READS the store (build the stubbed seam; resolves the long-open §C.3 "Rachel seam" = store-primary + transcript-fallback) · **M3** hydrate harness from store at session-start. Supersedes the "harness-first + auto-mirror" premise; finishes F3 + tonight's Option A. Mechanism calls (Rick-approved): mirror retires after a short backstop window · hydration session-start-first, mid-session fast-follow · accept the store-write cost (net ONE write — the double-write disappears). Design: `src/rnd/2026.06.16-store-canonical-task-management.md`. Lupin build = Mr Radio (bug `82e4eaf0` lineage); PIP doctrine sweep across 6 surfaces = María (task #15, with the dual-write-until-code-lands caveat). Grounded by exploration: self-poke reads the transcript not the store; store-read seam unbuilt.
- 2026-06-16 — **Worker subtask fan-out — all 4 design Qs RULED** (Rick, guided walkthrough, all per rec). The constrained middle tier: a worker may create a CHILD task iff `parent_id` is set AND it owns the parent; children inherit the parent's `accountable_manager`; no top-level worker writes. **D1 fan-out visibility → FYI, NO GATE, via `dm_send`** (Rick rider: the cheap inline directed channel, not a broadcast notify). **D2 nesting → ONE LEVEL only** (no grandchildren) to start. **D3 child escalation → DIRECT to the parent's inherited manager** (a block-on-the-manager reaches the manager; no worker hop — avoids recreating the #13 wait-chain). **D4 parent-close → REQUIRES all children terminal, server-side** (parent done ⇔ all children done+verified; no hidden/orphaned work). Design COMPLETE v0.2 (`src/rnd/2026.06.16-worker-subtask-fanout-design.md`). Remaining = lupin build (`parent_id` field + write-gate + the 4 §3 enforcement rules). Relates to the deferred prove-then-widen follow-up (this is the narrowest earlier-shippable slice of worker write-capability).
- 2026-06-16 — **Work-debt SOURCE OF TRUTH → STORE CANONICAL, everyone reads it (Option A)** (Rick, guided walkthrough; per rec). Resolves the dual-source-of-truth bug `82e4eaf0` + the long-open §C.3-Q4 "authoritative work-owed source." The unified store is the SINGLE source for work-debt; the cross-fleet arbiter AND the local self-poke both read it; the harness transcript is retained as a **fail-open fallback** for the local poke when the store is unreachable (keep-alive never depends on store uptime). Mirror bugs `9bf1dc4a` (write-gate) + `9b23d5bc` (/clear collision) are the **foundation fix** — a prerequisite, not optional (Option C alone = insufficient). Why: only A yields one source (arbiter + poke agree by construction) and it aligns with the prior **F3 store-canonical** ruling; the fail-open rider keeps the local keep-alive robust; B contradicts F3 + scales poorly. Live repro = María's plan session (harness 6+ open, store `project=plan`=0, self-poke correctly owed). Lupin build = Mr Radio (bug `82e4eaf0`). Folds into: unified-task-store design (F3 lineage) + heartbeat work-owed §C.3-Q4.
- 2026-06-16 — **Accountability #13 mechanism Q1 — escalation threshold → ADAPTIVE 2× MANAGER-TICK + CONFIG RIDER** (Rick, guided walkthrough). `T_escalate` (the age before a worker's `awaiting:manager` fires its single backstop poke) = 2× the manager's current adaptive tick interval, so it fires only on a plausibly-MISSED tick. **Rick's rider**: the multiplier must NOT be hardcoded — make it a `configuration_manager` INI key (fleet/lupin-side `lupin-app.ini`), default `2`, tunable over time. Record: design doc §4.3 + §6.
- 2026-06-16 — **Accountability #13 mechanism Q2 — verification rigor → TIERED** (Rick, guided walkthrough, per rec). A manager "verifying done" must cite a primary artifact (receipt / test / commons entry / job-id) for fleet-auditable or cross-persona work (per the no-confabulated-results rule); a plain manager attestation suffices for trivial in-lane items. Rigor scales with blast radius. Record: design doc §4.4 + §6.
- 2026-06-16 — **Accountability #13 mechanism Q3 — widening invariant → HOLDS** (Rick, guided walkthrough, per rec). When worker self-tracking is later widened (the deferred prove-then-widen follow-up), the manager STILL holds a distinct accountable item per assignment until he verifies — the worker's self-minted doing-item and the manager's accountable-item remain separate records. Widening adds worker self-tracking without removing manager accountability; the separation is what makes the deadlock-fix survive widening. → #13 design now COMPLETE (5/5 decisions ruled, v0.2). Record: design doc §6 + version history.
- 2026-06-16 — **Session-end commit ritual → SHOW A BRIEF RECEIPT (no gate)** (Rick, guided walkthrough D1/3). Now that the commit/merge *approval* gate is removed, a session-end commit runs autonomously (green+reviewed, standing manager authority) and then posts a short FYI receipt: hash + one-line summary + files touched. Why: what was removed was *approval*, not *visibility* — "act then announce" keeps transparency without re-introducing a gate; PUSH-readiness stays unsurfaced (Rick's call alone). **Unblocks the #11 procedural rewrite** of `session-end.md` / `branch-pr-and-merge.md` / `bug-fix-mode.md`: keep PUSH approval, drop COMMIT approval, add a commit-receipt step.
- 2026-06-16 — **Accountability design (#13) write scope → KEEP MANAGERS-FIRST** (Rick, guided walkthrough D2/3). The manager creates+assigns each item and holds an OPEN accountable tracking item until HE verifies done; workers don't self-create. Why: consistent with the ratified F4 (managers-first writes) + "accountability never transfers down"; widening to worker self-tracking stays the deferred, double-anchored prove-then-widen follow-up.
- 2026-06-16 — **Accountability design (#13) worker await behavior → PASSIVE + ESCALATE IF AGED** (Rick, guided walkthrough D3/3). A worker blocked on the manager declares `awaiting:manager:<name>` as a passive status the manager-tick surfaces (asymmetric chase preserved); only if the await ages past a threshold does it fire ONE escalation poke — a dead/stalled-manager deadlock backstop. Why: passive is right *until* the manager goes dark; a single aged poke closes the exact hole #13 exists to kill, at minimal noise. (#13c cadence already settled: adaptive self-paced, 2026-06-15 ruling — not re-asked.)
- 2026-06-16 — **MCP `task_create` → KEEP + DOCUMENT, do NOT delete** (Rick, broadcast `1309e40d`, superseding the earlier teach-mirror-then-delete pitch). Rick is skeptical the harness can learn typed/cross-persona minting via metadata but is "willing to give it a try"; so for now the verb stays and we **very explicitly document its purpose and how it differs from the harness `TaskCreate`**. María + Mr Radio converged independently (both: right goal, don't delete outright; only real delete-casualties = cross-persona assignment + decision/gate/bug classes — DM-born `source_qid` is server-side and survives). Split by repo: María owns the PIP `task-store-discipline.md` §3 delineation (DONE) + the global mandate blocks (DONE, incl. live `~/.claude/CLAUDE.md`); Mr Radio owns the Lupin `task_create` docstring (María supplied the `:356-362` mirror-hardcode + `source_qid` specifics via DM). Teach-mirror-metadata recorded as the future route to eventual deletion IF it proves out. Record: this TODO Pending item + the three workflow docs + live global config.
- 2026-06-16 — **Always-create-a-task-item policy → DOCS NOW + QUEUE THE HOOK** (Rick, voice + guided ask, option 1 of 4). Make the harness-first "open a task item per unit of work" reflex ingrained so SWE managers (now) / workers (on widening) never need per-session re-telling. Chosen path: doc-level mandate immediately (reversible, no shared-infra risk) — `task-store-discipline.md` §1 mandate + myth-buster, `swe-team-roles.md` v1.2 charters, `claude-config-global.md` new task-store block — with the Stop/session-start **hook deferred to its own staffed change** (the true mechanism layer; touches Lupin hook infra). Rejected: all-three-now (hook blast radius), charters-only (misses non-spawned managers), doc-only-discipline (changes no behavior). Trigger evidence: the 2026-06-16 Mr-Radio exchange — a manager-figure defaulted to the costlier MCP verb on the false "harness list is session-local" premise = live proof the reflex isn't ingrained (gap §11-A). Live `~/.claude/CLAUDE.md` edit gated on Rick's direct word. Record: this TODO Pending item + the three workflow docs.
- 2026-06-15 — **Always-On Manager-Tick Loop — all 5 design decisions RULED** (Rick, guided walkthrough; design doc `src/rnd/2026.06.15-always-on-manager-tick-loop.md` §6 v0.2). D1 cadence→**adaptive self-paced** · D2 self-management→**poke/chase ONLY** (reassign/reap+replace is a deliberate manager action OUTSIDE the tick) **+ KEY REFRAME: the loop serves the MANAGER-figure (María/Mr Radio/Tiberius) to stay on top of own + assigned work — NOT Rick; he is not in this loop** · D3 driver→**`/loop` now, `:8001` arbiter composes later** · D4 heartbeat overlap→**keep separate layers** (Stop-hook = keep-awake floor; tick = drive-agenda) · D5 empty-agenda→**low-frequency keep-alive while the store fills**. Why: make manager drive STRUCTURAL, not disposition-dependent (the Opus-4.8-laid-back-vs-Fable finding). Mechanism = `/loop` + `ScheduleWakeup`; agenda = `task_query(accountable_manager=me)` against the live task-store. Next: graduate to a `workflow/manager-autonomy.md` section. Composes with `task-store-discipline.md` (the agenda source) + the manager-figure predicate (§2.1).
- 2026-06-12 — **Task-store C2 gate pin (staffed→LANDED) → RICK-AFFIRMED, veto window closed** (post-game walkthrough D5, relayed by Tiberius qid `91e1889c`; exercised by Lupin task ts-e232f0d4). Companions: D3 = Phase-2 write-path hooks kick off TONIGHT off-peak; D4 = cosa-voice MCP wrappers ride the same lane. Why: the three-name concur (Clayton/Tiberius/María) on double-wedge evidence held; Rick converted the author-pinned strengthening into a ruling. Record: design doc §3.1 F2 cell.
- 2026-06-12 — **Fleet-wide session quota → SOFT CAP 8, ALL sessions count (managers/stewards + workers)** (Rick, post-game walkthrough D1 + same-hour §2 pin, both relayed by Tiberius qids `e7fc1d22`/`b5ce8683`; María = coordination point). Why: two Max-plan quota freezes in 12h; "the tight budget is the point — it forces cross-repo manager collaboration and vigilant spin-down." Mechanism: `fleet-allocation` commons topic, spawn/reap EVENTS + María reconciliation vs ground truth; store-migratable schema. Record: `src/rnd/2026.06.12-fleet-allocation-convention.md` (v0.2; graduates to workflow/manager-autonomy.md on ratification).
- 2026-06-12 — **Cascade heartbeat tick + pokes → TENTATIVE KEEP-WITH-TUNINGS** (Rick, voice, ~02:55Z; final ruling DEFERRED to a deeper dig into María's S108 observer experience — Pending Decisions item filed). Tunings tentatively in: delta-aware ticks · adaptive cadence honored · pane-state differential pre-poke. Why: emission proved 100% reliable and caught the night's one genuine stall; delivery + noise + unsaveable-pokes are the fixable half. Evidence base: María's 55-tick ledger on dm-maria.
- 2026-06-12 — **Task-store F4 (launch write-scope) → MANAGERS-FIRST writes, everyone reads** (Rick, walkthrough 4/4, per rec) **+ Rick's explicit rider, verbatim intent: "I just want to make sure this doesn't get lost after initial implementation — let's make a note of it"** → the widen-to-all-sessions follow-up is now DOUBLE-ANCHORED (oracle-poke widening item extended to cover store write-scope; see Pending Decisions follow-up). Why: prove-then-widen matched the ratified oracle scope; DM auto-create still captures worker items day one. Record: design doc §3 F4.
- 2026-06-12 — **Task-store F3 (canonical-truth boundary) → STORE = canonical for LIVE work; TODO.md = durable human narrative, partially RENDERED from the store** (Rick, walkthrough 3/4, per rec). Why: machines read only the store (no parallel truths — rendered sections are derived); humans keep the git-versioned greppable record; consistent with the 2026-06-10 "TODO.md is never oracle input" ruling. Record: design doc §3 F3.
- 2026-06-12 — **Task-store F2 (service home) → EXTEND `:7999` with `/api/tasks/*`** (Rick, walkthrough 2/4, per rec) **+ the rider becomes binding: the `:7999` async-handlers debt is a Phase-1 PREREQUISITE — staff it** (it is filed-debt/unstaffed today, NOT in-flight). Why: lowest integration cost (MCP reach + auth plumbing exist); the rider converts the saturation risk into scheduled work. Record: design doc §3 F2 + §4 Phase-1 row.
- 2026-06-12 — **Task-store F1 (storage substrate) → PostgreSQL NOW** (Rick, guided walkthrough 1/4; against María's SQLite-first rec, WITH Tiberius's horizon rider in view). Why: lupin-postgres is already operated (sunk cost), and with the GCP cutover Saturday a single-file SQLite risks becoming an immediate migration; skip the intermediate step. Record: `src/rnd/2026.06.11-unified-task-store-design.md` §3 F1.

- 2026-06-10 — **Global cross-repo LoC roll-up → STANDING-AUTHORIZED, not user-gated** (Rick: "this is a ridiculous gate"). The roll-up is a read-only aggregation + CSV/plot write (low blast radius, reversible) → run it freely (ad-hoc / session-end / hub-spoke manager handoff), surface via notify for visibility; NO per-invocation user word. **Sole exception:** a user-set explicit one-off hold this session ("hold globals until Tiberius coordinates") is a user-authored gate, lifted only by the user's direct word (a peer relay can't lift it — classifier-confirmed 2026-06-09/10). Why: the routine roll-up felt gated because the user kept setting explicit session holds; absent such a hold, it's standing. Record: `workflow/loc-delta-global.md § Standing Authorization`.

- 2026-06-10 — **Manager-role DEFINITION (the "is this a manager?" predicate)** → **the repo's env-keyed default/standing persona is implicitly a MANAGER-FIGURE** (carries standing spawn/harvest autonomy + explicit-TODO discipline + work-owed treatment) **with its SWE role PRESERVED** (Manager vs Steward kept — a Steward like María is a manager-figure, NOT a build-Manager). Plus the explicit source (spawned-as-Manager / cascade Manager). Why: both new mechanisms needed a concrete predicate; the per-repo default-persona config is a durable machine-readable source for the heartbeat oracle's managers-first scope. Record: `workflow/manager-autonomy.md` §2.1.

- 2026-06-10 — **Manager Spawn/Harvest Autonomy — doc HOME (walkthrough D1)** → **new canonical `workflow/manager-autonomy.md` + a global `~/.claude/CLAUDE.md` pointer**. Why: a standing permission grant every manager must see belongs in its own discoverable, globally-advertised doc (PIP hub-spoke), not buried in the comms ladder. Record: seed `src/rnd/2026.06.04-manager-spawn-harvest-autonomy.md` §7.
- 2026-06-10 — **Manager autonomy — cascade INHERITANCE (D2)** → **one doctrine; the cascade-review Manager inherits the same standing envelope as the fleet Manager**. Why: simplest, no special-casing; the same caps + still-gated commit/push line already bound it; least-privilege is handled by the cap, not by splitting doctrine.
- 2026-06-10 — **Manager autonomy — concurrency CAP (D3)** → **soft cap = 8 concurrent children per manager + pool-exhaustion alarm; exceeding it escalates to Rick** (already a §3.2 bound). Why: autonomous within the envelope, gated exactly at the fleet-storm/cost boundary, visible via alarm. Number anchored to the live fleet (Tiberius at 7 today → 8 leaves one slot before the alarm). Identity-continuous respawn = standing-with-continuity-seed (§3.0, already ratified 2026-06-04); reap-threshold adopts the doc default (idle + no-owed-work + no-declared-hold = reapable); announcement contract = María to spec.

- 2026-06-10 — **Acting-manager work-owed source (Item 1, guided walkthrough D1)** → **per-session runtime artifact** (`~/.claude/manager-todos/<sid>.md`), auto-mirrored from native TodoWrite via a `PostToolUse(TodoWrite)` hook; arbiter reads that, NOT shared `TODO.md`. Why: per-session ⇒ `owned_by_me` dissolves by construction, survives `/clear`, near-zero discipline. Carries: `blocked_on:<who>` + next-chase-timestamp (no "pending X" graves) + fail-open & flag-once on non-compliance. Record: PIP `TODO.md ## Pending Decisions` Item 1 (Tiberius-recommended, Rick-ratified).
- 2026-06-10 — **Acting-manager work-owed SCOPE (Item 1, D2)** → **managers-first** (oracle pokes-on-owed-TODO for managers only to start; mirror hook runs for all). Why: matches Rick's stated intent, smallest blast radius, prove-then-widen. **Rick flagged "don't let this slip"** → follow-up item filed to widen to all sessions (P1).
- 2026-06-10 — **Reaction-UI palette + rubric TIMING (Item 2, D3)** → **design now, build later**. Why: the rubric + consumption design (reaction → mementos/arbiter/feedback ledger) is UI-surface-independent and worth capturing while fresh; the UI build defers to post-multiplexer-cutover (the JS notifications client is deprecated this weekend), costing the Lupin sprint nothing.

- → **Decisions Log 2026-06-02 → 2026-06-09 archived** → `todo-archive/2026-06-02-to-09-decisions-log.md` (14 entries: arbiter actuation-redline · SWE-team 7-decisions · v2.2 closed-loop design+build · decision-walkthrough DD1-3 · coverage post-game · direct-state-visibility · heartbeat-hook v1 + continued-dev · manager-autonomy grant · all-tiers post-game). Full text greppable in the archive.

## Pending

- [ ] **🆕 SWE work-assignment review homework (Rick broadcast `5c3f4012`, 2026-06-16).** Hypothetical exercise, María + Mr Radio complete **SEPARATELY**: given a SWE team spin-up + a task assignment, enumerate **which levers you'd pull** (harness `TaskCreate` vs MCP `task_create` vs `spawn_sessions` vs `dm_send` vs charter-slice) and why, as of our **current** configuration/charters/workflows. **Methodology under Rick's decision** (his question to us): ask-in-place (WARM) vs clear-memories/fresh-session (COLD) to get a "true reflection of current configuration state." **María's rec**: warm answer = the *answer-key* (designed intent, but contaminated — we just authored this); the real TEST is a **fresh-context agent with no adjacency to this thread** (fresh-reviewer principle), **config + memories KEPT** (that mirrors how a real spun-up worker actually arrives); optional 2nd austere run with memories wiped isolates whether the DOCS alone teach it. The measurement is the **delta** warm-vs-cold = config-quality / discoverability-gap signal (ties to §11-A). | priority: P1 | raised: 2026-06-16 | owner: María + Mr Radio (separate answers) + Rick (methodology ruling)

- [ ] **🆕 Always-create-a-task-item policy — make the harness-first task reflex ingrained (Rick directive, 2026-06-16, voice; option-1 "docs now + queue the hook" ruled via guided ask).** Goal: SWE managers (now) and eventually SWE workers ALWAYS open a harness task item per unit of work WITHOUT being told each session. **✅ DOCS DONE (María, 2026-06-16):** (1) `workflow/task-store-discipline.md` §1 — upgraded from "use your list as you always have" to an explicit always-create-an-item MANDATE + a **"harness list is NOT session-local" myth-buster** citing the live 2026-06-16 exchange where a manager-figure (Mr Radio) reached for the MCP `task_create` verb first on the false premise that the harness list is session-local; (2) `workflow/swe-team-roles.md` (v1.2) — Manager EXPLICIT-TODO block brought into the task-store era (native `TaskCreate` first lever; mirror destination corrected from the old per-session file to the unified store; `review`-not-`done` limit) + new shared crew-level task-item reflex note (managers-first now, workers on widening); (3) `workflow/claude-config-global.md` — NEW `## UNIFIED TASK-STORE` mandate block (closes adoption gap §11-A: the missing CLAUDE.md pointer). **✅ LIVE GLOBAL APPLIED (Rick-authorized, 2026-06-16):** `~/.claude/CLAUDE.md` + `global/CLAUDE.md` snapshot both now carry the `## UNIFIED TASK-STORE` mandate block (classifier did not block — Rick's direct word carried). **✅ TWO-METHOD DELINEATION DOCUMENTED (Rick broadcast ruling 2026-06-16: KEEP the MCP `task_create`, do NOT delete; document its purpose + how it differs from harness create):** `task-store-discipline.md` §3 now opens with an explicit Harness-vs-MCP delineation table (self-owned generic `task` w/ mirror hardcode `:356-362` vs typed/cross-persona via the MCP verb; same `/api/tasks` destination, differ only in expressiveness). **✅ DONE — Lupin `task_create` wrapper docstring** committed (Mr Radio, `8657bde5` on wip-v0.1.8, path-scoped to `src/lupin_mcp/cosa_voice_mcp.py`, HELD not pushed): WHICH-DOOR box + the `:356-362` mirror-hardcode as the why-they-differ + `source_qid` correctly framed as RESERVED plumbing for a designed-but-NOT-built DM→task auto-create path. **No-confab catch (María fact-check, 2026-06-16)**: the draft asserted a server-side DM→task auto-creator EXISTS; verification showed the only `create_item` caller is `POST /api/tasks` itself (C10 still open) → held the commit, corrected both sides incl. my own earlier "survives-delete server-side" overclaim + the pre-existing §3 `review_request` "auto-created" cell. **🔲 STILL QUEUED:** (b) **the hook layer** (the TRUE mechanism) — session-start injection + Stop-hook "work happened, no task item" flag; its own staffed Lupin change w/ test pyramid. (c) **teach-mirror-metadata EXPERIMENT** (Rick: "skeptical the harness can learn this, but willing to try") — extend the mirror to read `metadata.item_class`/`owner_persona`/`gate_class`; IF it proves out, it becomes the future route to eventually deleting the MCP verb. | priority: P1 | raised: 2026-06-16 | owner: María (PIP docs ✅ + live global ✅) + Mr Radio (Lupin docstring) + Lupin (hook + mirror experiment)

- [ ] **🆕 Graduate the Manager-Tick Loop → `workflow/manager-autonomy.md` section (+ build the `/loop` tick)** (María, 2026-06-15; all 5 decisions RULED). Promote `src/rnd/2026.06.15-always-on-manager-tick-loop.md` (v0.2) into a canonical `## Manager-Tick Loop` section: adaptive self-paced `/loop` + `ScheduleWakeup`; agenda = `task_query(accountable_manager=me)`; **poke/chase ONLY** (reap/replace OUTSIDE the tick); keep-separate from the Stop-hook heartbeat; low-freq keep-alive on empty board. **Reframe to honor: the loop serves the manager-figure, NOT Rick.** Then stand up the reusable manager-tick prompt template + a global `~/.claude/CLAUDE.md` pointer so every repo's manager-figure inherits it. Usable-tomorrow target (fleet on the task store). | priority: P1 | raised: 2026-06-15 | owner: María (graduate) + fleet (dogfood)

- [ ] **🆕 S110-P0 — DM back-channel: eliminate 17x token bloat via notification system** (Rick + Mr. Radio, 2026-06-13). **✅ DESIGN DONE (2026-06-13)**: Rick + Mr. Radio worked out a plan that routes through the notification system (already built, proven E2E) — bypasses and possibly OBVIATES the DM system entirely, eliminating the ~17x token bloat. **🔲 EXECUTION: Monday 2026-06-16 ~11am EDT** — exercise/eliminate the bloat first, then spin everybody back up, then resume main work queue. María's role = update PIP DM prompting language after execution lands (Task #3 in session task list). **✅ 2026-06-15 UPDATE (María, S110-continued):** Phases 1–2 landed EARLY (Mr. Radio, 2026-06-15) — `dm_send` live (notification-native, inline body, ~204 vs ~3,700 tokens ≈18× cheaper), round-trip verified María 🌸 ↔ Mr. Radio 🦉. **PIP DM doc sweep DONE**: `workflow/cross-session-communication.md` (§1 surfaces + quick tool ref + §1.5 send/receive/threading + §6.5 bug-filing + §7 status table + version history), `workflow/claude-config-global.md`, `workflow/cosa-voice-integration.md`, `workflow/doc-viewer-links.md`, `global/CLAUDE.md` snapshot. **REMAINING (María-side):** (a) live `~/.claude/CLAUDE.md` DM-row edit — auto-mode classifier BLOCKED it (treats the personal global config as behind the hold gate); needs Rick's direct go to apply so snapshot==live again. (b) Follow-up migration pass: cascade-review heartbeat docs (`workflow/plan-review-cascaded*.md`) + the external `cascade_heartbeat_scheduler.py` still call `commons_send_to` for heartbeat pokes — migrate to `dm_send` jointly with the Lupin-side scheduler change (coordinate with the cascade-heartbeat KEEP-WITH-TUNINGS ruling; out of this sweep's scope). STT micro-prompt reduction (line below) still pending Mr. Radio design. | priority: P0 | raised: 2026-06-13 | owner: Rick + Mr. Radio (execution Mon) → María (PIP doc sweep after)

- [ ] **🆕 S110-P0 — Micro-prompt reduction for STT injection** (Rick, 2026-06-13). Every speech-to-text transcription that gets injected into Claude Code sessions carries a verbose micro-prompt. Reduce the text volume in these injections — goal: smaller per-turn token footprint without losing routing/context fidelity. Design pass: Rick + Mr. Radio (same design session as DM efficiency above); María updates any PIP workflow docs that govern injection language. | priority: P0 | raised: 2026-06-13 | owner: Rick + Mr. Radio (design) → María (PIP doc updates)

- [ ] **🆕 S110-P0 — TTS verbosity rule: max 3 sentences, retire word-count** (Rick, 2026-06-13). Replace the current "≤80 words" TTS brevity rule across all CLAUDE.md / rider language with a **3-sentence maximum**: sentence 1 = headline, sentences 2–3 = two supporting takeaways. Rationale: LLMs can count sentences reliably; word counting is not reliable (documented failure pattern across Tiberius/Mr. Radio/María). The abstract channel carries all rich detail; the spoken `message` is a headline + two takeaways only. **✅ PIP-SIDE DONE (María, 2026-06-13)**: `workflow/cosa-voice-integration.md` (3 spots: length discipline, hard cap, decision-framing note) · `workflow/claude-config-global.md` · `workflow/session-end.md` · memory files updated. **✅ LUPIN-SIDE DONE 2026-06-15 (Tiberius + builder, María consulted as messaging-plane authority)**: server-side rider went LIVE this session — sentence-based ("Max 3 sentences: headline + 2 takeaways") + char-cap NAMED as a failure boundary (server REJECTS over cap → whole notify fails); confirmed in the per-turn reminder. Cap single-sourced: INI `cosa voice spoken char cap = 500`; the `750` in the notify() docstring is the stale figure → Tiberius reconciling. María's authoritative replacement text went to the implementer verbatim (corrected Tiberius's initial char-based proposal to the ratified sentence-based rule; Rick adjudicated Option A = name the cap). | priority: P0 | raised: 2026-06-13 | owner: María ✅ (PIP done) + Rick/Mr. Radio (rider update)

- [ ] **🆕 Task-store Phase-2+ backlog — cold-review C8–C15 (2026-06-12, anchored per Tiberius fold-request qid fef1f7fa so nothing evaporates; numbering = Clayton's consolidated verdict DM `8a299833` [numbering of record], realigned + C15 re-anchored per his FOLD-VERIFIED delta-check).** From Clayton's cold review of `src/rnd/2026.06.11-unified-task-store-design.md`: **(C8)** write-side failure semantics — short hook timeout + on-disk spool + replay (reuse notify spool pattern; §5 steer added v0.4); a silently lost write = dual-write drift reborn · **(C9)** manager-TODO file mirror = MANDATORY-until-proven (I1's store-down fallback degrades to fail-BLIND if the mirror is "optional" and dropped) · **(C10)** acked-ledger scope inconsistency — either auto-create items for ALL expect_reply qids or scope the ledger fold to review-requests explicitly · **(C11)** F4 enforcement mechanics — cite the ratified manager-figure predicate (manager-autonomy.md §2.1) + reconcile "mirror hook runs ALL sessions" vs F4 managers-first WRITES (gate the store-write inside the hook on the predicate) + classify worker threaded-DM-close as a system-hook write on a manager-owned item · **(C12)** cluster: `next_chase_ts` consumer (expired chase re-enters owed-work query) · `→dropped` REQUIRES a reason (T3 escape hatch) · cross-item events query for Rick's fleet-wide audit · legal transition graph · I4 non-compliance detection mechanism · **(C13)** ✅ naming nits — FIXED in v0.4 · **(C14)** `taskstore_*` MCP tool-name prefix decision (collision with harness-native TaskCreate/TaskList) · **(C15 — re-anchored per delta-verify)** Phase-3/4 ownership tightening: "Lupin" is a repo not a crew, "normal" gate undefined, Phase-4 TODO.md-renderer CODE has no owner (María owns only the workflow docs) — tighten before Phase-3 pours · **(NEW residual, Clayton delta-check)** cross-SESSION respawn correlation: successor's rewrites hash-scope to its own session id and won't correlate with predecessor items — fix at hook-build time: session-start seed re-registers the new harness task id onto the item's `correlation_key` (no schema impact). Owner: Phase-2 build crew (Tiberius) + María (design steers). priority: P1 (gates Phase 2, not Phase 1) | raised: 2026-06-12

- → Stale Session 78–94 backlog (old bugs incl. broadcast-injection + FM-21/22, and May-dated "verify skill X" dogfood tasks) archived 2026-06-22 → `todo-archive/2026.06.22-aggressive-recut.md`.

## Completed (Recent)

- → Weeks-old completions (Sessions 46–93) archived 2026-06-22 → `todo-archive/2026.06.22-aggressive-recut.md`.

---

*Completed items older than 7 days can be removed or archived.*
