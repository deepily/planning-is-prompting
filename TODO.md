# TODO

Last updated: 2026-06-09 (María session `7cf3310c` — S105 evening: Arbiter Operator Loop built end-to-end + observer-verified + held; redline narrowing Rick-confirmed; AFK-interrupt lesson saved)

## 📍 Resume Here (next session — pick up where we left off, per Rick 2026-06-07)

**Session 105 (2026-06-09 evening) state — the Arbiter Operator Loop is BUILT + observer-verified, all HELD, deploy Rick-gated:**
1. **Arbiter operator loop (the :8001 closed-loop consumer) — Round 2a rename + Round 2b (live-push + Part-6 routing + bounded auto-poke) BUILT + INDEPENDENTLY VERIFIED, HELD unpushed** (Lupin `21189d4`/`06fb0b3`/`b4441d7`/`ba57416` @ wip-v0.1.8; Tiberius mgr + Tiffany impl; María observer re-ran every gate: 30/30/148/164 tests green). Redline narrowed "never actuate"→"never DESTRUCTIVELY actuate" (Rick-confirmed) under 3 hardening conditions (standing redline test · anti-storm ≤N-per-episode · escalate-reap-recommendation-only). **⏳ REMAINING (Rick-gated, shared-infra):** the `:8001` deploy/go-live (restart) + the push; then verify auto-poke in prod → stand down the manual poker stopgap (make-before-break). Post-game: `src/rnd/2026.06.09-arbiter-operator-loop-2b-observer-postgame.md`. Manual poker stopgap (María-detect / Tiberius-fire) stays live until then.
2. **POST-GAME discussion owed to Rick** — esp. the AFK-interrupt finding (memory `feedback_no_blocking_asks_during_afk_autonomy`): observer broke the walk-away with a blocking ask on a reversible call. Rick: "we'll talk in the post-game." Also open for that discussion: where to draw the observer-verification lane (re-run tests vs scrutinize receipts).
3. *(The S104 "B1 go-live" item is effectively SUBSUMED — the arbiter operator loop IS the consumer B1 deploys; same `:8001` deploy gate.)*

**Previously — Session 104 (2026-06-07) state — the B1 closed-loop arbiter is BUILT, held for Rick's push, NOT yet live:**
1. **B1 arbiter (:8001 standalone) — built (L1–L4 + R0) + reviewer-passed, HELD** (Lupin `622fe5c` @ wip-v0.1.8 — verified on disk 2026-06-08; Tiberius crew: Tiffany/Mr-Radio/Krishna). **⏳ GO-LIVE SEQUENCED AFTER the in-flight Lupin rename** `arbiter_vigilance`→`lupin_arbiter_app` & `fastapi_app`→`lupin_app` (Rick ruling 2026-06-08, María `7cf3310c`: cut over once on final names, not mid-rename — systemd unit/run-script/module paths all move). Design: `src/rnd/2026.06.07-arbiter-deploy-architecture.md` (host-side under systemd, R0–R5, §4.1 verified liveness) + R0 decommission spec sibling (both refreshed to new names + rename note). **GO-LIVE remaining (operational, Rick-gated, AFTER rename green):** R0 atomic cutover (flip `arbiter in-process bootstrap enabled=false` + bounce, break-before-make) → enable systemd unit → 2 build-verifies (R5 stamp-attribution on shared :7999; R1 supervisor root-rung) → warm-up → Loop B actuates. R0 flag currently `true` = in-process arbiter still live, :8001 not actuating.
2. **Broadcast-listener fix** — Rick-greenlit, assigned to **Clayton** (honor prose/@-mention addresses, not just strict `@Persona:`); gated source-fix → Krishna L3 → held. Bug doc `src/rnd/2026.06.02-empty-broadcast-injection-bug.md`.
3. **Global LoC roll-up** — DEFERRED to after Tiberius's Lupin per-repo CSV refresh (hub-spoke; he pings me, I aggregate last).
4. **:8000 e2e test-debt** — Clayton owns (7 clusters, Lupin TODO); v2.2 cleared with git evidence; in progress.

**Previously — Session 102/103 (pick up only if the above is parked):**

Session 102-cont closed clean (push + backup done at session-end; **cross-repo LoC roll-up ran as the post-hold finale** after Tiberius's crew landed the v2.1 implementation). **Top of the stack:**
1. **Heartbeat Arbiter v2.1 implementation (Lupin-side, Tiberius-driven)** — design is FINAL + folded into `lupin/…/03-arbiter-design.md` §10. Pipeline: Cheech reviews §10 → Clayton builds the 3 lanes (server per-MCP-stamp → MCP lane · tool-use-hook trivial bridge-mtime touch → hooks lane · arbiter render + snapshot push + `/api/arbiter/fleet-snapshot` → arbiter lane) → Tiberius reviews. María on call if design intent is needed. Lupin doc `03` commit is a Lupin session's call.
2. **Agentic Heartbeat Poker / arbiter-behavior design deep-dive** — Rick wants this as its own conversation (dependency graph, `owner_id` AFK pre-resolution, fleet throttle).
3. **Framework §13 fold** of the post-game P0/P1/P2 (still on María's list).
4. Lupin-side execution from PG rulings (Tiberius/Rachel lane): empty-broadcast bug, hermetic-config fixture, 2 prod-bug fixes, stale `CLAUDE.md §STRUCTURE` + PIP mirror.

*(Done in 102 / 102-cont: Heartbeat Hook v1 shipped+verified; v2 Arbiter built+certified; v2.1 Direct-State-Visibility designed+folded.)*

**New design thread (✅ RATIFIED 2026-06-06 — build queue open):**
- **SWE-Team Spin-Up Workflow** — canonize the standing build crew (Manager + Steward + Implementer/Reviewer/Tester) so one directive spins it up with roles pre-loaded. SEED+RULINGS at `src/rnd/2026.06.05-swe-team-spin-up-workflow.md` §6. **All 7 decisions ruled by Rick** (María drove guided walkthrough, Tiberius manager-rec): Q1 scalable roster · Q2 fresh-person/stable-charter · Q3 layered activation (`/spin-up-swe-team` command + broadcast-triggerable + intent wrapper) · Q4 one `workflow/swe-team-roles.md` · Q5 hard green+reviewed gate + always-post-game · Q6 standing pair + spin-up crew · Q7 symmetric teardown + mementos. **Build queue** → see the `## Pending` build-out item below. Composes with the manager-autonomy seed.

**AM pickup (2026-06-06):**
- **Doc-viewer CSV 404 fix** — `.docview.yml` got `io/git-loc-delta/` added to `allowed_prefixes` (the session-end §6.4 LoC-CSV link was 404ing; `io/` wasn't served). **UNCOMMITTED**; takes effect on next `lupin-rest-dev` restart (Rick declined a bounce mid-flow). Commit it + confirm the link resolves after the next natural service restart.
- **Workflow portability follow-up** — canonical `workflow/session-end.md` §6.4 emits `io/git-loc-delta/…` doc-links, so ANY consuming repo needs that prefix in its `.docview.yml`. Add an INSTALLATION-GUIDE / §6.4 note so others don't hit the same 404.

## Pending Decisions

*Queue for `/plan-decide` / "walk me through the pending decisions." One-line topics; the skill frames options live. See `workflow/decision-walkthrough.md`.*

- [x] **Heartbeat POKER / arbiter-behavior DESIGN deep-dive — ✅ DESIGNED 2026-06-06** (Rick guided walkthrough, 5/5 → `src/rnd/2026.06.06-arbiter-closed-loop-design.md`, "Arbiter Closed-Loop Autonomy v2.2"). The closed-loop = arbiter taps manager-on-duty (lineage-derived) → manager actuates → escalate-to-Rick on genuine triggers only. **Now a BUILD item** (Tiberius/SWE-team, lanes B1-B6) not a design question. *(Original framing:)* Rick: "drill down on the arbiter's behavior at a later date." The agentic Poker owns the cross-fleet arbiter role: dependency graph (who's waiting on whom), `owner_id` pre-resolution for AFK keep-alive (F2, `2026.05.30-…-observer-log.md`), fleet-wide poke throttle. Distinct from the now-ratified Heartbeat Hook. **2026-06-04 — partially reopened**: Rick activated a concrete first piece — the arbiter consuming the local Hook's **poke-responses** (Hook→Poker bridge, §0.2). That consumption gives this deep-dive real input data; design the full arbiter behavior on top of it. | priority: P0 | raised: 2026-06-03
- [ ] **Heartbeat HOOK — build v1 (Lupin-side)** — design RATIFIED 2026-06-03 (`src/rnd/2026.06.02-stop-hook-natural-heartbeat-poker.md` §0). Build the `Stop`-hook script + `.claude/settings.json` wiring; FIRST verify CC loop-guard empirically (`stop_hook_active`?); per-session poke-cap + opt-out bounded before any live install. Coordinate the shared `Stop`/`PostToolUse` substrate with Rachel (TODO line 11). | priority: P1 | raised: 2026-06-03 | owner: Lupin-side
- [ ] **Heartbeat Hook — arbiter consumption of poke-responses (Hook→Poker bridge)** (2026-06-04, María — Rick reopened continued dev). Promotes design §0 #2's "optional future" to active: the agentic **Heartbeat Poker (arbiter)** consumes the local Hook's **poke-events + poke-outcomes** (resumed / declared-hold / hit-cap) to build the cross-fleet liveness + dependency view. **Design steer: EMIT NOW, CONSUME LATER** — first extension step is forward-compatible poke-event emission from v1's poke path (a consumable record in the `.heartbeat-*` runtime family or a commons event), so the arbiter lands as a pure consumer with zero Hook retrofit. **Invariant**: local poke stays autonomous + `:7999`-free; the arbiter is an additive observer, never a dependency in the poke path. Concrete first piece of the P0 arbiter deep-dive (Pending Decisions). Owner: María (design) + Lupin-side (Tiffany/Rachel emission; Poker consumer later). Source: `src/rnd/2026.06.02-stop-hook-natural-heartbeat-poker.md` §0.2. | priority: P1
- [ ] **Heartbeat Hook v2 — authoritative work-owed source (§C.3 Q4)** — the live oracle needs an authoritative per-session TODO/work-owed source + `owned_by_me` semantics (how the adapter knows which TODO/Pending items THIS session owns vs another's). v1 ships hold-management only (`oracle_verdict=None`); v2 (the undeclared-lazy-stop FM-19 catch) is a small adapter change once this is ruled. Oracle module already built + 100% tested. Owner: María. Source: Lupin `…/2026.06.04-heartbeat-hook/02-stop-py-seam-factoring-proposal.md` §C.3 Q4. | priority: P2 | raised: 2026-06-04
- [ ] **Manager Spawn/Harvest Autonomy — workflow home + envelope shape** — codify Rick's 2026-06-04 standing-authority grant. Where does it live: a new canonical `workflow/manager-autonomy.md` + global `~/.claude/CLAUDE.md` pointer, or a new tier folded into `workflow/cross-session-communication.md`? And what's the standing-vs-gated envelope (fresh-spawn standing · reap standing · identity-continuous respawn memento-gated · shared-infra/over-cap user-gated)? Seed design: `src/rnd/2026.06.04-manager-spawn-harvest-autonomy.md`. | priority: P1 | raised: 2026-06-04
- [ ] Decision-walkthrough rollout — add the global `~/.claude/CLAUDE.md` pointer + `/plan-install-wizard` catalog entry now, or keep PIP-only until dogfooded a few more times? | priority: P2 | raised: 2026-06-02
- [ ] Coverage framework graduation (§11) — promote `src/rnd/2026.06.01-…-framework.md` to a canonical `workflow/` doc now (g1–g4 met) or wait for a 2nd validating run? | priority: P2 | raised: 2026-06-02
- [ ] Context-rate instrumentation for PREDICTIVE harvest (§5 framework hardening) — turn harvest-on-unproductive from proxy-based (tests-written / stall) to metric-based (sample token `usage` → tokens/min → project minutes-to-ceiling → harvest before degradation). No built-in rate exists; must derive. Which approach: (a) Agent-SDK accumulate `usage.input_tokens` per query; (b) Claude Code hook (Stop/PostToolUse) parses session JSONL → cosa-voice alert [fits our stack]; (c) Admin API org-level (~5min latency, authoritative). | priority: P2 | raised: 2026-06-02 | source: claude-code-guide research 2026-06-02

## Decisions Log

*ADR-lite: `YYYY-MM-DD — decision → ruling. Why: rationale.` Full records in the governing doc where noted.*

- 2026-06-09 — **Arbiter actuation-redline NARROWED** (Rick-confirmed yes, María-routed). "Arbiter never actuates" → "arbiter never DESTRUCTIVELY actuates": it MAY auto-poke (bounded, capped, non-destructive — a poke was already in its `send_to` verb set) but reap/kill/replace stay human/manager-only. Under 3 hardening conditions (standing redline AST-scan · anti-storm ≤N-per-stall-episode → 1 reap-recommendation → silence · escalate-only never auto-reap). Why: closes the operator loop so processes self-advance without a human, without granting destructive power. Built + verified in Round 2b-3 (`ba57416`); redline AST-scan held green through the crossing. Record: `lupin/src/rnd/v0.1.8/2026.06.04-heartbeat-hook/2026.06.09-arbiter-operator-loop-implementation-plan.md` + PIP post-game `src/rnd/2026.06.09-arbiter-operator-loop-2b-observer-postgame.md`.
- 2026-06-06 — **SWE-Team Spin-Up: all 7 decisions ruled** (Rick, guided walkthrough; María drove, Tiberius manager-rec). Q1 roster→SCALABLE (3=default not cap) · Q2 binding→FRESH-PERSON/STABLE-ROLE-CHARTER · Q3 activation→LAYERED (`/spin-up-swe-team` command + broadcast-triggerable for Tiberius + intent wrapper; brief in workflow/ doc) · Q4 charters→ONE `workflow/swe-team-roles.md` section-per-role · Q5 gates→HARD green+reviewed before commit + ALWAYS post-game (scaled) · Q6 scope→STANDING PAIR (Mgr+Steward) + spin-up crew (3 workers) · Q7 teardown→SYMMETRIC one-directive + mementos. Why: canonize the informal heartbeat-campaign crew so one directive instantiates it with roles pre-loaded. Record: `src/rnd/2026.06.05-swe-team-spin-up-workflow.md` §6. Build queue opened; NO build started yet.
- 2026-06-06 — **Arbiter Closed-Loop Autonomy (v2.2) — 5 decisions ruled** (Rick, guided walkthrough; closes the deferred P0 Heartbeat-Poker deep-dive). D1 push→commons DM-push · D2 mgr-keep-alive→local-hook+arbiter-DM (push-not-poll) · D3 escalate-to-Rick→genuine-trigger-only (deadlock·whole-fleet-stall·unmakeable-decision·manager-down) · D4 manager-down→escalate+HOLD (never auto-assign; acting-mgr succession=V2) · D5 manager-routing→lineage-derived+fallback (worker→spawner→persona; per-group auto-routing, never hardcoded). Why: workers finish without the human in the routine loop; Rick = exception's-exception. Record: `src/rnd/2026.06.06-arbiter-closed-loop-design.md` (build lanes B1-B6). Greenlit for build (Tiberius, SWE-team spin-up); commit-ready, push held.
- 2026-06-06 — **v2.2 closed-loop BUILT + committed-held** (SWE-team engagement #3). Lupin `0d7adad` (D1-D4, 16 files, +1450, Steward-verified) + `5aadab1` (Thread A test_f2 fix); Krishna 356-green re-verify, all 8 conformance anchors held. Post-game: `src/rnd/2026.06.06-arbiter-closed-loop-postgame.md`. **Key lesson:** the autonomy gap manifested live during the build — a re-loop sat ~90 min unactioned while the standing pair went dark → doctrine note "standing-pair-must-not-both-go-dark / Steward actively watches stalls" folded into the SWE-team workflow + memory `feedback_surface_stalls_during_afk`. **NEXT (the real close): deploy B1 — register the arbiter as a standing daemon on :8000** (built ≠ live). Interim poker (Tiberius) until then. Push held for Rick.
- 2026-06-02 — Decision-walkthrough shape (DD1) → PIP workflow + Agent Skill + `/plan-decide` command, wizard-installed. Why: the only option that standardizes; one-word trigger. Record: `src/rnd/2026.06.02-guided-decision-walkthrough-skill.md`.
- 2026-06-02 — Decision-walkthrough queue source (DD2) → `## Pending Decisions` in TODO.md. Why: reuses the file read at session-start; skill frames options live.
- 2026-06-02 — Decision-walkthrough outcome record (DD3) → lightweight `## Decisions Log` + inline. Why: durable + greppable + low-ceremony; full ADRs reserved for architectural calls.
- 2026-06-02 — Post-game framework D1–D5 + triage T1–T4 ratified. Record: `src/rnd/2026.06.02-cosa-coverage-campaign-post-game.md` §3.6 + §3.7.
- 2026-06-05 — Arbiter direct-state-visibility D1–D4 ruled (guided walkthrough w/ Rick). D2→passive layered (tool-hook + transcript-mtime + server auto-stamp; show last-seen ages). D1→change + heartbeat tick showing duration-since-last-change. D3→log file + queryable FastAPI endpoint + commons. D4→verify bridge-mtime saturation-resilience first, adopt if server-independent. Why: see state never infer it; consumer-side only, producer untouched. Record: `src/rnd/2026.06.05-arbiter-direct-state-visibility.md` §6.1. NO code yet — folds into lupin `03` §v2.1 on Rick's go.
- 2026-06-05 — Same design: Tiberius (manager) reviewed→APPROVED + Rick BLESSED the convergence. SYNERGY: all liveness writers (idle-waiter + tool-use hooks + server per-MCP-stamp) bump ONE host-side bridge-mtime clock → wedge-resilient, covers heads-down, AND upgrades the broadcast-list filter for free. Redlines: (1) trivial tool-hook touch only (2) no standalone arbiter HTTP — push to :7999 /api/arbiter/fleet-snapshot mirroring pool-status (3) converge on bridge-mtime, no parallel store (4) state vs liveness = 2 columns. Record: §6.2. DESIGN-FINAL; no code yet.
- 2026-06-03 — Heartbeat Hook design v1 RATIFIED (Rick live walkthrough). Local `Stop`-hook · declared-hold-only ("defend your quiescence") · native `decision:block` re-prompt (no tmux) · mandatory per-session poke-cap · per-session `.heartbeat-hold-<id>.json` JSON artifact. Separate from + complementary to the agentic Heartbeat Poker. Record: `src/rnd/2026.06.02-stop-hook-natural-heartbeat-poker.md` §0. Why: keep-alive is the precondition for unattended runs; local+self-contained avoids the fragile `:7999` plane.
- 2026-06-04 — **Heartbeat Hook: land v1 + use; reopen continued dev for arbiter-consumption** (Rick, post-Tiberius word). v1 (local self-poke / hold-management, `oracle_verdict=None`) ships AS-IS and goes into use now. Continued dev REOPENED on two tracks: (a) FM-19 lazy-stop catch (v2, gated on §C.3 Q4, María); (b) **arbiter consumption of poke-responses** — promotes §0 #2's "optional future" to active: the agentic Heartbeat Poker (arbiter) consumes the local Hook's poke-events+outcomes for the cross-fleet view; concrete first piece of the P0 arbiter deep-dive. Design steer (María): EMIT NOW, CONSUME LATER — v1's poke path writes a consumable poke-event record up front so the arbiter is a pure consumer with zero Hook retrofit; local poke never *depends* on the arbiter (§0 #2 invariant). Record: `src/rnd/2026.06.02-stop-hook-natural-heartbeat-poker.md` §0.2.
- 2026-06-04 — **Manager spawn/harvest standing authority GRANTED** (Rick, live, during Heartbeat-Hook build). Resolving the >1h Rachel-respawn freeze: Rick chose RESPAWN **and** gave the manager (Tiberius) standing authority to spin up personas as-needed going forward. → founding ratified decision for the new **Manager Spawn/Harvest Autonomy workflow** (seed `src/rnd/2026.06.04-manager-spawn-harvest-autonomy.md`). Why: a manager that defaults to freeze-and-ask instead of acting within bounds is the wrong default (same failure family as FM-19). Codification job = bound the grant (standing vs. gated envelope), not invent the authority.
- 2026-06-03 — All-tiers grind post-game PG-D1..D9 ratified (full walkthrough). Record: `src/rnd/2026.06.03-all-tiers-grind-postgame.md` §3.6. Rulings: D1 priority order P0/P1/P2 CONFIRMED · D2 heartbeat-poker BUILD (design = separate convo) · D3 PRIORITIZE empty-broadcast bug + confirm D1 landed · D4 AUTHORIZE hermetic fixture (gated land) · D5 `:8000` tier GREENLIT (scope post-game) · D6 fleet reap NO-ACTION (Rachel Rick-summoned) · D7 FIX+DE-ARM 2 prod bugs · D8 FIX stale §STRUCTURE + mirror in PIP · D9 GRANT LoC permission rule. Why: keep-alive is the precondition for any unattended run; blast-radius guards preserved on the systemic fixes.

## Pending

- [ ] **BUG — broadcast injection-suppression (prose-address variant)** (2026-06-06, María; diagnosed live when I missed Rick's `b9f90f3c`). A user broadcast that addresses a persona in PROSE ("accounting from @maria") is **NOT injected** into that session's turn — the per-session broadcast-listener auto-acks it `skipped — no applicable directive for this persona` (verified: my `e26d2909` ack + Tiberius's on `broadcast-acks`); body is intact on the `broadcasts` topic. **Root cause:** the injection filter fires only on a strict `@Persona:` directive and suppresses prose `@persona` mentions → the persona never sees a broadcast explicitly directed at them. FM-18 / empty-broadcast-injection family (per-session render layer); NEW variant. **Fix direction:** the filter must inject (not skip) any broadcast containing the persona's handle in any form, OR default-inject all `@all` broadcasts and let the model triage. Mitigation: `commons_read(topic="broadcasts")`. Owner: Lupin broadcast-listener lane; cross-ref `src/rnd/2026.06.02-empty-broadcast-injection-bug.md`. priority: HIGH (silently drops user directives).

- [ ] **FM-21 FINDING — hook-events.jsonl hermeticity leak (path-mandate violation)** (2026-06-06, María steward; Rick surfaced it tailing the events log, Tiberius root-caused). `lupin/src/tests/unit/test_heartbeat_integration.py` (PRE-EXISTING, committed `fd3d9e7` "Heartbeat hook v2 dormant" — **NOT v2.1 `7973376`**) leaks synthetic fixtures (sidB1/sidG4/abc12345/Mr-Radio rows) into the LIVE `lupin/io/claude_code_hooks/logs/hook-events.jsonl` — **Steward-verified: 39.8 MB, 1,708 fixture hits**. Triggered live by Tiberius's Thread-A `pytest -k "stop_hook or heartbeat"` grounding run. **ROOT CAUSE:** the events-log writer resolves its path via `Path(__file__).parents[2]` (watch-hook-events idiom) instead of `cu.get_project_root()` — a **direct PATH MANAGEMENT mandate violation** — so the test's `get_project_root`→tmp monkeypatch misses the write path. **FIX at source (not just test-isolation):** route the writer through `cu.get_project_root()` (mandate compliance fixes the leak), isolate the test, and **truncate the 39 MB polluted log**. (v2.1's own 8 suites are proven-isolated: re-run left live count 65→65.) Owner: Lupin-side. priority: P1 (FM-21 family; cf. 2026-06-05 flake).

- [ ] **FINDING — spawn persona-request fidelity + verify-before-narrate** (2026-06-06, Rick caught it; María steward note). First real SWE-team spin-up: Manager set `persona_preference=Clayton` and reported "Clayton honored," but the live worker allocated as **Rachel** (the only spun-up worker Rick saw). `spawn_sessions`' `persona_preference` is a **request, not a guarantee**; the requested name was narrated as fact (both Tiberius AND María echoed "Clayton" un-verified — no-confabulation lapse, user became the verifier). **Rule (codified in `workflow/swe-team-spin-up.md` §3 + Manager charter `swe-team-roles.md` §6):** after spawn, verify the ACTUAL allocated persona via `list_spawned_sessions` and narrate THAT; if a specific persona is needed, have the worker `request_persona` + confirm before using the name. **Live fix DONE:** the worker self-corrected — booted as Rachel, saw Rick's @all broadcast, called `request_persona("Clayton")` itself before the Manager's re-request landed; worker is now Clayton (`a9f5c9bf`), language consistent. (It worked reactively off a lucky broadcast; the charter fix above makes it PROACTIVE so it no longer depends on one.) **Possible deeper item:** investigate why `persona_preference=Clayton` wasn't honored at SessionStart (taken? pool-exhausted → Extra-N/fallback? allocator bug) — Lupin-side spawn/SessionStart lane. Owner: Tiberius (live fix + allocator investigation) + María (PIP doc codification ✅). priority: P1.

- [x] **SWE-Team Spin-Up — built + first run APPROVED** (2026-06-06). Items 1–2 (workflow docs) authored by María; item 3 (command + skill + README) by Tiberius; item 4 (first real spin-up, Heartbeat Arbiter v2.1 Thread B) **SHIPPED — committed `7973376` (v2.1-only selective-stage, Steward-verified) + `a7e79a1` docs; live-apply 88 tests green**. Post-game (final pass done): `src/rnd/2026.06.06-swe-team-first-run-postgame.md`. Workflow **🛡️ BATTLE-TESTED** 2026-06-06. Remaining: :8000 auth-matrix result (Mr-Radio, pending) + Rick's push call. *(Original build-queue detail retained below for record.)* Build queue: **(1)** graduate seed → canonical `workflow/swe-team-spin-up.md` (composed with `src/rnd/2026.06.04-manager-spawn-harvest-autonomy.md`); **(2)** author `workflow/swe-team-roles.md` — per-role charters (content: expectations + Q5 gate/always-post-game + test-ownership + no-confabulation discipline, one section per role per Q4); **(3)** build the `/spin-up-swe-team [task]` slash command + intent wrapper (Q3 layered activation: also broadcast-triggerable) + README link; **(4)** Tiberius runs the **first real spin-up** against a v0.1.8 thread (the manager-autonomy build-out below is the mechanics companion — sequence the two together). Owner: María (workflow + roles docs, Steward) + Tiberius (command/spawn + first run). priority: P1.

- [ ] **Manager Spawn/Harvest Autonomy workflow — build out from SEED** (2026-06-04, María — Rick directive: *"Managers need explicit permissions to make appropriate decisions."*). Seed design authored at `src/rnd/2026.06.04-manager-spawn-harvest-autonomy.md` (framing + principle + standing-vs-gated dimensions + 6 open questions). Founding ratified decision = Rick's 2026-06-04 standing-authority grant to Tiberius (Decisions Log). **Remaining**: resolve the home/shape decision (Pending Decisions); draft the standing-authority envelope as an action×reversibility×standing|gated table; codify the memento precondition for identity-continuous respawn; cross-link the harvest-discipline mandate (mechanism half) + three-tier commons autonomy (template) + blast-radius authorization (bounds); promote to canonical `workflow/` once Rick ratifies the envelope. Same failure family as FM-19 + the Heartbeat Hook (owed work idling for lack of authorization — here the owed work is fleet management). Owner: María. priority: P1.

- [ ] **🌅 RICK'S MORNING GATE (all-tiers grind close, 2026-06-03)** — handoff items from the overnight grind, captured by María (steward). Detail + evidence: `src/rnd/2026.06.03-all-tiers-overnight-grind-observer-log.md`.
  - **Push the 11 held Lupin-side commits** (test-only, certified; Rick's call per push-is-user-session-end).
  - **Reap-or-hold the parked fleet** (Tiberius + Cheech/Rachel/sam/Krishna).
  - **2 honestly-tripwired prod bugs (Lupin) — Rick's prod-fix gate** (prod-logic = his call, never the fleet's): (a) `dispatcher.py self.debug` uninitialized; (b) `cosa_interface.ask_yes_no` → missing `_dispatcher.ask_yes_no` on `AgentNotificationDispatcher` (Bug #12, `xfail(strict=True)`-pinned `533d273` — self-announces the moment the method is added).
  - **Stale `CLAUDE.md §STRUCTURE`** (Lupin) — flagged by Tiberius for refresh.
  - **Deferred harvest-deletes** + the **global hermetic-config fixture** (see FM-21 item below).
  - **Optional `:8000` integration tier** (unit tier is the certified 100%).

- [ ] **Empty-broadcast-injection BUG (OPEN, HIGH)** (2026-06-03, María — filed `src/rnd/2026.06.02-empty-broadcast-injection-bug.md`). A user broadcast arrives as an **empty `USER BROADCAST` system-reminder** (body dropped) while the **content is intact on the `broadcasts` commons topic** — verified across 2 sessions (María + Tiberius). Root-cause locus: the **per-session injection/render layer** (broadcast row → system-reminder composer), NOT send/storage. Silently loses fleet-wide directives (surfaced when Rick's AFK all-tiers directive reached every recipient blank). Suggested fix + regression test in the doc. Owner: cosa-voice / broadcast-listener (Lupin); Rachel's lane likely. Mitigation in place: content recoverable via `commons_read(topic="broadcasts")`.

- [ ] **FM-21 systemic fix — global autouse hermetic-config fixture** (2026-06-03, María steward ruling). FM-21 (non-hermetic config/registry test bleed) is cataloged + fixed at-source for tonight's instances. The SYSTEMIC kill is a global autouse fixture in a new `src/cosa/tests/conftest.py` (`cache_registry.invalidate_all()` + `ConfigurationManager(_reset_singleton=True)` + config-submodule sys.modules/parent-attr eviction). **Blast-radius-gated**: Rachel DESIGNS + isolation-verifies (full suite green with it active, zero currently-green tests broken); lands ONLY on a clean verification, between batches, revert-staged; **else Rick's morning gate**. Do NOT land a global autouse fixture into a live multi-author grind unilaterally. Owner: Rachel (design) + Rick (gate). Source: framework §8 FM-21.

- [ ] **FM-22 — WATCH (held, do not catalog yet)** (2026-06-03, María steward ruling). A single non-reproducing `test_io_files_router` red under the full combined run, mechanism never captured (`--tb=long` gone before it could be read; self-healed as the test-set moved under peer commits). Naming a failure-mode for an un-pinned mechanism = confabulation. **Promote to FM-22 ONLY on a recurrence WITH a captured `--tb=long` that names the mechanism.** Watch-note: Lupin `…/90-io-files-router-nonrepro-watchnote-2026.06.03.md`; held-pointer in framework §8.

- [ ] **🚨 WAVE-3 PREREQUISITES (Rick directives 2026-06-01) — REQUIRED before the next coverage batch** (framework §13 in `src/rnd/2026.06.01-dependable-coverage-campaign-framework.md`). **(A) Completion discipline** — never park on trivial/difficulty reasons; drive to 100% (codified §5; done). **(B) Messaging coordination-plane "black-holes"** — root-cause CONFIRMED two-layer (Tiberius): MCP-server saturation (sync handlers block on a fleet-loaded `:7999`, 5s MCP-transport timeout) + silent fire-and-forget drops → no delivery-guarantee / load-isolation / pull-fallback (FM-7/11/15/18 = ONE plane). **Fix to design + build (Lupin infra, Tiberius)**: concurrent/non-blocking MCP server + durable-queue/retry/ack + pull-able inbox + fleet concurrency-cap + decouple TTS from `:7999`. **(C) Harvest discipline** — mandate prompt reaping of unproductive workers (`extra-N` personas = pool-exhaustion alarm); FIX the reap mechanism (FM-3 dismiss_sessions bug + FM-16 cross-lineage). Owners: María (PIP §13 synthesis — stub to fill as the root-cause lands) + Tiberius (Lupin infra fix + 02-cold-start-runbook harvest/push-silence mandates).

- [ ] **Stop-Hook Natural Heartbeat Poker — R&D (POST-GRIND)** (2026-06-03, María — Rick-directed). Pursue as a serious design **after the CoSA-in-Lupin grind is up and running**. Use Claude Code stock hooks (the `Stop`/quiescence lifecycle event) as a *naturally-occurring* per-instance poker that `tmux send-keys` a "get back to work" nudge into idle instances with work owed — replacing external-daemon polling. Crux: the **hold-vs-lazy discriminator** (never poke a legitimately-holding instance; proposed principle *"a hold is only honored if it is declared"*) = mechanical FM-19 completion-discipline enforcement. Coordinate a SHARED `Stop`/`PostToolUse` hook substrate with Rachel's line-11 token-rate research (don't ship two competing hooks). Bound by construction (poke-cap / opt-out) — no new storm vector (cf. the TTS-storm postmortem). Owner: María. priority: P2 (post-grind) | source: `src/rnd/2026.06.02-stop-hook-natural-heartbeat-poker.md`

- [ ] **Framework graduation (§11) — promote from `src/rnd/` to a canonical `workflow/` doc** once WAVE-2 hits genuine 100% + g1–g4 captured + Rick ratifies. g3 ✅ held, g2 ✅ exercised (honest, gate held through reviewer swap), g4 ✅ accruing; g1 strong (G0.4 poker never triggered — manager never stranded, logged honest-not-proven). Owner: María. Source: `src/rnd/2026.06.01-dependable-coverage-campaign-framework.md` + WAVE-2 observer log.

- [ ] **CoSA post-game — remaining R/DI/PG fold + Lupin-side close-out** (2026-06-01, María). The Q4 framework + the WAVE-2 observer log are authored (the WAVE-1 R1/R2/DI-2/DI-3/PG items are largely folded into the framework FM catalog + §12/§13). Remaining: any un-codified R1/DI-1 items from the WAVE-1 observer log; Lupin-side — verify the dismiss_sessions source-fix (`3488b43`) actually reaps, the U4 403-queues verdict (intentional v0.0.9 auth → stale-mock fix, no tripwire), websocket_manager → 100%, the speech.py `app_debug` prod-fix. Cross-ref: `lupin/src/rnd/v0.1.8/2026.05.30-cosa-coverage-campaign/` + both observer logs.

- [ ] **Cascade review — remove rubber-stamp user gates (incorporate into the playbook; post-game item)** (2026-05-22, María — Rick directive). Rick flagged the Step-3 user-decomposition-approval gate AND inter-stage user gates as rubber-stamps: near-zero decision value, real latency (they parked `cascade-notif-sync` ~4h). Directive: the cast (Workflow Steward + Manager + Author + reviewers) self-ratifies and continues; the user is escalated to ONLY on a genuine trigger (foundational finding, cross-section conflict, hard contradiction with a prior user decision) — never a mandatory routine gate. Same family as Run-5 PG-10. **This is a playbook change** — `workflow/plan-review-cascaded.md` §Step 3 + the inter-stage progression (possibly `-common.md`) — distinct from the feedback memory already saved (a memory is NOT the playbook — Rick's explicit point). **Partial progress 2026-05-28**: user-tap reviewer-override rule codified in `workflow/plan-review-cascaded.md` §Step 4 (new subsection); ASCII process flow added to new `workflow/plan-review-cascaded-stage-specs.md` §3; new `workflow/plan-review-cascaded-parallelism.md` with 4-section worked example; new `workflow/plan-review-cascaded-input-spec.md` consolidating the cascade input spec + validation rubric + remediation flowchart; Cast Manifest mandate at TOP of planning doc; p-is-p-00/01/02 updated with Expected Output Shape callouts so planner output explicitly matches cascade input. **Still pending**: Step-3 cast-ratified-conditional gate redline; inter-stage no-mandatory-gate codification (these are the redlines on the existing Step-3 + Stage-progression text). Caveat (Mr Radio, 2026-05-22): this governs FUTURE cascades — it does NOT retroactively authorize a currently-parked cascade past an explicit user "no". Owner: María. Cross-ref: feedback memories `feedback_cascade_no_rubber_stamp_gates` + `feedback_user_tap_reviewer_override` + `feedback_no_workflow guidance_word`.

- [ ] **Fold Run-5 cascade workflow-guidance candidates into PIP cascade workflow docs (PG-1/2/3/5/6 + SA-1)** (2026-05-22, María — Workflow Steward; routed via the Run-5 Step-9 revision-handoff doc §6 + Step-0-prep doc §8 Post-Game table). Cascade Run 5 (heartbeat-poker review) surfaced process-improvement candidates for the cascade workflow itself. Captured here for the redline; full text + empirical anchors in `lupin/src/rnd/v0.1.7/2026.05.21-heartbeat-poker-cascade-step0-prep.md` §8. The workflow-doc redlines are substantive guidance changes — staged here as tracked work rather than applied unilaterally.
  - **PG-1 — codify participant context-clearing as an explicit workflow step.** Cascade participants (reviewers especially; arguably the Manager) should `/clear` before taking their seat — review cold/uncontaminated. Today ad-hoc. Fold target: `workflow/plan-review-cascaded.md` — a named pre-cascade setup step (who clears, when, why). [workflow guidance]
  - **PG-2 — daemon-launch permission-denial fallback.** The `dual_independent` daemon-kickoff rule + the heartbeat-poker design don't account for the host-harness permission classifier blocking a background-daemon launch (hit in Run 5 — the Workflow Steward fell back to a self-paced `ScheduleWakeup` probe loop). Fold target: specify a self-paced `ScheduleWakeup` fallback wake mechanism OR a pre-flight launch-approval step — `workflow/plan-review-cascaded-common.md` §Heartbeat Handling + the heartbeat-poker design-doc §10.x. [reflexive]
  - **PG-3 — peer-relayed user decisions must quote verbatim.** A Manager→reviewer role-trigger DM was permission-denied (false-positived as "fabricated user-decision content") because the relayed user ruling was paraphrased, not quoted. Fold target: cascade workflow guidance mandates verbatim quotation of any peer-relayed user decision; document host-permission-layer interference as a known operational hazard. [workflow guidance]
  - **PG-5 — Step-0 recon checklists must verify mandate scope-applicability.** Run-5 Step-0 recon asserted the Lupin-wide 100%-coverage mandate as applicable, but the design's code location (CoSA sub-repo) is mandate-EXCLUDED. Fold target: `workflow/plan-review-cascaded-common.md` §Step 0 — recon checklists verify a cited mandate's scope against the design's actual code location, never assume blanket applicability. [workflow guidance]
  - **PG-6 — heartbeat-daemon poke-delivery topic-name bug (reflexive).** `cascade_heartbeat_scheduler.py --manager "mr radio"` builds poke topic `dm-mr radio` (with a space) → fails `^[\w-]+$` topic validation → every poke 422s. Reflexively corroborates Fitness findings F-Rio-C1 / B1. Fold target: (a) fix `cascade_heartbeat_scheduler.py` to slugify recipient-derived topic names [Lupin-side script fix]; (b) the `HeartbeatPokerJob` design must specify topic-name slugification in poke-delivery [design-doc]. [reflexive]
  - **SA-1 — Manager-rehydration memento.** A fresh-context session re-taking the Manager seat after a planned `/clear`, via a hand-authored rehydration doc, is not codified in §Step 9 or the §Manager System Prompt. Surfaced by the Run-5 Step-9 self-audit sweep; empirically anchored (Run-5 Manager seat rehydrated by session `eac45c39` from a memento). Fold target: `workflow/plan-review-cascaded-common.md` — a new §Manager rehydration sub-section (or §Heartbeat Handling extension); cross-link PG-1. Adjacent to the "memento skill" item below — distinct (that = a general pre-`/clear` snapshot skill; SA-1 = cascade-workflow codification of Manager rehydration). [workflow guidance]
  - **Not routed here**: PG-7 (transient permission-classifier outage) is an operational incident, not a workflow-guidance candidate — flagged for Rick's 2026-05-23 discussion. PG-4 intentionally unused.
  - Owner: María (PIP redline) + Lupin-side for the PG-6(a) script fix + the design-doc §10.x reflexive retrofits. Source: Run-5 Step-9 handoff `lupin/src/rnd/v0.1.7/2026.05.22-cascade-run-5-revision-handoff.md` §6.

- [ ] **Create a "memento" skill — pre-context-clear state snapshot for rehydration** (Session 94 2026-05-21, María — Rick's explicit request). A Claude Code session about to undergo a context clear should write an explicit, structured snapshot of its current state — Rick's term: a **"memento"** — that the post-clear session reads to **rehydrate** into its role without reconstructing state from scratch. First instance was hand-authored this session at `.claude-memento.md` (PIP root, gitignored) before María cleared context to take the Observer seat for cascade Run 5 — use it as the exemplar. The skill should formalize: (a) **snapshot format** — role, current task, what-I'm-waiting-for, re-warm reading list, open loops, rehydration instructions; (b) **file location convention** — e.g. `.claude-memento.md` at project root, gitignored alongside `.claude-session.md`; (c) **rehydration mechanism** — ideally a SessionStart-hook integration so the fresh session auto-detects + reads the memento (the current manual approach requires the user to point the new session at the file); (d) **lifecycle** — when to write (before any deliberate `/clear`), when to discard (after successful rehydration), staleness handling; (e) **relationship to `.claude-session.md`** — the manifest tracks touched files; the memento captures cognitive/role state. They are complementary. Distinct from auto-memory (which is for cross-conversation durable facts; the memento is single-clear-cycle transient). Owner: María or next PIP session. Candidate skill name: `plan-memento` or `session-memento`.

- [ ] **Cross-repo rollup: per-repo CSV staleness — add a refresh pre-step** (Session 94 2026-05-21, María — surfaced by the first live `/plan-loc-delta-global` invocation). The global rollup reads each repo's per-branch CSV from `io/git-loc-delta/`; those CSVs are only refreshed when session-end §6 fires in that repo. On the first live run, 2 of 3 repos had stale CSVs (cosa from 2026-05-16, PIP from 2026-05-20) — a naive run reported only lupin's +1,526 and silently missed PIP's +1,293. Workaround applied this run: manually refreshed all 3 per-repo CSVs (`run_git_loc_delta --branch` each) before aggregating. Proper fix options: (a) `workflow/loc-delta-global.md` adds a Step 1.7 "refresh per-repo CSVs before aggregating" — invokes the per-repo analyzer for each discovered repo; (b) the cosa aggregator CLI gains a `--refresh` flag that regenerates inputs before reading; (c) accept staleness + surface a per-repo "CSV age" column in the output so the user sees which repos are stale. Lean: (a) or (c). Owner: María (PIP workflow) + possibly Rachel (cosa CLI if option b). Phase 1.x refinement.

- [ ] **Backfill `CLAUDE.md` into 6 scope-registered repos that lack one** (Session 94 2026-05-21, María — filed alongside the doc-link guidance reconciliation). Per Tiberius's authoritative enumeration: 8 registered scopes total per Lupin `lupin-app.ini § external repos` — `lupin`, `planning-is-prompting`, `cosa-voice`, `lupin-mobile`, `lookml`, `par-pacific`, `claude-plans`, `retail-ai-location-strategy`.
  - **2026-05-28 partial-progress audit** (María):
    - ✅ `lookml` (at `projects/google/lookml`) — CLAUDE.md already exists with Doc Viewer Scope section
    - ✅ `par-pacific` (at `projects/google/par-pacific-financials-er-442109` — note: directory name differs from scope name) — CLAUDE.md already exists with Doc Viewer Scope section
    - ✅ `lupin-mobile` (at `projects/lupin/src/lupin-mobile` — nested in lupin) — CLAUDE.md already exists with Doc Viewer Scope section
    - ✅ `retail-ai-location-strategy` (at `projects/google/retail-ai-location-strategy`) — **NEW thin CLAUDE.md written 2026-05-28 by María**; not auto-committed (different repo); user should `cd` into that repo and commit when convenient
    - ⏸ `cosa-voice` — repo not accessible on this machine's filesystem; deferred
    - ⏸ `claude-plans` — repo not accessible on this machine's filesystem; deferred
  - **Remaining work**: cosa-voice + claude-plans repos need CLAUDE.md backfill; both require a session running in those repos OR a path to write cross-host. Owner: TBD per repo.

- [ ] **Lupin-side `start-cascade-heartbeat.sh` `--observer` convenience flag** (Session 93 fifth continuation 2026-05-20, María — filed alongside the v1.1 codification pass). The new `heartbeat_daemon_kickoff_policy = dual_independent` default codified in `workflow/plan-review-cascaded-common.md` §Heartbeat Handling — Daemon kickoff procedure works via persona-name positional arg (`bash start-cascade-heartbeat.sh <observer-persona-name>`), but a `--observer` convenience flag would make the dual-launch ergonomics smoother and harder to forget in cascade-prep. Proposed shape: `bash start-cascade-heartbeat.sh <persona-name> [--manager | --observer]` with default `--manager` for backwards compat. Out-of-scope for PIP; filed as Lupin-side enhancement. Cross-link: this enhancement directly supports the v1.1 `dual_independent` default; without it, cascade-prep relies on remembering to launch both daemons separately.

- [ ] **`commons_send_to` recipient pool-key vs display-name routing gap — bump priority** (Session 93 fifth continuation 2026-05-20, María — reference to existing TODO from Session 93 second continuation 2026-05-19). The codification pass on Observer-mode Probe Protocol structurally exercises this routing path more heavily — Observer's probe-as-mitigation channel involves repeated DM-to-Manager calls. If the case-fragmentation bug surfaces during a Run 5+ cascade as Observer-probe friction, it'd block the very workflow guidance the v1.1 fold is codifying. Existing TODO entry below in Pending list (Lupin-side bug). Not a new item — just a priority-bump note on the existing one. Owner: Tiberius or next Lupin session.

- [ ] **Lupin/cosa-voice feature — Mute-Channel Bypass for Manager-Escalation** (Session 93, María — Run-3 §10.14 errata Item C, Rick's catch). When the user mutes a critical-path persona (manager) for chatter relief, the manager's `urgent`-priority `notify()` escalations should bypass the mute via a separate channel. Run-3 evidence: Rick muted Tiberius; Tiberius's urgent escalation request waited 14 min for time-out because `priority="urgent"` didn't bypass mute. Three design candidates: (a) default rule never mute manager (workflow-level fix; doesn't need MCP change); (b) priority-threshold mute-bypass via separate channel (cosa-voice MCP change — `priority="urgent"` triggers a separate auditory channel that mute doesn't suppress); (c) tool feature request — new `bypass_user_mute=True` flag on `notify()`. Owner: Tiberius or next Lupin session. Out-of-scope for PIP-side cascade workflow guidance; filed here as cross-reference because it surfaced as a Run-3 cascade observation. Refs: `pipeline-summary-20260519` commons topic; `workflow/plan-review-cascaded.md` Version History 2026-05-19 Run-3 workflow fold entry (named as out-of-scope item).

- [ ] **Lupin-side bug — `commons_send_to` recipient pool-key vs display-name routing gap** (Session 93, María — surfaced by Tiberius DM 2026-05-19T17:28). `commons_send_to(recipient="María", ...)` returns `recipient_resolution_error` because the persona pool key is lowercase no-diacritic ("maria"), but display-name variants ("María", "MARIA", "Maria") are what callers naturally write. Tiberius's workaround for now: lowercase + strip diacritics before passing to `recipient`. Proper fix: the resolver should accept any case + diacritic variant and normalize to the pool key before lookup. Owner: Tiberius or next Lupin session. Filed as Lupin bug. Same root-cause family as the topic-name case-fragmentation bug already on this list (Session 90); consider a single resolver-normalization pass that fixes both.

- [ ] **Phase D: Cascaded plan-review pipeline prototype run** (Session 91, María — design doc at `src/rnd/2026.05.17-cascaded-plan-review-pipeline.md` §6 Phase D, tasks #24-#28 in the TodoWrite list). Five sub-tasks: (1) pick a real ≥2-section input plan to use as prototype input (avoid the cascaded-pipeline plan itself — dogfooding loop risk; candidates: a recent feature implementation plan from another project, or a synthesized 2-section test plan); (2) define telemetry — intervention count, inter-session message count, wall-clock duration, per-stage timing breakdown; (3) run prototype end-to-end with 5 CC sessions launched in 5 tmux panes, user assigning roles at launch per `persona_casting_strategy = user_assigns_at_launch`; (4) baseline comparison — run serial `/plan-review` on the same input plan with the same telemetry; (5) write findings memo into design doc §10 (new section) — prototype-vs-baseline numbers + surprising behaviors + v2 recommendations. **Why deferred**: needs the user (Mr. Rick) live + 5 CC sessions hot simultaneously; not solo authoring work. Status: Phase A (scaffolding) + Phase B (manager spec) + Phase C (rubric refinements) + Phase E (workflow integration) all complete. Workflow is invokable now via `/plan-review-cascaded` (dynamically picked up by Claude Code as soon as the wrapper landed).

- [ ] **Cascaded plan-review: Testing Reviewer role-name decision** (Session 91, María). Phase C aligned the third reviewer's rubric with `/plan-review` Pass 2 Ownership-Language Audit (NOT generic test-coverage analysis). Role name "Testing Reviewer" kept as conversational shorthand from the design conversation; rubric is actually the ownership audit. Decision needed: rename role to "Ownership Reviewer" (or similar) across 3 files (`workflow/plan-review-cascaded-personas.md`, `workflow/plan-review-cascaded.md`, `workflow/plan-review-cascaded-defaults.md`) — OR leave the shorthand and let the rubric content carry the meaning. Trade-off: rename improves workflow clarity but breaks the convenient "testing" mental anchor from the design conversation.

- [ ] **Joint summary doc with María: DM functionality + iterative-correction-loop pattern** (Session 90, Tiberius — per Mr. Rick broadcast `7bff2029-7bb3-4093-941e-adf361ac9b85` 2026-05-16). Mr. Rick noticed in my Session 90 guidance refresh the line *"the iterative correction loop produced sharper output than either of us would have produced alone, the DM thread as a mini design doc pattern is a viable workflow for cross-session architectural alignment"* and wants this captured as a process artifact. Plan: create a summary document at `src/rnd/2026.05.16-iterative-correction-loop-dm-as-design-doc.md` (or similar) that (a) summarizes the accomplishments made possible by the new DM functionality with concrete examples from today's María ↔ Tiberius arc (the bug-fix-arc, the framework convergence, the instructions-payload review), (b) names the iterative-correction-loop workflow as a reusable pattern, (c) demonstrates the DM-thread-as-mini-design-doc with selected DM excerpts. Then update `README.md` with a pointer to the summary doc. Coordination: María will collaborate post-session-end-ritual; Tiberius standing by post-commit to begin.

- [ ] **Topic-name case-fragmentation bug** (Session 90, Tiberius, discovered live 2026-05-16). `commons_send_to` does `f"dm-{recipient}"` literally, so case-variant recipient names produce separate topic files on disk (e.g. `dm-Tiberius.md` vs `dm-tiberius.md`). Silent message loss between case variants. María's earlier DMs landed in capital-T variant while her later DMs converged to lowercase; my listener was reading capital-T, so 4 DMs from her were silently missed for ~30 minutes mid-session. Fix proposal: lowercase the persona name in the wrapper before formatting the topic name. Owner: María (Lupin-side fix). Filed: she acknowledged via DM 2026-05-16T00:19Z and queued for the post-doc-consensus bug-cleanup batch.

- [ ] **System-reminder payload truncation on push-injection** (Session 90, Tiberius, discovered live 2026-05-16). Long DM bodies (~4000 chars verified) appear truncated in the receiving session's `<system-reminder>` view even when the full body is correctly written to the topic file on disk. The recipient's read of the *system-reminder* preview disagrees with their read of the *file*. Mitigation Claude callers should learn: always `commons_read` the topic to fetch canonical body; don't trust the system-reminder excerpt. Fix proposal: either lift the truncation cap or include a "[truncated — call commons_read for full body]" footer when the cap fires. Owner: María (Lupin-side investigation needed). Filed: surfaced via DM 2026-05-16T00:20Z; queued for the post-doc-consensus bug-cleanup batch.

- [ ] **MCP `instructions` payload expansion follow-up** (Session 90, Tiberius — supporting María). María shipped a draft expansion of the cosa-voice MCP `instructions` payload + per-tool docstring upgrades (R&D doc at `<lupin>/src/rnd/v0.1.7/2026.05.16-mcp-discovery-surface-expansion.md`). My review covered all 5 questions she asked (section flow + tier markers + failure modes + cross-refs + receipt etiquette). Awaiting (a) María's incorporation of my Q1 forward-pointer fix (Speakerphone + VPSA → Startup Protocol dependency), (b) Mr. Rick's review, (c) commit ship. After ship, verify the planning-is-prompting `cross-session-communication.md` cross-references still resolve cleanly.

- [ ] **Lupin-side header pointer for Pass-2 rename** (Session 89, Rachel). PIP renamed Pass 2 from "Adversarial Review (Ownership Language)" → "Ownership-Language Audit" on 2026-05-15 and hard-broke the slash-command flag (`--from=adversarial` → `--from=ownership`, no alias). The originating Lupin artifacts at `<lupin>/src/rnd/v0.1.7/2026.04.23-cj-flow-async-multi-lane/05-adversarial-review-prompt.md` and `06-fitness-review-prompt.md` are marked `[DONE, DO NOT REEXECUTE]` and were preserved verbatim per archival-fidelity rule. Add a header pointer to BOTH Lupin files noting: (a) PIP-canonical workflow is at `workflow/plan-review.md`; (b) PIP renamed Pass 2 → "Ownership-Language Audit" on 2026-05-15; (c) Lupin's original order was Adversarial→Fitness while PIP swapped to Fitness→Ownership-Audit. Do NOT rename the Lupin filenames (preserves history). Owner: Maria or next Lupin session. Filed by Rachel (PIP session `02f528ee`).

- [ ] **Session 88 follow-on — fresh-session dogfood test of Preliminary 0 (Phase A MCP Startup)**. The 5-hook recovery plan from `src/rnd/2026.05.15-tts-brevity-mandate-self-violation-recovery.md` landed in `workflow/session-start.md` as a new "Preliminary 0" section, in `~/.claude/CLAUDE.md` + `global/CLAUDE.md` as Phase A strengthening, and in `workflow/claude-config-global.md`. Greps verify presence; live behavioral validation requires a NEW Claude Code session. Test plan: (a) start a fresh session and verify it calls `get_session_info()` BEFORE first user-facing text, (b) confirm it names its assigned persona in the first acknowledgment (chorus disambiguator), (c) confirm any doc-link emitted lives in `abstract` only (no `(/app/docs?...)` in spoken `message=`), (d) confirm the `doc_scope` envelope is extracted at startup (not re-derived later via manual INI parsing). If any of (a)–(d) fail, file as bug-fix-mode item against the canonical startup workflow. Filed by Session 88 (Rio, `8ebced94`).

- [ ] **Lupin-side cosa-voice surface drift fix** — the original audit finding from Session 88's opening (before it was redirected into the recovery plan): the `cosa_voice_mcp.py` MCP startup instructions blob (line ~572) calls `speakerphone_on=false` **"phone mode"** with legacy silent semantic ("respond normally; speaking via TTS only happens when YOU explicitly call notify()"), while the per-turn hook rider (`hook_common.py:_speakerphone_reminder_body`, line 1138-1148) and exit reminder (`speakerphone_exit_reminder`, line 1313-1322) both call the same state **"QUIET mode (per-session DND)"** with the OPPOSITE quiet-mode semantic (keep calling `notify()` with `priority='medium'`, `suppress_ding=False`). 2026-05-14 evening rewrite per `<lupin>/src/rnd/v0.1.7/2026.05.14-per-session-dnd-toggle-and-slider-move.md` updated the rider but missed the MCP startup blob. Fix: rewrite `cosa_voice_mcp.py:572` to match the rider's QUIET mode semantic — rename "phone mode" → "QUIET mode (per-session DND)" and swap the silent-semantic bullet for the quiet-mode directive. Owner: Maria / next Lupin session (outside PIP scope). Filed by Session 88 (Rio).

- [ ] **Tier-5 follow-on from Session 85 — update `~/.claude/skills/cosa-voice-notifications/SKILL.md` with Neither semantics**. Source-of-truth `ask_yes_no()` now returns ternary `yes`/`no`/`neither` with optional `[comment: ...]` qualifier on any value; the skill description and body still describe the binary contract. Update the skill's progressive-disclosure body to include the ternary parser pattern, anchor example showing a Neither re-frame, and trigger-rich description hook (`neither escape`, `re-frame`, `ternary`). Skill lives outside the PIP repo; this is a separate commit from Session 85's in-repo work. Reference the canonical hub at `workflow/cosa-voice-integration.md` → "Handling Neither — the Re-Frame Escape Hatch".

- [ ] **Cosa-voice MCP commons tools — audit consumers + update documentation** — After Lupin lands the 5 new commons MCP tools (`commons_post`, `commons_read`, `commons_who`, `commons_ask_sync`, `commons_ask_async`) on the cosa-voice MCP server (Lupin Phase 1 of the inter-session-commons initiative; design at `<lupin>/src/rnd/v0.1.7/2026.05.09-inter-session-commons/`), interrogate the new MCP interface and update documentation in EVERY repo that consumes cosa-voice MCP. Scope includes (non-exhaustive): Lupin (`CLAUDE.md` cosa-voice section, `src/docs/`, `src/lupin_mcp/README.md`), planning-is-prompting (`workflow/cosa-voice-integration.md`, `~/.claude/CLAUDE.md` live + `global/CLAUDE.md`, the cosa-voice-notifications skill at `~/.claude/skills/cosa-voice-notifications/SKILL.md`), and any other Claude-Code-using repo that references the MCP tool catalog. Goal: every consumer-facing reference reflects the expanded tool catalog (post + read + who + ask_sync + ask_async). Filed 2026-05-10 by Lupin session `f9608a41` during commons Phase 1 plan-review REUSE walk (finding 4/12 ratification proviso). **Session 86 progress (2026-05-14)**: PIP-side complete via the new Cross-Session Communication Guidance — `workflow/cross-session-communication.md` (new canonical guidance doc), `workflow/cosa-voice-integration.md` (cross-link), `global/CLAUDE.md` + `workflow/claude-config-global.md` (new `## CROSS-SESSION COMMUNICATION` section). Remaining: Lupin-side docs (`<lupin>/CLAUDE.md` cosa-voice section, `<lupin>/src/lupin_mcp/README.md`), and `~/.claude/skills/cosa-voice-notifications/SKILL.md` update — both outside PIP repo.

- [ ] **Update codebase-analysis SKILL.md with venv-selection guidance** - The SKILL.md at `~/.claude/skills/codebase-analysis/SKILL.md` documents `python -m cosa.repo.run_branch_analyzer` invocation but doesn't mention the PyYAML dependency or the canonical venv. Tier-2 verification of session-end Step 6 caught this: system Python raises `ModuleNotFoundError: No module named 'yaml'`; `$LUPIN_ROOT/.venv` has yaml (6.0.3 verified 2026-05-30 post-COSA-merge). Add a "Python interpreter selection" section to the skill (prefer `$LUPIN_ROOT/.venv` → system python3 fallback; the old `src/cosa/.venv` path was removed 2026-05-30 — it's a symlink to system python3 post-merge, not an isolated venv). Out of scope for Session 84 since the global skill lives outside the PIP repo and edits there are a separate concern.
- [ ] **Step 6 dogfood test** - Once the next `/plan-session-end` runs, confirm the new Step 6 fires correctly: rich path renders the markdown table, notify() carries headline + abstract, fallback path works when LUPIN_ROOT is unset. If anything breaks, file into bug-fix-queue.md per Test Ownership Mandate.
- [ ] **Filename-parsing artifact in BranchChangeAnalyzer output** - Tier-2 run against PIP showed `"ug-fix-queue.md"` in `files_changed_set` (corrupted from `bug-fix-queue.md`). Likely a leading-character strip in lupin's analyzer's diff parser. Out of scope here (lupin-side bug); flag and forward to lupin's bug-fix-queue when attention is on lupin.
- [ ] **Plan-review dogfood test** - Run `/plan-review` (or the standalone `/plan-review-reuse`) against `src/rnd/2026.04.27-promote-plan-review-pattern-to-pip.md` itself as a Pattern 3 single-doc test. Goal: verify the gate catches any ownership-language drift introduced by the writing process AND surface any shape-fit issues (Pattern 3 doc-set vs. multi-doc Pattern A/B/C calibration). Findings fold back into `workflow/plan-review.md`. **NOTE**: now that the canonical doc is fitness-first (Session 80 restructure), the dogfood run also validates the new ordering rationale §3.
- [ ] **Plan-review fidelity readthrough** (`EXECUTOR: HUMAN` — design-review fidelity check) - Read `workflow/plan-review.md` end-to-end against the source `<lupin>/src/rnd/v0.1.7/2026.04.23-cj-flow-async-multi-lane/{05-,06-}-*.md` prompts. Confirm no signal was lost in the abstraction. The AI cannot validate "this captures the spirit of the original" against its own extraction. **NOTE**: post-Session-80, the readthrough also has to confirm the Fitness-then-Adversarial swap doesn't drop signal that the Lupin Adversarial-then-Fitness ordering carried.
- [ ] **Plan-review Phase 3 (Lupin-side wrapper)** - In a Lupin-rooted session, create `<lupin>/.claude/skills/plan-review/SKILL.md` thin wrapper that auto-discovers docs in target dir, injects Lupin-specific tags (`EXECUTOR: AI/HUMAN`, `:7999`/`:8000` venues), and enforces Q-N decision-anchor format. Out of scope for this PIP-side change set; lives where projects own it.
- [ ] **Lupin source-artifact header pointers** - Add a header note to `<lupin>/src/rnd/v0.1.7/2026.04.23-cj-flow-async-multi-lane/{05-,06-}-*.md` pointing at PIP's canonical `workflow/plan-review.md`. Don't replace with stubs (revisionist); leave as historical instances + cross-reference. Note the Lupin order was Adversarial→Fitness; PIP is Fitness→Adversarial — the header pointer should call this out so future readers don't assume invariance.
- [ ] **Verify Test Ownership Mandate uptake across repos** - Session 78 added the mandate to `~/.claude/CLAUDE.md` / `global/CLAUDE.md` / `workflow/claude-config-global.md`. Start fresh sessions in Lupin and other active repos; make a small code change; confirm Claude's post-change behavior is "write + run tests, report pass/fail table" rather than "please verify this works."
- [ ] **Verify interactive-requirements-elicitation skill activation** - Start a fresh session and use vague phrasing ("I'm thinking about...", "Not sure exactly...") to confirm the skill fires and the compact stub behavior matches the previous inline section.
- [ ] **BUG**: `/plan-history-management` is hardcoded to Planning-is-Prompting project paths - when invoked from Lupin it manages PiP's history.md instead of Lupin's. Slash command needs to be project-aware (use project root detection, not hardcoded paths)
- [ ] Update install wizard (`workflow/installation-wizard.md`) to offer template-based installation for testing workflows (use `workflow/slash-command-templates/` instead of copying PiP commands verbatim)
- [ ] Audit other repos (besides Lupin) for verbatim-copy slash command bug in testing commands
- [ ] Verify testing-development skill activation in new Claude Code session
- [ ] Consider future extractions if CLAUDE.md drifts back toward 40k (candidates: PATH MANAGEMENT ~4.8k, Code Style ~3.9k) — monitor after a few sessions of organic use
- [ ] Add global mandate: all planning documents should assume serialization to the local repo's `src/rnd/` directory by default. Workflow should proactively ask user to confirm this default assumption. Goal: chronologically ordered, semantically titled planning artifacts that are easy to find in each project
- [ ] Verify Session Isolation Rules coverage: confirm the rules removed from `global/CLAUDE.md` (during Session 71 trimming) are fully covered by existing workflow documents (`workflow/session-start.md`, `workflow/bug-fix-mode.md`, `workflow/session-checkpoint.md`, `workflow/session-end.md`). Ensure no gaps in isolation enforcement for concurrent Claude sessions
- [ ] Test new token estimation on Lupin repo history.md after compaction
- [ ] Verify estimate is within 5% of Claude Code's reported count
- [ ] Consider updating get-token-count.sh script if it exists
- [ ] Test all four bug fix mode commands work correctly
- [ ] Update INSTALLATION-GUIDE.md with new command variants (bug-fix-mode split)
- [ ] Consider similar split for other argument-based commands

## Completed (Recent)

- [x] **All-tiers CoSA-in-Lupin coverage grind — Workflow Steward → honest, reviewer-audited 100%** (13,300 passed/0 failed, cosa 100% line+branch+function across 412 files, Krishna 8/8 0-hollow, 11 Lupin-side test-only commits held). Cataloged **FM-21**, HELD **FM-22** (evidence-gated), **git-verified both xfails myself** before co-signing the certification (zero mask). Observer log `src/rnd/2026.06.03-all-tiers-overnight-grind-observer-log.md` (per-wake ledger). - Session 100
- [x] **Sam-TTS-storm postmortem + FM-20** (`src/rnd/2026.06.02-sam-tts-storm-incident-postmortem.md`; root cause = lever-A durable outbox replaying an 8,692-row stale backlog through the Sam error persona on a deploy bounce; fix `8447eec` verified `:7999`+`:8000`). - Session 100
- [x] **Guided Decision Walkthrough skill** (PIP hub `workflow/decision-walkthrough.md` + design doc + `/plan-decide`; Lupin spoke installed by Tiberius). DD1–DD3 ratified (see Decisions Log). - Session 100
- [x] **TTS-cap workflow lock** (`workflow/cosa-voice-integration.md` → 500-char INI-tunable `cosa voice spoken char cap` + `override_size_limitation`). - Session 100
- [x] **Archive history.md (46,397 → 11,988 tokens; Sessions 57–93 → `history/2026-02-02-to-05-20-history.md`)** - Session 99
- [x] **CoSA post-game Q4: dependable-generic-framework + WAVE-2 observer log (FM-1..18 catalog, g1–g4 graduation ledger); Rick mitigation set ratified (§12.4); messaging root-cause + harvest-discipline codified (§13)** - Session 99

- [x] **Global `~/.claude/CLAUDE.md` § DOCUMENT VIEWER LINKS → canonical form** (was the deferred Lupin-context dotfile item). Done as part of the 2026-05-30 global-CLAUDE.md trim: (1) URL example → path-only `/app/docs?path=<scope>/<rel>` (legacy `&scope=` retired); (2) Phase-A hook (b) now extracts the single `project` string, `doc_scope` 4-field envelope removed (confirmed live: `get_session_info()` returned `project` only, no `doc_scope`); (3) built-in `docs`/`io` scope guidance dropped (already gone in the Notification-System compression); (4) cross-ref to `workflow/doc-viewer-links.md` added. **Refinement over the original spec**: the path-segment scope is sourced from the project-local `§ Doc Viewer Scope`, NOT raw `info["project"]` — a session's `project` may be a short alias (`plan`) while the doc-viewer segment is the full name (`planning-is-prompting`). — 2026-05-30 (María session `42a02847`, Rick-approved). **Note**: the PIP `global/CLAUDE.md` mirror is now out of sync with live (it was already drifted pre-edit, 51k vs live's pre-trim 47k) — reconciliation deferred pending a diff (mirror has content live lacks; no blind overwrite).
- [x] **Cross-repo rollup: depth-N discovery + venv ordering** (was the [NEXT VERSION] sub-directory-traversal item). Implemented multi-depth discovery — three explicit globs (`*/io/`, `*/*/io/`, `*/src/*/io/`) carrying each repo's **absolute path** through `--repos` (fixes the 2026-05-21 `google/lookml` under-report; also catches `lupin/src/lupin-mobile`). Flipped Step-2 PYBIN to `$LUPIN_ROOT/.venv` → `python3` and **purged the stale `$LUPIN_ROOT/src/cosa/.venv` reference** (a symlink→system-python3 post-COSA-merge) across `workflow/loc-delta-global.md`, the slash wrapper (v1.0→1.1), and `workflow/session-end.md` §6.2/§6.5. COSA→Lupin merge mooted the original nested-`cosa` sub-case. Both gaps were disk-validated before edit. — 2026-05-30 (María session `42a02847`, Rick-approved)

- [x] **Post-COSA-merge PIP doc refresh — LANDED 2026-05-29** (María session `ecdf8073`). The COSA→Lupin merge landed (Krishna, commit `0a01da3`, FLATTEN history, on wip-v0.1.8; 621 cosa files folded under `src/cosa/`; `src/cosa/.git` preserved on DATA02 = reversible). Refreshed the PIP docs that treated CoSA as a separate governance/test zone: `plan-review-cascaded-common.md` + `-input-spec.md` (PG-5 mandate-exclusion example annotated **historical** — post-merge cosa INHERITS the Lupin-wide 100% gate with an immediate grandfathering ramp; the verify-scope-applicability *lesson* preserved unchanged); `testing-baseline.md` (genericized "multiple independent suites" example + post-merge note → unified Option-1); `INSTALLATION-GUIDE.md` (multi-suite "Complex Project" example reframed as one-repo-with-`src/cosa/`-subtree); `loc-delta-global.md` + `session-end.md` (cosa removed from nested-sub-repo examples — its LoC now folds into `lupin`). **Axis-1 PRESERVE confirmed** — all `cosa.repo.*` / `src/cosa/` paths + `.venv` probes left intact (still resolve; no invocation rewrites). **Deliberately preserved per narrative-fidelity**: the dated 2026-05-16 cross-repo bug-filing example in `cross-session-communication.md` + the illustrative Rio "two commits" quote in `cosa-voice-integration.md` (honest pre-merge snapshots). 5-axis map: `~/.claude/plans/unified-kindling-feigenbaum.md`. — Session 96 (María)

- [x] **v1.1 Cascade-workflow codification pass — landed across 5 workflow files + design doc** (María + Tiberius, Session 93 fifth continuation 2026-05-20). 7 v1.1 workflow-guidance candidates folded + 1 placeholder held; 4 pre-committed re-evaluation gates locked in writing in §10.18.12. Files touched: `workflow/plan-review-cascaded-common.md` (7 distinct edits — NEW §Clarification Tier Vocabulary T1/T2/T3/T4, NEW §Author-side Discipline Grep-sweep Checklist, NEW §Observer-mode Probe Protocol, NEW §Multi-surface Footer-ratification Close Protocol + 3 extensions: §Manager System Prompt self-audit item 7, §Heartbeat Handling dual-independent daemon kickoff, §Step 9 cold-context test 5 → 6 questions + Manager close-out self-audit sweep sub-section); `workflow/plan-review-cascaded-personas.md` (NEW Persona 6 Workflow Steward + 2 extensions: Persona 1 Outputs gained `kind: manager_self_audit_sweep` artifact, Persona 2.A point 14 AC-table sweep gained Run-4 anchors #2 Krishna Q-1..Q-4 + #3 Tiberius Tiffany-rename — cumulative anchor base now 5 instances across Runs 3+4); `workflow/plan-review-cascaded-defaults.md` (NEW §Cascade-execution observability config section with 4 INI keys + 3 new kind enum values; config keys 28 → 32); `workflow/plan-authoring-cascaded.md` + `workflow/plan-review-cascaded.md` (version-history-only entries); `src/rnd/2026.05.17-cascaded-plan-review-pipeline.md` (NEW §10.18.12 Pre-committed re-evaluation gates with 4 gates anchored to Runs 5/6/7 + anti-pattern gate-slippage clause). Bilateral ratification via María ↔ Tiberius post-Run-4 retrospective DM thread (2 rounds; qids `0cfea56f` → `67ccf3f8` → `569eeba8`); codification-pass pointer-set delivered to Tiberius for per-artifact review via DM `830f4833`. Two new candidates emerged from the DM thread itself: #6 Manager close-out self-audit sweep (operationalizes the "Manager ad-hoc'd what should be codified" diagnostic — 3 validated empirical instances) + #7 Observer-probe-as-mitigation channel (standalone, not single-loop extension — structural distinction preserved). Two methodological catches surfaced during retrospective: phantom-lag-stripped artifact on §10.18.4 monotonic-timing data (recovered telemetry ≠ real saving); slot-vs-persona confound on §10.18.9 wisdom-vs-volume framing. Both correctable only with additional clean-sample runs — Gates 1 and 2 in §10.18.12 lock the re-evaluation. Standing by for Tiberius review-OK before commit. — Session 93 (María + Tiberius)

- [x] **Executive briefing for Rick's external manager** (María, Session 93 fifth continuation 2026-05-20). New subdirectory `src/rnd/executive-briefings/` with file `2026.05.20-cascaded-review-pipeline-executive-summary.md` — jargon-free ~7-min read covering the full 4-run arc from concept validation through Run-4 fully-autonomous close. Two mermaid diagrams (Diagram A: roles + reporting relationships, color-coded; Diagram B: information + data flows with closed-loop self-improvement architecture). Dedicated "How the Pipeline Self-Improves: The Workflow Steward Role" section covering observer's real-time + post-cascade duties + 4 empirical contributions from Run 4 + two improvement surfaces framing (plan vs implementation) + load-bearing rationale. README.md updated with link to the briefing. Standing by for Rick's redline before he forwards to his manager. — Session 93 (María)

- [x] **Run 4 (Phase 7a cascade) — Workflow Steward role + §10.18 retrofit row** (María, Session 93 fourth continuation 2026-05-20). Observer telemetry across full 1h 30 min cascade (01:55:34 → 03:30:47 UTC); zero T3/T4 escalations; Rick AFK + asleep for entire run as designed. Step 0 + Step 9 workflow first-application BOTH validated STRONG. Pre-cascade Recon eliminated 6 browser-API archaeology items upstream (Step 0 working). Krishna's Step 9 dual-administer cold-context test caught 2 additional friction points beyond Tiberius's self-administered 3 (Step 9 mechanic empirically additive). Failure mode #6 (`signal-density-obscures-needle`) captured + 3 mitigation surfaces enumerated. Monotonically-decreasing-stage-wall-clock pattern (38→33→21 min effective) recorded as first empirical anchor for cascade-learning-loop forward-asymmetry. §10.18 retrofit row added to `src/rnd/2026.05.17-cascaded-plan-review-pipeline.md` covering: four-row comparison table (Runs 2/3/4 + extension), Step 0 + Step 9 workflow validation evidence, Tier discipline outcome (100% T1/T2 silent, zero T3/T4 wake-ups — first cascade-run with zero user touches), cap utilization 57% with 50%+ headroom across all surfaces, v1.1 workflow-guidance candidates (5 promoted + 1 placeholder), reviewer-cycle-efficiency datapoint with inverse density-vs-workflow correlation observation. Cascade-complete signal fired @ 03:30:47 UTC posted to `cascaded-prototype-input-plan`. Daemons stood down on next universal-step-zero tick. — Session 93 (María)

- [x] **Step 0 (Cascade Preparation) workflow guidance — María + Tiberius, Session 93 third continuation 2026-05-20**. Codifies the cascade-preparation phase that v1 workflow omitted (Rick's catch surfaced via Mr Radio's Phase 7 onboarding empirical anchor — ~1500-word verbal brief should have been a single read). Requirements doc at `src/rnd/2026.05.20-step-0-cascade-preparation-workflow guidance.md` (Tiberius-authored, 399 lines). Workflow codification across 5 files (María-coded): `plan-authoring-cascaded.md` §Step 0 (6-sub-step authoring spec: 0.1 input intake + 0.2 slicing manifest + 0.3 per-slice design docs + 0.4 Q-decision matrix + 0.5 pre-cascade user ratification + 0.6 cascade-readiness gate); `plan-review-cascaded.md` §Step 0 (lighter 3-sub-step variant); `plan-review-cascaded-common.md` §Step 0 — Cascade Preparation (Shared Acceptance Criteria) with 5-question cold-context test + 6-criterion light-review gate + pre-cascade Recon checklist REQUIRED + `cascade_input_ready` state semantics + full cascade lifecycle state machine; `plan-review-cascaded-personas.md` Persona 1 (Manager) outputs extended; `plan-review-cascaded-defaults.md` closure_action enum + kind enum + 3 new config keys. NEW `cascade_input_ready` state distinct from `cascade_complete` + `implementation_handoff_ready`. Design doc §10.17 anchors the meta-lesson. **Step 0 + Step 9 together close the cascade workflow's end-to-end shape** — lifecycle: `raw_design_received` → `cascade_input_ready` → `cascade_complete` → `implementation_handoff_ready` → `shipped`. — Session 93 (María + Tiberius)

- [x] **Step 9 (Implementation-Handoff Synthesis) workflow guidance — María + Tiberius, Session 93 second continuation**. Codifies the implementation-handoff phase that v1 cascade workflow omitted per Rick's broadcast `d3a89a21`. Requirements doc at `src/rnd/2026.05.19-step-9-synthesis-and-handoff-workflow guidance.md` (Tiberius-authored, 312 lines). Workflow codification across 5 files (María-coded): `plan-authoring-cascaded.md` §Step 9 (3-artifact spec: synthesis doc + parent design-doc amendments + execution plan); `plan-review-cascaded.md` §Step 9 (1-artifact spec: revision-handoff doc); `plan-review-cascaded-common.md` §Step 9 — Synthesis & Handoff (Shared Acceptance Criteria) with 5-question cold-context test + 5-criterion light-review gate + 1-revision-turn cap; `plan-review-cascaded-personas.md` Persona 1 (Manager) outputs extended; `plan-review-cascaded-defaults.md` closure_action enum + kind enum + 3 new config keys (`synthesizer_authorship_policy`, `light_review_required`, `cold_context_test_mode`). NEW `implementation_handoff_ready` state distinct from `cascade_complete`. Design doc §10.16 anchors the meta-lesson with Run-3 empirical evidence (Tiberius's 1,225 LOC of post-cascade ad-hoc work + Roscoe's 2 pre-flight gaps). DM-thread-as-design-doc pattern validated again (4-DM cycle, 16-second convergent-independent-draft posting delta between María and Tiberius). — Session 93 (María + Tiberius)

- [x] **§10.14 redline of cascade workflow docs — Run-3 workflow fold (María, Session 93)** — Folded the 12 workflow-guidance candidates from Tiberius's Run-3 §8 pipeline summary (+ 3 Rick-voice catches: Manager Reassignment Latitude, Reassignment Bias Risk Guardrail, Mute-Channel Bypass) into the canonical cascade workflow docs. Edits across 4 files:
  - `workflow/plan-review-cascaded-common.md`: NEW §Reviewer Reassignment with Manager Latitude 5-element workflow guidance + Bias Risk Guardrail + Rate-Limit failure-mode entry; NEW §Cascade-Learning-Loop Sub-patterns (forward-only-asymmetry + symmetric-application + context-aware-application); expanded `closure_action` enum (3 new values); Manager System Prompt self-audit item 6 (`blocked_waiting_on_user`); 18-min user-attention-block cap in §Escalation Taxonomy
  - `workflow/plan-review-cascaded-personas.md`: Persona 2.A point 14 (forward-sweep on revision-mechanism change with 3 sub-patterns); Persona 5 §Stage-3 Cosmetic-Cluster Recognition sub-section
  - `workflow/plan-review-cascaded-defaults.md`: closure_action worked-example table; new commons `kind` enumeration (3 new + 6 pre-existing formalized)
  - `workflow/plan-authoring-cascaded.md` + `workflow/plan-review-cascaded.md`: version-history bumps cross-referencing the canonical edits in common.md
  - `src/rnd/2026.05.17-cascaded-plan-review-pipeline.md`: NEW §10.15 Run-3 actuals (third row of comparison table — 37× count reduction vs 10× predicted, 3-4× better than prediction); §10.11 verdict extended to reflect Run-3 empirical strengthening
  - Cross-repo paired with Tiberius's Lupin-side Phase 6C consolidation track. Mute-Channel Bypass filed as Lupin/cosa-voice feature request (above on this list); CSS-var visible-text safety + ask_multiple_choice Path-B subprocess-restart cost noted as out-of-scope. — Session 93 (María)

- [x] **Per-Repo Preferred-Persona Env Var (`COSA_VOICE_PREFERRED_PERSONA__<PROJECT>`)** — Feature shipped end-to-end. PIP-side (María): plan doc `src/rnd/2026.05.19-cosa-voice-preferred-persona-env-var.md`, workflow integration via new `workflow/session-start.md` Preliminary -1 (env-var path) + Preliminary 0.5 (slash-command swap path), slash-command shim `.claude/commands/plan-session-start.md` v1.0 → v1.1 ($ARGS handling), `workflow/INSTALLATION-GUIDE.md` "Optional: Per-Repo Default Persona via Env Var" subsection. Cross-repo Lupin allocator at Tiberius commit `3bc7b9e` (49/49 tests green). Rollout (plan §9) executed end-to-end: Rick exported `__PLAN=María` + `__LUPIN=Tiberius`, restarted PIP session, María session `ac2d05c0` is the proof point (`voice_persona.name=maria` deterministically on Phase A startup). Path A locked on `/clear` semantics: env var fires on FRESH allocation only; preserved across `/clear`. — Session 93 (María)

- [x] **Cross-Session DM Guidance + Daily LoC Integration** — Wired María's new `cosa.repo.git_loc_delta` module into `workflow/session-end.md` §6.2 as the new default daily LoC trace (with §6.2.alt for `--rich` branch_analyzer opt-in); refreshed `~/.claude/skills/codebase-analysis/SKILL.md` with Decision Rule + tool routing for "code analytics" intent; major refresh of `workflow/cross-session-communication.md` (2→3 surfaces with full DM mechanics in §1.5, new patterns section §6.5 incl. mermaid-diagrammed cross-session bug-filing flow); added thin `## CROSS-SESSION COMMUNICATION` pointer in `~/.claude/CLAUDE.md` per María's 5-layer doc-architecture framework; filed two upstream bugs in Lupin's queue (cross-repo CSV path + `commons_send_to` FunctionTool), both fixed by María in commit `f4e0370` within 4 min, verified end-to-end via live DM round-trip (`push_mode_active: true` + `dm_dispatched: true`). Plan: `src/rnd/2026.05.16-git-loc-delta-session-end-integration.md` (status 🟢 IMPLEMENTED + §9 upstream-bug-report resolved). Process insight: iterative-correction-loop with peer session via DM thread produced sharper output than either alone (5-layer doc architecture refined jointly; framework convergence across multiple correction cycles). — Session 90 (Tiberius)

- [x] **Plan-Review Pass 2 Rename — drop "Adversarial"** — Eliminated the word "adversarial" from PIP's plan-review nomenclature after Mr. Rick reported session-level confabulation (Pass 2 mis-characterized as security/threat-model review with OWASP-style examples like path traversal, manifest tampering, Unicode bypass). Investigation confirmed canonical guidance was correct (Pass 2 = ownership-language audit / executor-tagging) but the word "adversarial" pulled sessions into attacker-mindset semantics. Rename: Pass 2 (Adversarial Review (Ownership Language)) → **Pass 2: Ownership-Language Audit**. Doc title (Fitness + Adversarial Gate) → (Fitness + Ownership Gate). Prompt body "hostile outsider" → "forensic auditor". Slash flag hard-break `--from=adversarial` → `--from=ownership` (no alias). NEW top-of-doc disambiguation banner: "NOT A SECURITY REVIEW". NEW §13 anti-pattern row: "Treating Pass 2 as a security/threat-model review (path traversal, manifest tampering, Unicode bypass, attack surface)". Scope: 6 live files patched, 49 occurrences renamed, 0 stale "Pass 2 Adversarial" references remain (all surviving "adversarial" mentions are rename-callouts or historical Lupin-origin references). Plan: `src/rnd/2026.05.15-plan-review-rename-drop-adversarial.md`. Verification: 7 greps clean. Lupin-side header-pointer work filed as new TODO (owner: Maria). — Session 89 (Rachel)

- [x] **Sync workflow language with `ask_yes_no()` Neither option** — cosa-voice MCP `ask_yes_no()` now returns ternary `yes` / `no` / **`neither`** (Neither = "the question needs re-framing", never a default, requires explicit user click). Hub-and-spoke patch across PIP: canonical §Handling Neither subsection (~70 lines) added to `workflow/cosa-voice-integration.md` with ternary parser, anti-pattern table, destructive-op CRITICAL rule. Response Format table expanded 4→6 variants. Global config sync pair (`~/.claude/CLAUDE.md` live + `global/CLAUDE.md` mirror) re-synced byte-identical. Template `workflow/claude-config-global.md` updated. Callsite spokes patched: `session-end.md` (2 callsites + 2 hint lines), `session-checkpoint.md` (2), `branch-pr-and-merge.md` (4 — destructive ops tagged **CRITICAL**), `bug-fix-mode.md` (4), `installation-wizard.md` (1), `uninstall-wizard.md` (1 — **CRITICAL**), `session-start.md` (1 mention), `.claude/commands/plan-review.md` (2), `.claude/commands/plan-skills-management.md` (1). Verification: all 5 greps clean (hub subsection present, 6 response-format rows, zero "Binary yes/no" hits, every Tier-3 spoke references Neither, live↔mirror byte-identical). Plan: `src/rnd/2026.05.11-neither-response-language-sync.md`. Tier-5 (cosa-voice-notifications skill outside repo) queued as follow-on TODO - Session 85

- [x] **TTS Brevity Mandate concision pass** (refinement of Session 81 mandate). Three new principles added to `workflow/cosa-voice-integration.md` and `~/.claude/CLAUDE.md`/`global/CLAUDE.md` headline: (1) **tiered length cap** — routine close-outs ≈ 60 words / 20s, substantive turns ≈ 80–120 words / 30s; (2) **headline, don't enumerate** — numbers, file lists, percentages live in `abstract` only; speak the verdict; (3) **no justification for non-actions in spoken line** — skip confidence statements, process meta, deferred-work rationale. **Anchor example** added to canonical doc memorializing a 190-word real-use case → 45-word tightening (76% reduction). Memory file `feedback_tts_brevity.md` updated with refinements - Session 82
- [x] **Conversation-mode reinforcement across PIP** + new global **TTS Brevity Mandate**. Hub-and-spoke pattern: canonical spec in `workflow/cosa-voice-integration.md` (new §Conversation Mode with full TTS Brevity Mandate sub-section), live `~/.claude/CLAUDE.md` re-synced to `global/CLAUDE.md` with new headline rule, callouts added to 14 workflow files + 13 slash-command wrappers (31 files total). Verification clean: every spoke that uses cosa-voice tools references the hub AND mentions the brevity mandate; live + repo CLAUDE.md byte-identical. Memory persisted at `~/.claude/projects/.../memory/feedback_tts_brevity.md` - Session 81
- [x] Wire `/plan-review` into install wizard (entry [N] under new "REVIEW WORKFLOWS" section in `workflow/installation-wizard.md`; Step 3 dependency validation block; Step 5 install instructions; bulk-select strings updated). Catalog/flag-set fidelity verified across canonical doc, wrapper, wizard, and INSTALLATION-GUIDE.md (`--from=reuse|fitness|adversarial` consistent in all four). Added "Plan Review Gate Workflow" section to INSTALLATION-GUIDE.md - Session 80
- [x] Restructure `workflow/plan-review.md` to Fitness-first ordering (REUSE → Pass 1 Fitness → Pass 2 Adversarial). Added §3 Pass Ordering rationale. Synced wrapper, p-is-p-00 mermaid + prose, p-is-p-02 conventions section (13 Pass references swapped), README [C.5] entry. Origin-artifact note preserved that Lupin's order was Adversarial→Fitness - Session 80
- [x] Extract INTERACTIVE REQUIREMENTS ELICITATION to skill (`~/.claude/skills/interactive-requirements-elicitation/SKILL.md`) and reduce CLAUDE.md to a compact stub; live/repo CLAUDE.md now 38,297 chars (reclaimed 4,122 chars, under the 40k performance-warning threshold again) - Session 78
- [x] Add TEST OWNERSHIP MANDATE — embedded role-separation rule in `~/.claude/CLAUDE.md`, `global/CLAUDE.md`, `workflow/claude-config-global.md`; reinforced in 3 testing workflow docs; revised anti-sentiment in `workflow/bug-fix-mode.md` Step 7; reframed scope options in `workflow/testing-remediation.md`; cross-referenced in `testing-development` skill - Session 78
- [x] Update `global/CLAUDE.md` template to match trimmed `~/.claude/CLAUDE.md` (full replacement, 1378→845 lines) - Session 72
- [x] Reduce ~/.claude/CLAUDE.md below 40k chars (42.4k → 31.7k) - Session 71
- [x] Create cosa-voice-notifications skill with full API reference - Session 71
- [x] Implement persistent TODO.md pattern - Session 50
- [x] Add suppress_ding parameter to cosa-voice documentation - Session 49
- [x] Fix token estimation undercount bug - Session 48
- [x] Split bug fix mode into separate slash commands - Session 47
- [x] Implement bug fix mode workflow - Session 46

---

*Completed items older than 7 days can be removed or archived.*
